export const TEST_CONTRACT = Object.freeze({ id: "TEST-CONTRACT" });
export const TEST_INVARIANTS = Object.freeze([{ id: "TEST-INVARIANT" }]);
export const TEST_ACTIONS = Object.freeze(["observe"]);
export const TEST_OBSERVATIONS = Object.freeze(["snapshot"]);
export const TEST_VISIBLE_ASSERTIONS = Object.freeze(["TEST-VIS-001"]);
