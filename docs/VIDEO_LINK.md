# Demo Video

Status: `PUBLIC_PRIMARY_VERIFIED_MIRROR_PUBLISHED`

## Public URLs

- Primary video: <https://youtu.be/R_AA3WqmVyw>
- Public backup mirror: <https://vimeo.com/1222716472>

The YouTube URL is the canonical `Video URL` for the HackerEarth submission. The Vimeo
URL is a redundant public mirror and is repeated in the submission description.

## Uploaded-file identity

- filename: `BeyondGreen-final-upload.mp4`;
- duration: `294.310998` seconds (`4:54.31`);
- size: `24,267,123` bytes;
- format: `1920x1080`, H.264 video at 60 fps, AAC mono audio at 44.1 kHz;
- SHA-256: `78dbefc9868a1734ae416478403386ede80a6d3c0545ee67a66d4a677e3f6d80`;
- archive policy: the media file is excluded; only the public URLs ship.

The final export replaces stale “43 iterations” wording with `40+ ITERATIONS` and adds
the closing disclosure `Built by one engineer with AI-agent assistance`. It decoded in
full without error. Its decoded audio is identical to the previously reviewed final
master. The owner confirmed that the blurred context fragment does not expose product
code or an identifiable product interface and that any legible values are synthetic.

## Access verification

- YouTube signed-out check: `2026-08-31T14:31:57Z`;
- method: fresh browser profile with no cookies or Google authentication;
- evidence: the page showed `Sign in`, identified the video as `Unlisted`, and the
  player advanced to `0:01 / 4:54` without a login or access request;
- Vimeo publication check: `2026-08-31T14:38:21Z`;
- evidence: Vimeo's unauthenticated public metadata endpoint returned the title,
  duration, thumbnail, and embeddable player for video `1222716472`. Automated browser
  playback encountered Vimeo's human-verification anti-bot screen, not an account gate;
  Vimeo therefore remains the backup rather than the canonical qualification URL.
