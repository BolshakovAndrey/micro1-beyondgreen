# StateShift Guardian — Improvement Changelog Projection

**Projection of:** `docs/PROJECT_SPEC.md@1.0.0`
**Requirements:** `AR-003`, `AR-004`, `EV-007`, `EV-009`, `EV-010`
**State:** specification only; no product or benchmark run exists

## ITR-000 — Initial comparison-contract candidate

- **Observed problem:** a patch may pass visible tests while violating a hidden
  behavioral or lifecycle contract.
- **Hypothesis:** comparing Guardian against both a mechanical transform and a
  legacy-green coding-agent policy will isolate value beyond syntax rewriting and
  beyond ordinary test-driven stopping.
- **Exact change:** drafted three arms, 12-case denominator, 5/7 split, BPMR,
  false-green stop rate, supporting metrics, fairness rules, and unblinding policy.
- **Command family:** the four benchmark/evaluation commands projected in
  `docs/EVALUATION.md`.
- **Evaluation version:** `eval-v1.0.0`.
- **Evidence:** `docs/PROJECT_SPEC.md` sections 8–10 and `docs/EVALUATION.md`.
- **Run IDs:** none; no executable product work or result exists.
- **Decision:** `revised`; semantic freeze was not approved.
- **Human checkpoint:** global specification and open decisions await user approval.
- **Next action:** after approval and implementation readiness, freeze fixture prose
  and provenance before writing any fixture source.

## ITR-001 — Candidate-spec review and corrected revision

- **Observed problem:** independent Codex review and a separate read-only Claude Opus
  5 rubric audit found verdict ambiguity, endogenous false-green comparison,
  insufficient process/filesystem oracle isolation, non-credible checkpoint fields,
  inconsistent rubric tokens, and schema mismatches.
- **Hypothesis:** a verdict-aware K=0 contract with fixed controls, dual manifests,
  exact count gates, truthful approvals, and canonical evidence mappings removes the
  ambiguity without expanding product scope.
- **Exact change:** adopted the reconciled supervisor corrections A–G only. Evaluation
  v1 now has one attempt and one seed, delivered-patch BPMR, explicit decision
  correctness, fixed verifier controls, K=0 feedback, process/mount isolation,
  benchmark policy-gate semantics, subordinate delivery hierarchy, canonical tokens,
  schema-oriented indexes, and an excluded current authoring transcript.
- **Evaluation command:** specification consistency checks, YAML parsing, unit tests,
  control preflight, and implementation preflight; no benchmark command was run.
- **Evaluation version:** `eval-v1.0.0` candidate.
- **Evidence:** corrected `docs/PROJECT_SPEC.md`, projections, boundary, templates,
  indexes, and verification outputs from this task.
- **Run IDs:** none; no product or benchmark execution occurred.
- **Review agents:** Codex independent review and Claude Opus 5 read-only rubric audit.
  Claude raw review remains external and is not submission eligible pending scan and
  human review.
- **Decision:** `revised`; candidate awaits explicit human approval.
- **Human checkpoint:** pending; no approval is fabricated or backdated.
- **Next action:** human reviews open decisions, then future implementation starts in
  a new neutral task before Phase 0.5.

Every later iteration must record observed failure, hypothesis, exact change, exact
command/version, before/after evidence, run IDs, agent/model, trajectory IDs,
errors/retries, genuine human checkpoint, decision (`kept`, `revised`, or `removed`),
and next action. Held-out results cannot drive version 1 tuning.

Strongest change, removed experiment, remaining failure, and hot take remain
unclaimed until measured evidence exists.
