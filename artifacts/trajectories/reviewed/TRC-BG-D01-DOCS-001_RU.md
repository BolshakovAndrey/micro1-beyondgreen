# TRC-BG-D01-DOCS-001 — проверенный комплект документационных траекторий

**Статус проверки:** владелец одобрил; ровно три захвата включены в индекс  
**Граница сессии:** `SES-20260830-005`  
**Объём:** только документация TypeScript в точной границе из 42 файлов

## Исходные слои и проверка приватности

Этот проверенный слой охватывает основной успешный JSONL-захват Codex CLI и два
разрешённых владельцем документационных продолжения. Неизменяемые JSONL остаются во
внешнем сыром слое. Сырое содержимое, внешние пути, stderr и неуспешный launcher здесь
не воспроизводятся.

Все три реализации прошли `micro1-safe-trace-review scan-raw`:

| Захват | Байт | SHA-256 | Абсолютные машинные пути | Приватные термины после редактуры путей | Email | Присваивания секретов |
| --- | ---: | --- | ---: | ---: | ---: | ---: |
| `TRC-BG-D01-DOCS-001` | 391943 | `bcdc2cafcf4a9b6b9a25e47a491016b2fe94aa7eec57679ebd5f9aa99a410a29` | 119 | 0 | 0 | 0 |
| `TRC-BG-D01-DOCS-001-CONT-001` | 358576 | `f49e526919a20c02d0b663cc610021a6cf726014dfd5cf83dee785b43ccccc09` | 9 | 0 | 0 | 0 |
| `TRC-BG-D01-DOCS-001-CONT-002` | 269192 | `4c667f5c84363cdb90ea5e395c163a22e189fb85ca25af75b78ee2d2cf63487e` | 1 | 0 | 0 | 0 |

В каждом захвате один приватный термин встречался только внутри машинного пути.
Детерминированная редактура абсолютных путей удаляет эти совпадения, не удаляя
техническое содержание. До такой редактуры сырой слой нельзя включать в подачу.

## Сохранённые ошибки и повторы

1. Первый launcher сочетал несовместимые `--approve-for-me` и
   `--sandbox workspace-write`. JSONL не появился, а захват закрылся на пустом
   входе. Отдельный stderr содержит 206 байт и имеет SHA-256
   `a4579eae6ed0babd16921d1d981b791d153330ecd6040053025a71a3cbab52ab`.
   Это диагностическое доказательство, а не реализационная траектория.
2. Безопасная проверка stderr не нашла приватных терминов, адресов электронной
   почты, присваиваний секретов или абсолютных пользовательских путей. Сырое
   содержимое stderr не читалось и не воспроизводилось.
3. Владелец разрешил один исправленный запуск с ранее доказанной формой аргументов,
   без `--sandbox workspace-write` и `--ignore-user-config`. Основной захват успешно
   завершился.
4. Первый независимый аудит AST — абстрактного синтаксического дерева TypeScript —
   нашёл три действительно недокументированных экспорта и один предположительно
   короткий JSDoc. `CONT-001` исправил эти четыре ограниченные находки.
5. Повторный аудит уже не нашёл пропущенного JSDoc, но сохранил одну находку короткого
   комментария. Владелец разрешил `CONT-002` только для неё.
6. Остаточная находка оказалась ложноположительной: TypeScript представляет JSDoc с
   `{@link ...}` как массив структурированных частей, а первая версия аудита считала
   только строковые комментарии. Исправленный read-only парсер учёл полный текст и
   прошёл без новых изменений исходников.

## Результат документации

- Изменены ровно 42 разрешённых TypeScript-файла; новых TypeScript-путей вне границы
  нет.
- Исправленный аудит насчитал 179 экспортирующих объявлений, ноль пропущенных JSDoc,
  ноль слишком коротких JSDoc и документацию уровня модуля во всех девяти
  приоритетных модулях.
- Документация объясняет архитектуру, экспортируемые контракты, `K=0`, физическую
  изоляцию скрытого эталона, неизменяемость решений, политику процессов, целостность
  автономного воспроизведения и закрытый отказ при неопределённости.
- Намеренных изменений поведения или формы публичного API нет.

## Проверки после захвата и детерминированное согласование

Координатор выполнил проверки после трёх JSONL-захватов. Эти действия являются
локальным доказательством после захвата и не выдаются за действия модели внутри трёх
сырых траекторий.

1. `npm run compile` прошёл.
2. Первый `npm test` остановился на шести ожидаемых несовпадениях хешей исходников:
   комментарии изменили байты файлов, защищённых неизменяемыми манифестами; 50 тестов
   прошли.
3. `d01:manifests:write` и `d01:vertical:manifest:write` обновили только
   детерминированные проекции. Второй `npm test` остановился на шести ожидаемых
   несовпадениях code-owned manifest digests — хешей манифестов, закреплённых в коде;
   50 тестов прошли.
4. Координатор обновил только ожидаемые привязки хешей arm-visible и verifier-only
   manifests/packages и объединённый digest вертикального манифеста, затем повторно
   создал vertical manifest. Семантика вариантов решения и скрытого эталона не
   менялась.
5. Итоговый `npm test` прошёл. `d01:verify` прошёл, два автономных воспроизведения
   дали одинаковые хеши JSON, HTML и replay, все проверки манифестов прошли, а
   `d01:hash` вернул
   `da73beb0b3b5aba72013a90388cbf457614479f9b8f02125b99f598b7487f69f`.
6. Вертикальный манифест охватывает 37 файлов и имеет package SHA-256
   `47edb457f93a1d84bf9ebb84ec22eb17b11b94463eadab37c1cf92c169344440`.
7. Контрольные суммы охватывают 235 файлов. Контрольная предварительная проверка
   вернула `READY_FOR_CLEAN_BRANCH`, реализационная — `READY_FOR_IMPLEMENTATION` с
   ожидаемым предупреждением о грязном дереве. Четыре прежних бинарных файла всё ещё
   требуют проверки человеком.
8. `git diff --check`, сравнение 42 путей и структурная проверка SES-005 прошли.

## Решение проверки

Владелец одобрил ровно эти три захвата как допустимые документационные доказательства
и разрешил однократно включить их в `actual_traces`. После детерминированной
редактуры путей технический смысл сохраняется; редактура остаётся обязательной перед
подачей. Согласование хешей после захвата остаётся отдельно классифицированным
доказательством координатора. Официальный или оцениваемый запуск, BG-D02+, Claude,
Chromium, commit и push не выполнялись.
