# TRC-BG-D01-SCALE-003 — проверенная траектория реализации

## Источник и границы проверки

- Источник — выбранный владельцем штатный экспорт Codex Desktop
  `Copy as Markdown`.
- Неизменяемый внешний захват: 49 485 байт, SHA-256
  `0fe4e5b8be4ffff71b849bda538e4ecc6d2d44b94a9dcfdf423b1209855d4400`.
- Байты из буфера точно совпали с квитанцией и до смысловой проверки прошли строгий
  побайтовый круговой тест UTF-8.
- Сканирование исходного захвата нашло два абсолютных машинных пути и не нашло
  приватных терминов, email или присваиваний секретов. Машинные пути категориально
  исключены из версии для отправки; их значения и исходное содержимое не раскрыты.
- Этот файл — хронологическая проверенная проекция, а не заявление о дословном
  штатном слое. Скрытые рассуждения, внутренние события Codex Desktop и полный вывод
  оболочки не заявляются.

## Разрешение и ограниченная цель

До чтения продуктового кода задача создала границу `SES-20260830-003`. Владелец
разрешил одну допустимую траекторию только для четырёх подтверждённых блокеров D01:
общего пути движка, проверяемых доказательств прав, рекурсивной привязки пакета и
согласования текущих проекций. Одобрение сохранило `SCALE_READY=false` и запретило
официальный или оцениваемый запуск, живую модель, Claude, Chromium, BG-D02, изменение
методики или замороженных входов, commit и push.

## Хронология и выполненные изменения

### Общий путь исполнения

Настоящие оркестратор, процессы групп, наблюдателя и оценщика, повторное
воспроизведение и отчёт теперь проходят через один внедряемый диспетчер
`FixtureEngineBindings`. D01 остаётся единственным реальным корнем композиции только
из данных. `BoardObservation` явно передаётся за пределами общего слоя схем.
Описатель `TEST-ONLY`, существующий только в тестах, проходит настоящий конвейер и
проверяет внедрённые схемы, проверки, рассуждение, выбор оценщика, воспроизведение и
отчёт, не создавая BG-D02 или другой оцениваемой фикстуры.

### Проверяемые доказательства прав

Запуск и идентичность рабочего процесса связаны хешами. Правила песочницы и
разрешённых путей вычисляются повторно в каноническом виде. Роль, профиль, слот
исполнения и доказательство должны образовывать точное взаимно-однозначное
соответствие. Для каждой группы проверяется строгий автомат состояний событий, а
события оценщика связаны с неизменяемым хешем решения своей группы. Автономное
воспроизведение независимо пересчитывает `reasonCorrectReject` и
`oracleAcceptedCandidate`. Пропуск, изменение, противоречие, перестановка или
смешивание доказательств разных процессов приводит к закрытому отказу.

### Рекурсивная привязка пакета

Нормализованное рекурсивное сравнение теперь принимает корректные вложенные
зарегистрированные структуры. Прямой отрицательный тест меняет байты уже
зарегистрированного вложенного исходника и подтверждает закрытый отказ. Незаявленные
файлы, символические ссылки, выход за корень, дубликаты, несовпадение исходника и
подмена manifest по-прежнему отклоняются.

### Согласование проекций

Согласованы только проекции текущего состояния и новые добавочные записи итерации.
Пять видимых утверждений, байты кандидатов D01, фиксация поведения, скрытый эталон,
методика оценки, цели и знаменатели не менялись.

## Сбои, решения владельца и повторы

1. Первая компиляция остановилась с `TS2688`: зависимости из неизменённого lockfile
   не были установлены. Владелец разрешил `npm ci` из этого lockfile и ровно один
   повтор компиляции. Установлено 54 пакета, уязвимостей не найдено.
2. Повтор остановился на пяти ограниченных ошибках TypeScript: трёх ошибках
   `executionSlot` и двух циклических ошибках вывода типа `D01_ENGINE`. Владелец
   разрешил исправить только их и один раз повторить компиляцию; она прошла.
3. Первый захват вывода тестов был неполным. Неизменённый повтор дал достоверные
   `51/55`: четыре сбоя `Candidate observation evidence is invalid`.
4. Ограниченная диагностика показала, что наблюдатель использовал первую группу
   описателя вместо группы неизменяемого решения. Владелец разрешил только эту
   правку и по одному запуску компиляции и тестов. Оба прошли. После этого перед
   целевой проверкой механически пересчитан вертикальный manifest.
5. Безопасные обёртки сначала закрыто отказали, потому что делегированный worktree
   не являлся разрешённым корнем. Владелец разрешил перенос; координатор сохранил
   исходное состояние checkout в именованном stash, исправил только классификацию
   управляющей задачи и повторил разрешённые проверки в правильном корне.
6. Первое сравнение буфера корректно отклонило изменённый буфер размером 171 байт.
   Владелец снова скопировал штатный экспорт задачи без повторного raw-захвата.
   Второе сравнение совпало с квитанцией и прошло строгий UTF-8. Большой проверенный
   текст превысил окно вывода координатора, поэтому смысловая проверка завершена по
   исходной задаче через приложение Codex без создания ещё одной raw-копии в
   репозитории.

## Доказательства проверки

- `npm run compile` — успешно.
- `npm test` — `55/55`, итог `TASK_PASSED test:all`.
- `npm run task -- d01:verify` — видимые проверки `5/5`, шаг `1/1`, oracle `3/3`,
  физическая граница `4/4`, вертикальный набор `19/19`, неоцениваемая сквозная
  проверка, неизменяемые manifest и вертикальный manifest из 30 файлов — успешно.
- SHA-256 вертикального manifest:
  `d6e199439b41b0edd5c842bcdcf7f8c386863b37cf0e00f737abfef62c3eae5b`.
- SHA-256 JSON:
  `e68895a8ae55374173ab4dc7462bdffcf820abaa082f01efb0cb2c32cb9a7e70`.
- SHA-256 статического HTML:
  `458e09c1589c013a5753c17e2a215b76b9a2b8aabb6e220bef39acd1a4e014ff`.
- Безопасная контрольная проверка — `READY_FOR_CLEAN_BRANCH`.
- Безопасная проверка реализации — `READY_FOR_IMPLEMENTATION`; остались только
  ожидаемое предупреждение о незаписанном дереве и четыре уведомления о ручной
  проверке бинарных материалов.
- Контрольные суммы до добавления этой версии — `196/196`, после согласования —
  `200/200`; `git diff --check` прошёл.
- Загрязнений не найдено.

## Текущий статус и объём одобрения

Владелец одобрил технически проверенную и безопасную по приватности траекторию как
допустимое доказательство реализации; она включена в `actual_traces`. Допустимость
траектории не равна готовности к масштабированию: отдельная приёмочная матрица Codex
остаётся следующим гейтом, `SCALE_READY=false`.

Одобрение разрешило только включить `TRC-BG-D01-SCALE-003` в `actual_traces`. Оно не
разрешило
масштабирование, BG-D02, официальный или оцениваемый запуск, живую модель, Claude,
Chromium, изменение методики или замороженных входов, commit либо push.
