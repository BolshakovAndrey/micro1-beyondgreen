# TRC-BG-D01-SCALE-002 — проверенная траектория реализации

## Источник и границы проверки

- Источник — штатный видимый владельцу экспорт Codex Desktop `Copy as Markdown`.
- Неизменяемый внешний захват: 43 616 байт, SHA-256 `8d60978fcfce04f7c6fe3fa777d5b438a62347328c482da85f60993e48c3247a`.
- Очищенный штатный слой: `artifacts/trajectories/reviewed/TRC-BG-D01-SCALE-002-NATIVE.md`, 43 540 байт, 571 строка, SHA-256 `ba3b29f14abe3ea0524b711d15b390b5fabb63a28583334163e4da80e3d24444`.
- Выполнены ровно две замены машинно-зависимого корня репозитория на `<repository-root>`. Ни одно техническое действие, результат, повтор, решение или указание владельца не удалены.
- До замены clipboard-байты точно совпали с внешней квитанцией и прошли строгую проверку UTF-8.
- Скрытые рассуждения, недоступные внутренние события приложения и полный вывод оболочки не заявляются.

## Сохранённая хронология и решения

До чтения кода задача полностью прочитала локальные контракты и ожидающую одобрения границу `SES-20260830-002`. Безопасные обёртки подтвердили разрешённый корень, ветку `impl/beyondgreen`, исходный HEAD `f0418bc6051002d8d42ccf84822d2e1f2e25b02e`, идентификатор траектории, внешние границы сканера и хранилища, а также результаты `READY_FOR_CLEAN_BRANCH` и `READY_FOR_IMPLEMENTATION`. Владелец разрешил только перечисленные исправления движка, доказательств прав, полноты каталога, отрицательных тестов и проекций.

Траектория реализации была создана до первой инспекции кода. Инспекция подтвердила три ограниченные проблемы: специализация D01 оставалась распределена по движку; утверждения о правах создавал родительский процесс; привязка пакета доверяла списку manifest и не перечисляла каталог.

### Полная привязка каталога

`PackageBinding` теперь содержит разрешённый корень. Проверка рекурсивно перечисляет его и до выполнения решения отклоняет незаявленные файлы, символические ссылки, неподдерживаемые элементы, выход за корень, дубликаты, несовпадение исходника и подмену manifest. Целевой набор прошёл `1/1`.

### Доказательства прав от runner и процессов

Runner сохраняет доказательства фактически применённого профиля macOS sandbox, модели разрешений Node, хеша разрешённых путей чтения, запрета сети, отсутствия унаследованного `PATH` и завершения дочернего процесса. Каждый вариант, наблюдатель и оценщик сохраняет хеш идентичности процесса и выполняет взаимные проверки разрешённого и запрещённого чтения. Общий хеш доказательства проходит через окончательные решения, наблюдения, вход и результат оценщика, события, JSON/HTML и автономное воспроизведение.

Отсутствующее доказательство, изменённый хеш, противоречивый доступ, повтор идентичности процесса, отсутствующее событие, преждевременный запуск оценщика и недетерминированные наблюдения приводят к безопасному отказу. Первый целевой запуск после усиления схем прошёл `18/19`, поскольку старый синтетический helper не создавал обязательное доказательство процесса. Helper исправили без изменения поведения продукта; повтор прошёл `19/19`.

### Движок, управляемый описателем

Описатель теперь задаёт идентификаторы вариантов, категории риска, размеры наблюдений и типы записей воспроизведения. Единый реестр и план выполнения получают схемы, проверки вариантов, механизм рассуждения, оценщик, воспроизведение и отчёт через внедряемые контракты. D01 остаётся единственным реальным описателем. Общность доказана вариантом `TEST-ONLY` только в памяти, с несвязанными идентификаторами и другими размерами. BG-D02, новый candidate, oracle, описание поведения или manifest не создавались.

Первое выделение фабрики схем расширило точные идентификаторы кандидатов до общего `string` и вызвало 15 ошибок компиляции. Итоговое исправление сохранило точный union D01 и общий характер фабрики. Повторная компиляция прошла.

### Согласование проекций и проверок

Публичный invariant, пять видимых утверждений, байты кандидатов, oracle, фиксация поведения и методика оценки не менялись. Согласованы происхождение, область компиляции, инструкция воспроизведения, журнал улучшений и английская/русская проекции. Ограничение валидности конструкции раскрыто явно: публичный invariant называет заложенное семейство дефекта и не доказывает обнаружение неизвестного скрытого дефекта.

Первая каноническая проверка прошла все кодовые и поведенческие этапы и остановилась только на устаревшем vertical manifest. Разрешённая сверка расширила его до 30 файлов; повторная проверка прошла полностью.

## Доказательства проверки

- `npm run compile` — успешно.
- `npm test` — `55/55`.
- D01: видимые проверки `5/5`, контракт шага `1/1`, oracle `3/3`, физическая граница `4/4`, вертикальные контракты `19/19`, неоцениваемая сквозная проверка, четыре неизменяемых package manifest и vertical manifest из 30 файлов — успешно.
- SHA-256 vertical manifest: `6be3ea9671143f2df228102a348df5e0f41d3570535f510d7eec4af331282739`.
- SHA-256 JSON: `db3d64776e38bfbfba6be2c174a68b7c96bd3a4a543fc64f892a74a3551aa074`.
- SHA-256 HTML: `dd34b63a3b63a6faac7596e911c5008ec7a8df349059e255a7e7fc2770adbc87`.
- Автономное воспроизведение — успешно, `reportArtifactsMatched=true`.
- Безопасная проверка Phase 0.5 прошла без живой модели и браузера.
- Контрольные суммы до захвата — `187/187`; `git diff --check` прошёл.
- Безопасные preflight: `READY_FOR_CLEAN_BRANCH` и `READY_FOR_IMPLEMENTATION`; загрязнений нет, остаются ожидаемое предупреждение о dirty tree и четыре уведомления о ручной проверке бинарных материалов.

## Проверка захвата и текущий статус

До захвата владелец отменил дополнительную промежуточную проверку Claude; Claude не вызывался. Затем владелец явно подтвердил `Copy as Markdown` и разрешил только безопасный захват и последующую проверку. Raw scan обнаружил одно совпадение приватного термина, полностью ограниченное двумя машинными путями. После двух категориальных замен совпадений приватных терминов нет; email и присваивания секретов не найдены. Все 571 строка очищенного штатного слоя проверены: одобрение, хронология, ошибки, повторы, команды, доказательства, решения и итоговая остановка сохранены.

Владелец одобрил эту технически проверенную и безопасную по приватности траекторию как допустимое доказательство реализации; она включена в `actual_traces`. Допустимость траектории не означает готовность к масштабированию. `SCALE_READY=false`; отдельная координационная задача отвечает за приёмочную матрицу Codex и возможное явное разрешение масштабирования. Официального или оцениваемого запуска, живой модели продукта, Chromium, BG-D02, изменения методики, кандидатов, oracle или фиксации поведения, commit и push не было.

После захвата контрольные суммы прошли `192/192`; безопасные control и implementation preflight повторно вернули `READY_FOR_CLEAN_BRANCH` и `READY_FOR_IMPLEMENTATION`, загрязнений не найдено.
