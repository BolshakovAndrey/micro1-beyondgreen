# TRC-BG-D01-RECOVERY-001 — reviewed source-derived trajectory

Status: pending; not eligible and not indexed in `actual_traces`.

This document is a privacy-reviewed, source-derived summary of the SES-007 native
Codex Desktop Markdown export. It does not include or claim hidden reasoning,
internal application events, post-capture approvals, capture-interface operations,
or later review decisions. Those later events are recorded separately in
`artifacts/trajectories/reviews/TRC-BG-D01-RECOVERY-001-POST-CAPTURE.yaml`.

## Instruction and boundary

The repository owner authorized a fresh SES-007 clean-recovery task from baseline
`f0418bc6051002d8d42ccf84822d2e1f2e25b02e`. SES-005 and SES-006 were excluded
under the recorded categories. Only the public package install required by
`npm ci` was permitted outside the clean root; commit and push remained prohibited.
The complete task packet is preserved in
`artifacts/trajectories/instructions/TRC-BG-D01-RECOVERY-001.md`, and the factual
boundary is `artifacts/trajectories/session-boundaries/SES-20260829-007.yaml`.

## Representative source-derived sequence

1. The agent read the mandatory repository contracts and current product
   projections, then treated the repo-local D01 draft as untrusted input.
2. The first Git-state inspection unnecessarily exposed machine-specific worktree
   metadata. No external worktree content was read or used. The owner later approved
   categorical removal of those values while preserving the incident itself.
3. The agent recorded the SES-005 and SES-006 exclusions without importing either
   session and without recording external names or paths.
4. The draft was audited for deadline independence, symbol-based risk analysis,
   typed decision reasons, evaluator integrity, immutable evidence binding, and
   deterministic offline report reproduction. The implementation was rewritten
   where the audit found material gaps.
5. Compilation required two code-only correction cycles. The later native export
   preserves the existence and ordering of those cycles, but it does not provide
   enough source-visible detail to support the previously stated exact compiler
   diagnoses; those diagnoses are therefore omitted here.
6. Contract verification and evidence generation ran after the rewrite. One
   unchanged repository-local generator was rerun after the combined command did not
   expose its final completion marker. This was not a model retry, official scored
   attempt, or evaluator-feedback round.
7. Two unchanged offline replays reproduced matching JSON, HTML, and replay
   evidence. The export preserves the successful replay outcome without serving as
   the sole authority for every submitted digest.
8. The final repo-local validation shown by the export includes successful compile,
   ordinary tests, the complete unscored D01 verification path, safe Phase 0.5
   verification without live execution, document validation, and
   `git diff --check`.
9. The first repo-local private-denylist preflight stopped before scanning because
   the required owner-controlled input was unavailable under the task boundary. No
   fabricated denylist, bypass, or forbidden retry occurred.
10. After explicit owner checkpoints, the work was handed to the authorized
    repository root and the owner-controlled control and implementation preflights
    completed without contamination findings.

## Outcome represented by the source

The unscored D01 demonstration keeps both candidate hashes unchanged. The status-quo
arm accepts the visible false green; BeyondGreen rejects it. Both arms accept the
behavior-preserving candidate. The independent evaluator runs only after immutable
arm decisions, and pre-decision evaluator feedback remains `K=0`. Model, browser,
Chromium, Claude, connected-app, and private-MCP calls are absent from the D01 run;
commit and push were not performed.

The source-derived trajectory preserves the main instruction, action, failure,
retry, checkpoint, verification, and outcome sequence. It deliberately does not
assert the exact compiler error causes, exact package-install count, exact generator
wait duration, exact typed reason code, native-capture metadata, or final eligibility
promotion because the comparison did not establish those statements from the exact
immutable source capture.

## Current review limitation

A later user-visible export gave high semantic coverage of the trajectory but did
not match the byte count or digest of the externally anchored immutable capture.
Consequently, this reviewed layer is aligned to the material that was actually
available for semantic comparison, but technical-meaning preservation relative to
the exact immutable capture remains unproven. The trace stays pending and outside
`actual_traces`.
