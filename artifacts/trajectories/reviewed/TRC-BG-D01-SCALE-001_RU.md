# TRC-BG-D01-SCALE-001 — исправления готовности D01 к масштабированию

Статус: владелец одобрил допустимую траекторию реализации, и она включена в
`actual_traces`; повторный независимый checkpoint вернул `PASS_WITH_CONCERNS` и
`SCALE_READY=no`; для дальнейших исправлений всё ещё нужна новая одобренная граница.

## Классификация захвата

- Источник: штатный видимый пользователю экспорт Codex Desktop `Copy as Markdown`.
- Размер неизменяемого источника: 56 127 байт.
- SHA-256:
  `605d014dd5e631d7be4b9d746eee222ebc9bd5acaa679a0e2e9e71e36bcddfb0`.
- Исходник хранится вне репозитория; его путь не раскрыт.
- Безопасный scanner исходника не нашёл приватных терминов, адресов электронной
  почты или присваиваний секретов. Он нашёл два абсолютных пользовательских пути;
  здесь они категориально заменены относительными путями репозитория.
- Экспорт представляет видимую пользователю хронологию. Скрытые рассуждения,
  внутренние события приложения и пропущенный интерфейсом полный stdout не заявляются.

## Назначение и разрешение

Владелец одобрил `SES-20260830-001` только для перечисленных исправлений D01.
Сессия унаследовала ранее проверенное изменённое рабочее дерево на ветке
`impl/beyondgreen` с HEAD
`f0418bc6051002d8d42ccf84822d2e1f2e25b02e`.

Были запрещены официальные и оцениваемые запуски, вызовы живой продуктовой модели,
Chromium, BG-D02 и другие сценарии, commit, push и внешний доступ, кроме разрешённых
интерфейсов приватного scanner и raw-trace. Решение о live adapter — подключении
живой модели — и любые изменения межплатформенной изоляции с новой зависимостью,
контейнером, политикой среды выполнения или изменением этапа 0.5 оставлены для
отдельного решения владельца.

## 1. Граница сессии и trace-first gate

До изменения продукта агент прочитал контракты репозитория, создал и проверил
`SES-20260830-001`, подтвердил относительный корень, ветку, HEAD и унаследованное
состояние, затем выполнил разрешённые проверки scanner и trace-interface. Результаты:
`READY_FOR_CLEAN_BRANCH` и `READY_FOR_IMPLEMENTATION`, ноль загрязнений, ожидаемое
предупреждение об изменённом рабочем дереве и четыре существующих уведомления о
ручной проверке бинарных материалов.

Первая попытка разобрать YAML границы не нашла установленный локальный YAML-модуль.
Никакая зависимость не устанавливалась и сеть не использовалась; вместо этого были
выполнены ограниченные текстовые и репозиторные проверки. После этого владелец явно
одобрил точную границу и объём исправлений.

## 2. Решения реализации

Траектория содержит двенадцать связанных решений. Подробная карта требований,
исходников, тестов и команд находится в
`artifacts/trajectories/reviews/TRC-BG-D01-SCALE-001-DECISION-EVIDENCE.md`.

1. `FixtureDescriptor` стал владельцем значений D01: сценария, кандидатов, путей,
   схем, запуска и привязки пакетов.
2. `ProbePlan` выводится только из видимых инвариантов и инвентаризации рисков;
   отдельно записано `seededDefectKnowledgeUsed=false`.
3. Родительский процесс, принимающий решения, не проверяет verifier-only пакет до
   завершения решений обоих вариантов.
4. Автономное воспроизведение проверяет финальные решения и заново вычисляет связь
   оценщика с решением, парой наблюдений и verifier-manifest.
5. EV-009 содержит измеренные моменты начала и окончания, монотонную длительность,
   принадлежность сценария и признак ложной тревоги.
6. Утверждения о порядке выполнения и границе эталона выводятся из измеренных событий
   и отвергаются при пропуске, перестановке или несовместимых правах.
7. Каждый вариант дважды наблюдается после решения в отдельных процессах; расхождение
   закрывает выполнение с ошибкой до запуска оценщика.
8. Обратный физический тест доказывает: оценщик видит эталон, но не может перечислить,
   проверить или прочитать кандидатов.
9. Метаданные, хеши файлов и хеш arm-visible пакета проверяются по значениям,
   принадлежащим коду, и в родителе, и в процессе варианта.
10. Пять видимых утверждений имеют один исполняемый источник для visible tests и
    arm checks.
11. JSON и HTML показывают ограниченный контрпример: не более пяти элементов и число
    пропущенных элементов.
12. Инструкция воспроизведения, происхождение, роли трасс, область компиляции,
    предварительная проверка, тема, спецификация, оценивание, журнал и контрольные
    суммы теперь отличают старую допустимую проверку от новой ожидающей трассы
    реализации.

Байты кандидатов, пять видимых ожиданий поведения и результаты ground truth —
эталонной истины — не менялись. Изоляция macOS через `sandbox-exec` по-прежнему
закрывается с ошибкой на неподдерживаемой платформе; ослабленный режим с недоказанным
`K=0` не добавлен.

## 3. Повторы и отрицательные результаты

- Комбинированный patch с удалением и добавлением одного файла был отклонён до
  записи; изменение разделили на допустимые patch.
- Обновление версии схемы выявило при компиляции два устаревших литерала; их заменили
  значениями из descriptor.
- Первый целевой запуск дал 25/26: регулярное выражение запрета ввода-вывода случайно
  совпало с `deriveExecutionClaims`. Выражение ограничили реальными API выполнения;
  повтор дал 29/29.
- Слишком широкая пробная область компиляции `scripts/**/*.ts` и `tests/**/*.ts`
  включила исторические нетипизированные управляющие утилиты вне продуктовой области
  D-сценариев. Область заменили масштабируемыми продуктовыми шаблонами D-сценариев,
  не меняя зафиксированные утилиты этапа 0.5.
- Первая финальная проверка контрольных сумм правильно обнаружила устаревшую проекцию
  после разрешённых изменений. Штатная `checksums:write` синхронизировала её;
  повтор прошёл 171/171.

Отрицательные тесты покрывают подмену manifest, независимость от заложенного дефекта,
перестановку событий, подмену связи evaluator, недетерминизм наблюдений, обратный
запрет candidate и ограничение размера контрпримера. Они ещё не опровергают
отсутствующие события или независимо измеренные доказательства физических прав;
текущие признаки прав объявляет родительский процесс.

## 4. Локальная проверка

- `npm run compile` — успешно.
- `npm test` — 53 из 53.
- `npm run task -- d01:verify` — успешно: visible 5/5, positive-step 1/1,
  oracle 3/3, физическая граница 4/4, вертикальные контракты 19/19,
  неоцениваемый E2E, четыре базовых manifest и manifest из 24 файлов.
- `npm run task -- d01:replay` — отчёты совпали.
- `npm run task -- phase0.5:verify` — успешно без живой модели и браузера:
  лицензии 54/54 и наборы 11/11, 6/6, 3/3, 4/4, 3/3 и 4/4.
- После разрешённой синхронизации контрольные суммы прошли 171/171.
- `git diff --check` прошёл.
- Финальные безопасные предварительные проверки прошли с нулём загрязнений,
  ожидаемым предупреждением о рабочем дереве и четырьмя уведомлениями о бинарных
  материалах.

Хеши сохранённых доказательств:

- канонический JSON:
  `47035ed40c9ab16473d3a5866bc2071138712d01992a20a617a48bf0e19cd674`;
- HTML:
  `9f552bb43399463afaf8257a265dad9c235343be5d836b1215f0b14b8d0f88ed`;
- автономное воспроизведение:
  `4ba082113fae1e1f97139c26a3c42e807a1530c66777bc980f5a5346c889b9e0`;
- объединённый manifest D01:
  `69894e4ab39484a89d300391317fc4a79d895c8790f8c1f7edb0f1a8ba6fdb92`.

## 5. Захват после реализации

Задача реализации остановилась с `SCALE_READY=false`, потому что трасса и повторный
checkpoint ещё отсутствовали. Владелец отдельно разрешил точный штатный захват,
сверку английской и русской версий и — только после успешной проверки трассы —
read-only checkpoint Claude Opus 5.

Первая интерактивная попытка захвата получила пустой ввод и безопасно завершилась
`ERROR: empty trace payload`; raw-артефакт не был создан. После подтверждения
владельца, что `Copy as Markdown` готов, clipboard был направлен прямо в разрешённый
wrapper без показа или отдельного чтения содержимого. Wrapper закрепил точный источник
из 56 127 байт. Scanner исходника прошёл; нужны только документированные замены двух
машинных путей.

## Итог и ограничения

Реализация и локальная проверка завершены. Байты clipboard точно совпали с внешней
квитанцией, строгая проверка UTF-8 прошла, две категориальные замены путей выполнены
механически, все 729 строк штатного проверенного слоя просмотрены. Этот reviewed-слой сохраняет видимые
инструкции, разрешения, решения реализации, ошибки, повторы, измерения и человеческие
checkpoint, исключая машинные пути. Исключения не меняют техническую причинную цепь.

Повторный checkpoint Claude Opus без изменения файлов вернул
`D01_CHECKPOINT=PASS_WITH_CONCERNS` и `SCALE_READY=no`. Codex подтвердил: физические
права пока объявляются родителем, движок остаётся существенно специализированным для
D01, а привязка видимого пакета не доказывает полноту каталога. Вопрос конструкции
публичного инварианта и межплатформенная изоляция требуют отдельных решений
владельца. Полная сверка находится в
`artifacts/trajectories/reviews/BG-D01-SCALE-CLAUDE-CHECKPOINT_RU.md`.

Владелец одобрил эту проверенную coding-agent trajectory и разрешил включить её в
`actual_traces`. Её допустимость честно фиксирует траекторию реализации вместе с
непройденным гейтом масштабирования; она не означает готовность к масштабированию и
не зависит от будущего checkpoint `PASS/yes`. Для дальнейших изменений продукта или
оценивания нужна новая явно одобренная граница реализации. Официальных или
оцениваемых запусков, живой продуктовой модели, Chromium, BG-D02, commit или push не
было.
