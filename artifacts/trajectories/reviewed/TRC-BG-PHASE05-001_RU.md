# TRC-BG-PHASE05-001 — русская версия отчёта Phase 0.5

**Статус:** безопасная проверка читаемого слоя и проверка владельцем EN/RU прошли.  
**Граница:** `SES-20260829-002`, ветка `impl/beyondgreen`, начальная точка
`2a41d4d2bab8a8b25406400a8ec7d8cd21112a4e`.  
**Происхождение сведений о модели:** `Codex desktop / GPT-5.6 Sol (owner-observed current default; raw export has no model field)`. Задача не задавала модель отдельно
и унаследовала текущую модель приложения. То, что это Sol, сообщил и наблюдал
владелец; машинный raw-export не содержит поля модели, поэтому это не машинно
подтверждённый факт.  
**Область:** только исследование, решения, измерительный прототип и условное закрытие
подготовительного этапа Phase 0.5. Код продукта, оцениваемые примеры, скрытые эталоны,
официальное сравнение, commit и push не выполнялись.

Этот читаемый отчёт собран по фактам и доказательствам внутри репозитория. Задача
упаковки не открывала внешний исходный файл и не узнавала его путь. Внутренние номера
переписки/вызовов и не относящееся к Phase 0.5 вступление исключены.

## 1. Начальная проверка и исследование

Граница чистой работы и обязательные документы были проверены повторно. По открытым
данным зафиксирован набор: Node `22.22.3`, npm `10.9.8`, TypeScript `5.9.3`,
React/ReactDOM `19.2.8`, jsdom `30.0.1`, Zod `4.5.2`, типы Node `22.20.1`, React
`19.2.18`, ReactDOM `19.2.5`, jsdom `30.0.0` и
`@preact/signals-react@3.12.0` с лицензией MIT.

Самостоятельно придуманные классы поведения разделены так: четыре для разработки —
`BG-D01` (устаревший снимок состояния, музейная доска), `BG-D02` (очередь обновлений,
игровой счёт), `BG-D03` (вычисляемое состояние, сборы в поездку), `BG-D04` (очистка,
публичные часы); шесть отложенных — `BG-H01` (сброс по свойствам, викторина),
`BG-H02` (порядок асинхронных действий, астрономия, сложный случай), `BG-H03`
(идентичность, список чтения), `BG-H04` (условный жизненный цикл, погода), `BG-H05`
(внешнее хранилище, единицы измерения), `BG-H06` (откат, тема оформления).

Вариант Responses API с OpenAI SDK и временный выбор `gpt-5.6-terra` ради цены были
рассмотрены, но владелец отклонил их как лишнее усложнение при фиксированной цене
подписки. Остался необязательный живой интерфейс:

```text
codex exec --ephemeral --ignore-user-config --json --output-schema <schema> --sandbox read-only --model gpt-5.6-sol
```

Для будущего случая разрешён один вызов, без автоматического повтора и замены модели,
с общим пределом 180 секунд. Цена отдельного вызова и число токенов записываются как
`not_measured`, если CLI не сообщает их устойчиво. API-ключ, Responses API и SDK не
используются. Обязательный воспроизводимый путь — `offline-replay-jsonl-v1`, то есть
проверка по заранее сохранённым безопасным событиям без сети.

Браузерный протокол использует независимую доску распределения музейных групп: 300
синтетических карточек, одинаковые наблюдаемые результаты двух вариантов, заданные
массовые действия, сначала полная корректность, затем счётчики перерисовок и разница
CDP `TaskDuration`. Зафиксированы 5 прогревов и 30 измерений на вариант с чередованием;
подбирать число карточек после результата запрещено.

При проверке исследовательских документов были только технические повторы: ложные
несовпадения чисел EN/RU из-за локального формата, ошибка разбора списка Git, один
устаревший patch без изменения и отсутствие denylist в обычной оболочке, после чего
разрешённая безопасная команда прошла.

## 2. Разрешение измерительного прототипа

Владелец получил вопрос:

`Одобряете технический Phase 0.5 spike с подписочным codex exec/gpt-5.6-sol без API, одним вызовом без повторов и сценарием на 300 карточек?`

Ответ: `Одобряю spike (Recommended)`. Разрешён только ограниченный Phase 0.5, не код
продукта и не оцениваемые материалы.

`npm install --package-lock-only --ignore-scripts` и `npm ci --ignore-scripts`
прошли. Первая проверка лицензий через `npm query . --json` завершилась ошибкой
`EQUERYNODEPTYPE`; npm напечатал машинный внешний путь. Его не открывали и в этот
отчёт не переносили.

## 3. Лицензии

Владелец разрешил единственный исправленный повтор локальным сканером:

`Разрешаете одну исправленную попытку transitive-license audit через локальный Node-сканер, после которой spike продолжится только при полном PASS?`

Ответ: `Разрешаю retry (Recommended)`.

Сканер читает только `package-lock.json` и локальные `package.json`. Тесты нашли две
ошибки самого сканера (`SEE LICENSE` и ESM-вспомогательная функция); после исправления
15/15 тестов прошли. Проверено 54 пакета: 51 разрешён, два `MIT-0`, один
`BlueOak-1.0.0`, пропущенных и неизвестных лицензий нет. Проверка правильно
остановилась.

Затем владелец одобрил точное расширение:

`Одобряете MIT-0 и BlueOak-1.0.0 как допустимые permissive-лицензии, обязательный THIRD_PARTY_NOTICES.md и один финальный повтор audit?`

Ответ: `Одобряю и продолжить (Recommended)`.

Добавлены только эти два SPDX-кода. Тесты прошли 16/16, последний audit — 54/54.
Хеш lockfile: `1beb2987e3e0654cb525b80c2f97a2f6577f92399e8a78fcd8eb9f9ab6487d4c`,
инвентаря: `68519a270c035c5cb2c8cb92f6d27b44ed47958a4c31b134bcbdb7f4a364ef18`,
отчёта: `7d4d0477585c61e7179fbb6bdd1ff7bf019083536e93362b7fc12a173595ec92`,
`THIRD_PARTY_NOTICES.md`:
`6345e22330c67f4091526d3b516bf5a2e06c2694e61b057cd05aabfd8a6f35de`.

## 4. Стек, воспроизведение и CLI

Первый тест стека подтвердил десять возможностей, но настройка пыталась изменить
защищённый `globalThis.navigator` в Node 22. Лишняя строка удалена, повтор прошёл
11/11. Тесты адаптера, JSONL и offline replay сначала прошли 4/4, затем 6/6.
Формат `beyondgreen-replay-jsonl@1.0.0` использует UTF-8/LF, RFC 8785 JCS и SHA-256,
не обращается к сети, не запускает процессы и не пишет в рабочие файлы. Приватное и
скрытые эталоны воспроизводить запрещено.

Первый точный живой вызов завершился кодом 1 примерно через 3,93 с с безопасным
классом `MODEL_TRANSPORT_FAILURE`; корректного ответа по схеме не было. Текст ошибки
не сохранялся. Его SHA-256:
`3b93186506b30e5901974d0a6f8f0c3c876309b7920735f66205cef575d0c264`.

Владелец разрешил один безопасный повтор:

`Разрешаете один privacy-safe диагностический повтор той же подписочной команды codex exec без замены модели?`

Ответ: `Разрешаю один retry (Recommended)`.

После тестируемой очистки ошибок та же команда и модель снова завершились кодом 1:
3006 мс, класс `unknown`, SHA-256
`1393963b62d2b0ef4064d9462d967f6b8688db20939a0fcc4b024e7b06ec4ba5`, ноль
совпадений во всех пяти категориях очистки. Сырой текст не сохранялся. Всего было
два живых вызова, автоматических повторов 0, замен модели 0.

Владелец разрешил диагностику без модели:

`Разрешаете non-model диагностику Codex CLI (login status в exact-mode и codex doctor) с сохранением только безопасного класса результата?`

Ответ: `Разрешаю диагностику (Recommended)`.

В `codex-cli 0.148.0` exact-mode status не принял `--ignore-user-config` (код 2),
doctor вернул ошибку транспорта/DNS (код 1), usage — `unknown` (код 1). Модель не
запускалась. Последний doctor дал `DNS_RESOLUTION`, код 1, 1883 мс; SHA-256 stdout
`0f66f123b52a301b10fb72b164351966e806c7aad07f8cb7ba4d86c42a4d0035`, пустого
stderr `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, общий
`94d219b818416f92b7b06b4bb7a7443d984ea3e7b84b405343179e5ac7e6cacf`. До
классификации заменены 14 путей и один идентификатор аккаунта. Безопасное имя хоста
извлечь не удалось; сырой вывод не сохранялся.

Проверка без учётных данных для `chatgpt.com`, `auth.openai.com`, `api.openai.com`
показала работающие DNS/TLS и HTTP `4xx` быстрее 250 мс для всех трёх. IP, заголовки,
cookie, тело ответа, ключи и пути URL не сохранялись. Причину двух ошибок модели эта
проверка не доказала.

## 5. Отказ от вложенной проверки и Chromium

Владелец решил:

`Я не вижу смысла в этом запуске, мы и так работаем через Sol сейчас, когда я отправляю эти запросы. Возможно, это не работает при вызове из приложения ChatGPT на Mac, но это и не важно, потому что запросы проходят и ответы приходят. Я хочу скипнуть эту проверку и, если других препятствий нет, закрыть эту часть`.

Это отменяет только проверку вложенного CLI в среде desktop. Две ошибки не стали
успехом. Наблюдение владельца о работе Sol в приложении — контекст, а не доказательство
для отправки. `codex exec` остаётся необязательным интерфейсом обычного терминала и
не проверен здесь; проверенный воспроизводимый путь — offline replay.

Chromium-прототип прошёл с неизменными N=300. Первый успешный запуск сохранил только
сводку и не сохранил отдельные измерения. Один повтор инструмента сохранил все 60
измерений без изменения N, действий, порядка, прогревов или числа повторений. Вместе
с прогревами в двух запусках было 140 образцов; настройки после результата не меняли.

В `Chrome/152.0.7977.64` совпали все 300 значений, выбранные идентификаторы, журнал
действий, сброс и наблюдаемые результаты; ошибок console и необработанных исключений
0. Выполнены 5 прогревов и 30 измерений на вариант. SHA-256 файла:
`688baad33e10966944f8d73dd09e03af1f7aa4301cd2d2c66d7efe1b03bf6b41`,
наблюдений: `064dc4a20841c306c1fff12c94fb2a6c8a12f510e6154e8c0f70b4322aef01a8`,
журнала: `5273d540115aa4e44a4d47738a0b5537ef78450f5e042132d6fd2310de83cabf`.
Перерисовок карточек: 2400 против 1900, то есть на 20,83% меньше только в этом
синтетическом прототипе. Средняя `TaskDuration`: 4,7071 мс против 5,0037 мс, поэтому
улучшение CPU не доказано и не заявляется.

Два временных профиля Chromium сначала остались из-за слишком ранней очистки.
Широкая разрушительная команда была отклонена до выполнения; два точных локальных
каталога удалены узко и без чтения. Теперь процесс ожидает завершения, тест это
проверяет, остаточных профилей 0.

## 6. Условное закрытие и проверки

Координатор подтвердил 60 измерений, 30 на вариант, неизменные порядок/N, совпадение
корректности и приведённые числа. Условие владельца было выполнено, и Phase 0.5
зафиксирован `2026-08-29T13:41:51Z` с явным исключением вложенного CLI. Это не
разрешило разработку продукта.

Прошли: основные тесты 16/16; стек 11/11; модель/offline 6/6; приватность CLI 3/3,
doctor 4/4, сеть 3/3; Chromium 4/4; лицензии 54/54; YAML/Markdown/EN-RU/приватность;
`git diff --check`; 74 контрольные суммы; безопасные preflight без загрязнений.
Implementation-preflight показал структурную готовность вместе с ожидаемым
предупреждением о незаписанных Git-изменениях и четырьмя бинарными файлами. Это не
человеческое разрешение.

Отдельно зафиксированы три повтора только валидаторов: исправленный вызов Ruby YAML,
учёт запятой в русских десятичных числах и более точное правило поиска секретов.
В русскую карточку добавили точные версии. Доказательства и выводы не менялись.

## 7. Очистка для отправки и текущий запрет

Переданные координатором сведения о неизменяемом сыром файле:

- размер `3511076` байт;
- SHA-256 `031477de3fd7e42591ba5e7c1820ac39cb2dd1b691dbae29ae850a2aee895b86`;
- абсолютных пользовательских путей: 667;
- приватных совпадений до замены путей: 2;
- приватных совпадений после замены путей: 1;
- адресов электронной почты: 0;
- присваиваний секретов: 0;
- автоматическая проверка сырого файла: `failed`.

Все машинные пути заменены относительными путями репозитория или
`<REDACTED_MACHINE_PATH>`. Единственное оставшееся приватное совпадение исключено:
оно не копировалось, не называлось, не описывалось и не угадывалось. Также удалены
внутренние номера переписки/инструментов и вступление до Phase 0.5. При этом в отчёте
остались все подтверждённые репозиторием решения, категории команд, исходы, повторы,
согласования, хеши и измерения. Но исходная проверка не прошла, а контекст оставшегося
совпадения этой задаче недоступен. Читаемый слой прошёл real-denylist control с нулём
приватных и машинных/внутренних находок; остались четыре предупреждения о бинарных
официальных файлах. После этого владелец подтвердил точность и безопасность обеих
версий и сохранение технического смысла после описанных исключений:
`technical_meaning_preserved=true`. Исходный raw scan остаётся `failed`; его числа и
ограничения не переименованы в успех.

Владельцу был показан точный вопрос:

`Подтверждаете русскую и английскую версии TRC-BG-PHASE05-001 как точный и безопасный отчёт, в котором после указанных исключений сохранён технический смысл?`

Выбран ответ `Подтверждаю одобрение (Recommended)`. Это разрешает только перенос
траектории в утверждённый список, но не код продукта, оцениваемые материалы, запуск
модели или браузера, commit либо push.

Проверка этого отчёта не разрешает код продукта, оцениваемые примеры/эталоны,
официальное сравнение, повтор модели/Chromium, commit или push.
