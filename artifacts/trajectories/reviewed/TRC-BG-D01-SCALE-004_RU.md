# TRC-BG-D01-SCALE-004 — проверенный комплект траекторий реализации

**Статус проверки:** владелец одобрил комплект; все восемь захватов включены в индекс  
**Граница сессии:** `SES-20260830-004`  
**SCALE_READY:** `true`

## Объём и исходные слои

Этот проверенный слой охватывает основную JSONL-траекторию Codex SCALE-004 и семь
отдельно разрешённых владельцем продолжений. Неизменяемые исходные JSONL-захваты
стандартного вывода находятся вне репозитория. Захваты потока ошибок и попытки
launcher, завершившиеся до работы модели, учтены отдельно и не считаются
доказательством реализации. Необработанное содержимое и внешние пути здесь не
воспроизводятся.

Все восемь захватов реализации прошли `micro1-safe-trace-review scan-raw`. Перед
подачей из каждого требуется детерминированно удалить абсолютные машинные пути.
После такой категориальной редактуры совпадений с приватными терминами нет; адреса
электронной почты и присваивания секретов также не обнаружены.

## Полная хронология

1. Основная траектория сделала настоящий оркестратор и общий runtime рабочих
   процессов параметризованными через `FixtureEngineBindings`, добавила точные
   политики слотов, связи доказательств и обязательные отрицательные тесты. Первая
   компиляция остановилась на семи ограниченных ошибках типов общего runtime.
2. `CONT-001` исправила эти семь ошибок и остановилась, потому что в arm-visible части
   TEST-ONLY описателя не хватало четырёх полей привязки пакета.
3. `CONT-002` добавила только эти поля и остановилась на таком же несоответствии
   verifier-only части.
4. `CONT-003` добавила только четыре verifier-only поля; компиляция прошла.
5. `CONT-004` выполнила один обычный тестовый гейт во вложенной среде и остановилась
   из-за недоступности изоляции дочерних процессов.
6. `CONT-005` отделила композицию arm и observer от verifier-only импортов, оставив
   подключение oracle только оценщику. Компиляция обнаружила одну ошибку: параметру
   локальной функции композиции оценщика требовался явный тип.
7. `CONT-006` исправила только этот тип. Компиляция прошла, но вложенная целевая
   проверка остановилась, поскольку вложенная песочница не могла применить системную
   песочницу.
8. Затем отдельно разрешённые прямые проверки в Codex Desktop подтвердили ту же
   границу ролей `5/5`, обычные тесты `56/56`, полный `d01:verify` и автономное
   воспроизведение.
9. `CONT-007` детерминированно записала и проверила manifest вертикального среза из
   37 файлов.
10. Разрешённая владельцем регенерация неоцениваемых доказательств изменила только
    JSON и HTML; JSONL воспроизведения уже побайтно совпадал. Последующее автономное
    воспроизведение прошло и точно восстановило оба отчётных артефакта.

## Технический результат

- A1: настоящий оркестратор и общие функции рабочих процессов принимают внедряемый
  движок; D01 entrypoint остаются тонкой обвязкой композиции.
- A2: TEST-ONLY проходит настоящий transport процессов, рабочих процессов, оценщик,
  воспроизведение и построение отчёта.
- S1: запуск и replay сравнивают точную каноническую политику песочницы и разрешённых
  путей для каждого слота.
- S2: наблюдения и ответы оценщика позиционно связаны с доказательством, ролью,
  профилем и слотом; межролевая подмена закрыто отклоняется.
- S3: хеш события каждой группы связан с её фактическим неизменяемым решением;
  внутренне согласованный произвольный хеш закрыто отклоняется.
- A3, S4, P1, P2 и D1 сохранены.

## Проверки

- Компиляция прошла.
- Обычные тесты прошли `56/56`.
- Полный `d01:verify` прошёл: видимые тесты `5/5`, контракт шага `1/1`, oracle
  `3/3`, физическая граница и граница ролей `5/5`, вертикальные контракты `19/19`,
  сквозная проверка и manifests.
- `d01:replay` прошёл с точным совпадением отчётных артефактов.
- Проверка этапа 0.5 прошла `4/4`.

## Решение

Владелец одобрил основную трассу и все семь продолжений как допустимые доказательства
реализации, разрешил включить восемь записей в `actual_traces` и отдельным явным
решением установил `SCALE_READY=true`. Перед подачей остаётся обязательной
детерминированная редактура машинных путей. Официального или оцениваемого запуска,
BG-D02, Claude, Chromium, изменений зависимостей или методики, commit и push не было.
