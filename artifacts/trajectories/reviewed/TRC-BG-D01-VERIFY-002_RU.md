# TRC-BG-D01-VERIFY-002 — ограниченная независимая проверка D01

Статус: ожидает одобрения владельца репозитория; пока не включена в
`actual_traces`, то есть в список допущенных к отправке трасс.

## Что именно было сохранено

- Источник: нативная пользовательская выгрузка Codex Desktop
  `Copy as Markdown` («Копировать как Markdown»).
- Размер неизменённого источника: 24 318 байт.
- SHA-256 источника:
  `6cb44af2ae43b03035fa41044dc2f2bdf9ee54d009ccc699205c2da420e32305`.
- Независимая проверка подтвердила точное совпадение байтов и строгую корректность
  UTF-8.
- Выгрузка сохраняет видимую хронологию беседы и видимые карточки инструментов. Она
  не содержит скрытых рассуждений модели, полного вывода всех команд и внутренних
  событий приложения; наличие этих данных не заявляется.

Квитанции захвата, транспортные повторы и действия review — независимой проверки
трассы — произошли уже после завершения исходной задачи и вынесены в отдельную
запись происхождения.

## Цель и ограничения задачи

Владелец репозитория поручил независимо и в строго ограниченном объёме проверить
существующую неоцениваемую реализацию BG-D01, путь воспроизведения, целостность
доказательств и изоляцию скрытого эталона.

В исходной задаче были запрещены:

- официальный или оцениваемый запуск;
- доступ к другим репозиториям и рабочим каталогам;
- использование браузера, подключённых приложений, MCP и приватного состояния;
- чтение общей памяти и внешних путей;
- запуск Claude, Codex CLI, живой модели, Chromium и сетевой диагностики;
- commit и push;
- изменение продуктового или оценочного кода без отдельного разрешения владельца;
- захват трассы изнутри исходной задачи.

При каждой детерминированной ошибке агент должен был остановиться и не исправлять её
самостоятельно.

## 1. Граница сессии

Агент прочитал обязательные локальные контракты и проекции, проверил только
разрешённое состояние репозитория и создал
`artifacts/trajectories/session-boundaries/SES-20260829-008.yaml`.

В границе были зафиксированы грязное рабочее дерево, запрет commit/push, точный набор
разрешённых проверок, отсутствие внешнего доступа и запрет использовать
заблокированную предыдущую трассу как источник или доказательство. Захват после
завершения задачи оставался обязанностью владельца репозитория.

До одобрения границы ни одна проверочная команда не запускалась.

## 2. Первое решение владельца

Владелец одобрил границу и разрешил только локальный read-only review — проверку без
изменений — и следующие команды:

- `npm run compile`;
- `npm test`;
- `npm run task -- d01:verify`;
- `npm run task -- d01:replay`;
- `npm run task -- phase0.5:verify`;
- `npm run task -- checksums:check`;
- `git diff --check`.

Исправления, официальный запуск, внешний доступ, commit и push не разрешались.

## 3. Первая остановка

Видимая выгрузка фиксирует локальную проверку документации, происхождения,
манифестов, оркестрации, правил решения, воспроизведения и границы скрытого эталона.

Агент сообщил, что D01 остаётся неоцениваемой разработческой демонстрацией, кандидат
не изменяется, решение фиксируется до запуска независимого оценщика, скрытый эталон
изолирован отдельным процессом, а результат можно воспроизвести без сети. Полный
вывод команд отсутствует, поэтому это именно выводы агента, а не независимо видимый
полный журнал.

Результаты:

- `npm run compile` — успешно;
- `npm test` — ошибка, прошли 48 из 49 тестов.

В новом файле границы scanner — автоматическая проверка приватности — обнаружил два
абсолютных пользовательских пути в полях `repository.root` и
`context.repository_root`.

Агент немедленно остановился. Остальные команды не запускались, исправления не
вносились.

## 4. Второе решение владельца

Владелец разрешил изменить ровно два значения в файле границы. Абсолютные пути были
заменены безопасным обозначением `current_clean_repository_root`. Других изменений
не разрешалось.

Также владелец разрешил повторить весь исходный набор проверок с самого начала и
снова остановиться при следующей детерминированной ошибке.

## 5. Повторная проверка и вторая остановка

Выгрузка фиксирует следующие результаты:

- компиляция — успешно;
- обычные тесты — 49 из 49;
- полная проверка D01 — успешно;
- автономное воспроизведение D01 — успешно;
- безопасные проверки Phase 0.5 — успешно;
- проверка контрольных сумм — ошибка `CONTROL_CHECKSUMS_MISMATCH`.

Агент сообщил об успешных видимых тестах, контракте шага, самопроверке эталона,
физической изоляции, вертикальном сценарии, воспроизведении и проверках Phase 0.5.
Точные внутренние количества и хеши считаются сообщёнными агентом, потому что полный
вывод команд не входит в выгрузку.

На ошибке контрольных сумм агент снова остановился. `git diff --check` тогда не
запускался, а контрольные суммы не исправлялись.

## 6. Третье решение владельца и завершение

Владелец разрешил одну локальную диагностику контрольных сумм. Перезапись манифеста
была разрешена только при единственной причине — новом файле границы. Любое другое
расхождение требовало остановки.

Диагностика сообщила ровно одно расхождение:

- `artifacts/trajectories/session-boundaries/SES-20260829-008.yaml` — новый файл,
  отсутствующий в прежнем манифесте.

После этого агент выполнил разрешённые обновление и проверку контрольных сумм, а
также `git diff --check`. Он сообщил, что манифест записан и проверен для 156 файлов,
а `git diff --check` завершился без вывода. Утверждение о том, что при согласовании
изменился только манифест контрольных сумм, остаётся сообщением агента: полный diff в
нативной выгрузке отсутствует.

## Итог

Весь разрешённый объём проверки завершился успешно после двух ограниченных и явно
одобренных повторов:

1. безопасной замены двух абсолютных путей в границе;
2. добавления нового файла границы в манифест контрольных сумм.

Официальный или оцениваемый запуск не выполнялся. В видимой истории отсутствуют
внешний доступ, commit и push. Исходная задача закончилась готовностью к отдельному
захвату и review, но сама по себе не объявляла трассу допущенной к отправке.

## Ограничения доказательств

- Это полная нативная пользовательская история задачи, но не внутренний raw trace —
  полный машинный журнал.
- Скрытые рассуждения и внутренние события приложения недоступны и не заявляются.
- Часть ответов инструментов видна только как свёрнутые карточки или итоговые
  сообщения агента.
- Детализированные результаты без полного stdout — вывода команд — помечены как
  сообщённые агентом.
- Трасса подтверждает только ограниченную неоцениваемую проверку D01. Она не
  доказывает результат официального benchmark — сравнительного оценочного запуска —
  или готовность к промышленному использованию.
