# TRC-BG-D02-D04-INTEGRATION-001-CONT-009 — authorized continuation

You are executing the owner-authorized continuation trajectory TRC-BG-D02-D04-INTEGRATION-001-CONT-009 inside the current isolated BeyondGreen worktree under session boundary SES-20260830-009. Stay inside this worktree for every Codex action. Do not inspect, read, or modify the primary checkout or any external state. Do not print environment-variable values or absolute machine paths.

Before acting, read AGENTS.md and every mandatory repository contract it names in full, including current product projections. Preserve all existing integration records and evidence from TRC-BG-D02-D04-INTEGRATION-001 and CONT-001 through CONT-008. Preserve the already authorized scanner span changes and their tests.

This continuation has exactly this authorized scope:

1. Use the already verified repository-local scanner spans to normalize only evaluation/manifests/BG-D02/SUBAGENT_REPORT_RU.md. Before any write, require:
   - exactly 3 absolute-user-path findings in that file;
   - exactly 3 total spans, exactly one span on each finding;
   - line numbers exactly 72, 80, and 97;
   - every span has patternId exactly absolute-user-path-macos-users-v1.
   Do not display, log, copy to another file, or include in JSON/messages any original matched value, source line, prefix, suffix, or replacement context.
2. In one in-memory atomic transformation, replace exactly each of those three complete scanner spans with the literal neutral marker <redacted-local-path>. There must be exactly one replacement on each of lines 72, 80, and 97.
3. Before saving, validate in memory that exactly three replacements occurred and that every byte outside those three original spans is unchanged. Save only after all preconditions and validation pass. Do not create a temporary script or raw file. Use a quoted literal heredoc or equivalent stdin mechanism that prevents shell interpolation.
4. After saving, require:
   - exactly three replacements;
   - the target-only scanner reports zero absolute-user-path findings;
   - a structural byte-level comparison proves that the only changes are the three complete original spans replaced by the exact marker.
   Never print a raw diff of this report or any original value. If any requirement fails, atomically restore the original in memory without printing it and stop.
5. Reconcile only directly derived manifests, registries, trajectory/provenance indexes, projections, and checksums necessitated by this authorized normalization and the already approved D02-D04 integration. Preserve frozen behavior, oracles, candidates, visible assertions, evaluation methodology, goals, dependencies, execution policy, and immutable artifacts except for already owner-authorized deterministic derived metadata.
6. Then complete the remaining previously approved unchanged verification: npm test, all required replay/regression checks, documentation parity, checksum verification, and both preflight checks. Use the repository's existing commands and D01-D04 verification architecture. Do not perform any official or scored run. Stop on the first new unexpected failure; deterministic expected manifest/checksum reconciliation within the approved integration trajectory is allowed.
7. Record this exact continuation instruction and a complete natural-Russian owner-facing result or stop report with commands, safe aggregate evidence, remaining risks, and final status. Code, JSDoc, and comments remain English. Do not include raw path values or raw target lines in any artifact or output.

Hard prohibitions: no commit or push; no official/scored run; no Claude; no Chromium; no live product model; no browser or private MCP; no second nested Codex CLI; no primary-checkout or external-state access; no trajectory beyond CONT-009. Use no tee, temporary raw file, or manual raw copying. Work autonomously until completion or the first genuine unexpected failure.
