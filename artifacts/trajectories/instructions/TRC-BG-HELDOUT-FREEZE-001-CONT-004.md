# TRC-BG-HELDOUT-FREEZE-001-CONT-004 — owner-approved categorical clean-room recreation

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the root contamination stop.
This remains `SES-20260830-010`; do not create a new session boundary. Work only in
the current clean repository root on branch
`codex/beyondgreen-d01-scale003-correct` at exact `HEAD`
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Do not inspect another checkout,
worktree, sibling repository, external workspace, browser state, connected app,
private MCP state, global memory, or external path. Do not print environment-variable
values or absolute machine paths. Do not launch another Codex CLI, subagent, Claude,
live product model, Chromium, or browser process. Do not commit or push. Do not
apply, pop, drop, clear, or otherwise modify any stash.

Before acting, read `AGENTS.md` and every mandatory repository contract it names in
full: `docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`,
`docs/CLEAN_ROOM_POLICY.md`, `docs/TRACE_POLICY.md`,
`docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, and
`config/topic.yaml`. Do not read the three affected SES-010 service files before the
categorical recreation below. The owner has already reviewed the safe finding
category and authorizes this exact continuation without another intermediate
approval.

## Owner-approved categorical recreation

The prior safe control preflight identified exactly four positions with one
suppressed private-denylist category. Never display, log, copy, compare, infer, or
recover the matched term. Independently recreate only those four complete lines by
their safe scanner line identifiers. Use a deterministic in-place line-index
replacement that does not output the old line values, validates the expected file
line counts before writing, preserves UTF-8 and LF, and changes no other line:

1. In `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`, replace
   line 45 with exactly:
   `1. A neutral user and independently synthetic public-facing domain with no private-system dependency.`
2. In `artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`,
   replace line 68 with exactly:
   `1. Нейтрального пользователя и независимо синтетическую публичную область, не связанную ни с какой`
3. In that same Russian owner card, replace line 86 with exactly:
   `о неразглашении, и не связанным с каким-либо частным`
4. In `artifacts/trajectories/session-boundaries/SES-20260830-010.yaml`, replace
   line 171 with exactly:
   `    - "Independently synthetic expression with no private-system dependency."`

This is an owner-approved categorical recreation, not an automated claim that the
old material was safe. Record the four positions, neutral replacements, owner
authorization, and the fact that the suppressed term was neither read nor disclosed
in a complete natural-Russian review record. Do not modify any raw capture and do
not promote `CONT-003`, `CONT-004`, or any other held-out trace into
`actual_traces`.

After the four-line recreation, read the sanitized SES-010 service history needed to
reconcile the current prose-freeze packet, including the prior stop cards,
`CONT-003`, and its root stop. Preserve all historical failures and capture facts.
Do not change H01-H06 behavior, assignments, invariants, provenance, public anchors,
licenses, visible-test blind spots, candidate prose contracts, oracle boundary, or
`K=0` semantics.

Complete only deterministic derivative reconciliation:

- update the final natural-Russian freeze-approval card so it accurately records
  the owner-approved categorical recreation, `CONT-003` capture and stop, this
  `CONT-004` capture status, all retries and omissions, and the remaining owner gate;
- restore the held-out freeze manifest from blocked/stale state to validated pending
  owner approval only if every check below passes;
- recompute every affected SHA-256 binding and exact manifest membership;
- run `npm run task -- checksums:write`, then exactly once run
  `npm run task -- checksums:check`;
- run `git diff --check` and the existing repository-local structural checks for
  YAML, Markdown, 4-development/6-held-out membership, ten-class bijection,
  `BG-H02` as the sole challenging case, 34/34 freeze-manifest paths, EN/RU parity,
  provenance, access dates, and license/terms.

Only after hashes/checksums and structural checks pass, run exactly once each from
this authorized root, in this order:

```text
micro1-safe-preflight control
micro1-safe-preflight implementation
```

At any new contamination category, any remaining private-denylist finding, semantic
change, uncertain provenance/license, unexpected nondeterministic failure, or first
failed preflight, stop immediately. Write a complete natural-Russian `CONT-004` stop
card containing only safe category/status evidence. Do not retry the failed command,
run the later preflight, inspect external state, or widen scope. A deterministic
hash/checksum mismatch caused solely by the four authorized line recreations or by
the final review-card update may be reconciled before the preflights without another
approval.

Do not run `npm test`, compile, fixture verification, evaluator self-check, replay,
benchmark, official/scored evaluation, model probes, Chromium, package, or publish.
Do not create fixture implementation, preserving or false-green candidate files,
oracle code/data, hidden expected results, evaluator code, test code, executable
fixtures, benchmark/scored artifacts, or unblinding output. Do not modify D01-D04
implementation, H01-H06 semantics, candidates, oracles, visible assertions,
evaluation methodology, targets, denominators, dependencies, lockfile, runtime
policy, or `K=0` isolation.

On full success, do not assert the freeze for the owner. Produce one complete,
natural-Russian final freeze-approval card that explains the six domains and
invariants, public sources/licenses, all hashes and commands, the four categorical
recreations, all stops/retries, capture and trace-review limitations, privacy status,
residual risks, and the exact approval/non-approval scope. Include one exact Russian
phrase authorizing only the H01-H06 prose and public-provenance freeze while leaving
implementation, candidates, oracles, tests, official/scored runs, live product
models, Claude, Chromium, commit, and push unauthorized. Then stop for the owner's
decision.
