# TRC-BG-HELDOUT-FREEZE-001-CONT-002 — authorized safe-preflight continuation

You are the single automatically captured Codex CLI continuation explicitly
authorized after `TRC-BG-HELDOUT-FREEZE-001-CONT-001` stopped fail-closed. Work only
in the current isolated BeyondGreen worktree at exact base commit
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Do not inspect or modify the primary
checkout, another Git worktree, sibling repositories, external workspaces, browser
state, connected apps, private MCP state, global memory, or any external path. Do
not print environment-variable values or absolute machine paths. Do not launch
another Codex CLI, subagent, Claude, live product model, Chromium, or browser
process. Do not commit or push.

Before acting, read `AGENTS.md` and every mandatory repository contract it names in
full: `docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`,
`docs/CLEAN_ROOM_POLICY.md`, `docs/TRACE_POLICY.md`,
`docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, and
`config/topic.yaml`. Then read the complete authorized SES-010 service history:

- `artifacts/trajectories/session-boundaries/SES-20260830-010.yaml`;
- `artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-PRECAPTURE-STOP_RU.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-COORDINATOR-STOP_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-CONT-001-STOP_RU.md`;
- this continuation instruction,
  `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-002.md`.

All listed stop cards are authorized service history and must be preserved unchanged.
The existing BG-H01 through BG-H06 prose/freeze packets and these partial derivative
records from CONT-001 are expected, not deviations:

- `evaluation/fixture-assignments.yaml`;
- `evaluation/challenging-cases.yaml`;
- `evaluation/HELD_OUT_PROSE_FREEZE_SUMMARY.md`;
- `artifacts/trajectories/reviews/BG-HELDOUT-PROSE-FREEZE-CHECKPOINT_RU.md`;
- the CONT-001 change in `config/topic.yaml`.

Treat the owner checkpoint as a draft: its `PENDING_RUN` claims are not valid until
the safe checks complete, and it references a manifest that does not yet exist.
Do not claim the freeze complete merely because the partial text exists.

## Exact safe-preflight authorization

The owner authorizes you to safely determine the undisclosed authorized launcher
root through `git rev-parse --git-common-dir`, without printing or persisting it.
You may inspect only safe command usage or safe status needed to identify an existing
supported interface that unambiguously targets the current isolated worktree while
the launcher itself runs from that authorized root. Never read files from the
primary checkout and never run a check whose target is the primary checkout.

Before either preflight, retain the current isolated worktree root only in memory and
prove from the existing safe interface contract that the scan target is exactly that
root. Do not invent an environment variable, unsupported flag, symlink, copied tree,
or wrapper replacement. Do not fake `PWD`, bypass the launcher root check, invoke
repository scripts directly with private inputs, or access the private denylist.

If the existing safe interface cannot unambiguously target the current isolated
worktree while running from the authorized launcher root, stop immediately before
either scan. Write a complete natural-Russian CONT-002 stop card containing only safe
usage/status evidence, without paths or external values. Do not retry or substitute.

If and only if target identity is proven, run exactly once each through the safe
interface:

```text
micro1-safe-preflight control
micro1-safe-preflight implementation
```

Record their exact safe outputs and statuses honestly. At the first failure or
unexpected result, stop, write a complete natural-Russian CONT-002 stop card, and do
not retry.

## Completion allowed only after both checks pass

If both safe preflights pass for the current isolated worktree, finish only the
directly derivative prose-freeze surface already authorized by SES-010 and CONT-001:

1. Revalidate the exact 4-development/6-held-out split and the ten-class bijection.
2. Revalidate that BG-H02 is the sole predeclared challenging case.
3. Revalidate complete EN/RU owner coverage and semantic parity for BG-H01–BG-H06.
4. Revalidate public provenance, access dates, licenses/terms, and independent
   synthetic clean-room attestations.
5. Create `evaluation/held-out-prose-freeze.yaml` with SHA-256 bindings for every
   behavior, provenance/freeze, owner companion, assignment, challenging-case, and
   summary input. Do not include future implementation paths as existing files.
6. Reconcile only the derivative checksum coverage required by existing repository
   convention, preserving self-exclusion and exact intended-path coverage.
7. Validate YAML/Markdown structure, repository-relative paths, privacy, exact
   manifest coverage, hash values, and `git diff --check` without compile or tests.
8. Replace every draft `PENDING_RUN` statement with the exact verified safe result
   and make the Russian owner checkpoint internally consistent.

Preserve the reported H03/H04 service retry exactly: Ruby/Psych lacked
`safe_load_file`, and the corrected structural read used
`safe_load(File.read(...))` without changing product content. Preserve that the
three native subagents' internal raw events are not claimed as exported traces.
Preserve both earlier coordinator stops and the CONT-001 stop. Do not mark this or
any raw capture as submission eligible before separate scan/review and owner
promotion.

Do not run `npm test`, compile, fixture verification, evaluator self-check, replay,
benchmark, official/scored evaluation, model probes, or Chromium. Do not create
fixture implementation; preserving or false-green candidate files; oracle code,
oracle data, hidden expected results, evaluator code; test code or executable
fixtures; benchmark/scored artifacts; unblinding output; or Chromium artifacts. Do
not modify D01-D04 implementation, candidates, oracles, visible assertions,
evaluation semantics, targets, denominators, dependencies, lockfile, runtime policy,
or K=0 isolation.

On full success, stop before any candidate, oracle, fixture, or test code. Finalize
one complete natural-Russian owner freeze checkpoint with exact files, hashes,
commands, preflight results, omissions, all stops/retries, provenance/license risks,
trace-review status, and one exact Russian phrase authorizing only the prose and
public-provenance freeze. The phrase must not authorize implementation, candidates,
oracles, tests, official/scored runs, live product models, Claude, Chromium, commit,
or push.
