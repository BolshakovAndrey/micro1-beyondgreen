# TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001 — bounded held-out fixture integration

You are the single automatically captured Codex CLI integration coordinator
authorized by owner-approved `SES-20260830-011`. Work only in the current saved main
BeyondGreen worktree. Do not inspect any other worktree, checkout, sibling
repository, external workspace, browser state, connected app, private MCP state,
global memory, private data, or external path. Do not print environment-variable
values, private denylist terms, raw trace payloads, or absolute machine paths. Do not
launch another Codex CLI, subagent, Claude, live product model, Chromium, or browser.
Do not perform an official/scored run or unblinding. Do not commit or push.

Before acting, read `AGENTS.md` and all mandatory contracts named there in full:
`docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`, `docs/CLEAN_ROOM_POLICY.md`,
`docs/TRACE_POLICY.md`, `docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`,
`docs/EVALUATION.md`, and `config/topic.yaml`. Then read in full:

- `artifacts/trajectories/session-boundaries/SES-20260830-011.yaml`;
- `artifacts/trajectories/reviews/BG-HELDOUT-FIXTURES-SESSION-BOUNDARY-CHECKPOINT_RU.md`;
- `artifacts/trajectories/reviews/BG-HELDOUT-PROSE-FREEZE-CHECKPOINT_RU.md`;
- this instruction file.

The exact clean Git base is the owner-approved freeze checkpoint
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`. The only expected pre-integration
working-tree addition is this instruction file. The three authorized fixture-only
source commits, all based on that exact freeze checkpoint, are:

1. BG-H01/BG-H02: `2b61a9954d43686f4c07f10d48bb726d5ed156f0`,
   `feat(Evaluation):[BeyondGreen] Add prop reset and async ordering fixtures`;
2. BG-H03/BG-H04: `bf94ab99a0162d9a940b9ba129602b0cc59b0f67`,
   `feat(Evaluation):[BeyondGreen] Add identity and lifecycle fixtures`;
3. BG-H05/BG-H06: `745b118b4c4da34dde367d84eae8005c3af3c4a2`,
   `feat(Evaluation):[BeyondGreen] Add external store and rollback fixtures`.

Verify the base and exact source identities without inspecting other worktrees. Apply
only these three commits in the listed order with `git cherry-pick --no-commit` so no
integration commit is created. Preserve every frozen behavior, provenance, public
anchor, candidate byte, oracle byte, visible assertion, evaluator mapping, manifest,
and worker report. Resolve only deterministic integration conflicts caused by these
three exact commits. Any semantic conflict is a stop condition.

The workers deliberately did not change shared paths. Integrate BG-H01 through
BG-H06 into the existing generic descriptor/fixture/task architecture using the
smallest necessary shared changes. You may change only directly necessary shared
fixture registry, typed task registry/runner, root package scripts if actually
required by the established interface, shared checksum projection, integration
tests, current EN/RU projections, Improvement Changelog, provenance/trajectory
indexes, and this trajectory's complete natural-Russian result report. Do not edit
fixture-local implementation merely to simplify integration. Deterministic
fixture-specific compile/test/hash/path/JSDoc corrections are permitted only when
required for integration and must preserve frozen semantics and be recorded.

Maintain these contracts:

- exactly ten fixtures with the frozen D01-D04/H01-H06 class bijection and a 4/6
  development/held-out split;
- BG-H02 remains the sole predeclared challenging case;
- exactly two neutral candidates per held-out fixture, with mapping only in
  verifier-only packages;
- arm-visible code and candidate processes cannot read verifier-only paths;
- evaluator processes cannot read candidate source paths except through the
  approved canonical execution boundary;
- `K=0`: no evaluator-derived feedback reaches either arm before its immutable
  decision;
- public/important TypeScript APIs retain concise English JSDoc and key boundaries
  retain short English why-comments;
- owner-facing reports are complete natural Russian.

Run only non-official development verification. At minimum:

1. compile the complete repository TypeScript surface;
2. run ordinary tests and all H01-H06 visible tests;
3. run every H01-H06 verifier self-check and reciprocal denied-access/isolation
   probe for both arms and both candidates;
4. validate all fixture, arm-visible, candidate, verifier-only, implementation, and
   aggregate manifests and immutable hashes;
5. add and run integration tests for exact registry/task/descriptor/fixture/class
   bijections, 4/6 membership, challenging-case identity, neutral candidate
   cardinality, physical isolation, and `K=0` bindings;
6. scan arm-visible and candidate surfaces for verifier mapping/oracle leakage;
7. run deterministic replay/regression checks that are safe and already part of the
   development architecture, without an official/scored run or held-out unblinding;
8. reconcile the shared checksum index deterministically, then verify it;
9. run both owner-approved safe preflights, control and implementation, without
   printing their external paths or private values.

Ordinary compile/test/hash/path/JSDoc failures may be diagnosed and corrected
deterministically inside this trajectory when the correction stays within the
approved integration scope. Stop immediately on contamination, uncertain
provenance/license, semantic drift, oracle leakage, loss of physical isolation,
methodology change, an external/private path or context, or any need for official
execution/unblinding. Do not hide failures, retries, negative results, or limitations.

Create
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`
as a complete natural-Russian coordinator report. It must explain the three source
commits, exact shared changes, commands/results, deterministic corrections and
retries, manifest/hash/isolation/K=0/checksum/preflight evidence, omissions, residual
risks, automatic-capture status, the fact that native subagent internal raw events
are not claimed as exported traces, and the exact owner-acceptance scope. If a stop
condition occurs, make the same path a truthful stop report instead.

Finish with all authorized integration changes present but uncommitted. Confirm
`git status`, explicitly state that no integration commit or push occurred, and stop
for separate repository-owner acceptance and later explicit integration-commit
approval. Your JSONL stdout is captured automatically and must remain a faithful
trajectory.
