# TRC-BG-D02-D04-INTEGRATION-001-CONT-012 — corrected documentation-parity continuation

Execute the owner-authorized corrected retry
`TRC-BG-D02-D04-INTEGRATION-001-CONT-012` inside the current isolated BeyondGreen
worktree under `SES-20260830-009`. Preserve `CONT-010` and `CONT-011` as immutable
unsuitable attempts and do not reuse or alter them. Stay inside this worktree. Do
not inspect the primary checkout or external state, and do not print absolute
machine paths or environment values.

Before acting, read `AGENTS.md` and every mandatory repository contract and current
projection it names in full. Then perform exactly this authorized scope:

1. Make exactly two documentation insertions and no other semantic documentation
   edits. In section 15 of `docs/PROJECT_SPEC.md`, add the exact line
   `npm run task -- development:verify` immediately after
   `npm run task -- d01:verify` in the `Required command families` block. Make the
   identical insertion at the symmetric point in section 15 of
   `docs/PROJECT_SPEC_RU.md`. Require unique insertion points and prior absence;
   afterward prove exactly one inserted line per file and no other existing-line
   change from this parity correction.
2. Run the bounded EN/RU parity check for that command marker and
   `git diff --check`.
3. Reconcile only directly derived manifests, trajectory/provenance indexes,
   projections, and checksums required by the approved D02-D04 integration and the
   two-line correction. Preserve frozen behavior, candidates, oracles, visible
   assertions, evaluation methodology, goals, dependencies, lockfile, execution
   policy, and immutable semantics. Do not modify or promote `actual_traces`.
4. Run the checksum check, both safe preflight checks
   (`micro1-safe-preflight control` and `micro1-safe-preflight implementation`),
   and only the previously approved unchanged final checks genuinely necessary to
   establish the final state. Never reveal denylist values, external paths, or raw
   absolute machine paths. Reuse successful `CONT-009` evidence for unaffected
   expensive checks when repository contracts permit. Never perform an official or
   scored run.
5. Stop on the first new unexpected failure. Do not retry or invoke another nested
   Codex CLI.
6. Write a complete natural-Russian `CONT-012` result or stop card recording the
   exact edits, commands, safe evidence, final gates, remaining risks, and
   owner-review readiness. Include a separate suitability matrix for
   `TRC-BG-D02-D04-INTEGRATION-001` and every continuation `CONT-001` through
   `CONT-012`. Classify each conservatively as eligible for technical/owner review,
   unsuitable, or pending trace validation. Account for reported machine-path
   emissions in `CONT-007` and `CONT-009`; keep `CONT-010` and `CONT-011`
   immutable and unsuitable; mark `CONT-012` pending capture/owner review when the
   card is generated. Do not change `actual_traces`.
7. Keep code, JSDoc, and comments in English. All owner-facing reporting must be
   complete natural Russian.

Forbidden: primary checkout or external state; official/scored run; Claude;
Chromium; live product model; browser/private MCP; commit; push; automatic trace
promotion; a second nested Codex CLI; or any trajectory beyond `CONT-012`.
