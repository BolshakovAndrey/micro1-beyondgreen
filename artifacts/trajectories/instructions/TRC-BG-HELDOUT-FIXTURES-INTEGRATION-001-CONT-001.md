# TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-001 — corrected automatic transport

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the transport-only stop recorded
in
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`.
This continuation inherits the complete scope, requirements, checks, stop conditions,
and prohibitions from
`artifacts/trajectories/instructions/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001.md`
and owner-approved `SES-20260830-011`.

The failed launcher produced no JSONL and performed no integration work. Its separate
206-byte stderr capture with SHA-256
`a4579eae6ed0babd16921d1d981b791d153330ecd6040053025a71a3cbab52ab` is immutable,
unused diagnostic evidence. Do not read, delete, replay, decode, inspect, summarize,
or otherwise access its raw contents. Do not infer a product or integration result
from it.

This is the only authorized corrected transport retry. The outer launcher preserves
instruction-file stdin, `--ephemeral`, `--ignore-user-config`, `--json`, the same
model, direct unchanged JSONL stdout to `micro1-safe-trace`, and a separate safe
stderr capture. The incompatible `--approve-for-me` and explicit
`--sandbox workspace-write` options have been removed. Do not launch another Codex
CLI or any subagent.

Before acting, read in full `AGENTS.md`, every mandatory contract it names,
`artifacts/trajectories/session-boundaries/SES-20260830-011.yaml`, its Russian owner
card, the original integration instruction, the transport stop report, and this
continuation. Work only in the current saved main BeyondGreen repository. Do not
inspect another worktree, external path, private state, global memory, browser,
connected app, or private MCP. Never print absolute machine paths, private values,
denylist terms, or raw trace payloads.

Confirm exact HEAD `830793ce16cb2273e4c9b294a11c7aca952bc7a7`. The only expected untracked inputs are
the original integration instruction, its Russian transport stop report, and this
continuation instruction. Then execute the full previously approved integration from
the original instruction using only these exact commits, in order, with
`git cherry-pick --no-commit`:

1. `2b61a9954d43686f4c07f10d48bb726d5ed156f0` — BG-H01/BG-H02;
2. `bf94ab99a0162d9a940b9ba129602b0cc59b0f67` — BG-H03/BG-H04;
3. `745b118b4c4da34dde367d84eae8005c3af3c4a2` — BG-H05/BG-H06.

Integrate only the necessary shared registry, typed tasks/runner, package scripts if
required, integration tests, EN/RU projections, Improvement Changelog,
provenance/trajectory indexes, checksum projection, and complete natural-Russian
result evidence described by the original instruction. Preserve all fixture-local
frozen semantics, candidates, oracles, visible assertions, mappings, manifests, and
reports unless a deterministic fixture-specific compile/test/hash/path/JSDoc
correction is strictly required and fully recorded.

Run the complete non-official verification required by the original instruction:
compile, ordinary and held-out fixture-specific tests, every verifier self-check,
reciprocal denied-access and physical-isolation checks for both arms/candidates,
manifest/hash/cardinality/bijection checks, mapping-leak checks, safe deterministic
replay/regression, checksum reconciliation and verification, and both safe preflights.
Do not run an official/scored workflow or unblind held-out benchmark results.

Update
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`
from its truthful transport-stop state into a complete natural-Russian continuation
result report while preserving the failed-launch history and diagnostic-evidence
limitations. Record commands, results, deterministic corrections/retries, shared
changes, omissions, risks, capture status, subagent raw-event limitation, and exact
owner-acceptance scope.

On any new transport failure, contamination, semantic drift, uncertain
provenance/license, oracle leakage, loss of physical isolation, external/private
context, or shared-methodology change, stop immediately without another nested CLI.
All prohibitions of `SES-20260830-011` remain in force: no integration commit, push,
official/scored run, unblinding, live product model, Claude, Chromium, browser,
private MCP, private data, packaging, publication, or submission.

Finish with the fully verified integration changes uncommitted and stop for separate
repository-owner acceptance. Your JSONL stdout is captured automatically and must
remain a faithful trajectory.
