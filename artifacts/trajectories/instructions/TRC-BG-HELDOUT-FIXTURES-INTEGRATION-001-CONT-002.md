# TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-002 — workspace-write continuation

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the valid read-only/no-change
attempt `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-001`. This continuation
inherits the complete integration scope, requirements, checks, stop conditions, and
prohibitions from owner-approved `SES-20260830-011`, the original instruction, and
`CONT-001`.

The original launcher failure and `CONT-001` are immutable evidence. `CONT-001`
successfully captured JSONL but made no repository change. Preserve it as a valid
read-only/no-change attempt; do not delete, alter, replay, decode, inspect, or
automatically promote its external raw capture. Do not read any saved raw stderr or
JSONL from either earlier attempt.

This is the only authorized corrected continuation. The outer launcher makes exactly
one change relative to `CONT-001`: it adds `--sandbox workspace-write`. It preserves
instruction-file stdin, `--ephemeral`, `--ignore-user-config`, `--json`, the same
model, direct unchanged JSONL stdout to `micro1-safe-trace`, and separate safe stderr
capture. `--approve-for-me` remains forbidden. Do not launch another Codex CLI,
subagent, Claude, product model, Chromium, or browser.

Before acting, read in full `AGENTS.md`, every mandatory contract it names,
`artifacts/trajectories/session-boundaries/SES-20260830-011.yaml`, its Russian owner
card, the original integration instruction, `CONT-001`, the Russian result/stop
report, and this instruction. Work only in the current saved main BeyondGreen
repository. Do not inspect another worktree, checkout, external path, private state,
global memory, browser, connected app, or private MCP. Never print absolute machine
paths, private values, denylist terms, or raw trace payloads.

Confirm exact HEAD `830793ce16cb2273e4c9b294a11c7aca952bc7a7`. The only expected untracked inputs are
the original instruction, both continuation instructions, and the Russian
result/stop report. Then perform the full previously approved integration using only
these exact commits, in this order, with `git cherry-pick --no-commit`:

1. `2b61a9954d43686f4c07f10d48bb726d5ed156f0` — BG-H01/BG-H02;
2. `bf94ab99a0162d9a940b9ba129602b0cc59b0f67` — BG-H03/BG-H04;
3. `745b118b4c4da34dde367d84eae8005c3af3c4a2` — BG-H05/BG-H06.

Apply only those commits. Preserve their fixture-local frozen semantics, candidate
bytes, oracle bytes, visible assertions, verifier mappings, manifests, provenance,
and reports. Resolve only deterministic integration conflicts; any semantic conflict
is a stop condition.

Integrate BG-H01 through BG-H06 into the existing generic descriptor/fixture/task
architecture with the smallest necessary shared changes. Modify only directly
necessary shared fixture registry, typed task registry/runner, root package scripts
if required, integration tests, current EN/RU projections, Improvement Changelog,
provenance/trajectory indexes, checksum projection, and the complete natural-Russian
result report. Do not simplify or rewrite fixture-local implementations.

Maintain exact ten-fixture class bijection and 4/6 membership, BG-H02 as the sole
predeclared challenging case, exactly two neutral candidates per held-out fixture,
verifier-only mapping, reciprocal physical access denial, immutable candidate and
oracle bindings, and `K=0` evaluator feedback. Keep public/important TypeScript APIs
documented with concise English JSDoc and boundary/invariant rationale in short
English why-comments. Owner-facing evidence must be complete natural Russian.

Run the full non-official integration verification from the original instruction:

1. complete TypeScript compile;
2. ordinary tests and all H01-H06 visible tests;
3. every H01-H06 evaluator self-check and reciprocal denied-access/isolation probe
   for both arms and candidates;
4. all fixture, arm-visible, candidate, verifier-only, implementation, and aggregate
   manifest/hash checks;
5. exact registry/task/descriptor/fixture/class bijections, 4/6 membership,
   challenging-case identity, neutral candidate cardinality, physical isolation, and
   `K=0` integration tests;
6. mapping/oracle leakage scans over arm-visible and candidate surfaces;
7. safe deterministic development replay/regression, without official/scored
   execution or held-out result unblinding;
8. deterministic checksum reconciliation and verification;
9. both owner-approved safe preflights, control and implementation, without
   disclosing external paths or private values.

Deterministic compile/test/hash/path/JSDoc corrections and directly required shared
integration changes are permitted within this trajectory when they preserve all
frozen semantics and are fully recorded. Stop immediately on any new transport
failure, contamination, uncertain provenance/license, semantic drift, oracle
leakage, loss of physical isolation, shared-methodology change, external/private
context, or need for official execution/unblinding. Do not conceal a failure, retry,
negative result, or limitation.

Update
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`
into a complete natural-Russian integration result while preserving the exact
history and limitations of the original failed launcher and `CONT-001`. Include the
three source commit IDs, exact shared changes, commands/results, deterministic
corrections/retries, manifest/hash/isolation/K=0/checksum/preflight evidence,
automatic-capture status, omissions, residual risks, the fact that native subagent
internal raw events are not claimed as exported traces, and the exact owner-
acceptance scope.

All other `SES-20260830-011` restrictions remain: no integration commit, push,
official/scored run, unblinding, live product model, Claude, Chromium, browser,
private MCP, private data, packaging, publication, or submission. Finish with all
verified integration changes present but uncommitted and stop for separate owner
acceptance. Your JSONL stdout is captured automatically and must remain faithful.
