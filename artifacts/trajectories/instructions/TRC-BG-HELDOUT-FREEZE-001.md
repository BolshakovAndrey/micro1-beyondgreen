# TRC-BG-HELDOUT-FREEZE-001 — held-out prose freeze coordinator

You are the single automatically captured Codex CLI coordinator authorized by the
repository owner under `SES-20260830-010`. Work only in the current isolated
BeyondGreen worktree at exact base commit
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Do not inspect or modify the primary
checkout, another Git worktree, sibling repositories, external workspaces, browser
state, connected apps, private MCP state, global memory, or any external path. Do
not print environment-variable values or absolute machine paths. Do not launch
another Codex CLI, subagent, Claude, live product model, Chromium, or browser
process. Do not commit or push.

Before acting, read `AGENTS.md` and every mandatory repository contract it names in
full: `docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`,
`docs/CLEAN_ROOM_POLICY.md`, `docs/TRACE_POLICY.md`,
`docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, and
`config/topic.yaml`. Then read
`artifacts/trajectories/session-boundaries/SES-20260830-010.yaml` and
`artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`
in full. The owner supplied the exact approval phrase recorded there.

Three native Codex subagents independently authored one pair each in isolated clean
worktrees. Their internal raw events are not claimed as exported traces. The files
have been transferred into this coordinator worktree without claiming coordinator
authorship. Inspect and integrate exactly these six held-out fixture packets:

- `BG-H01` / `prop_reset`;
- `BG-H02` / `async_ordering`, retaining its predeclared challenging-case label;
- `BG-H03` / `identity_stability`;
- `BG-H04` / `conditional_lifecycle`;
- `BG-H05` / `external_store`;
- `BG-H06` / `rollback`.

The transferred surface consists only of the new BG-H01 through BG-H06 Markdown and
YAML files under `evaluation/behavior-specs/` and `evaluation/manifests/BG-H01/`
through `evaluation/manifests/BG-H02/`, plus the SES-010 boundary, its Russian owner
card, and this instruction. Preserve independent authorship and disclose the one
H03/H04 structural-validation retry: Ruby/Psych lacked `safe_load_file`; the
corrected structural read used `safe_load(File.read(...))` and passed without
changing product content. Do not claim subagent raw-event capture.

For every fixture, confirm and, only when necessary for consistency, minimally edit
the prose/freeze packet so it contains all of the following without implementation:

1. A neutral user and independently synthetic public-facing domain with no private-system dependency.
2. Frozen observable actions and invariants.
3. The visible-test blind spot.
4. A behavior-preserving candidate contract and one seeded false-green candidate
   contract, both prose only.
5. Physical oracle and capability boundaries with K=0 and no oracle code, data, or
   hidden expected values.
6. Public provenance with source URL, author/project, access date, license or terms,
   and an independent synthetic clean-room attestation.
7. Planned future files, tests, and manifests clearly marked as not created in this
   wave.
8. Expected failure modes, uncertainties, and fail-closed stop conditions.
9. A complete natural-Russian owner companion explaining behavior, evidence,
   omissions, retries, provenance/licenses, privacy, risks, and exact approval and
   non-approval scope without requiring the English packet.

Integrate the six packets using existing repository conventions. Create or update
only directly necessary prose-freeze records: fixture assignment/challenging-case
records, provenance index entries, a machine-readable held-out freeze manifest,
English freeze summary, complete natural-Russian owner checkpoint, and derivative
checksum coverage. It is acceptable to normalize packet layout or add missing
prose-only provenance/owner companions, but do not create any future implementation
path listed inside a packet. Keep code, JSDoc, and comments English if encountered;
all owner-facing material must be complete natural Russian.

Validate, without compile or tests:

- exact ten-fixture membership with a 4-development/6-held-out split;
- a bijection between BG-D01 through BG-H06 and all ten frozen behavior classes;
- `BG-H02` remains the sole predeclared challenging case with a prose rationale;
- all six packets have complete EN/RU owner coverage and semantic parity;
- SHA-256 hashes bind every held-out behavior, provenance, and owner packet;
- public provenance, access dates, and licenses/terms are complete and no source has
  uncertain or prohibited provenance;
- prose-only manifest and checksum coverage is exact, with no missing or extra
  intended held-out freeze files;
- YAML and Markdown structure, repository-relative paths, privacy, and
  `git diff --check` pass;
- `micro1-safe-preflight control` and `micro1-safe-preflight implementation` both run
  through the safe interface and their exact safe status is recorded honestly.

Do not run `npm test`, compile, fixture verification, evaluator self-check, replay,
benchmark, official/scored evaluation, model probes, or Chromium. Do not create
fixture implementation; preserving or false-green candidate files; oracle code,
oracle data, hidden expected results, or evaluator code; test code or executable
fixtures; benchmark/scored artifacts; unblinding output; or Chromium artifacts. Do
not modify existing D01-D04 implementation, candidates, oracles, visible assertions,
evaluation semantics, targets, denominators, dependencies, lockfile, runtime policy,
or K=0 isolation.

Only deterministic derivative prose-manifest/checksum reconciliation may be handled
inside this trajectory. At the first other unexpected failure, stop immediately,
write a complete natural-Russian stop card with safe evidence, and do not retry,
diagnose through external state, or widen scope.

On success, stop before any candidate, oracle, fixture, or test code. Create one
complete natural-Russian owner freeze checkpoint that lists the six domains and
invariants at an owner-reviewable level, public-source/license evidence, hashes,
commands and safe results, omissions, the H03/H04 retry, risks, the non-exported
subagent-event limitation, and one exact Russian phrase authorizing or rejecting
only the prose/provenance freeze. That future phrase must not authorize
implementation, candidates, oracles, tests, official/scored runs, live product
models, Claude, Chromium, commit, or push.
