# TRC-BG-D02-D04-INTEGRATION-001 — bounded integration instruction

You are the single Codex CLI implementation process authorized by
`SES-20260830-009`. Work only in the current isolated worktree. Do not inspect or
modify the primary checkout, sibling repositories, external workspaces, browser
state, connected apps, private MCP state, global memory, or any external path.
Do not launch another Codex CLI, subagent, Claude, live product model, Chromium, or
browser process. Do not commit or push.

Before acting, read `AGENTS.md` and all mandatory contracts named there in full:
`docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`, `docs/CLEAN_ROOM_POLICY.md`,
`docs/TRACE_POLICY.md`, `docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`,
`docs/EVALUATION.md`, and `config/topic.yaml`. Then read
`artifacts/trajectories/session-boundaries/SES-20260830-009.yaml` and its Russian
owner card in full. The repository owner supplied the exact approval phrase recorded
in that boundary. A corrected transport retry subsequently passed
`micro1-safe-trace probe` by resolving the capture sink root privately through
`git rev-parse --git-common-dir`; no path or external value was disclosed.

Start from exact HEAD/base
`18510e26fd38e890c0370481ccce114f90cf4ca6`. The only authorized source commits are:

- BG-D02 queued/batched updates:
  `c52bacc0b1d9497990b58bd73d5afdbca95035d5`;
- BG-D03 derived state:
  `fd96a3ffefaaac9de4cabe098632ecc4876a67be`;
- BG-D04 subscription cleanup:
  `140041f82971f73eca17e2735050ecb0ec8de77d`.

Apply only those three commits with `git cherry-pick --no-commit` so the authorized
fixture patches enter the working tree without creating a commit. Resolve only
conflicts caused by integrating those exact patches. Failed D02-001, D04-001, and
D04-002 attempts remain unused. BG-D02 and BG-D03 have owner-reported valid
representative Codex captures. BG-D04 was completed by a native Codex subagent whose
internal raw events are not claimed.

Retain the BG-D02 TypeScript `ParcelDispatchBoard` family. Record the MJS
`QueueBatchCounter` prototype as a discarded experiment and do not retain it in the
integrated implementation. Connect BG-D02 through BG-D04 to the typed common
fixture, descriptor, and task registry and to the existing D01 verification
architecture. Add cross-fixture integration tests for exactly D01 through D04.

Do not change frozen behavior specifications, verifier-only oracles, immutable
candidates, visible assertions, evaluation methodology, targets, denominators,
dependencies, lockfile, runtime policy, K=0, or candidate/oracle semantics. Only
deterministic expected manifest/checksum reconciliation caused by the authorized
integration is permitted. Update only directly necessary shared task-runner files,
README reproduction instructions, current EN/RU projections, Improvement Changelog,
provenance and trajectory indexes, manifests, checksums, and integration evidence.
Owner-facing reports must be complete natural Russian. Code, JSDoc, and comments
must be English.

Verify all of the following in this one trajectory:

1. D01-D04 compile.
2. Every D01-D04 visible gate passes.
3. Every D01-D04 evaluator self-check passes.
4. Oracle/capability isolation passes for both arms and every candidate.
5. Candidate bytes, hashes, before/after identities, and immutable contracts remain
   unchanged.
6. Fixture, arm-visible, verifier-only, candidate, and vertical manifests reconcile.
7. Descriptor, fixture, task, role, profile, proof, runner, and execution-slot
   bijections are exact.
8. The development fixture count is exactly four: BG-D01, BG-D02, BG-D03, BG-D04.
9. Deterministic replay and cross-fixture regression pass.
10. English/Russian documentation parity, checksum coverage/values, and both safe
    preflights pass.

Use the existing typed task runner and repository conventions. Discover exact task
names from repository-local files. An expected deterministic manifest/checksum
mismatch caused only by integration may be reconciled and rerun inside this
trajectory. At the first other unexpected failure, stop immediately, preserve the
evidence in repository-local owner-facing Russian form, and do not retry or widen
scope.

Do not perform an official/scored run, held-out work, unblinding, repair, package,
publication, commit, or push. Finish with a concise factual summary of changes,
commands, results, any expected reconciliation, and remaining review/promotion gates.
