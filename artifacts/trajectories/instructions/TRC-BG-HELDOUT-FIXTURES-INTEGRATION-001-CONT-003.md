# TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-003 — worktree-only patch integration

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the `.git` write-capability stop
in `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-002`. This continuation inherits
the complete integration scope, verification requirements, stop conditions, and
prohibitions from owner-approved `SES-20260830-011`, the original instruction, and
`CONT-001`/`CONT-002`.

Preserve the original failed launcher, the valid read-only/no-change `CONT-001`, and
the valid `.git`-write-denied/no-change `CONT-002` as immutable previous attempts.
Do not read, decode, replay, delete, modify, or automatically promote any saved raw
JSONL or stderr from those attempts.

This is the only authorized `CONT-003` nested Codex CLI. The outer launcher preserves
instruction-file stdin, `--ephemeral`, `--ignore-user-config`, `--json`, the same
model, `--sandbox workspace-write`, direct unchanged JSONL stdout to
`micro1-safe-trace`, and separate safe stderr capture. `--approve-for-me` and
`danger-full-access` are forbidden. Do not launch another Codex CLI, subagent,
Claude, product model, Chromium, or browser.

Before acting, read in full `AGENTS.md`, every mandatory contract it names,
`artifacts/trajectories/session-boundaries/SES-20260830-011.yaml`, its Russian owner
card, the original integration instruction, all prior continuation instructions, the
complete Russian result/stop report, and this instruction. Work only in the current
saved main BeyondGreen repository. Do not inspect another worktree, checkout,
external path, private state, global memory, browser, connected app, or private MCP.
Never print absolute machine paths, private values, denylist terms, or raw trace
payloads.

Confirm exact HEAD and freeze base
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`. Confirm there are no fixture, product,
shared, staged, or unstaged changes; only the expected integration instruction and
Russian service files may be untracked. Verify the following three commits have the
exact expected IDs, subjects, common freeze parent, and fixture-only path sets:

1. `2b61a9954d43686f4c07f10d48bb726d5ed156f0` — BG-H01/BG-H02;
2. `bf94ab99a0162d9a940b9ba129602b0cc59b0f67` — BG-H03/BG-H04;
3. `745b118b4c4da34dde367d84eae8005c3af3c4a2` — BG-H05/BG-H06.

Do not use `cherry-pick`, `--index`, `--cached`, or any operation that writes to
`.git`. Do not create a temporary patch file. Apply the exact binary tree diff of
each verified commit relative to the common freeze base, strictly in the order above.
For each commit, first regenerate the exact diff as a direct pipe and check it:

```text
git diff --binary 830793ce16cb2273e4c9b294a11c7aca952bc7a7 <commit> | git apply --check --binary
```

Only if that command exits successfully, regenerate the same exact direct stream and
apply it to the working tree:

```text
git diff --binary 830793ce16cb2273e4c9b294a11c7aca952bc7a7 <commit> | git apply --binary
```

Do not redirect either stream to a file, retain it in a shell variable, use `tee`, or
alter it. Stop immediately on any check or apply mismatch. After all three streams,
prove the resulting fixture-local working-tree bytes equal the union of the three
authorized commit trees and that no frozen base or unrelated path changed.

Then perform the full previously approved shared integration. Integrate BG-H01
through BG-H06 into the existing generic descriptor/fixture/task architecture with
the smallest necessary shared changes. Modify only directly necessary shared fixture
registry, typed task registry/runner, root package scripts if required, integration
tests, current EN/RU projections, Improvement Changelog, provenance/trajectory
indexes, checksum projection, and the complete natural-Russian result report. Do not
simplify or rewrite fixture-local implementation.

Preserve exact ten-fixture class bijection and 4/6 membership, BG-H02 as the sole
predeclared challenging case, exactly two neutral candidates per held-out fixture,
verifier-only mapping, reciprocal physical access denial, immutable candidate and
oracle bindings, and `K=0`. Public/important TypeScript APIs require concise English
JSDoc; key boundaries and invariants require short English why-comments. All owner-
facing evidence must be complete natural Russian.

Run all previously approved non-official checks:

1. complete TypeScript compile;
2. ordinary tests and every H01-H06 visible test;
3. every H01-H06 evaluator self-check and reciprocal denied-access/isolation probe
   for both arms and candidates;
4. every fixture, arm-visible, candidate, verifier-only, implementation, and
   aggregate manifest/hash check;
5. exact registry/task/descriptor/fixture/class bijections, 4/6 membership,
   challenging-case identity, neutral candidate cardinality, physical isolation, and
   `K=0` integration tests;
6. arm-visible/candidate mapping and oracle-leak scans;
7. safe deterministic development replay/regression only, with no official/scored
   execution or unblinding;
8. deterministic checksum reconciliation and verification;
9. both owner-approved safe preflights, control and implementation, without
   disclosing external paths or private values.

Deterministic compile/test/hash/path/JSDoc corrections and directly necessary shared
integration changes are allowed when they preserve frozen semantics and are fully
recorded. Stop immediately on a new transport failure, any patch mismatch,
contamination, uncertain provenance/license, semantic drift, oracle leakage, loss of
physical isolation, shared-methodology change, external/private context, or need for
official execution/unblinding. Do not conceal failures, retries, negative results, or
limitations.

Update
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`
into a complete natural-Russian integration result while preserving all earlier
attempt history and limitations. Record the exact three source commits, patch-stream
checks/applies, byte-equivalence proof, shared changes, commands/results,
deterministic corrections/retries, manifest/hash/isolation/K=0/checksum/preflight
evidence, automatic-capture status, omissions, residual risks, the native-subagent
raw-event limitation, and exact owner-acceptance scope.

All other `SES-20260830-011` restrictions remain: no `.git` write, integration
commit, push, official/scored run, unblinding, live product model, Claude, Chromium,
browser, private MCP, private data, packaging, publication, or submission. Finish
with all verified integration changes present but uncommitted and stop for separate
owner acceptance. Your JSONL stdout is captured automatically and must remain
faithful.
