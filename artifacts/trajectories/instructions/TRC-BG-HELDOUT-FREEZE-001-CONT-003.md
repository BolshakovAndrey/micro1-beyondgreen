# TRC-BG-HELDOUT-FREEZE-001-CONT-003 — owner-authorized main-root handoff completion

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the fail-closed stop in
`TRC-BG-HELDOUT-FREEZE-001-CONT-002`. This is a continuation of
`SES-20260830-010`, not a new product session. Work only in the current clean
repository root on branch `codex/beyondgreen-d01-scale003-correct` at exact `HEAD`
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. The current changes were restored by
an owner-authorized handoff from the preserved stash named
`bg-heldout-freeze-handoff`; do not apply, pop, drop, clear, or otherwise modify any
stash. Do not inspect another checkout, worktree, sibling repository, external
workspace, browser state, connected app, private MCP state, global memory, or any
external path. Do not print environment-variable values or absolute machine paths.
Do not launch another Codex CLI, subagent, Claude, live product model, Chromium, or
browser process. Do not commit or push.

Before acting, read `AGENTS.md` and every mandatory repository contract it names in
full: `docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`,
`docs/CLEAN_ROOM_POLICY.md`, `docs/TRACE_POLICY.md`,
`docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, and
`config/topic.yaml`. Then read the complete authorized SES-010 service history:

- `artifacts/trajectories/session-boundaries/SES-20260830-010.yaml`;
- `artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-PRECAPTURE-STOP_RU.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-COORDINATOR-STOP_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-CONT-001-STOP_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-002.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-CONT-002-STOP_RU.md`;
- this instruction,
  `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-003.md`.

The owner has explicitly authorized the stash handoff into this current saved main
working copy and this exact `CONT-003` trajectory. Treat the existing BG-H01 through
BG-H06 prose/freeze packets, all prior stop cards, and the partial derivative files
as expected authorized input. Preserve the stop history unchanged. Preserve the
reported H03/H04 service retry: Ruby/Psych lacked `safe_load_file`, and the corrected
structural read used `safe_load(File.read(...))` without changing product content.
The internal raw events of the three native authoring subagents are not claimed as
exported traces.

Complete only the text integration and freeze of `BG-H01` through `BG-H06`. Inspect
the current prose/freeze surface and add the missing parity materials required by
existing repository conventions. In particular, ensure each fixture has complete
English and natural-Russian behavior/freeze/provenance/owner-assignment coverage,
including behavior, frozen observable actions and invariants, visible-test blind
spot, prose-only behavior-preserving and seeded false-green candidate contracts,
physical oracle/capability boundaries with `K=0`, public source URL, author/project,
access date, license or terms, independent synthetic clean-room attestation, planned
future files clearly marked not created, expected failures, uncertainties, and
fail-closed stop conditions. Do not create any planned future implementation path.

Create or update only the directly necessary prose-freeze records: the six EN/RU
behavior/freeze/provenance/owner packets, fixture assignment and challenging-case
records, machine-readable held-out freeze manifest, English freeze summary, complete
natural-Russian final owner freeze checkpoint, `config/topic.yaml` projection, and
derivative hash/checksum coverage. Deterministic corrections to repository-relative
paths, EN/RU parity projections, SHA-256 bindings, and checksum manifests are
authorized without another approval. Do not change product semantics.

Validate, without compile or tests:

1. Exact ten-fixture membership with a 4-development/6-held-out split.
2. A bijection between `BG-D01` through `BG-H06` and all ten frozen behavior classes.
3. `BG-H02` is the sole predeclared challenging case, with its prose rationale.
4. Complete semantic parity of English and natural-Russian behavior, freeze,
   provenance, assignment, challenging-case, manifest, and owner materials for all
   six held-out fixtures.
5. SHA-256 bindings for every held-out behavior, freeze, provenance, owner, assignment,
   challenging-case, summary, and manifest input required by the repository convention.
6. Complete public provenance, access dates, licenses/terms, and clean-room
   attestations, with no uncertain or prohibited provenance.
7. Exact prose-only manifest and checksum coverage, with no missing or unintended
   held-out freeze paths.
8. YAML and Markdown structure, repository-relative paths, privacy, and
   `git diff --check`.
9. `npm run checksums:write`, followed by `npm run checksums:check`.
10. `micro1-safe-preflight control` and `micro1-safe-preflight implementation` from
    this current authorized root, recording their exact safe statuses honestly.

Do not run `npm test`, compile, fixture verification, evaluator self-check, replay,
benchmark, official/scored evaluation, model probes, or Chromium. Do not create
fixture implementation; preserving or false-green candidate files; oracle code,
oracle data, hidden expected results, evaluator code; test code or executable
fixtures; benchmark/scored artifacts; unblinding output; or Chromium artifacts. Do
not modify D01-D04 implementation, candidates, oracles, visible assertions,
evaluation semantics, targets, denominators, dependencies, lockfile, runtime policy,
or `K=0` isolation.

Only deterministic path, parity-projection, hash, and checksum corrections may be
handled after an ordinary validation discrepancy. At the first contamination
finding, scope change, uncertain provenance/license, private-context indication, or
other nondeterministic failure, stop immediately, write a complete natural-Russian
`CONT-003` stop card containing only safe evidence, and do not retry, inspect
external state, or widen scope.

On full success, stop before any candidate, oracle, fixture, or test code and do not
claim that the owner has approved the freeze. Finalize one complete natural-Russian
owner freeze-approval card that explains the six domains and invariants, exact files
and hashes, commands and results, omissions, all prior stops and the H03/H04 retry,
provenance/license evidence, privacy status, residual risks, trace capture/review
status, the non-exported subagent-event limitation, and the exact scope the owner
would approve. Include one exact Russian approval phrase authorizing only this prose
and public-provenance freeze. That phrase must explicitly leave implementation,
candidates, oracles, tests, official/scored runs, live product models, Claude,
Chromium, commit, and push unauthorized.
