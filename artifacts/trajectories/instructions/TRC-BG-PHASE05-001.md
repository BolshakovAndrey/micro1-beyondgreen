# TRC-BG-PHASE05-001 — Coordinator-issued clean Phase 0.5 instruction scope

The repository owner authorized Phase 0.5 through a sequence of explicit checkpoints.
Codex coordinator issued bounded clean-task packets implementing those decisions.
This file consolidates the material Phase 0.5 instructions; it is not presented as a
single verbatim owner message. It excludes the unrelated trace-first prelude,
internal thread/message/tool-call identifiers, private coordination content, and the
later trace-packaging instruction.

## Clean boundary

- Continue only under approved boundary `SES-20260829-002` on branch
  `impl/beyondgreen` from clean checkpoint
  `2a41d4d2bab8a8b25406400a8ec7d8cd21112a4e`.
- Read and apply `AGENTS.md`, challenge/rules, clean-room, trace, preflight,
  `PROJECT_SPEC` section 20, `EVALUATION`, and `config/topic.yaml`.
- Work only inside the clean repository root. Do not use private repositories,
  sibling workspaces, home/shared memory, browser state, connected apps, MCP, or
  employer/client material.
- Never expose environment values, raw private paths, denylist terms, credentials,
  identity metadata, or unrelated conversation content.
- Do not commit or push.

## Decision research scope

Prepare an English normative candidate, a full Russian counterpart, a plain-Russian
owner card, and a machine-readable proposal covering all seven section-20 decisions:

1. exact public signals package/version/license/capabilities;
2. exact Node/TypeScript/React/ReactDOM/jsdom/Zod/support-package versions and npm
   lockfile policy;
3. provider-neutral reasoning adapter and deterministic offline fallback;
4. enforceable call/time/budget policy;
5. versioned deterministic JSONL replay format and acceptance checks;
6. exact four-development/six-held-out behavior-class assignment with `BG-D01` first
   and `BG-H02` challenging;
7. a correctness-first Chromium protocol.

Use only allowed public npm metadata and official upstream repository/license URLs.
Do not install during research, execute a model, run Chromium, create product code,
fixtures, candidates, hidden oracles, or an official benchmark.

The final owner-selected live candidate contract was local ChatGPT-authenticated
`codex exec`, model `gpt-5.6-sol`, fixed subscription billing, one call, zero
automatic model/transport retries, no substitute, a 180-second total timeout, and
`not_measured` marginal USD/tokens when the CLI does not report them stably. The
OpenAI Responses API/SDK alternative and temporary cost-driven Terra candidate were
rejected. Offline replay remained mandatory.

The Chromium protocol was frozen as an independently authored neutral museum visit
group allocation board: 300 synthetic cards in an ordinary CSS grid, identical
baseline/advanced observables, ordered bulk actions, complete correctness before
performance, explicit userland render counters, CDP `TaskDuration` before/after
deltas, five warmups plus 30 measured samples per arm in alternating order, and no
card-count tuning after results.

## Measured-spike authorization

The owner was shown:

`«Одобряете технический Phase 0.5 spike с подписочным codex exec/gpt-5.6-sol без API, одним вызовом без повторов и сценарием на 300 карточек?»`

Selected response: `«Одобряю spike (Recommended)»`.

This authorized only the bounded measured spike: exact dependency/lockfile changes,
`npm ci` reproducibility, a transitive-license hard gate, spike-only stack/replay/
model/Chromium harnesses, and one live subscription command. Product implementation,
scored fixtures/candidates/oracles, official benchmark/scoring, repair demo, commit,
and push remained forbidden.

## License checkpoints

After the initial npm-query audit failed, the owner authorized exactly one corrected
repository-local audit:

- Question:
  `«Разрешаете одну исправленную попытку transitive-license audit через локальный Node-сканер, после которой spike продолжится только при полном PASS?»`
- Selected response: `«Разрешаю retry (Recommended)»`.

The scanner had to read only `package-lock.json` and installed repository-local
`node_modules/**/package.json`, enumerate normalized SPDX licenses, fail closed on
missing/unknown/non-allowlisted expressions or lock/install mismatch, emit a
privacy-safe report, and have focused tests. The external npm debug log was forbidden.

When that audit found only `MIT-0` and `BlueOak-1.0.0` outside the allowlist, the
owner authorized the precise amendment:

- Question:
  `«Одобряете MIT-0 и BlueOak-1.0.0 как допустимые permissive-лицензии, обязательный THIRD_PARTY_NOTICES.md и один финальный повтор audit?»`
- Selected response: `«Одобряю и продолжить (Recommended)»`.

Only those SPDX identifiers could be added. The final audit had to pass 54/54 against
the unchanged dependency inventory/lockfile, and `THIRD_PARTY_NOTICES.md` had to be
generated reproducibly.

## Live-call and diagnostic checkpoints

The first live call failed. The owner then authorized exactly one privacy-safe retry
of the same command/model:

- Question:
  `«Разрешаете один privacy-safe диагностический повтор той же подписочной команды codex exec без замены модели?»`
- Selected response: `«Разрешаю один retry (Recommended)»`.

Before that retry, stderr had to be classified in memory into a narrow safe enum and
redacted for paths, emails, account/project IDs, tokens/secrets, and machine
identifiers. Raw stderr or arbitrary excerpts could not be persisted. This was the
second and final Phase 0.5 live invocation; no third invocation or substitute was
allowed.

After the second failure, the owner authorized non-model CLI diagnostics only:

- Question:
  `«Разрешаете non-model диагностику Codex CLI (login status в exact-mode и codex doctor) с сохранением только безопасного класса результата?»`
- Selected response: `«Разрешаю диагностику (Recommended)»`.

No model or `codex exec` call was allowed. Only exact-mode status, `codex doctor`,
advertised usage/limit/status diagnostics, tested redaction, and a privacy-safe
DNS/HTTPS matrix for allowlisted public OpenAI/ChatGPT hosts were permitted. Persist
only safe categories, booleans, exit codes, hashes, elapsed buckets, and redaction
counts—never raw output, IPs, headers, cookies, bodies, credentials, private paths,
or unknown hostnames.

## Owner waiver and completion

The repository owner then made this exact conditional decision:

`«Я не вижу смысла в этом запуске, мы и так работаем через Sol сейчас, когда я отправляю эти запросы. Возможно, это не работает при вызове из приложения ChatGPT на Mac, но это и не важно, потому что запросы проходят и ответы приходят. Я хочу скипнуть эту проверку и, если других препятствий нет, закрыть эту часть»`.

Apply it only to the nested CLI gate for Phase 0.5. Do not relabel either failed call
as PASS or claim the CLI path is proven. App-level Sol operation is owner-observed
context, not submission evidence. Keep `codex exec` as an optional normal-terminal
adapter contract with no retries and runtime validation deferred/unverified. Treat
deterministic offline replay as the verified reproducibility path.

Proceed only to the fixed 300-card Chromium spike. Freeze Phase 0.5 only if its
correctness/evidence condition passes, while disclosing any negative performance
result. Do not authorize product/scored work, official benchmark, commit, or push.

## Trace and release boundary

Preserve all decisions, commands, failures, retries, owner checkpoints, measurements,
waiver limitations, and validation results in the Improvement Changelog and evidence.
Any submission-layer trajectory requires immutable external capture, automated scan,
privacy review, full English/Russian traces, a plain-Russian owner card, and explicit
repository-owner human review before entry in `actual_traces`.
