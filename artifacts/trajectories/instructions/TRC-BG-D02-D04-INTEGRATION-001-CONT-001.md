# TRC-BG-D02-D04-INTEGRATION-001-CONT-001 — authorized continuation

You are the single Codex CLI continuation process authorized after the operational
TS2688 stop in `TRC-BG-D02-D04-INTEGRATION-001`. Work only in the current isolated
worktree. Do not inspect or modify the primary checkout, sibling repositories,
external workspaces, browser state, connected apps, private MCP state, or global
memory. Do not launch another Codex CLI, subagent, Claude, live product model,
Chromium, or browser process. Do not commit or push.

Before acting, read `AGENTS.md` and every mandatory contract named there in full.
Then read these repository-local records in full:

- `artifacts/trajectories/session-boundaries/SES-20260830-009.yaml`;
- `artifacts/trajectories/reviews/BG-D02-D04-INTEGRATION-SESSION-BOUNDARY-CHECKPOINT_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-D02-D04-INTEGRATION-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-D02-D04-INTEGRATION-001-STOP_RU.md`.

The owner classified TS2688 as an operational clean-worktree dependency blocker,
not an integration defect. The coordinator verified that `package-lock.json` is
byte-unchanged relative to exact base
`18510e26fd38e890c0370481ccce114f90cf4ca6`, with SHA-256
`1beb2987e3e0654cb525b80c2f97a2f6577f92399e8a78fcd8eb9f9ab6487d4c`, and then
ran exactly one successful `npm ci --ignore-scripts` using the public npm registry.
Preserve the original trace as an immutable failed first part.

The three authorized fixture commits have already been applied with
`git cherry-pick --no-commit` and no conflicts. `HEAD` remains the exact base. Do not
cherry-pick again and do not alter the index merely to restage or rewrite source
history.

Resume exactly with:

```text
npm run compile
```

If compile passes, complete only the integration scope already authorized in
`SES-20260830-009`:

- retain the BG-D02 TypeScript `ParcelDispatchBoard` family;
- keep the MJS `QueueBatchCounter` prototype discarded and record that disposition;
- connect BG-D02, BG-D03, and BG-D04 to the typed common fixture, descriptor, task
  registry, and existing D01 verification architecture;
- add cross-fixture integration tests for exactly D01 through D04;
- update only directly necessary task-runner, README reproduction, current EN/RU
  projections, Improvement Changelog, provenance/trajectory indexes, manifests,
  checksums, and integration evidence.

Do not change frozen behavior specifications, verifier-only oracles, immutable
candidates, visible assertions, evaluation methodology, targets, denominators,
dependencies, lockfile, runtime policy, K=0, or candidate/oracle semantics. Only
deterministic expected manifest/checksum reconciliation caused by this authorized
integration is permitted. Owner-facing reports must be complete natural Russian;
code, JSDoc, and comments must be English.

Perform the complete previously approved D01-D04 verification: compile, all visible
gates, evaluator self-checks, physical oracle/capability boundaries for both arms and
all candidates, immutable candidate/hash/manifest checks, descriptor/fixture/task/
role/profile/proof/runner/slot bijections, exact four-development-fixture count,
deterministic replay, cross-fixture regression, EN/RU documentation parity,
checksum coverage and values, and both safe preflights. Discover exact repository
task names from the typed registry and local scripts. Do not perform any official or
scored run.

An expected deterministic manifest/checksum mismatch caused solely by the authorized
integration may be reconciled and rerun inside this continuation. At the first other
unexpected failure, stop immediately, create a complete natural-Russian stop card,
and do not retry or widen scope.

On complete success, create a complete natural-Russian owner checkpoint card that
states changes, exact commands, results, expected reconciliation, unchanged frozen
surfaces, trace review status, remaining gates, and the exact scope a later owner
approval would authorize. Finish with a concise factual summary. Do not promote the
new captures into eligible evidence without the ordinary automated scan, complete
EN/RU review, and separate owner approval.
