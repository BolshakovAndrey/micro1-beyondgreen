# TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-004 — clean-room recovery verification

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the fail-closed clean-room stop
in `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-003`. This continuation inherits
the full integration scope, verification requirements, stop conditions, and
prohibitions from owner-approved `SES-20260830-011` and all earlier instructions.

Before this capture, the parent coordinator verified that exactly these three
repository-local service worktrees were clean and at the exact authorized commits,
then removed only them with ordinary `git worktree remove` and without `--force`:

- `.codex-worktrees/h01-h02` at
  `2b61a9954d43686f4c07f10d48bb726d5ed156f0`;
- `.codex-worktrees/h03-h04` at
  `bf94ab99a0162d9a940b9ba129602b0cc59b0f67`;
- `.codex-worktrees/h05-h06` at
  `745b118b4c4da34dde367d84eae8005c3af3c4a2`.

Their branch refs and commits remain preserved. Do not inspect another checkout,
worktree, external path, raw trace, private state, browser, connected app, private
MCP, or global memory. Do not recreate or remove any worktree. Do not read, decode,
replay, delete, modify, or automatically promote saved raw JSONL/stderr from earlier
attempts.

This is the only authorized `CONT-004` nested Codex CLI. The outer launcher uses
instruction-file stdin, `--ephemeral`, `--ignore-user-config`, `--json`, the same
model, `--sandbox workspace-write`, direct unchanged JSONL stdout to
`micro1-safe-trace`, and separate safe stderr capture. `--approve-for-me` and
`danger-full-access` are forbidden. Do not launch another Codex CLI, subagent,
Claude, product model, Chromium, or browser.

Before acting, read in full `AGENTS.md`, every mandatory contract it names,
`artifacts/trajectories/session-boundaries/SES-20260830-011.yaml`, its Russian owner
card, the original instruction, every prior continuation instruction, the complete
Russian result report, and this instruction. Work only in the current saved main
BeyondGreen repository. Never print absolute machine paths, private values, denylist
terms, raw trace payloads, or text from the prior scanner findings.

Confirm HEAD remains
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`. Preserve the already applied union of
all `103/103` fixture-local files from the three authorized commits exactly; do not
reapply, rewrite, simplify, or stage them. Confirm no `.codex-worktrees/h01-h02`,
`h03-h04`, or `h05-h06` directory remains, without listing or inspecting any other
worktree metadata.

Continue the incomplete shared integration already present from `CONT-003`. First
fix only the known too-broad new integration-test assertion that scanned the entire
descriptor file and incorrectly matched historical D04 names; scope it precisely to
the six held-out neutral candidate declarations without changing frozen semantics or
methodology. Then diagnose and correct only deterministic integration-scope
compile/test/hash/path/JSDoc issues as they arise. Directly necessary shared registry,
typed tasks/runner, package scripts if required, integration tests, EN/RU
projections, Improvement Changelog, provenance/trajectory indexes, checksum
projection, and Russian result evidence remain in scope. Do not change evaluation
targets, denominators, dependencies, runtime policy, official-run behavior, or `K=0`.

Maintain exact ten-fixture class bijection and 4/6 membership, BG-H02 as the sole
predeclared challenging case, exactly two neutral candidates per held-out fixture,
verifier-only mapping, immutable candidates/oracles, reciprocal physical isolation,
and zero evaluator feedback before immutable decisions. Public/important TypeScript
APIs require concise English JSDoc; boundary/invariant comments must be short English
why-comments. Owner-facing evidence must be complete natural Russian.

Run and record the complete non-official verification:

1. `npm run compile`;
2. full `npm test` after the service worktree removal and deterministic test fix;
3. every H01-H06 fixture verification task, visible test, evaluator self-check, and
   reciprocal denied-access/isolation probe for both arms and both candidates;
4. all fixture, arm-visible, candidate, verifier-only, implementation, and aggregate
   manifest/hash checks;
5. integration checks for exact registry/task/descriptor/fixture/class bijections,
   4/6 membership, challenging-case identity, neutral candidate cardinality,
   physical isolation, and `K=0`;
6. arm-visible/candidate mapping and oracle-leak scans;
7. safe deterministic development replay/regression only, without official/scored
   execution or unblinding;
8. deterministic checksum reconciliation and verification;
9. `git diff --check`;
10. both owner-approved safe preflights, control and implementation, without
    disclosing external paths, private values, denylist terms, or raw findings.

The previously observed scanner results from removed service worktrees are historical
stop evidence, not current contamination evidence. If the clean rerun reports any
new contamination, machine-path, private-context, semantic-drift, oracle-leakage,
isolation, provenance/license, methodology, or external-path failure, stop
immediately without another nested CLI. Ordinary deterministic integration-scope
compile/test/hash/path/JSDoc failures may be corrected and rerun within this one
trajectory, with every failure and correction recorded. Do not hide retries,
negative results, or limitations.

Update
`artifacts/trajectories/reviews/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-RESULT_RU.md`
into the complete natural-Russian integration and owner-acceptance packet while
preserving the exact history of all earlier attempts and the pre-capture worktree
recovery. Record commands/results, deterministic corrections/retries, fixture byte
preservation, shared changes, manifest/hash/isolation/K=0/checksum/diff/preflight
evidence, automatic-capture limitations, residual risks, and that native subagent
internal raw events are not claimed as exported traces.

All other restrictions of `SES-20260830-011` remain: no `.git` write, integration
commit, push, official/scored run, unblinding, live product model, Claude, Chromium,
browser, private MCP, private data, packaging, publication, or submission. Finish
with the fully verified integration changes uncommitted and stop for separate owner
acceptance. Your JSONL stdout is captured automatically and must remain faithful.
