# TRC-BG-HELDOUT-FREEZE-001-CONT-001 — authorized prose-freeze continuation

You are the single automatically captured Codex CLI continuation coordinator
explicitly authorized by the repository owner after the fail-closed stop in
`TRC-BG-HELDOUT-FREEZE-001`. Work only in the current isolated BeyondGreen worktree
at exact base commit `f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Do not inspect
or modify the primary checkout, another Git worktree, sibling repositories, external
workspaces, browser state, connected apps, private MCP state, global memory, or any
external path. Do not print environment-variable values or absolute machine paths.
Do not launch another Codex CLI, subagent, Claude, live product model, Chromium, or
browser process. Do not commit or push.

Before acting, read `AGENTS.md` and every mandatory repository contract it names in
full: `docs/CHALLENGE.md`, `docs/HACKATHON_RULES.md`,
`docs/CLEAN_ROOM_POLICY.md`, `docs/TRACE_POLICY.md`,
`docs/PREFLIGHT_CHECKLIST.md`, `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, and
`config/topic.yaml`. Then read all of the following authorized service history in
full:

- `artifacts/trajectories/session-boundaries/SES-20260830-010.yaml`;
- `artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`;
- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-PRECAPTURE-STOP_RU.md`;
- `artifacts/trajectories/reviews/TRC-BG-HELDOUT-FREEZE-001-COORDINATOR-STOP_RU.md`;
- this continuation instruction,
  `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001-CONT-001.md`.

The owner explicitly authorized both stop cards as service history. Their presence
is expected and is not an input-surface deviation. Preserve them unchanged. The
original coordinator read none of the six packets and made no integration changes,
so continue from the transferred prose surface without inheriting semantic findings
from the stopped attempt.

The expected new input surface is exactly:

- the six authorized service-history files listed above;
- the BG-H01 through BG-H06 Markdown/YAML files currently under
  `evaluation/behavior-specs/` and `evaluation/manifests/BG-H01/` through
  `evaluation/manifests/BG-H02/`.

Three native Codex subagents independently authored one pair each in isolated clean
worktrees: H01/H02, H03/H04, and H05/H06. Their internal raw events are not claimed
as exported traces. Preserve independent authorship. Preserve and verify the one
reported H03/H04 structural-validation retry: Ruby/Psych lacked `safe_load_file`;
the corrected structural read used `safe_load(File.read(...))` and passed without
changing product content.

Integrate exactly these assignments and no others:

- `BG-H01` / `prop_reset`;
- `BG-H02` / `async_ordering`, retaining its predeclared challenging-case label;
- `BG-H03` / `identity_stability`;
- `BG-H04` / `conditional_lifecycle`;
- `BG-H05` / `external_store`;
- `BG-H06` / `rollback`.

For every fixture, verify and minimally normalize only prose/freeze records so they
contain a neutral user/domain, frozen observables and invariants, visible-test blind
spot, prose-only behavior-preserving and seeded false-green candidate contracts,
physical oracle/capability boundaries with K=0 and no hidden content, public source
URL/project/access date/license or terms, independent synthetic clean-room
attestation, future planned files/tests/manifests clearly marked not created,
expected failures and fail-closed stop conditions, and a complete natural-Russian
owner companion. Do not create any planned future implementation path.

Use existing repository conventions. Create or update only directly necessary
prose-freeze records: exact fixture assignment/challenging-case records, provenance
index entries, one machine-readable held-out freeze manifest, one English freeze
summary, one complete natural-Russian owner freeze checkpoint, and derivative
checksum coverage. It is acceptable to normalize packet layout or add missing
prose-only provenance/owner companions. Owner-facing material must be complete,
natural Russian and must explain evidence, omissions, retries, provenance/licenses,
privacy, risks, and exact approval/non-approval scope without requiring English
documents.

Validate, without compile or tests:

1. Exact ten-fixture membership with a 4-development/6-held-out split.
2. A bijection between BG-D01 through BG-H06 and all ten frozen behavior classes.
3. `BG-H02` remains the sole predeclared challenging case with a prose rationale.
4. All six packets have complete English/Russian owner coverage and semantic parity.
5. SHA-256 hashes bind every held-out behavior, provenance, freeze, and owner packet.
6. Public provenance, access dates, licenses/terms, and clean-room attestations are
   complete and contain no uncertain or prohibited source.
7. Prose-only manifest and checksum coverage has no missing or unintended paths.
8. YAML and Markdown structure, repository-relative paths, privacy, and
   `git diff --check` pass.
9. `micro1-safe-preflight control` and `micro1-safe-preflight implementation` both
   run through the safe interface and their exact safe results are recorded honestly.

Do not run `npm test`, compile, fixture verification, evaluator self-check, replay,
benchmark, official/scored evaluation, model probes, or Chromium. Do not create
fixture implementation; preserving or false-green candidate files; oracle code,
oracle data, hidden expected results, evaluator code; test code or executable
fixtures; benchmark/scored artifacts; unblinding output; or Chromium artifacts. Do
not modify D01-D04 implementation, candidates, oracles, visible assertions,
evaluation semantics, targets, denominators, dependencies, lockfile, runtime policy,
or K=0 isolation.

Only deterministic derivative prose-manifest/checksum reconciliation may be handled
inside this continuation. At the first other unexpected failure, stop immediately,
write a complete natural-Russian continuation stop card with safe evidence, and do
not retry, inspect external state, or widen scope.

On success, stop before any candidate, oracle, fixture, or test code. Create a
complete natural-Russian owner freeze checkpoint recording the six domains and
invariants at an owner-reviewable level, public-source/license evidence, hashes,
commands and safe results, omissions, both prior stops, the H03/H04 retry, risks,
the non-exported subagent-event limitation, trace-review status, and one exact
Russian phrase authorizing only the prose/provenance freeze. The future phrase must
not authorize implementation, candidates, oracles, tests, official/scored runs,
live product models, Claude, Chromium, commit, or push.
