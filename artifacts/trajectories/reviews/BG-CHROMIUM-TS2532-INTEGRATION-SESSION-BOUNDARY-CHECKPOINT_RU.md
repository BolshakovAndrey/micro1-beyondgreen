# Контрольная карточка границы переноса исправления TS2532

**Граница:** `SES-20260830-014`<br>
**Режим:** ограниченный перенос одного уже проверенного исправления<br>
**Статус:** граница одобрена; перенос и проверки прошли; разрешён один checkpoint commit без push

## Что нужно одобрить

Нужно одобрить только перенос ранее проверенного исправления ошибки компилятора
TypeScript `TS2532` из clean worktree — отдельной чистой рабочей копии — задачи
`01a053fa-f863-75e1-a8b6-6416aa10e02f` в основную рабочую копию BeyondGreen.
Разрешаемая поверхность исходника ограничена отфильтрованной разницей Git ровно
одного файла: `spikes/phase-0.5/chromium-runner.test.ts`.

До точного одобрения исходная рабочая копия, её файлы и её разница не читались;
`git apply --check` не запускался, исправление не применялось, тесты и компиляция не
запускались. Эта карточка не утверждает, что будущий patch — набор переносимых
изменений — уже повторно проверен в основной рабочей копии.

## Проверенное исходное состояние

- Работа ведётся только в сохранённом clean repository root — чистом корне
  репозитория хакатона.
- Ветка: `codex/beyondgreen-d01-scale003-correct`.
- Точный `HEAD`: `e08f2f91440a484df9f9c9ae772461bd3d9d49d1`.
- Начальный `git status --porcelain=v1` пуст: рабочее дерево было чистым.
- Git blob — объект содержимого файла — для
  `spikes/phase-0.5/chromium-runner.test.ts` в этом `HEAD` в точности равен
  `0a4cc431bac82ea10d2245be9f3525e0a2920442`.
- Полностью прочитаны `AGENTS.md`, `docs/CHALLENGE.md`,
  `docs/HACKATHON_RULES.md`, `docs/CLEAN_ROOM_POLICY.md`,
  `docs/TRACE_POLICY.md`, `docs/PREFLIGHT_CHECKLIST.md`,
  `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, `config/topic.yaml` и
  `templates/SESSION_BOUNDARY.yaml`.
- Исходная рабочая копия задачи, соседние репозитории, внешняя память, браузерное
  состояние, подключённые приложения и private MCP — частные подключения
  инструментов — не инспектировались.

До одобрения созданы только два служебных файла: сама граница
`artifacts/trajectories/session-boundaries/SES-20260830-014.yaml` и эта карточка.
Код, тесты, конфигурация, проекции, индексы, трассы и контрольные суммы не менялись.

## Что разрешит точное одобрение

После одобрения разрешена только следующая последовательность:

1. Найти clean worktree исходной задачи, не читая ничего кроме отфильтрованной
   разницы единственного разрешённого файла.
2. Получить patch только для
   `spikes/phase-0.5/chromium-runner.test.ts`.
3. Сначала выполнить `git apply --check` в основной рабочей копии. Эта команда
   проверяет применимость patch без изменения файлов.
4. Только после успешной проверки применить этот единственный patch.
5. Подтвердить SHA-256 итогового файла:
   `de0ea02cfe777fa21e5720b8ce7832b17d81cf6de36508c3884707a0e5306cb5`.
6. Запустить только целевой тест `chromium-runner.test.ts` и получить `4/4`.
7. Выполнить `npm run compile`.
8. Выполнить `git diff --check` для проверки пробелов и корректности разницы.
9. Согласовать только непосредственно необходимые производные записи контрольных
   сумм, если их уже существующая область охватывает изменённый файл или эту
   границу; затем повторно проверить точность их области.
10. Остановиться перед `commit` и передать владельцу полные результаты, ошибки,
    ограничения и оставшиеся риски.

Если `HEAD`, исходный blob или рабочее дерево неожиданно изменятся, patch затронет
больше одного файла, `git apply --check` не пройдёт, итоговый SHA-256 не совпадёт,
тесты или компиляция завершатся ошибкой либо область производных контрольных сумм
окажется неоднозначной, задача обязана немедленно остановиться. Расширять исправление
или самостоятельно устранять новую проблему нельзя.

## Что нельзя переносить или читать

- `SES-20260830-013` и любые связанные с ним изменения.
- `node_modules`.
- Любой другой файл, полную разницу исходной рабочей копии, незарегистрированные
  файлы или фоновые изменения.
- Необязательные производные артефакты, новые трассы, индексы, конфигурацию,
  продуктовые проекции, кандидаты, скрытые эталоны и фикстуры.

Разрешение относится только к отфильтрованной разнице единственного файла. Оно не
разрешает использовать исходную рабочую копию как общий источник контекста.

## Что останется запрещено даже после одобрения

- Официальный либо оцениваемый запуск и раскрытие отложенных результатов.
- Фактический запуск Chromium; разрешён только целевой тест его локального runner —
  управляющего модуля — без браузера.
- Вызов живой модели продукта.
- Управление браузером, подключёнными приложениями или MCP.
- Любой нецелевой тестовый набор.
- Любые изменения вне единственного целевого файла и строго необходимых
  производных контрольных сумм.
- `commit`, `push`, публикация, упаковка и отправка.

## Риски и предел одобрения

Главный риск — случайно перенести соседнее изменение из исходной рабочей копии или
принять более широкий patch за исправление одной ошибки. Поэтому сначала проверяются
исходный `HEAD`, blob, чистота дерева и однофайловая область, затем применяется
только прошедший `git apply --check` patch и сверяется заранее известный SHA-256.

Второй риск — обновить лишние контрольные суммы и тем самым скрыть нежелательное
изменение. Разрешены только уже необходимые производные записи с явной связью с
целевым файлом или этой границей; при любой неоднозначности требуется остановка.

Это одобрение не подтверждает будущий результат, не делает задачу автоматически
пригодной для отправки судьям и не разрешает коммит. После проверок всё изменённое
состояние остаётся незакоммиченным до отдельного решения владельца.

## Результат и отдельное разрешение на checkpoint commit

После точного одобрения однофайловый patch прошёл `git apply --check` и был применён.
SHA-256 итогового `spikes/phase-0.5/chromium-runner.test.ts` совпал с ожидаемым:
`de0ea02cfe777fa21e5720b8ce7832b17d81cf6de36508c3884707a0e5306cb5`.
Целевой тест прошёл `4/4`, `npm run compile` и `git diff --check` завершились
успешно, а `docs/CONTROL_PLANE_SHA256SUMS` подтвердил `504/504` записей.
`SES-20260830-013`, `node_modules` и фоновые изменения не переносились.

Владелец отдельно разрешил обе preflight-проверки — предварительные комплексные
проверки чистой среды — и один checkpoint commit, то есть контрольный коммит, с
сообщением `fix(Evaluation):[BeyondGreen] Narrow final Chromium snapshot`. В commit
могут войти ровно четыре подтверждённых файла этой задачи. Перед commit обязательна
проверка `git diff --cached --check`. `push` остаётся запрещён.

## Точная фраза одобрения

Если граница верна, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-014 и разрешаю только однофайловый перенос исправления TS2532 для spikes/phase-0.5/chromium-runner.test.ts из задачи 01a053fa-f863-75e1-a8b6-6416aa10e02f по русской owner-карточке; SES-20260830-013, node_modules и фоновые изменения не читать и не переносить; остановиться перед commit.
