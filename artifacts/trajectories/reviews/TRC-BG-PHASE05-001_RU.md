# TRC-BG-PHASE05-001 — одобренная карточка владельца

Мы проверяем, точно ли безопасный русский и английский отчёты передают завершённую
работу Phase 0.5 после удаления приватных сведений. Автоматическая проверка читаемых
файлов прошла, и владелец подтвердил обе версии как точный и безопасный отчёт с
сохранённым после исключений техническим смыслом.

## Что было сделано

Phase 0.5 — подготовительный этап перед кодом продукта. На нём зафиксировали версии
библиотек и lockfile, библиотеку signals, правила модели и offline replay (повтора
без сети), десять классов поведения и браузерный протокол на 300 музейных карточках.
Код продукта и оцениваемые примеры не создавались.

Модель задачи обозначена как `Codex desktop / GPT-5.6 Sol (owner-observed current default; raw export has no model field)`. Простыми словами: отдельной настройки
модели в задаче не было, поэтому она унаследована от приложения; Sol назвал и
наблюдал владелец. В машинном raw-export нет поля модели, следовательно, это честно
помеченное наблюдение владельца, а не автоматически выгруженное подтверждение.

## Что подтверждено

- Лицензии прошли 54/54; `THIRD_PARTY_NOTICES.md` воспроизводим.
- Стек прошёл 11/11, offline/model-контроли 6/6.
- В Chromium сохранены ровно 60 измерений, 30 на вариант, после 5 прогревов на
  вариант. Совпали все 300 значений, выбор, журнал действий и сброс; ошибок 0.
- Перерисовки: 2400 против 1900, на 20,83% меньше только в этом синтетическом опыте.
  CPU: 4,7071 мс против 5,0037 мс — улучшение CPU не доказано и не заявляется.
- Хеш браузерного доказательства:
  `688baad33e10966944f8d73dd09e03af1f7aa4301cd2d2c66d7efe1b03bf6b41`.

## Что не прошло и какие были повторы

- Два разрешённых живых вызова `codex exec` с `gpt-5.6-sol` завершились ошибкой.
  Их не переименовали в успех. Автоматических повторов и замены модели не было.
- Владелец отменил только вложенную проверку CLI в desktop-среде. Работа Sol в
  текущем приложении — наблюдение владельца, а не доказательство для отправки.
  Необязательный CLI-интерфейс остаётся непроверенным; offline replay — проверенный
  путь воспроизводимости.
- Первая npm-проверка лицензий не смогла разобрать дерево. После двух отдельных
  разрешений владельца локальный сканер был исправлен, список разрешённых лицензий
  уточнён и последний audit прошёл 54/54.
- Стек запускался повторно после удаления лишнего присваивания защищённого свойства.
- Chromium запускался повторно один раз, потому что первый успешный запуск сохранил
  сводку без 60 отдельных измерений. Параметры не менялись.
- Два временных профиля Chromium были точечно удалены; исправление теперь ждёт
  завершения процесса. Широкая команда удаления была отклонена до выполнения.
- Были также повторы только проверяющих команд из-за формата русских чисел, синтаксиса
  YAML и слишком широкого правила поиска секретов; данные и выводы не менялись.

## Что скрыто ради приватности

Исходный неизменяемый файл имеет `3511076` байт и SHA-256
`031477de3fd7e42591ba5e7c1820ac39cb2dd1b691dbae29ae850a2aee895b86`.
Его исходная автоматическая проверка завершилась статусом `failed`:

- 667 совпадений абсолютных пользовательских путей;
- 2 приватных совпадения до замены путей и 1 после неё;
- адресов электронной почты 0;
- присваиваний секретов 0.

Пути заменены на относительные или `<REDACTED_MACHINE_PATH>`. Оставшееся приватное
совпадение полностью исключено: мы не копируем, не называем, не описываем и не
угадываем его. Удалены внутренние номера переписки/инструментов и не относящееся к
Phase 0.5 вступление. Внешний файл и его путь эта задача не открывала.

## Сохранённое ограничение

Исходная проверка не прошла, и одно приватное совпадение осталось после замены путей.
Этот факт не скрыт и не переименован в успех. Читаемые файлы прошли real-denylist
control: приватных находок и машинных/внутренних идентификаторов 0; остались четыре
обычных предупреждения о бинарных официальных материалах. Владелец сравнил русский
и английский отчёты и подтвердил, что после описанных исключений технический смысл
сохранён: `technical_meaning_preserved=true`.

## Что именно одобрил владелец

Вопрос владельцу:

`Подтверждаете русскую и английскую версии TRC-BG-PHASE05-001 как точный и безопасный отчёт, в котором после указанных исключений сохранён технический смысл?`

Выбранный ответ: `Подтверждаю одобрение (Recommended)`.

Это разрешает перенести только эту историю Phase 0.5 в утверждённый список
траекторий. Одобрение не разрешает писать код продукта, создавать оцениваемые
примеры или скрытые эталоны, проводить официальный benchmark, повторять
модель/Chromium, делать commit или push.
