# SES-037 — пакет владельца по verifier-runtime correction

Дата проверки: 2026-08-31 11:19 UTC<br>
Статус: **ОСТАНОВЛЕНО НА НОВОМ БЛОКЕРЕ BG-D04**

## Что было разрешено

Сессия `SES-20260831-037` разрешала исправить только два независимо доказанных
несоответствия исполнительной обвязки проверяющего эталона:

1. добавить отсутствующий повторный `reset` в нейтральный сценарий `BG-D01`;
2. сохранить пять точных действий сценария `BG-D03`, включая раздельные `observe`
   и `render-summary`, и полностью потребить их в evaluator bridge — переходнике
   проверяющего процесса.

Также были разрешены безопасная запись отказа evaluator, неофициальная полная
репетиция 20 слотов × 2 плеча и подготовка, но не запуск, `POSTDECISION-004`.

## Выполненные изменения

### BG-D01

В neutral scenario добавлен ровно один второй `reset` перед `dispose`. Других
изменений сценария нет. Неизменный canonical oracle по-прежнему требует девять
наблюдений и отдельно проверяет повторный сброс.

### BG-D03

- `scenario-provider.ts` и `oracle-assertions.ts` не изменялись;
- observer теперь сохраняет точный порядок действий:
  `construct`, `add`, `set-packet-count`, `observe`, `render-summary`;
- публичный `TrayPlannerSummary` загружается только observer-процессом;
- кадр `render-summary` содержит публично отрисованную строку и snapshot;
- evaluator bridge полностью потребляет пять кадров, проверяет согласованность
  отрисованной сводки со snapshot и только затем применяет неизменный oracle.

### Безопасная диагностика evaluator

Для будущего create-once результата добавлена запись evaluator failure с полями
роли, стадии, кода, идентификатора запроса и запрета повторной попытки. Raw stderr,
текст исключения, stack trace — стек вызовов, — внешние пути и скрытые значения в
запись не попадают. Регрессионный тест реально создаёт такую запись во временном
каталоге, проверяет её состав и удаляет каталог.

### POSTDECISION-004

Новый create-once root зарезервирован в коде, но не создан и не запускался. Он
должен заново получить все 80 observer captures; повторное использование данных
`POSTDECISION-003` не предусмотрено.

## Проверки и результаты

- `npm run compile`: **PASS**.
- Целевые тесты после согласования манифестов: **23/23 PASS**.
- D01 preserving/false-green self-check: **PASS**.
- D03 preserving даёт `preserving`, seeded defect даёт `false_green`: **PASS**.
- Потерянный или переименованный шаг D03 блокируется: **PASS**.
- Safe evaluator failure record создаётся без диагностического текста: **PASS**.
- Static official preflight: **PASS**, текущий inventory SHA-256
  `5eb206c286a7fba03885f2e2ebed0250fd154b42f13237c2525eb4781af541cf`.
- `git diff --check`: **PASS**. Отдельная необязательная попытка разобрать boundary
  через `require("yaml")` не состоялась, потому что такого npm-модуля нет в
  неизменном lockfile; зависимости не добавлялись и другой runtime не использовался.

Первая production-equivalent evaluator rehearsal остановилась с безопасным кодом
`HANDLER_FAILURE` на стадии `EXECUTE_EVALUATOR`. Поскольку первая сводка не содержала
слот, в разрешённый безопасный вывод добавлен только уже проверяемый `requestId`.
Повторная неофициальная репетиция локализовала отказ:

```text
request=evaluator:BG-D04:preserving:status-quo
role=evaluator
code=HANDLER_FAILURE
stage=EXECUTE_EVALUATOR
```

Обе попытки не вызывали arm или модель, не создавали official output и не меняли
решения RUN-002.

## Независимо установленная причина нового блокера

Причина находится вне разрешённого D01/D03 scope и поэтому не исправлялась.

`BG-D04` public harness возвращает из `dispose()` непосредственно объект счётчиков:

```text
{ harbor: number, orchard: number }
```

Observer сохраняет этот объект в `transcript.disposal.observation`. Однако текущий
`BG-D04` evaluator bridge ошибочно передаёт в неизменный oracle поле
`disposal.subscribers`. Такого поля нет, поэтому oracle получает `undefined` и
падает при чтении `unmountCounts.harbor`. Это детерминированное несоответствие формы
переходника; оно не является отказом кандидата и не требует изменения oracle,
сценария, ground truth или scoring.

## Целостность и ограничения

- RUN-001: 4 файла, не изменён.
- RUN-002: 44 файла и 40 immutable arm decisions, не изменён.
- POSTDECISION-002: 4 файла, не изменён.
- POSTDECISION-003: 92 файла, 592772 байта, не изменён.
- POSTDECISION-004: отсутствует.
- Arm calls: `0`.
- Model calls: `0`.
- `evaluation:run`: не запускался.
- Commit и push: не выполнялись.

Во время SES-037 владелец параллельно изменял `README.md` и `docs/README_RU.md`.
Эти файлы не читались, не изменялись и не относятся к SES-037 scope.

## Почему сессия остановлена

Разрешение позволяло изменять только D01/D03. Исправление `BG-D04` без нового
owner gate — явного разрешения владельца — расширило бы scope. Кроме того, главный
гейт 40/40 ещё не зелёный, поэтому commit и запуск `POSTDECISION-004` запрещены.

## Рекомендуемое следующее разрешение

Следующая сессия должна разрешить только детерминированные verifier-bridge
несоответствия, которые выявляет полная репетиция и которые однозначно выводятся из
неизменных scenario-provider, public harness и oracle. Для текущего отказа разрешённая
правка должна передать в D04 oracle сам `disposal.observation`, а не отсутствующее
поле `subscribers`.

Чтобы не тратить оставшееся время на отдельное одобрение каждого аналогичного
переходника, допустим ограниченный цикл: после каждого исправления повторять
неофициальную репетицию; исправлять следующий bridge только при доказуемом
shape/cardinality mismatch — несовпадении формы или количества — без изменения
scenario, oracle или оценки. Любой ground-truth mismatch, невыводимая неоднозначность
или необходимость менять frozen semantics должны немедленно остановить цикл.

До нового одобрения остаются запрещены: исправление BG-D04, дальнейшая репетиция,
commit, запуск `POSTDECISION-004`, arm/model calls, `evaluation:run` и push.
