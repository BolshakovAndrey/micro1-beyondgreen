# BG-D01 SCALE-003 — приёмочная матрица Codex

**Проверяемая трасса:** `TRC-BG-D01-SCALE-003`  
**Допустимость трассы:** одобрена владельцем, трасса включена в `actual_traces`  
**Итог проверки масштабирования:** `PASS_WITH_CONFIRMED_BLOCKERS` — проверка прошла,
но подтвердила оставшиеся блокеры  
**SCALE_READY:** `false`

Допустимость SCALE-003 как доказательства работы сохраняется. Эта матрица решает
только вопрос масштабирования и не отменяет уже одобренную трассу.

## Что прошло

- `A3`: D01-схема `BoardObservation` вынесена из общего слоя и передаётся явно.
- `S4`: автономное воспроизведение самостоятельно пересчитывает
  `reasonCorrectReject` и `oracleAcceptedCandidate`; их подмена отклоняется.
- `P1`: рекурсивные пути глобально сортируются, включая сложную пару
  `a/source.ts` и `a.ts`.
- `P2`: прямой отрицательный тест меняет байты зарегистрированного вложенного
  исходника и получает закрытый отказ.
- `D1`: активные документы согласованы — SCALE-003 одобрена и находится в
  `actual_traces`, а `SCALE_READY=false` до этой матрицы и отдельного решения.
- Компиляция, все `55/55` обычных тестов и полный `d01:verify` прошли.

## Пять оставшихся блокеров

1. **A1 — настоящий runtime не является общим.** Интерфейс общего движка существует,
   но реальные оркестратор и рабочие entrypoint напрямую импортируют `D01_ENGINE`.
   Общая функция исполнения не получает движок как внедряемую зависимость, поэтому
   D01 пока не является только набором данных и тонкой обвязкой.
2. **A2 — TEST-ONLY тест проверяет поддельный dispatcher.** Он вручную вызывает
   фиктивные callback и не проходит настоящий жизненный цикл оркестратора, процессов,
   оценщика, воспроизведения и отчёта.
3. **S1 — replay доверяет самосогласованному allowlist.** Хеши правил пересчитываются,
   но не сравниваются с точным ожидаемым набором путей и политикой для конкретного
   слота. К согласованному доказательству можно добавить лишний разрешённый путь.
4. **S2 — output не привязан к своему слоту.** Доказательство процесса может
   существовать в общем списке, но schema не требует, чтобы конкретный observation
   принадлежал именно своему observer-slot, а evaluator result — evaluator-slot.
5. **S3 — event digest не сверяется с реальным решением.** Автомат событий требует
   внутренне одинаковый `decisionSha256`, но replay не сравнивает его с неизменяемым
   решением соответствующей группы.

## Последний ограниченный объём SCALE-004

Разрешается только:

1. провести настоящий оркестратор и worker-runtime через внедряемый
   `FixtureEngineBindings`, оставив D01 тонкой data-only обвязкой;
2. заменить фиктивное доказательство TEST-ONLY интеграционным тестом настоящего
   конвейера с ограниченным внедрённым transport процессов;
3. проверять точные ожидаемые политики sandbox и разрешённых путей для каждого слота
   как при запуске, так и при replay;
4. ввести точную позиционную связь output → proof → slot и отрицательные тесты на
   подмену роли;
5. связать digest каждого события оценщика с фактическим неизменяемым решением;
6. согласовать только затронутые тесты, manifest, provenance, changelog и активные
   статусы.

Не разрешаются изменения фикстуры, кандидатов, oracle, пяти видимых утверждений,
методики оценки, BG-D02, benchmark reduction, официальный или оцениваемый запуск,
живая модель, Claude, Chromium, зависимости, runtime-policy, commit и push.

## Следующее действие

Нужна одна свежая submission-eligible задача `SES-20260830-004`. Она сначала создаёт
границу и останавливается для явного одобрения владельца. После реализации требуется
захват и проверка одной траектории SCALE-004, повтор этой матрицы и отдельное решение
владельца о `SCALE_READY`.
