# Результат восьмого продолжения интеграции BG-D02—BG-D04

**Продолжение:** `TRC-BG-D02-D04-INTEGRATION-001-CONT-008`  
**Исходная траектория:** `TRC-BG-D02-D04-INTEGRATION-001`  
**Граница:** `SES-20260830-009`  
**Результат:** разрешённое исправление сканера и ограниченная диагностика завершены

## Исправление сканера

Находка категории `absolute-user-path` теперь содержит массив `spans`. Каждый его
элемент имеет только поля `start`, `end` и `patternId`. Сканер перечисляет все
подходящие неперекрывающиеся совпадения на строке, сортирует их детерминированно и
удаляет дубли. Одиночные поля `start`, `end` и `patternId` сохранены и выбирают
первый элемент массива для обратной совместимости.

Две минимальные целевые проверки выполнены командой
`node --test --experimental-strip-types --test-name-pattern='single-file scanning|enumerates every non-overlapping' tests/preflight-check.test.ts`.
Обе проверки прошли. Они покрывают несколько путей на одной строке,
детерминированный порядок и отсутствие дублей, выбор первого интервала одиночными
полями и отсутствие исходных значений путей в сериализованной находке.

## Безопасный результат однофайловой диагностики

Сканер применён в однофайловом режиме только к
`evaluation/manifests/BG-D02/SUBAGENT_REPORT_RU.md`. Получены ровно три находки
категории `absolute-user-path` на требуемых строках.

| Строка | Число интервалов | `patternId` | Интервалы ровно с одним маркером `/micro1.hackerearth/` | Равны ли хэши префиксов всех применимых маркерных интервалов |
| ---: | ---: | --- | ---: | --- |
| 72 | 1 | `absolute-user-path-macos-users-v1` | 0 | нет |
| 80 | 1 | `absolute-user-path-macos-users-v1` | 0 | нет |
| 97 | 1 | `absolute-user-path-macos-users-v1` | 0 | нет |

Сами хэши и интервальные смещения целевой диагностики не выводились. Совпавшие
значения, строки, фрагменты строк, абсолютные пути, префиксы и суффиксы не
публиковались и не сохранялись в этой карточке.

## Сохранённые границы и невыполненные действия

Целевой отчёт не изменён, пути не нормализованы. Производные манифесты, контрольные
суммы и индексы не обновлялись. Все записи исходной траектории и продолжений
`CONT-001`—`CONT-007` сохранены.

Не выполнялись `npm test`, компиляция, воспроизведение, регрессия, проверка
документационного паритета, контрольные суммы, предварительные проверки или другие
интеграционные проверки. Не выполнялись официальный или оцениваемый запуск, Claude,
Chromium, живая модель продукта, браузер, приватный MCP, вложенный Codex CLI,
обращение к основному checkout или внешнему состоянию, commit и push.

Изменение ограничено сканером, его целевыми тестами и двумя служебными записями
`CONT-008`. Оно не разрешает нормализацию целевого отчёта или продолжение прежнего
интеграционного набора проверок.
