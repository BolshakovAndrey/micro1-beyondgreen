# BeyondGreen D01 SCALE-003 — карточка одобрения границы correction-сессии

## Решение, которое требуется сейчас

Нужно одобрить или отклонить только новую границу реализации
`SES-20260830-003`. До одобрения исправления не выполнялись: продуктовый код,
тесты, проекции, фикстуры, варианты решения, скрытые эталоны и отчёты не менялись;
компиляция и продуктовые проверки не запускались.

Одобрение этой карточки разрешит одну допустимую для submission траекторию
`TRC-BG-D01-SCALE-003` — то есть одну последовательную запись работы агента,
которую можно будет отдельно захватить, проверить на приватность и представить как
доказательство реализации. Одобрение не делает саму будущую трассу автоматически
допустимой: после реализации ей всё равно потребуются точный захват, автоматическая
проверка, ручная проверка и отдельное решение владельца.

## Проверенное исходное состояние

- Работа ведётся внутри разрешённого чистого репозитория; абсолютный машинный путь
  намеренно нигде не сохранён.
- Git находится в состоянии отсоединённого `HEAD`: это означает, что текущий commit
  открыт без локально выбранного имени ветки. Исходный commit —
  `f0418bc6051002d8d42ccf84822d2e1f2e25b02e`.
- Рабочее дерево уже было незаписанным до этой сессии: 26 отслеживаемых изменений и
  70 новых файлов. Его обезличенный SHA-256 —
  `06ec023eed75fa1ab203bda703922d64068da5909845064d74c599674ac86a8e`.
- Это состояние импортировано из текущего рабочего дерева после одобренного
  владельцем продвижения `TRC-BG-D01-SCALE-002`. Ни один из унаследованных файлов
  не приписывается новой сессии.
- `TRC-BG-D01-SCALE-002` присутствует в индексе допустимых трасс.
- В активных проекциях `SCALE_READY=false`: D01 не признан готовым к
  масштабированию.
- С удалённым Git-состоянием сетью не связывались; commit и push не выполнялись.

## Что было сделано до гейта

Полностью прочитаны `AGENTS.md`, финальный контракт задачи, правила хакатона,
политика чистой комнаты, политика трасс, предварительный контрольный список,
нормативная спецификация продукта, проекция оценивания и `config/topic.yaml`.

Выполнены только структурные проверки: принадлежность Git-репозиторию, состояние
ветки/`HEAD`/рабочего дерева, наличие исходной трассы в индексе, сохранение
`SCALE_READY=false`, доступность нового идентификатора сессии и
`git diff --check`. Последняя команда не обнаружила ошибок пробелов или конфликтных
маркеров в уже существующей разнице.

Не запускались scanner — автоматический проверяющий процесс, compile, тесты,
`d01:verify`, replay — автономное воспроизведение, demo, Phase 0.5, фикстуры,
варианты решения, скрытые эталоны, benchmark — официальный набор измерений,
Claude, живая модель или Chromium. Глобальная память, браузер, подключённые
приложения, приватный MCP, соседние репозитории и внешние рабочие области не
открывались. Значения, пути и содержимое `MICRO1_PRIVATE_DENYLIST` и
`MICRO1_PRIVATE_TRACE_DIR` не читались и не выводились.

## Точная ограниченная область после одобрения

### 1. Единый настоящий путь движка

Настоящие orchestrator/workers — управляющий процесс и рабочие процессы — должны
проходить через один общий внедряемый путь `FixtureEngineBindings` и descriptor.
`FixtureEngineBindings` — набор явно переданных зависимостей движка; descriptor —
декларативное описание конкретной фикстуры. D01 должен остаться только composition
root, то есть узлом, который собирает зависимости и данные, но не содержит общей
логики движка.

Будет добавлен только тестовый интеграционный descriptor. Он обязан пройти настоящий
pipeline — реальную цепочку выполнения — и доказать вызов внедрённых схем,
проверок, рассуждения, оценщика, автономного воспроизведения и построения отчёта.
`BoardObservation` нужно вынести из общего слоя схем либо сделать схему наблюдения
явно внедряемой зависимостью.

### 2. Фальсифицируемое доказательство физических возможностей

Доказательство, созданное runner — запускающим процессом, — должно допускать
проверяемое опровержение подмены. Реализация обязана проверить:

- связь конкретного запуска с identity рабочего процесса, то есть с его проверяемой
  идентичностью;
- канонические digests — SHA-256-хеши нормализованных политик sandbox и путей;
- bijection — точное взаимно однозначное соответствие роли, профиля прав и
  доказательства;
- отдельный автомат состояний событий для каждой arm, то есть для каждой ветви
  сравнения;
- связь событий независимого оценщика с неизменяемым хешем решения каждой ветви.

Автономное воспроизведение должно независимо пересчитать и проверить
`reasonCorrectReject` — верность причины отклонения — и
`oracleAcceptedCandidate` — принятие кандидата скрытым эталоном.

### 3. Рекурсивная сверка пакета

Сравнение путей пакета нужно исправить так, чтобы корректные вложенные каталоги и
файлы принимались рекурсивно. Будет добавлен прямой отрицательный тест: после
регистрации исходного файла меняются его байты, и проверка обязана закрыться с
ошибкой.

### 4. Только активные проекции текущего состояния

После появления кодовых доказательств можно согласовать только:

- `README.md`;
- `config/cleanroom.json`;
- `docs/PROJECT_SPEC.md`;
- `docs/PROJECT_SPEC_RU.md`;
- `docs/TRACE_POLICY.md`;
- `docs/EVALUATION.md`;
- `docs/PREFLIGHT_CHECKLIST.md`;
- `config/topic.yaml`.

Исторические границы, трассы, отчёты проверок, решения и результаты запусков нельзя
переписывать. Допустима только новая добавочная запись текущей итерации и новые
материалы `TRC-BG-D01-SCALE-003`, если этого требует политика трасс.

## Что останется неизменным

- Поведение D01, пять видимых утверждений, байты кандидатов, скрытый эталон,
  ground truth — правильный ответ оценивания, методика `eval-v1.1.0`, целевые
  показатели и знаменатели.
- BG-D02 и любые другие фикстуры, варианты решения или скрытые эталоны.
- Зависимости, lockfile — файл точных версий зависимостей, политика среды выполнения,
  контейнеры, Phase 0.5, переносимость на Linux и физическое требование `K=0`.
- Официальные или оцениваемые запуски, benchmark, раскрытие held-out — отложенной
  части набора, и неоцениваемая демонстрация ремонта D01.
- Исторические записи.
- `SCALE_READY=false` до отдельной координационной матрицы принятия Codex и нового
  явного решения владельца.

## Порядок работы после одобрения

1. Начать одну траекторию `TRC-BG-D01-SCALE-003` и осмотреть только минимальный
   локальный код, относящийся к четырём группам исправлений.
2. Вносить изменения небольшими группами, сохраняя запускаемую унаследованную основу.
3. Для каждой существенной итерации записывать гипотезу, изменение, точную команду,
   доказательство, ошибку или повторную попытку и решение сохранить либо пересмотреть
   изменение.
4. После реализации запустить `npm run compile`, `npm test` и целевую неоцениваемую
   проверку D01.
5. При первом детерминированном сбое остановиться без самостоятельного повтора или
   расширения решения и запросить отдельное решение владельца.
6. Только после успешной локальной проверки использовать разрешённые внешние
   интерфейсы: scanner-read через `MICRO1_PRIVATE_DENYLIST` и raw-trace write через
   `MICRO1_PRIVATE_TRACE_DIR`. Их значения, пути и содержимое раскрывать нельзя.
7. Подготовить проверенные доказательства для отдельной координационной матрицы
   принятия Codex. Эта будущая матрица, а не данная сессия, решит гейт масштабирования.

## Риски и условия немедленной остановки

- Любой путь за пределами чистого корня, приватный контекст, неизвестное
  происхождение или лицензия, сходство с запомненной приватной структурой,
  утечка скрытого эталона либо нерешаемое загрязнение требуют немедленной остановки.
- Любая потребность изменить зависимость, контейнер, политику runtime, Phase 0.5,
  методику оценивания, D01 behavior freeze или ослабить `K=0` выходит за границу и
  требует отдельного решения.
- Отсоединённый `HEAD` зафиксирован как исходное состояние. Одобрение не разрешает
  создавать или переключать ветку.
- Успешные локальные проверки не означают автоматически, что трасса допустима или
  что D01 готов к масштабированию.

## Ошибки, повторы и исключения этого подготовительного шага

Ошибок и повторных попыток не было. Приватные редактирования и исключения не
применялись, потому что внешний захват ещё не выполнялся. Абсолютный машинный путь
не сохранялся по политике трасс. Удалённое состояние Git не проверялось, так как это
не требуется для границы и могло бы обратиться к сети.

## Точный объём одобрения

Если граница верна, ответьте точной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-003 и разрешаю одну eligible implementation-траекторию TRC-BG-D01-SCALE-003 строго в описанном bounded correction scope; SCALE_READY не менять, commit и push не выполнять.

Эта фраза разрешит только ограниченную реализацию, новые тесты в перечисленной
области, `npm run compile`, `npm test`, целевую неоцениваемую проверку D01 и затем
безопасный захват/проверку новой трассы. Она не разрешит официальный или оцениваемый
запуск, benchmark, Claude, живую модель, Chromium, BG-D02, ремонт D01, изменение
методики, масштабирование, commit, push или публикацию.
