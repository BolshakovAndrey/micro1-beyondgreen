# BG-D01 SCALE-004 — остановка реализации

**Статус:** `STOPPED_ON_FIRST_DETERMINISTIC_FAILURE`  
**SCALE_READY:** `false`

## Команда и безопасный класс ошибки

Первая и единственная команда проверки `npm run compile` завершилась ошибкой.
Безопасный класс: `TYPESCRIPT_GENERIC_RUNTIME_BINDING_TYPE_MISMATCH` — несогласование
типов в новом общем runtime-контракте, без исполнения продукта и без доступа к
приватным данным.

Компилятор сообщил семь локальных ошибок в `src/d01/orchestrator.ts`:

- пять ошибок относятся к потере точного типа группы при построении слотов observer
  и evaluator после внедрения `FixtureEngineBindings`;
- две ошибки относятся к тому, что общий тип arm-visible/verifier package binding не
  содержит все поля существующего `PackageBinding`.

Сырой вывод, секреты, значения переменных окружения, приватные термины и абсолютные
машинные пути в этот checkpoint не включены.

## Затронутые блокеры

Ошибка блокирует доказательство `A1` и вследствие этого интеграционное доказательство
`A2`. Результаты `S1`, `S2` и `S3` не проверялись командами после этой остановки.
Сохранённые контракты `A3`, `S4`, `P1`, `P2` и `D1` не переоценивались.

## Что не выполнялось

- Повтор `npm run compile` не выполнялся.
- `npm test` и `npm run task -- d01:verify` не запускались.
- Зависимости и `package-lock.json` не изменялись.
- Официальный или оцениваемый запуск, живая модель, Chromium, BG-D02 и ремонт D01
  не выполнялись.
- Commit и push не выполнялись.

## Требуемое решение владельца

Для продолжения нужно отдельное явное решение владельца: разрешить ровно одну
ограниченную коррекцию этих семи TypeScript-ошибок в типах обобщённого runtime и
ровно один новый запуск `npm run compile`, после которого дальнейшие команды снова
подчиняются последовательности SCALE-004. Без такого разрешения текущая траектория
остаётся остановленной, а `SCALE_READY=false`.

## Продолжение `TRC-BG-D01-SCALE-004-CONT-001`

**Статус:** `STOPPED_ON_FIRST_DETERMINISTIC_FAILURE`  
**SCALE_READY:** `false`

Владелец разрешил ограниченную коррекцию семи исходных ошибок и ровно один новый
запуск `npm run compile`. В общем контракте `FixtureEngineBindings` тип результата
`verifyFinalizedDecision` был уточнён до `FinalizedDecision`, поэтому пять мест
построения слотов observer и evaluator сохраняют точный тип группы. В описания
`armVisible` и `verifierOnly` были добавлены обязательные поля существующего
`PackageBinding`, что устранило две исходные ошибки совместимости пакетов.

Единственный разрешённый запуск `npm run compile` завершился ошибкой. Безопасный
класс: `TYPESCRIPT_TEST_ONLY_DESCRIPTOR_PACKAGE_BINDING_MISMATCH` — локальное
несоответствие типов тестового описателя уточнённому общему контракту, без исполнения
продукта и без доступа к приватным данным. Компилятор сообщил одну ошибку:
`TEST_ONLY_ENGINE` не соответствует новому типу, поскольку его секция `armVisible`
не содержит `packageId`, `packageKind`, `sourcePaths` и `expectedPackageSha256`.
Сырой вывод и машинные абсолютные пути здесь не сохраняются.

Повтор компиляции, `npm test`, `d01:verify`, replay, demo, preflight и другие
продуктовые команды не выполнялись. После ошибки правки исходного кода и тестового
описателя не выполнялись. Зависимости, `package-lock.json`, runtime policy,
продуктовые fixtures, candidates, oracles, visible assertions, методика оценки,
цели, знаменатели, BG-D02, A3, S4, P1, P2 и D1 не изменялись. Commit и push не
выполнялись. Работа по A1, A2, S1, S2 и S3 остаётся непроверенной командами;
`SCALE_READY=false`.

Для продолжения требуется отдельное явное решение владельца: разрешить ровно одну
ограниченную коррекцию package-binding полей тестового описателя
`tests/fixtures/test-only-engine.ts`, необходимых для соответствия уже уточнённому
`FixtureEngineBindings`, и ровно один последующий запуск `npm run compile`. До такого
решения работа остаётся остановленной; иных правок или команд выполнять нельзя.

## Продолжение `TRC-BG-D01-SCALE-004-CONT-002`

**Статус:** `STOPPED_ON_FIRST_DETERMINISTIC_FAILURE`  
**SCALE_READY:** `false`

В `TEST_ONLY_ENGINE.armVisible` добавлены ровно четыре разрешённых поля:
`packageId`, `packageKind`, `sourcePaths` и `expectedPackageSha256`; значения выведены
из существующего локального TEST-ONLY описателя и его пакетного доказательства.
Единственный запуск `npm run compile` завершился ошибкой. Безопасный класс:
`TYPESCRIPT_TEST_ONLY_VERIFIER_PACKAGE_BINDING_MISMATCH` — секция `verifierOnly`
того же тестового описателя также не содержит этих четырёх обязательных полей.

Повтор компиляции, тесты и другие продуктовые команды не выполнялись. После ошибки
правки не вносились. Все ранее указанные исключения неизменны; `SCALE_READY=false`,
commit и push не выполнялись. Для продолжения требуется отдельное явное разрешение
владельца на коррекцию `verifierOnly` и новый однократный запуск компиляции.
