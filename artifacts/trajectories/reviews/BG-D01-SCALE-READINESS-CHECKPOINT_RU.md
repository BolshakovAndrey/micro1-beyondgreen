# BeyondGreen D01 — пакет проверки готовности к масштабированию

## Текущий вывод

Перечисленные владельцем исправления D01 реализованы в одобренной границе
`SES-20260830-001`. Это остаётся неоцениваемой локальной разработкой: официальный
или оцениваемый запуск не выполнялся, модель и Chromium не вызывались, BG-D02 не
создавался, commit и push не выполнялись.

Заявлять `SCALE_READY=yes` нельзя. Точный штатный захват
`TRC-BG-D01-SCALE-001`, строгая проверка UTF-8, механическая замена двух машинных
путей, полная проверка 729 строк и безопасное сканирование прошли. Повторный
независимый checkpoint Claude Opus завершён с точным выводом
`D01_CHECKPOINT=PASS_WITH_CONCERNS` и `SCALE_READY=no`. Codex независимо подтвердил
оставшиеся архитектурные проблемы и недостатки доказательств. Владелец отдельно
одобрил траекторию как допустимое доказательство реализации и разрешил включить её в
`actual_traces`. Её допустимость не зависит от будущего гейта масштабирования.

## Что исправлено

- Все значения D01, пути, кандидаты, версии схем и контрольные хеши собраны в
  `FixtureDescriptor`; arm-visible и verifier-only пакеты проверяются по хешам,
  принадлежащим коду.
- План дополнительных проверок выводится из видимых контрактов инвариантов и
  инвентаризации рисков. В схеме явно записано, что знание заложенного дефекта не
  использовалось.
- Родительский процесс, принимающий решения, не читает verifier-only пакет до
  завершения решений обоих вариантов.
- Автономное воспроизведение заново проверяет финальные решения и связь результата
  оценщика с решением, двумя наблюдениями и verifier-manifest.
- Для EV-009 записываются измеренные начало, окончание и длительность, принадлежность
  к development/held-out и признак ложной тревоги.
- Утверждения о порядке выполнения и границе скрытого эталона выводятся из измеренных
  событий. Отрицательные тесты отвергают перестановки и несовместимые права доступа.
- После решений каждый вариант наблюдается дважды в отдельных процессах. Любое
  расхождение закрывает выполнение с ошибкой до запуска оценщика.
- Добавлено обратное физическое доказательство: оценщик видит эталон, но не может
  перечислить, проверить или прочитать candidate.
- Пять видимых утверждений имеют один исполняемый источник.
- JSON и HTML содержат ограниченный контрпример: не более пяти элементов и число
  опущенных элементов.
- Исправлены роль старой трассы, инструкция воспроизведения, область компиляции и
  текущая проекция preflight. Старая `TRC-BG-D01-VERIFY-002` остаётся допустимым
  историческим доказательством, но не доказательством этой реализации.

## Сохранённые ограничения и нерешённые решения

- Текущий D01 законно работает офлайн. Перед путём с оцениванием владельцу отдельно
  нужно решить, требуется ли live reasoning — рассуждение через живую модель.
- macOS-изоляция продолжает использовать `/usr/bin/sandbox-exec` и закрывается с
  ошибкой на неподдерживаемых платформах. Если переносимость на Linux или среду судьи
  потребует зависимости, контейнера, новой политики runtime либо изменения
  зафиксированного этапа 0.5, агент обязан представить варианты и остановиться.
- Ослабленный режим с заявлением `K=0` без физического доказательства не добавлен.

## Результаты локальной проверки

- Компиляция прошла.
- Все обычные тесты прошли: 53 из 53.
- Полная проверка D01 прошла: 5 из 5 видимых утверждений, 1 из 1 контракт шага,
  3 из 3 проверки эталона, 4 из 4 проверки физической границы, включая обратный
  запрет, и 19 из 19 контрактов вертикального сценария.
- Неоцениваемый E2E-путь, четыре базовых manifest и manifest из 24 файлов прошли.
- Автономное воспроизведение подтвердило совпадение отчётов. Хеш канонического JSON:
  `47035ed40c9ab16473d3a5866bc2071138712d01992a20a617a48bf0e19cd674`;
  HTML: `9f552bb43399463afaf8257a265dad9c235343be5d836b1215f0b14b8d0f88ed`;
  replay: `4ba082113fae1e1f97139c26a3c42e807a1530c66777bc980f5a5346c889b9e0`.
- Замороженная проверка этапа 0.5 прошла без запуска модели или браузера: лицензии
  54 из 54 и наборы 11/11, 6/6, 3/3, 4/4, 3/3 и 4/4.
- Первая проверка контрольных сумм ожидаемо обнаружила устаревшую проекцию после
  разрешённых изменений. Штатное обновление синхронизировало её, повтор прошёл
  171 из 171. `git diff --check` прошёл.
- Безопасные предварительные проверки вернули `READY_FOR_CLEAN_BRANCH` и
  `READY_FOR_IMPLEMENTATION`; загрязнений не найдено. Сохранились ожидаемое
  предупреждение о незаписанном рабочем дереве и четыре уведомления о необходимости
  ручной проверки официальных бинарных материалов.
- После точной проверки трассы и согласования checkpoint проекция контрольных сумм
  прошла 177 из 177; `git diff --check` прошёл повторно.
- После продвижения трассы и подготовки `SES-20260830-002` актуальная проекция
  контрольных сумм прошла 178 из 178; обе safe-проверки снова прошли с нулём
  загрязнений.
- Корень, ветка и исходный HEAD не изменились: корень `.`, ветка
  `impl/beyondgreen`, HEAD `f0418bc6051002d8d42ccf84822d2e1f2e25b02e`.

## Проверка трассы

Карта «решение → код → отрицательный тест → команда проверки» находится в
`TRC-BG-D01-SCALE-001-DECISION-EVIDENCE.md`. Исходная штатная трасса имеет размер
56 127 байт и SHA-256
`605d014dd5e631d7be4b9d746eee222ebc9bd5acaa679a0e2e9e71e36bcddfb0`; она хранится
вне репозитория без раскрытия пути. Механически очищенный штатный слой имеет SHA-256
`251fbc93e7d884269c6b773a4f2e0094a8f2bdc0a8b96bd344f725c24c73c2ac`.
Проверены полные английский и русский слои. Исходный ответ Claude: 25 138 байт,
SHA-256 `535c7d0a6988aa521d9429492717d0fb9e7b4a7e8472af085f0e6b20e9a59b5a`;
в репозиторий он не включается. Полная русская сверка замечаний находится в
`BG-D01-SCALE-CLAUDE-CHECKPOINT_RU.md`.

Подтверждённые блокеры: признаки физических прав объявляются родителем, движок
остаётся D01-специализированным, а manifest не доказывает полноту arm-visible
каталога. Карта доказательств исправлена: существующие тесты проверяют перестановку
событий, но пока не проверяют отсутствие события и независимо подменённые
доказательства физических прав.

## Следующий отдельный гейт

Текущие решения владельца и следующий гейт:

1. Точно проверенная трасса продвинута в `actual_traces`; это подтверждает качество
   доказательства, но не меняет `SCALE_READY=no`.
2. Владелец сохранил одобренную фиксацию публичного инварианта D01 и разрешил честно
   раскрыть ограничение конструкции; методика оценивания не открывается заново.
3. Нужно отдельно одобрить подготовленную границу `SES-20260830-002` для единого descriptor-driven движка,
   независимо измеренных физических прав, полноты arm-visible каталога и
   соответствующих отрицательных тестов.

Решение о Linux/среде судьи остаётся отдельным гейтом, если потребует зависимости,
контейнера, новой политики runtime или изменения этапа 0.5. До нового успешного
checkpoint `SCALE_READY=false`.
