# Финальная проверка submission-пакета Claude Opus 5 — карточка владельца

**Класс проверки:** независимый аудит по опубликованной 100-балльной методике  
**Режим:** только чтение через `Read`, `Glob` и `Grep`; без команд, сети, браузера и записи  
**Версия Claude CLI:** 2.1.251  
**Модель:** Claude Opus 5 через локальный alias `opus`  
**SHA-256 запроса:** `1545dc3738b99dbc2e00af9aa739bcc3df55bf095dc072a1ed3113c6bf9f30b7`  
**SHA-256 полного ответа:** `6e010164523449527835aebeb514be3e3c9fbdb49518dc0c0d4db6103d1587cc`

## Итог

Claude дал вердикт `PASS_WITH_CONCERNS`: содержательного блокера подачи нет, а после
механических проверок квалификационный гейт оценивается как `PASS`. Прогноз баллов:
консервативно 75, наиболее вероятно 84, при сильном прочтении доказательств 92 из 100.
Codex независимо сверил все практические замечания с файлами и получил практически тот же
прогноз — 85/100.

Административная eligibility остаётся `UNKNOWN` только потому, что возраст, регистрация,
единственность заявки и платёжные реквизиты нельзя доказать содержимым репозитория. В коде
нет признака нарушения. Полнота и воспроизводимость проходят; у целостности упаковки и
индекса трасс были замечания, перечисленные ниже.

## Что нашёл Claude и что подтвердил Codex

### 1. Объяснение результата 3/10 было неточным

Документы говорили о смысловом распознавании класса нарушения. На деле проверяющий код
приводит обоснование к нижнему регистру, заменяет все не-ASCII-буквы и разделители на `-` и
ищет буквальную закрытую метку класса поведения либо неуспешного действия.

Это видно на паре H02/H04. H02 точно описывает устаревшее асинхронное завершение и называет
замороженный идентификатор `BG-H02-INV-CURRENT-EPOCH`, но не содержит закрытые строки
`async-ordering` или `complete-r1`, поэтому балл причины не получает. Сопоставимое
обоснование H04 получает балл, потому что его открытый идентификатор случайно содержит
строку `conditional-lifecycle`.

Сам результат `3/10` честный и остаётся неизменным. Исправляется только интерпретация: это
полнота в рамках строгого словарного показателя, а не количество всех смыслово верных
диагнозов. Никакой подгонки результата, оракула или скоринга не разрешено и не выполнено.

### 2. Состав ZIP расходился с конфигурацией чистой комнаты

В `config/cleanroom.json` был один список исключений, а два упаковочных скрипта использовали
другой захардкоженный список. Поэтому в предварительную версию попали девять файлов вопреки
конфигурации. Два русских служебных документа одновременно не проходили обычный scanner.
Claude отдельно прочёл их и не нашёл приватного пути, почты, токена, секрета или учётных
данных, поэтому немедленной утечки нет; проблема была в недоказанном release-контроле.

Исправление: один общий модуль читает exclusions из конфигурации и понимает каталоги как
префиксы; оба сборщика используют его. Тест доказывает, что ни один исключённый путь не
может попасть в пакет. Публичные текстовые проекции `CHALLENGE.md` и
`HACKATHON_RULES.md` остаются в пакете, а закрытые owner-документы — вне его.

### 3. Центральный индекс траекторий отстал от финального результата

Он заканчивался на 30 августа, хотя официальные решения и post-decision evidence созданы
31 августа. Также в декларации агентов отсутствовали COD-CODEX-008–012, две уже
существующие reviewed-траектории не имели явного статуса, а счётчик Claude-проверок устарел.
Индекс нужно синхронизировать, не выдавая непроверенные поздние сессии за representative
traces: для них фиксируются boundary и честный eligibility disposition.

### 4. Семь обоснований модели написаны по-русски

Модель ответила на языке локали оператора. После фиксации решений переводить эти тексты
нельзя, поэтому они сохраняются неизменными. Русский язык не объясняет основной провал
метрики: шесть из семи незасчитанных false-green обоснований написаны по-английски. Факт
нужно только раскрыть.

### 5. Флаги execution-plan легко неверно понять

Три значения `false` в `execution-plan.json` относятся к неисполняемому статическому плану,
а не утверждают, что официального запуска не было. Исполнение доказывают 40 неизменяемых
arm records и provenance в том же RUN-002.

### 6. Разница inventory hash раскрыта, но требовала объяснения

После решений чинились только кардинальность транскрипта и перенос идентичности между
процессами. Кандидаты, решения, семантика оракулов и скоринг не менялись. Дополнительная
проверка — заранее объявленное базовое плечо снова получило ровно 10/20 при том же
распределении истины 10/10.

### 7. Три мелких расхождения команд

Исправляются название команды license audit, установка через `make setup` /
`npm ci --ignore-scripts` и явная пометка macOS-only возле физической D01 demo-команды.

### 8–9. Информационные пункты

Корневой LICENSE для собственного кода отсутствует, но правила не делают его
квалификационным требованием. Отладочный каталог под `tmp/` игнорируется и исключён из
упаковки; это только локальная уборка.

## Прогноз по критериям

| Критерий | Максимум | Наиболее вероятно |
| --- | ---: | ---: |
| Проблема и ценность для пользователя | 15 | 13 |
| Агентное решение и инженерия | 30 | 25 |
| Сквозное качество | 20 | 17 |
| Измеренное улучшение | 15 | 12 |
| Воспроизводимость | 15 | 13 |
| Практический вывод | 5 | 4 |
| **Итого** | **100** | **84** |

Самая сильная сторона — архитектура доказательств: взаимные запреты процессов, `K=0`,
хэши кандидата до и после плеча, повторные независимые захваты, неизменяемые решения,
офлайн-повтор и сохранение всех неудачных запусков. Главный потолок оценки — продуктовый
агент делает один вызов на кандидата, тогда как большая часть сложности находится в
проверяющей инфраструктуре.

## Остаточные риски и гейт выпуска

- Изменяемые результаты RUN-001, RUN-002 и POSTDECISION-004 должны совпасть с начальными
  отпечатками SES-040.
- После всех правок обязательны compile, полный набор тестов, checksums, две безопасные
  предварительные проверки и чистая распаковка ZIP.
- Состав архива должен полностью выводиться из `config/cleanroom.json`.
- Официальный прогон, модельные вызовы, commit, push и загрузка запрещены.
- Финальный ZIP можно создать один раз после зелёных проверок и передать владельцу вместе с
  размером, количеством файлов и SHA-256.

**Решение карточки:** продолжить только механическое закрытие SES-040; scored evidence не
трогать. После зелёных гейтов пакет квалификационно готов к замене предварительной версии.
