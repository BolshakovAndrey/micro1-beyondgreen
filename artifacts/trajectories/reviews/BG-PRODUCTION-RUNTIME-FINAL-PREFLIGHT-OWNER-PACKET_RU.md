# BeyondGreen — финальная проверка production runtime

## Граница и разрешение

Проверка выполняется в `SES-20260831-030` на ветке
`codex/beyondgreen-official-runner` от исходного `HEAD`
`3d916e73a13553583660bc88633366f5bc8eede9`.

Владелец разрешил компиляцию, полную синтетическую репетицию
`20/40/80/40`, обе безопасные preflight-проверки, проверку контрольных сумм,
`git diff --check` и checkpoint commit только при полном успехе всех гейтов.

Повтор transport canary, официальный или оцениваемый прогон, раскрытие скрытых
эталонов, вызов живой модели и push запрещены.

## Измеренные результаты

- TypeScript-компиляция: **PASS**.
- Синтетическая runtime-репетиция: **PASS, 12/12 тестов**.
- Полный сценарий сохранил ожидаемые количества: 20 слотов, 40 решений,
  80 независимых захватов и 40 оценок после одного детерминированного
  transport-abstain на десятом слоте.
- Проверки divergent, missing и malformed captures завершились закрытым отказом до
  запуска evaluator.
- Проверка уже существующего каталога вывода остановила выполнение до запуска плеч.
- Физическая изоляция macOS, точные filesystem-capabilities и запрет сети подтверждены.
- Рабочий каталог одноразового runtime создаётся только внутри чистого корня
  репозитория.

## Итоговые гейты

- `micro1-safe-preflight control`: **READY_FOR_CLEAN_BRANCH**.
- `micro1-safe-preflight implementation`: **READY_FOR_IMPLEMENTATION**.
- Контрольные суммы: **PASS, 730/730** до финального согласования этой карточки.
- `git diff --check`: **PASS**.
- Официальный каталог результата отсутствует, одноразовых runtime-каталогов не осталось.
- Checkpoint commit разрешён после повторного согласования checksum manifest и проверки
  staged scope.

Обе preflight-проверки напомнили о четырёх бинарных копиях официальных материалов,
которые требуют человеческого просмотра перед ZIP. Это известное предупреждение, а не
ошибка. Implementation preflight также ожидаемо сообщил о незакоммиченном рабочем дереве:
текущая задача как раз создаёт разрешённый checkpoint.

Первая попытка `control` preflight выявила абсолютный machine-specific путь в двух
служебных проекциях уже выполненной команды SES-029. Ровно эти два префикса заменены
на `<clean-repository-root>` без изменения смысла команды и измеренного результата.
После категориальной редактуры весь гейт запускается повторно.

## Что эта проверка не утверждает

Это не официальный и не оцениваемый прогон. Реальные кандидаты не исполнялись,
скрытые результаты не раскрывались, live model не вызывалась. Успешный публичный
transport canary из обычного Terminal уже зафиксирован в `SES-20260831-029` и здесь
не повторяется.
