# BG-D01 SCALE-004 — контрольная точка остановки обычных тестов

**Статус:** `TEST_FAILED_STOPPED`  
**SCALE_READY:** `false`

## Единственная разрешённая команда

Команда `npm test` была запущена ровно один раз и завершилась с ненулевым кодом.
Повторов, исправлений и других продуктовых команд после сбоя не было.

Доступные безопасные итоги:

- всего тестов: 55;
- пройдено: 46;
- не пройдено: 9;
- отменено: 0;
- пропущено: 0.

Сырой вывод, машинно-зависимые абсолютные пути, значения переменных среды,
внешние пути, закрытые термины и содержимое трассировки в эту карточку не
переносились.

## Безопасный класс сбоя

Сбой классифицирован как детерминированная недоступность применения локальной
изоляции дочерних процессов в текущей среде запуска. Четыре проверки физической
границы завершились служебным кодом отказа изолятора; вслед за этим одна проверка
внедрённого TEST-ONLY описателя и четыре проверки вертикального среза не смогли
завершить предусмотренные дочерние процессы.

Эта попытка не даёт основания считать поведенческий шлюз A1/A2/S1/S2/S3
пройденным. В частности, сквозное доказательство A2 не получено, а совокупная
обычная проверка сохранности A1, S1, S2 и S3 остаётся незавершённой. По имеющимся
безопасным итогам нельзя приписывать сбой изменению fixtures, candidates, oracles,
видимых утверждений или методики оценки.

## Сохранённые границы

До и после команды не изменялись продуктовый и тестовый исходный код, manifests,
проекции, зависимости, `package-lock.json`, политика среды выполнения, fixtures,
candidates, oracles, visible assertions, методика оценки, цели, знаменатели,
BG-D02 и объём A1/A2/S1/S2/S3. Контракты A3/S4/P1/P2/D1 остаются сохранёнными.

`d01:verify`, replay, demo, preflight, форматирование, установка зависимостей,
официальный или оцениваемый запуск и любые иные продуктовые команды не
выполнялись. Изменений после сбоя и повторных попыток не было.

## Оставшиеся ворота

Обычный тестовый шлюз не пройден. Целевая проверка D01 не запускалась и остаётся
отдельным будущим шлюзом. Также остаются проверка и продвижение захваченной
траектории, повторная матрица приёмки и отдельное решение владельца о готовности к
масштабированию.

`SCALE_READY=false`. Commit и push не выполнялись.

## Точное решение, требуемое от владельца

Для продолжения требуется отдельное явное решение владельца: разрешить ровно один
повтор `npm test` без каких-либо правок в чистой среде, где применение штатной
изоляции дочерних процессов разрешено, либо оставить SCALE-004 остановленным.
Даже при разрешении и успешном результате такого повтора `d01:verify` потребует
отдельного последующего разрешения.
