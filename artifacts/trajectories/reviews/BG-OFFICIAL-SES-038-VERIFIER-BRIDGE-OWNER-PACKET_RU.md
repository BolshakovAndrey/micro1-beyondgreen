# SES-038 — пакет владельца по закрытию verifier-мостов

Дата завершения: 2026-08-31 12:28 UTC<br>
Базовый commit: `295890fe525697e36979a32a4c167d1eecc53d72`<br>
Итог: **ПОЛНЫЙ EVALUATOR REHEARSAL УСПЕШЕН**

## Что выполнено

Исправлен ровно один заранее одобренный дефект BG-D04. Публичный замороженный контракт
`evaluation/arm-visible/BG-D04/harness.ts:32` определяет, что `dispose()` возвращает
карту счётчиков подписчиков напрямую. Evaluator теперь передаёт
`transcript.disposal.observation` неизменному oracle без попытки читать отсутствующее
поле `subscribers`.

Добавлен regression test, который воспроизводит шесть канонических наблюдений и прямой
результат disposal `{ harbor: 0, orchard: 0 }`. Он защищает именно публичную форму
контракта, а не желаемую оценку кандидата.

Для запуска репетиции привязка управляющего скрипта переведена с завершённой SES-037 на
одобренную SES-038. Это не меняет runtime-семантику и лишь запрещает запуск из другой
сессии.

## Доказательства

- `npm run compile` — успешно.
- Targeted test suite — 7 из 7 успешно.
- Package-local manifest BG-D04 согласован с точными байтами runtime export.
- `RUN-001`, `RUN-002`, `POSTDECISION-002` и `POSTDECISION-003` не имеют ни одного
  изменённого файла относительно базового commit.
- `POSTDECISION-004` не существует и не запускался.
- Official/scored run не выполнялся.
- Arm-процессы и модель не вызывались.

## Репетиция и причина остановки

Выполнена одна разрешённая команда:

```bash
MICRO1_EVALUATOR_REHEARSAL_SESSION_BOUNDARY=SES-20260831-038 \
  npm run task -- evaluation:evaluator-rehearsal
```

Исправленный BG-D04 больше не является точкой отказа. Репетиция дошла до следующей
фикстуры и остановилась с результатом:

```text
Frozen ground-truth mismatch for BG-H03:candidate-a
```

Это не generic runtime failure, а прямое срабатывание контрольного условия против
подгонки. Владелец заранее потребовал при любом ground-truth mismatch остановиться.
Поэтому BG-H03 не исследовался, его observer/evaluator bridge не изменялся и повторная
репетиция не запускалась.

## Одобренное продолжение BG-H03

После отдельной read-only диагностики владелец выбрал точный транспорт ссылочной
идентичности. Observer теперь создаёт один `WeakMap` перед первым кадром и сохраняет его
для всех `frames[]` одного capture. До неизменяемой JSON-сериализации каждой новой
ссылке `selectionHandle` присваивается непрозрачный порядковый токен. Evaluator строго
проверяет непрерывность и структурную устойчивость токенов, затем восстанавливает одну
ссылку для одинакового токена и разные ссылки для разных токенов перед передачей
наблюдений неизменному canonical oracle.

Токен не содержит идентификатор кандидата, ground truth, данные oracle или значения из
другой capability-зоны. Scenario-provider, публичный contract/harness, canonical
oracle, candidates, ground truth и scoring не менялись.

Новые доказательства:

- `npm run compile` — успешно;
- targeted tests — 16 из 16 успешно;
- последовательность токенов восьми кадров — `1,1,1,2,2,2,3,3`;
- два независимых capture дают идентичную последовательность и одинаковые байты;
- искусственное нарушение ссылочной идентичности вызывает fail-closed
  `OBSERVATION_CAPTURE_DIVERGENCE`;
- evaluator восстанавливает ссылки и получает `preserving` на публично определённом
  каноническом наблюдении;
- package-local manifest BG-H03 согласован с runtime export.

## Потеря финального свидетельства rehearsal

После зелёных compile и targeted tests была ровно один раз запущена разрешённая
команда rehearsal. Она завершилась, но после фонового yield PTY-транспорт координатора
не вернул финальную строку и код выхода. Rehearsal не пишет repository-local receipt,
поэтому достоверно восстановить его PASS/FAIL из файлов невозможно.

Команда не повторялась. Этот пакет не выдаёт отсутствие свидетельства за успешный
результат и не угадывает следующую фикстуру. Это ошибка доставки результата
координатору, а не доказанный новый verifier mismatch.

Владелец отдельно разрешил один транспортный повтор. Он был выполнен с сохранённым
`session_id` и остановился на точном несовпадении BG-H05. Это подтвердило исправление
BG-H03 и определило следующую диагностическую точку без чтения ground truth или
candidate bytes.

## Исправление BG-H05

Read-only диагностика выявила два независимо доказанных bridge-дефекта.

Первый: публичный store обязан возвращать тот же snapshot до реального изменения, но
покадровая JSON-сериализация уничтожала ссылочное отношение. В H05 observer добавлен
один `WeakMap` на все восемь кадров capture. Он выдаёт последовательность
`1,2,3,3,4,4,4,4`; evaluator восстанавливает одинаковые ссылки для одинаковых токенов.

Второй: публичный `dispose()` возвращает полный `UnitStoreObservation`, однако
evaluator отбрасывал восьмой dispose-frame. Фильтрация удалена; финальное наблюдение с
нулём подписчиков передаётся неизменному canonical oracle ровно один раз.

Scenario-provider, public contract/harness, canonical oracle, candidates, ground
truth и scoring не менялись.

## Финальная проверка SES-038

- `npm run compile` — успешно;
- targeted tests — 18 из 18 успешно;
- scenarios — 20;
- fresh captures — 80;
- независимые пары captures — 40;
- evaluator records — 40;
- schema-valid records — 40 из 40;
- совпадения с frozen ground truth — 40 из 40;
- arm executions — 0;
- model invocations — 0;
- digest результата —
  `90b14804aea1aa9fb48a46732eee158a7e52088df3becb10f5a3374fb9c653f9`.

## Что не выполнено

- полный гейт 20 scenarios / 80 captures / 40 evaluator records подтверждён;
- `POSTDECISION-004` не создавался и не запускался;
- полные тесты, checksums и preflight не запускались после обязательной остановки;
- commit и push не выполнялись.

## Риски и требуемое следующее решение

Verifier-bridge closure достигла полного зелёного rehearsal. Следующие действия
отдельно гейтятся: полный repository verification/checksums/preflight, checkpoint
commit и owner-controlled `POSTDECISION-004`. В рамках текущего разрешения
`POSTDECISION-004`, commit и push не выполнялись.
