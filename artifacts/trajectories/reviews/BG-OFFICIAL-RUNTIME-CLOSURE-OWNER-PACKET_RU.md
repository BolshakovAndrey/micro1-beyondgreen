# SES-025 — пакет одобрения закрытия production runtime

Статус: **RUNTIME_READY, OFFICIAL_RUN_NOT_STARTED**.

`Production runtime` — это исполняемая обвязка, которая в будущем проведёт единственный официальный запуск. В SES-025 она полностью собрана и проверена только на синтетических данных. Реальные кандидаты, живая модель, раскрытие скрытых эталонов и оцениваемый запуск не выполнялись.

## Что готово

- Создан production root для всех 10 фикстур и обоих сравнительных подходов.
- Status quo выполняется в изолированном дочернем процессе без модели.
- BeyondGreen получает ровно один вызов Codex через JSONL, без повторной попытки и без verifier-контекста.
- Файловая изоляция обеспечивается Node Permission Model, сетевой доступ дочерних процессов запрещён macOS Seatbelt.
- После фиксации всех решений отдельные процессы выполняют нейтральный сценарий, два независимых наблюдения и независимую оценку.
- Для всех 10 verifier-пакетов созданы одинаковые точки входа scenario provider и evaluator; для D01 добавлен недостающий нейтральный provider.
- Публичная категория D03 `render-summary` приведена к read-only операции `snapshot`.
- `evaluation:run` зарегистрирована как настоящая production-команда, но дополнительно требует явный `MICRO1_OFFICIAL_SESSION_BOUNDARY`. Команда не запускалась.

## Проверенные свойства

- TypeScript compile: **PASS**.
- Совместная synthetic/physical integration matrix: **48/48 PASS**.
- Verifier runtime exports: **6/6 PASS**.
- Verifier providers вместе с унаследованными SES-022 тестами: **18/18 PASS**.
- Реальный дочерний процесс на синтетических данных прошёл цепочку status quo → scenario provider → observer → evaluator.
- Физический отрицательный тест подтвердил отказ чтения запрещённого пути и отказ сети.
- `git diff --check`: **PASS**.

## Инцидент и замена verifier-subagent

Первая verifier-попытка нарушила границу чтения: поиск затронул два candidate-файла BG-D03. Она не создала допустимых результатов, все временные правки были побайтово восстановлены, а попытка классифицирована как `contaminated/no-deliverable`.

После отдельного одобрения владельца запущен fresh replacement без наследования истории. Он не читал и не искал candidate paths, создал 10 стандартизованных wrappers и D01 provider, прошёл проверки и не обнаружил новых privacy/contamination findings.

## Что намеренно не сделано

- `evaluation:run` не запускалась.
- Реальные candidates не исполнялись.
- Live model calls: **0**.
- Official/scored runs: **0**.
- Unblinding: **false**.
- Claude, browser, MCP, Chromium, commit и push не использовались.

## Следующий гейт

Следующим отдельным действием нужны финальные безопасные preflight-проверки, согласование производных checksums и checkpoint commit без push. После него создаётся новая boundary для единственного official run. Сам official run остаётся запрещённым до отдельного явного одобрения владельца.
