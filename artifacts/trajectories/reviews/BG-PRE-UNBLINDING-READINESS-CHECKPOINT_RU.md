# Контрольная карточка готовности BeyondGreen перед единственным раскрытием результатов

**Граница:** `SES-20260830-015`<br>
**Задача:** `BeyondGreen — pre-unblinding readiness`<br>
**Режим:** только ограниченная подготовка до раскрытия held-out результатов — результатов шести отложенных примеров<br>
**Статус:** boundary/readiness gate завершён; реализация ожидает точного одобрения владельца

## Что проверено на этом ходе

Работа начата в текущей изолированной чистой рабочей копии репозитория. Точный
`HEAD` совпадает с заданным:
`cb51e7de1e9908088dbef4dbeb29946d1edbc16b`. Этот коммит является вершиной
`codex/beyondgreen-d01-scale003-correct`. Рабочая копия была в состоянии detached
HEAD — указатель находился непосредственно на коммите, — однако именованная ветка
указывает на тот же коммит. Начальный вывод `git status --short` был пустым.

Полностью прочитаны `AGENTS.md`, `docs/CHALLENGE.md`,
`docs/HACKATHON_RULES.md`, `docs/CLEAN_ROOM_POLICY.md`,
`docs/TRACE_POLICY.md`, `docs/PREFLIGHT_CHECKLIST.md`,
`docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, `config/topic.yaml` и
`templates/SESSION_BOUNDARY.yaml`. Общая память вне репозитория не читалась,
поскольку clean-room contract — договор чистой среды — прямо запрещает выходить за
корень этой рабочей копии.

Статическим просмотром, без компиляции и тестов, подтверждено наличие всех десяти
пакетов `BG-D01`–`BG-D04` и `BG-H01`–`BG-H06`:

- каталог arm-visible — доступная проверяемому процессу часть — присутствует для
  всех `10/10` примеров;
- каталог verifier-only — скрытая часть независимого оценщика — присутствует для
  всех `10/10` примеров;
- присутствуют все `20/20` каталогов кандидатов, по два на пример;
- присутствуют все `10/10` файлов ground truth — эталонных решений;
- присутствуют каталоги манифестов для всех `10/10` примеров.

Это только проверка наличия. Она не утверждает, что кандидаты компилируются, тесты
проходят или хеши совпадают: такие команды до одобрения намеренно не запускались.

## Подтверждённые пробелы готовности

1. В типизированном реестре задач отсутствуют четыре нормативных имени:
   `baseline:verify`, `beyondgreen:verify`, `evaluation:run` и `replay`. Они пока
   существуют только как требуемый контракт в документации, а не как безопасные
   исполняемые точки входа.
2. Ранняя manifest-backed ZIP rehearsal — репетиция ZIP с проверяемым перечнем
   содержимого — не закрыта. Нет подтверждённого прохода «создание временного ZIP →
   чистая распаковка → точные команды судьи → сверка манифеста».
3. Официальный или оцениваемый запуск не выполнялся. Однократное раскрытие
   `BG-H01`–`BG-H06` также не выполнялось.
4. Статусные проекции частично отстали от фактического Git-состояния:
   `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001` уже продвинут в `actual_traces`
   коммитом `e08f2f91440a484df9f9c9ae772461bd3d9d49d1`, но
   `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md` и `config/topic.yaml` ещё содержат
   формулировки о незавершённом продвижении. Исправление TypeScript `TS2532` и его
   служебные материалы уже входят в текущий `HEAD`, но этот завершённый checkpoint
   — контрольная точка — не согласован во всех статусных проекциях.
   `docs/CONTROL_STATUS_RU.md` дополнительно сохраняет исторические незакрытые фазы
   и прежнее имя StateShift Guardian/`SSG`, поэтому не является точным снимком
   текущей готовности.

Ни один из этих выводов не является новым benchmark result — результатом
сравнительного измерения. Это read-only инвентаризация состояния репозитория.

## Что разрешит одобрение

Одобрение разрешит только один ограниченный этап подготовки перед необратимым
раскрытием результатов.

### 1. Общий freeze self-test всех 20 кандидатов

Freeze self-test — контроль неизменности и внутренней корректности замороженного
набора — должен:

- подтвердить компиляцию и видимые проверки `20/20` кандидатов;
- получить у независимого оценщика ровно `10/10` корректных `accept` для
  сохраняющих поведение кандидатов и `10/10` обоснованных `reject` для ложнозелёных
  кандидатов; обоснование обязано правильно назвать нарушенный класс поведения или
  семейство инвариантов;
- сверить существующие хеши кандидатов и манифестов, не меняя их;
- доказать взаимный физический запрет доступа к скрытому оракулу, точное количество
  примеров и кандидатов, разбиение `4/6`, взаимно однозначное назначение десяти
  классов поведения и единственный сложный пример `BG-H02`.

Любое отклонение требует остановки. Самостоятельно чинить кандидата, скрытый оракул,
видимую проверку или хеш нельзя.

### 2. Безопасные точки входа и автономное воспроизведение

Разрешается реализовать типизированные точки входа
`baseline:verify`, `beyondgreen:verify`, `evaluation:run` и `replay` с обязательной
проверкой точной версии `eval-v1.1.0`. Их устройство должно технически отделять
безопасную проверку готовности и offline replay — автономное воспроизведение без
сети — от официального запуска и раскрытия результатов.

На этом этапе разрешено проверять только контракты, отказоустойчивость, хеши,
количество запусков и воспроизводимость. Нельзя расходовать официальный единственный
запуск, вызывать живую модель, передавать оценщику обратную связь до решения или
раскрывать held-out результат пути оптимизации.

### 3. Ранняя ZIP-репетиция

Разрешается создать только временный ZIP с манифестом в отдельном временном
каталоге, распаковать его в новый чистый временный каталог и выполнить там точные
судейские команды, которые относятся к этому ограниченному этапу. Проверка должна
охватить состав архива, лицензии, обе части всех пакетов, согласованность
манифестов, автономное воспроизведение и физический запрет доступа к скрытому
оракулу.

Финальный `dist/submission.zip`, видео, публикация, загрузка и отправка заявки не
разрешаются. В репозитории не должен оставаться новый архив или официальный run
record — постоянная запись запуска.

### 4. Синхронизация статусов

После измеренных проверок разрешено исправить только фактические статусные разделы
в `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, `config/topic.yaml` и
`docs/CONTROL_STATUS_RU.md`, а также непосредственно необходимые производные
контрольные суммы. Нельзя менять нормативный смысл, формулы, цели, методику,
кандидатов, скрытые ответы, видимые проверки или ранее зафиксированные хеши.

## Что останется запрещено после этого одобрения

- официальный или оцениваемый `baseline:verify`, `beyondgreen:verify` либо
  `evaluation:run` на замороженном наборе;
- единственное раскрытие held-out результатов `BG-H01`–`BG-H06`;
- вызов живой модели продукта и расходование её единственной попытки;
- Chromium, управление браузером, подключённые приложения, MCP и Claude;
- исправление, перегенерация или изменение кандидатов;
- изменение семантики или байтов скрытого оракула, видимых утверждений,
  замороженного поведения, scoring — правил подсчёта, methodology — методики, целей
  или существующих хешей;
- финальное видео, финальный архив, публикация, загрузка и отправка;
- `commit` и `push`.

Для официального запуска и однократного раскрытия понадобится отдельное
необратимое одобрение владельца. В нём должны быть указаны точный чистый `HEAD`,
хеши всех замороженных манифестов и оценщика, точные команды, единственная попытка,
правила остановки и прямое последствие: после раскрытия нельзя настраивать v1.1 по
held-out результатам.

## Риски, остановки и предел полномочий

Главный риск — превратить проверку готовности в скрытый официальный запуск или
случайно раскрыть held-out информацию пути разработки. Поэтому точки входа должны
иметь явный необратимый gate — отдельный барьер одобрения — и fail-closed поведение:
при неоднозначности выполнение прекращается.

Второй риск — обнаружить несоответствие замороженных байтов и «исправить» его,
уничтожив честность заранее объявленного benchmark. Такое несоответствие является
стоп-сигналом, а не разрешением на изменение.

Третий риск — собрать ZIP, который работает только в исходной рабочей копии. Поэтому
репетиция считается успешной только после чистой распаковки во временный каталог,
сверки манифеста и выполнения точных разрешённых команд без внешней памяти,
приватных подключений и сетевых учётных данных.

До одобрения не создавались product code, runner, archive, run records или новые
benchmark results; не запускались compile, tests, preflight, capture, replay либо
packaging. Не выполнялись `commit`, `push`, официальный запуск, unblinding, вызов
живой модели, Chromium, browser/MCP или Claude.

## Точная фраза одобрения

Если граница, риски и разрешённый объём верны, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-015 и разрешаю только bounded pre-unblinding implementation: общий freeze self-test всех 20 кандидатов, безопасные task-registry entrypoints и offline replay contracts, раннюю manifest-backed ZIP rehearsal во временном каталоге и синхронизацию только статусных проекций по измеренным результатам; official/scored run, unblinding, live model, Chromium, browser/MCP, Claude, commit и push не разрешаю; при любом изменении candidates, oracle semantics, visible assertions, frozen behavior, scoring, methodology или hashes остановиться.
