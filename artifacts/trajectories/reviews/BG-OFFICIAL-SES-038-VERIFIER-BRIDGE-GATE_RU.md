# SES-038 — контрольная точка закрытия verifier-мостов

Дата подготовки: 2026-08-31 11:39 UTC<br>
Базовый commit: `295890fe525697e36979a32a4c167d1eecc53d72`<br>
Статус: **ОЖИДАЕТ ОДОБРЕНИЯ ВЛАДЕЛЬЦА; РЕАЛИЗАЦИЯ НЕ НАЧАТА**

## Зачем нужна сессия

SES-037 исправила независимо доказанные несоответствия BG-D01 и BG-D03 и сохранила
полный частичный комплект `POSTDECISION-003`. Неофициальная репетиция остановилась на
следующем детерминированном дефекте: evaluator BG-D04 трактует результат `dispose()` как
объект с полем `subscribers`, хотя публичный замороженный контракт возвращает карту
счётчиков напрямую.

SES-038 должна закрыть этот дефект и только аналогичные дефекты verifier-мостов BG-D04 и
BG-H01–H06. Она не повторяет решения плеч, не вызывает модель и не меняет методику.

## Предлагаемое разрешение

1. В BG-D04 передать неизменному oracle значение `transcript.disposal.observation`
   напрямую. Правильную форму однозначно задаёт сигнатура `dispose()` в замороженном
   публичном `evaluation/arm-visible/BG-D04/harness.ts`.
2. После каждого исправления повторять только неофициальную production-equivalent
   репетицию evaluator.
3. Если в BG-D04 или BG-H01–H06 найдено следующее несовпадение формы, названия или
   количества шагов, исправлять только observer normalization либо evaluator bridge и
   только когда одна конкретная замороженная строка или сигнатура однозначно задаёт
   правильную форму.
4. Для каждой правки записывать дефект, определяющий артефакт и точное место контракта,
   минимальное изменение и regression test — тест, предотвращающий возврат дефекта.
5. Если единственного определяющего артефакта нет, считать вопрос неоднозначным и
   немедленно остановиться для решения владельца.

## Гейт завершения

- 20 нейтральных сценариев;
- 80 заново полученных наблюдений;
- 40 из 40 записей evaluator проходят схему;
- все результаты точно совпадают с ранее замороженным ground truth;
- ноль повторных arm-вызовов и ноль model-вызовов;
- `POSTDECISION-004` только подготовлен и не запущен;
- compile, тесты, манифесты, checksums, обе safe preflight-проверки и diff-check зелёные.

Совпадение с ground truth используется только как проверка. Если независимо выведенные
правки не дают совпадения, агент обязан остановиться, а не подстраивать сценарий,
транскрипт или evaluator.

## Что остаётся запрещено

- любые изменения `RUN-001`, `RUN-002`, `POSTDECISION-002` и `POSTDECISION-003`;
- изменения BG-D01/BG-D03 после SES-037;
- изменения сценариев, oracle semantics, candidates, ground truth, scoring или
  evaluation v1.1;
- `evaluation:run`, `POSTDECISION-004`, arm/model calls;
- внешние рабочие пространства, browser/MCP и приватное состояние;
- изменения четырёх документов README/REPRODUCTION;
- commit и push без отдельного разрешения.

## Требуемое решение владельца

До явного одобрения этого boundary разрешена только его фиксация в рабочем дереве.
Product/runtime code не изменялся, репетиция не запускалась, commit и push не
выполнялись.
