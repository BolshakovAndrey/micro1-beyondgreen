# Контрольная карточка границы сессии BG-D04

## Итог

Создана только пограничная запись `SES-20260830-008` и эта русская карточка. До
Вашего одобрения не создано и не прочитано содержимое поведения BG-D04, исходный
код React или signals, кандидаты, скрытый эталон, тесты, манифесты, результаты
измерений либо трассы.

Предлагаемая задача — независимо создать синтетическую development-фикстуру
`BG-D04` для класса ошибок `subscription_cleanup`: утечки подписок, дублирование
подписок или неверный порядок очистки при переносе состояния React на signals.
После отдельного одобрения потребуются ровно два неизменяемых кандидата: один
сохраняющий поведение и один намеренно «ложно-зелёный», который проходит видимые
проверки, но нарушает скрытый инвариант. Это не официальный и не оцениваемый запуск.

## Проверенное исходное состояние

- Работа начата в чистом изолированном рабочем дереве с отсоединённым HEAD.
  Базовый и текущий commit до новых служебных файлов совпадали:
  `18510e26fd38e890c0370481ccce114f90cf4ca6`.
- Команда `git diff --quiet 18510e26fd38e890c0370481ccce114f90cf4ca6` завершилась
  успешно: до этой границы изменений исходников не было. Удалённое состояние Git не
  проверялось; commit и push не выполнялись.
- По указанию координатора для BG-D04 выбран `SES-20260830-008`; его коллизия
  проверена до продолжения.
- Полностью прочитаны `AGENTS.md`, финальный контракт задачи, правила хакатона,
  политика чистой комнаты, политика трасс, предварительный контрольный список,
  нормативная спецификация, проекция оценивания и `config/topic.yaml`.
- По обязательной стартовой инструкции `AGENTS.md` были только для чтения открыты
  общие файлы состояния и правил. Они не использовались как источник поведения,
  структуры, кандидатов, скрытого эталона или происхождения BG-D04. Поэтому текущая
  подготовительная переписка остаётся `pending_review` — ожидающей проверки — и не
  заявляется как допустимая авторская трасса.
- Не запускались компиляция, тесты, проверка D01, воспроизведение, предварительная
  проверка, сканер, захват трассы, вложенный Codex CLI, Claude, живая модель,
  Chromium, браузер, подключённые приложения или приватный MCP. Значения и пути
  `MICRO1_PRIVATE_DENYLIST` и `MICRO1_PRIVATE_TRACE_DIR` не читались и не выводились.

## Что разрешит одобрение

Только одну автоматически захватываемую траекторию `TRC-BG-D04-001` в следующих
путях:

- `evaluation/behavior-specs/BG-D04*`;
- `evaluation/arm-visible/BG-D04/**`;
- `evaluation/verifier-only/BG-D04/**`;
- `evaluation/manifests/BG-D04/**`;
- `candidates/BG-D04/**`;
- только тесты и скрипты, в имени которых есть `BG-D04`.

Сначала будет независимо написано поведение на естественном языке: нейтральная
синтетическая предметная область, публичные основания, действия пользователя,
наблюдаемые результаты и инварианты очистки. Этот текст, происхождение и SHA-256
будут зафиксированы до кода. Затем можно создать только в указанных путях
видимую часть задачи, физически отделённую скрытую эталонную проверку и два
неизменяемых кандидата. Публичные и экспортируемые TypeScript API, а также важные
модули получат краткий JSDoc на английском; важные инварианты — короткие английские
комментарии, объясняющие причину ограничения.

Если понадобится изменить общий движок, реестр, пакет, настройки, проекцию,
индекс происхождения, индекс трасс или контрольные суммы, я только сообщу об этом:
редактировать их в этой траектории нельзя.

## Что останется запрещено

Нельзя изменять `package.json`, `package-lock.json`, `tsconfig.json`, `src/d01/**`,
общие реестры, `config/topic.yaml`, `docs/PROJECT_SPEC*`, `docs/EVALUATION.md`,
`docs/IMPROVEMENT_CHANGELOG.md`, индексы происхождения и трасс, контрольные суммы,
а также любой файл BG-D01 или другой фикстуры. Нельзя выполнять официальный или
оцениваемый запуск, разворачивать результаты, вызывать Claude, живую модель,
Chromium, браузер, MCP или приватное состояние. Commit и push запрещены.

## Автоматический захват и проверка

После одобрения до первого вложенного запуска Codex CLI будет отдельно доказано,
что машинно читаемый JSONL — последовательность JSON-событий по одному на строку —
идёт без изменения байтов из stdout Codex CLI прямо в stdin `micro1-safe-trace`.
Сырой слой хранится только во внешнем каталоге, доступном через
`MICRO1_PRIVATE_TRACE_DIR`; промежуточный файл в репозитории, `tee`, ручное
копирование, `Copy as Markdown` и смешивание stderr с JSONL запрещены. Ошибка
проверки транспорта остановит работу без запасного ручного способа.

После авторинга будут проверены: соответствие путей разрешённому списку, полнота
поведения и происхождения, неизменяемые хеши кандидатов, одинаковая видимая
проверка для обоих кандидатов, только BG-D04-специфичные тесты и сохранение
физической изоляции скрытого эталона и правила `K=0` (ноль обратной связи от
независимого оценщика до неизменяемого решения). При первой реальной ошибке работа
останавливается: повтор, расширение области, изменение скрытого эталона, кандидата
или общих путей требует нового одобрения.

## Риски и границы

Любой внешний путь, приватный контекст, неясная лицензия или происхождение,
сходство с запомненной приватной структурой, утечка скрытого эталона или
неразрешимое загрязнение означает немедленную остановку и запрос решения. Эта
граница не делает результаты или трассу автоматически допустимыми: после неё всё
ещё обязательны автоматический захват, проверка и отдельное рассмотрение владельцем.

## Точное одобрение

Если граница верна, ответьте в точности:

> Одобряю SESSION_BOUNDARY SES-20260830-008 и разрешаю одну траекторию TRC-BG-D04-001 только для синтетической фикстуры BG-D04 subscription_cleanup в указанном fixture-specific scope с обязательным automatic JSONL → micro1-safe-trace; shared paths не изменять, при первом сбое остановиться; official/scored run, Claude, Chromium, live model, browser/MCP/private state, commit и push запрещены.

Эта фраза разрешит только описанное независимое авторство BG-D04, связанные
проверки и последующее рассмотрение трассы. Она не разрешает изменения общих
файлов, интеграцию в реестры, официальный запуск, публикацию, commit или push.
