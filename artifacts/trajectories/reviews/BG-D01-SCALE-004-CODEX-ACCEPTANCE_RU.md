# BG-D01 SCALE-004 — приёмочная матрица Codex

**Класс проверки:** независимая локальная проверка готовности к масштабированию  
**Вход:** одобренный владельцем комплект SCALE-004, включённый в `actual_traces`  
**Вердикт:** все критерии пройдены, решение владельца получено  
**SCALE_READY:** `true`

| ID | Результат | Доказательство |
| --- | --- | --- |
| A1 | PASS | Настоящие arm, observer, evaluator и вертикальный срез принимают `FixtureEngineBindings`; общие функции находятся в `src/d01/orchestrator.ts:251-644` и `src/d01/worker-runtime.ts:14-140`, а D01 scripts только собирают зависимости. |
| A2 | PASS | `tests/d01-descriptor-injection.test.ts:10-40` проводит TEST-ONLY через настоящий transport процессов, оценщик, replay и отчёт. |
| A3 | PASS | D01-схема наблюдения остаётся вне общего фабричного слоя и явно передаётся в `src/d01/schemas.ts:6-13`. |
| S1 | PASS | `src/d01/capability.ts:68-116`, `src/d01/execution.ts:72-92` и `src/d01/replay.ts:59-65` выводят и сравнивают точную политику каждого слота; тесты лишнего, отсутствующего, изменённого и переставленного пути находятся в `tests/d01-vertical-slice.test.ts:447-485`. |
| S2 | PASS | `src/d01/execution.ts:68-127` требует точного взаимно-однозначного соответствия proof, роли, профиля и слота; межролевая подмена отклоняется в `tests/d01-vertical-slice.test.ts:569-573`. |
| S3 | PASS | `src/d01/execution.ts:136-161` сверяет события с фактическим неизменяемым решением группы; подмена решением другой группы и произвольным согласованным хешем отклоняется в `tests/d01-vertical-slice.test.ts:504-515`. |
| S4 | PASS | Replay самостоятельно пересчитывает выводы оценщика в `src/d01/replay.ts:31-57`; инверсия полей отклоняется тестами `tests/d01-vertical-slice.test.ts:574-579`. |
| P1 | PASS | Рекурсивный перечень глобально сортируется в `src/d01/candidate.ts:31-48`; вложенная структура проверяется в `tests/d01-package-binding.test.ts:19-31`. |
| P2 | PASS | Изменение байтов зарегистрированного вложенного файла закрыто отклоняется в `tests/d01-package-binding.test.ts:60-67`. |
| D1 | PASS | Текущие review, provenance, topic, changelog и индекс трасс согласованно содержат восемь одобренных записей и `SCALE_READY=true`. |

## Итоговые проверки

- Обычные тесты прошли `56/56`.
- Полный `d01:verify` прошёл компиляцию, видимые тесты `5/5`, контракт шага `1/1`,
  oracle `3/3`, границу `5/5`, вертикальные контракты `19/19`, E2E и manifests.
- Автономное воспроизведение прошло и точно восстановило JSON и HTML.
- Проверка этапа 0.5 прошла `4/4`.
- Все восемь implementation JSONL-захватов прошли автоматический raw-скан. До подачи
  требуется детерминированная редактура машинных путей; после неё приватных терминов,
  адресов электронной почты и присваиваний секретов нет.

## Решение

Все десять критериев пройдены. Владелец включил все восемь проверенных захватов в
`actual_traces` и явно установил `SCALE_READY=true`. Это решение не разрешает
официальный или оцениваемый запуск, commit либо push.
