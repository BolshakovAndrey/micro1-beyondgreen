# BeyondGreen BG-D02 — карточка одобрения границы авторской сессии

## Решение, которое требуется сейчас

Нужно одобрить только границу `SES-20260830-006`. До одобрения не будут создаваться
описание поведения фикстуры, код React или signals, кандидаты, скрытый эталон,
тесты, манифесты, результаты измерений или трассы. Не будут запускаться официальное
или оцениваемое выполнение, живая модель, Chromium, браузер, MCP, Claude, commit или
push.

Одобрение разрешит одну ограниченную траекторию авторства для синтетической
фикстуры `BG-D02` класса `queued_batched_updates` — очередные и пакетные обновления.
После неё всё ещё потребуются отдельные проверки, безопасный захват трассы, проверка
происхождения и отдельное одобрение владельца; сама эта фраза не делает результат
автоматически допустимым для отправки.

## Проверенное исходное состояние

- Работа идёт только в разрешённом чистом репозитории. Абсолютный путь рабочей
  машины намеренно не записывается.
- Изолированная рабочая копия находится в состоянии отсоединённого указателя Git;
  ветка не выбрана. Точный исходный commit:
  `18510e26fd38e890c0370481ccce114f90cf4ca6`.
- Команда `git status --short --untracked-files=all` не вывела строк: рабочее дерево
  чистое.
- Следующий свободный идентификатор после имеющихся записей — `SES-20260830-006`.
- Удалённое состояние Git не проверялось. Commit и push не разрешены и не выполнялись.

## Что уже сделано и что сознательно не делалось

Полностью прочитаны `AGENTS.md`, финальный контракт задачи, правила хакатона,
политика чистой комнаты, политика трасс, предварительный контрольный список,
нормативная спецификация `PROJECT_SPEC.md`, проекция оценивания и
`config/topic.yaml`. Для формата границы прочитан локальный шаблон
`templates/SESSION_BOUNDARY.yaml`. Проверены только локальные метаданные Git и
уникальность номера сессии.

Созданы только эта русская карточка и
`artifacts/trajectories/session-boundaries/SES-20260830-006.yaml`. Не читались и не
менялись исходники продукта, D01, другие фикстуры, кандидаты, скрытые эталоны,
манифесты, реестры или контрольные суммы. Не запускались компиляция, тесты,
предварительный сканер, команды продукта или измерения.

Значения, внешние пути и содержимое `MICRO1_PRIVATE_DENYLIST` и
`MICRO1_PRIVATE_TRACE_DIR` не читались и не выводились. Глобальная память, внешний
файловый контекст, браузер, подключённые приложения, приватный MCP и Claude не
использовались.

## Граница после одобрения

Разрешены только следующие пути фикстуры:

- `evaluation/behavior-specs/BG-D02*`;
- `evaluation/arm-visible/BG-D02/**`;
- `evaluation/verifier-only/BG-D02/**`;
- `evaluation/manifests/BG-D02/**`;
- `candidates/BG-D02/**`;
- только тесты и сценарии, чьи имена содержат `BG-D02`.

Запрещено менять `package.json`, `package-lock.json`, `tsconfig.json`, `src/d01`,
общие реестры, `config/topic.yaml`, `docs/PROJECT_SPEC*`, `docs/EVALUATION.md`,
`docs/IMPROVEMENT_CHANGELOG.md`, индексы происхождения и трасс, контрольные суммы,
а также любые файлы D01 или других фикстур. Любая необходимая общая интеграция будет
сообщена владельцу, но не изменена.

После одобрения для публичных или экспортируемых TypeScript API и важных модулей
будут добавляться краткие JSDoc-комментарии на английском. Ключевые инварианты будут
объясняться короткими английскими комментариями, чтобы сохранить проверяемые причины
изоляции скрытого эталона и неизменности кандидата.

## План безопасной трассы

До любого разрешённого авторства сначала будет проверена политика автоматической
передачи: неизменённый JSONL, то есть поток JSON-событий по одной строке, из
стандартного вывода Codex должен напрямую поступать в `micro1-safe-trace`. Только
эта безопасная обёртка может использовать `MICRO1_PRIVATE_TRACE_DIR` для внешнего
сырого слоя. Сырой поток нельзя сохранять в репозитории, выводить, дублировать через
`tee`, подставлять в команду или помещать в переменную оболочки. Разрешено сохранить
лишь безопасные метаданные после проверки и обзора. Если такая передача не доказана,
работа остановится без ручной замены экспорта.

## План проверки

Сначала будут подтверждены одобренный объём, публичный источник и происхождение,
политика передачи JSONL и отсутствие изменений в запрещённых путях. Затем — только
локальные детерминированные проверки BG-D02: видимые проверки, неизменность хешей
кандидатов, самопроверка оценщика и физический запрет доступа к скрытому эталону.
Официальное или оцениваемое выполнение, живая модель, Chromium, браузер, MCP и
Claude останутся запрещёнными.

## Риски и условия остановки

Работа немедленно останавливается при внешнем пути, приватном контексте, неясном
происхождении или лицензии, сходстве с запомненной приватной структурой, утечке
скрытого эталона, загрязнении, которое нельзя независимо воспроизвести, либо при
необходимости изменить общий путь. Успешная локальная проверка не даёт права на
официальный запуск, commit или push.

## Точный объём одобрения

Если граница верна, ответьте точной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-006 и разрешаю одну eligible fixture-authoring-траекторию BG-D02 только в перечисленных fixture-specific путях для queued_batched_updates, с автоматическим JSONL → micro1-safe-trace transport; общие пути не менять, официальный/scored run, live model, Chromium, browser/MCP, Claude, commit и push не выполнять.

Эта фраза разрешит только ограниченное авторство BG-D02, необходимые проверки
фикстуры и последующую безопасную проверку трассы. Она не разрешит изменение общих
путей, запуск официальной оценки, доступ к приватному состоянию, commit, push или
публикацию.
