# Контрольная карточка границы интеграции BG-D02—BG-D04

**Граница:** `SES-20260830-009`  
**Будущая трасса:** `TRC-BG-D02-D04-INTEGRATION-001`  
**Состояние:** ожидает отдельного точного одобрения владельца; интеграция не начата

## Что владелец проверяет сейчас

Эта карточка сопровождает только новую служебную границу будущей интеграции трёх
разрешённых фикстур — синтетических тестовых примеров — BG-D02, BG-D03 и BG-D04.
Сейчас владелец не принимает реализацию и не подтверждает результаты тестов. Он
проверяет точную исходную точку, разрешённые источники, будущий объём изменений,
способ автоматического захвата трассы, запреты, порядок проверки и условия
остановки.

До отдельного одобрения созданы только
`artifacts/trajectories/session-boundaries/SES-20260830-009.yaml` и эта русская
карточка. Их структурная проверка — единственная разрешённая последующая операция
этого этапа. Ни один commit с фикстурой не применён, продуктовые и общие файлы не
изменены, команды продукта не запускались, доказательства реализации не создавались.

## Точная исходная точка и три разрешённых источника

Изолированное чистое рабочее дерево открыто ровно на базовом commit
`18510e26fd38e890c0370481ccce114f90cf4ca6`. Commit — это неизменяемый снимок
состояния Git. В начале задачи рабочее дерево было чистым, состояние удалённого
репозитория не проверялось, commit и push не выполнялись.

Для будущей интеграции владелец указал ровно три локальных fixture-only checkpoint
commit — контрольных снимка, содержащих только соответствующие фикстуры и
разрешённых владельцем:

- BG-D02, отложенные и пакетные обновления:
  `c52bacc0b1d9497990b58bd73d5afdbca95035d5`;
- BG-D03, производное состояние:
  `fd96a3ffefaaac9de4cabe098632ecc4876a67be`;
- BG-D04, очистка подписки:
  `140041f82971f73eca17e2735050ecb0ec8de77d`.

На этом гейте — обязательной точке остановки перед изменениями — проверено только,
что база совпадает с текущим HEAD и что все четыре значения существуют локально как
commit-объекты. Содержимое трёх исходных commit не просматривалось и не применялось.

## Происхождение трасс и неиспользуемые попытки

По предоставленным владельцем данным, BG-D02 и BG-D03 имеют допустимые
репрезентативные захваты Codex: сохранённые последовательности действий, ответов
инструментов, ошибок и решений, достаточные для представления соответствующей
работы. BG-D04 завершена нативным подагентом Codex. Для BG-D04 внутренние необработанные
события подагента не заявляются как захваченные и не должны быть представлены
судьям как доступный сырой журнал.

Это запись полученных от владельца фактов, а не новая проверка доказательств на
текущем гейте. Неудачные попытки `D02-001`, `D04-001` и `D04-002` остаются
неиспользуемыми: они не являются источниками интеграции, не наследуются в новую
трассу и не служат доказательством реализации.

## Что именно разрешит отдельное одобрение

Одобрение разрешит ровно одну ограниченную интеграционную траекторию
`TRC-BG-D02-D04-INTEGRATION-001`. Траектория — это последовательная запись
инструкций, действий, ответов инструментов, сбоев, повторов и решений агента.

Будут разрешены только следующие операции:

1. Выполнить `cherry-pick` — применить выбранный commit поверх текущей базы без
   переноса остальной истории — только для трёх commit BG-D02, BG-D03 и BG-D04,
   перечисленных выше.
2. Разрешать только конфликты, непосредственно возникшие при объединении этих трёх
   фикстур.
3. Сохранить для BG-D02 TypeScript-семейство `ParcelDispatchBoard`. TypeScript здесь
   означает основной типизированный язык реализации проекта.
4. Подключить BG-D02, BG-D03 и BG-D04 к общим типизированным контрактам фикстуры,
   descriptor — декларативному описателю запуска — и реестру задач, а также к уже
   существующей архитектуре проверки D01.
5. Не менять замороженное поведение, скрытые эталоны, неизменяемые варианты
   решения, видимые утверждения, методику оценивания, цели, знаменатели, зависимости
   и политику выполнения.
6. Обновить только непосредственно необходимые общие материалы: реестр и средство
   запуска задач, команды воспроизведения в README, текущие проекции, журнал
   улучшений, индексы происхождения и траекторий, контрольные суммы и затронутые
   манифесты.
7. Добавить интеграционные тесты, проверяющие совместную работу D01, D02, D03 и D04.

Прототип BG-D02 на MJS `QueueBatchCounter` записывается как удалённый эксперимент.
MJS — модульный файл JavaScript. Этот прототип не сохраняется в итоговой
реализации; остаётся TypeScript-семейство `ParcelDispatchBoard`. Удаление должно
быть честно отражено в журнале улучшений как эксперимент, который был опробован и
отброшен, а не скрыт из истории решений.

## Автоматический захват одной интеграционной трассы

После одобрения должен работать один процесс локального Codex CLI — интерфейса
командной строки Codex — с обязательным параметром `--ignore-user-config`, который
исключает наследование пользовательской конфигурации. Одновременно запускать другой
вложенный Codex CLI запрещено.

Codex CLI должен выдавать JSONL — поток JSON-событий по одному на строку — в
стандартный вывод. Этот поток без изменения байтов напрямую передаётся на
стандартный ввод `micro1-safe-trace`, безопасного средства внешнего захвата трассы.
Сырой слой сохраняется только во внешнем каталоге, управляемом
`MICRO1_PRIVATE_TRACE_DIR`; его путь и содержимое не раскрываются и не попадают в
репозиторий.

До запуска единственного вложенного процесса транспорт проверяется на соответствие
политике репозитория. Такая проверка сама не запускает Codex CLI. Запрещены сырой
промежуточный файл в репозитории, `tee`, переменная оболочки с полным потоком,
ручное копирование, отредактированный запасной экспорт и смешивание диагностического
потока ошибок с JSONL. Если безопасный транспорт или единственность процесса нельзя
доказать, работа останавливается и запрашивается решение владельца.

## Обязательная проверка будущей интеграции

После разрешённых изменений нужно проверить весь объединённый набор D01—D04, а не
только новые файлы:

- компиляцию всех четырёх фикстур;
- все видимые гейты D01—D04;
- самопроверки независимого оценщика для D01—D04;
- физическую изоляцию скрытого эталона и границы полномочий обоих сравниваемых
  вариантов для каждого кандидата;
- неизменность байтов кандидатов, их SHA-256, исходных и итоговых идентификаторов;
- манифесты фикстур, видимых пакетов, скрытых пакетов, кандидатов и вертикальных
  срезов, включая их хеши;
- точное взаимно-однозначное соответствие descriptor, фикстур, задач, ролей,
  профилей полномочий, доказательств процессов и слотов запуска;
- ровно четыре development fixture — фикстуры разработки — с идентификаторами
  BG-D01, BG-D02, BG-D03 и BG-D04;
- детерминированное автономное воспроизведение и регрессионные проверки между
  фикстурами;
- полное смысловое соответствие английской и русской документации, причём все
  предназначенные владельцу материалы должны естественно объяснять результаты,
  доказательства, сбои, повторы, риски и объём одобрения без чтения английской
  версии;
- охват и значения контрольных сумм;
- обе безопасные предварительные проверки: контрольную и реализационную.

Код, JSDoc — структурированные комментарии к публичным контрактам — и обычные
комментарии остаются на английском языке. Все отчёты для владельца должны быть
полностью и естественно написаны по-русски. Английскими могут оставаться точные
команды, пути, идентификаторы, имена библиотек и официальные названия; неизбежный
английский термин объясняется по-русски при первом употреблении.

## Ошибки, допустимое согласование и немедленная остановка

При первом настоящем неожиданном сбое работа останавливается. Нельзя самовольно
повторять команду, расширять область изменений или подменять источник. Единственное
заранее разрешённое исключение — детерминированное ожидаемое согласование манифестов
и контрольных сумм, если их изменение вызвано только применением трёх разрешённых
fixture commit и не затрагивает замороженную семантику. Такое согласование остаётся
внутри той же трассы и должно быть явно записано.

Немедленная остановка также обязательна при внешнем пути, приватном контексте,
неясном происхождении или лицензии, сходстве с запомненной приватной структурой,
утечке скрытого эталона, нарушении `K=0` — отсутствия обратной связи от оценщика до
неизменяемого решения — либо загрязнении, которое нельзя устранить независимым
пересозданием.

## Что останется запрещено даже после одобрения

- Изменение замороженного поведения, эталонов, кандидатов, видимых утверждений,
  методики, целей, знаменателей, зависимостей, политики выполнения или изоляции
  `K=0`.
- Любая held-out fixture — отложенная фикстура — раскрытие отложенной части,
  официальный или оцениваемый запуск, демонстрация ремонта и публикация результата.
- Claude, Chromium, живая продуктовая модель, браузер и его приватное состояние,
  подключённые приложения, приватный MCP — протокол подключения инструментов к
  модели — глобальная память, соседние репозитории и внешние рабочие области.
- Commit, push, публикация, сборка отправляемого архива или отправка результата.

Успешная локальная проверка не делает новую трассу допустимой автоматически. После
захвата потребуются автоматическое сканирование, полная техническая проверка,
естественная русская карточка результата и отдельное одобрение владельца перед
включением трассы в допустимые доказательства.

## Риски и ограничения текущего гейта

- Сведения о допустимых репрезентативных захватах D02/D03 и происхождении D04
  предоставлены владельцем и сейчас не перепроверялись.
- Содержимое трёх fixture commit не читалось; возможные интеграционные конфликты и
  необходимость ожидаемого согласования пока не являются известными результатами.
- Компиляция, тесты, самопроверки оценщика, воспроизведение, контрольные суммы и обе
  предварительные проверки не запускались, поэтому эта карточка не заявляет их
  успешность.
- Внешние значения и пути `MICRO1_PRIVATE_DENYLIST` и
  `MICRO1_PRIVATE_TRACE_DIR` не читались и не раскрывались.
- Эта текущая управляющая беседа не становится доказательством реализации; новая
  допустимая траектория может начаться только после точного одобрения границы.

## Точный объём одобрения

Если граница верна, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-009 и разрешаю одну допустимую интеграционную траекторию TRC-BG-D02-D04-INTEGRATION-001: выполнить только cherry-pick трёх commit фикстур c52bacc0b1d9497990b58bd73d5afdbca95035d5, fd96a3ffefaaac9de4cabe098632ecc4876a67be и 140041f82971f73eca17e2735050ecb0ec8de77d, разрешать только интеграционные конфликты, сохранить TypeScript-семейство ParcelDispatchBoard для BG-D02 и выполнить автоматический однопроцессный захват Codex JSONL → micro1-safe-trace с --ignore-user-config без одновременных вложенных CLI; замороженное поведение, скрытые эталоны, варианты решения, видимые утверждения, методику оценивания, цели, зависимости и политику выполнения не менять, официальный или оцениваемый запуск, Claude, Chromium, живую модель продукта, браузер, приватный MCP, commit и push не выполнять.

Эта фраза не разрешает ничего за пределами перечисленной fixture-only интеграции,
связанных интеграционных тестов, служебного согласования и полной неоцениваемой
проверки D01—D04. Она не разрешает commit, push, официальный или оцениваемый запуск
либо включение новой трассы в допустимые доказательства без отдельной проверки и
отдельного решения владельца.
