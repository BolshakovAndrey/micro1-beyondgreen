# Контрольная карточка границы реализации BG-H01—BG-H06

**Граница:** `SES-20260830-011`  
**Исходный HEAD:** `f2afbdb2cde3f4677c351093baccfcb5a202c7d5`  
**Ветка:** `codex/beyondgreen-d01-scale003-correct`  
**Статус заморозки:** `OWNER_APPROVED_PROSE_AND_PUBLIC_PROVENANCE_FREEZE`  
**Состояние:** точная фраза получена `2026-08-30T17:16:22Z`; реализация ещё не начата

## Что проверено на этом гейте

Гейт — обязательная точка остановки перед изменениями — подготовлен в сохранённой
основной рабочей копии BeyondGreen. Текущий Git HEAD, то есть точный исходный снимок
репозитория, совпадает с
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`; активна ветка
`codex/beyondgreen-d01-scale003-correct`. Рабочая копия намеренно не чистая: в ней
находится незакоммиченный пакет уже одобренной владельцем заморозки текстов поведения
и публичного происхождения H01—H06. Его утверждённый статус
`OWNER_APPROVED_PROSE_AND_PUBLIC_PROVENANCE_FREEZE` зафиксирован в
`artifacts/trajectories/reviews/BG-HELDOUT-PROSE-FREEZE-CHECKPOINT_RU.md`.

Перед подготовкой границы полностью прочитаны `AGENTS.md`, официальный контракт
задачи, правила хакатона, чистой комнаты, траекторий и предварительной проверки, а
также нормативная спецификация продукта `docs/PROJECT_SPEC.md`, её проекция
оценивания `docs/EVALUATION.md` и `config/topic.yaml`. Домашняя глобальная память,
соседние репозитории, приватные рабочие области, скрытые результаты и содержимое
скрытых эталонов не читались.

На этом ходе созданы только
`artifacts/trajectories/session-boundaries/SES-20260830-011.yaml` и эта карточка.
Структурно проверяться будут только эти два служебных файла. Реализация, кандидаты,
скрытые эталоны, тесты, commit, worktree, подагенты, компиляция, запуск тестов,
захват траектории и предварительные проверки до точного одобрения запрещены.

## Что разрешит единое одобрение владельца

Одно точное одобрение разрешит всю заранее ограниченную последовательность без
дополнительных промежуточных запросов для обычных детерминированных исправлений
внутри конкретной пары фикстур.

Сначала координатор создаст ровно один checkpoint commit — контрольный снимок Git —
текущего одобренного пакета заморозки, без отправки в удалённый репозиторий, с точным
сообщением:

```text
docs(Evaluation):[BeyondGreen] Freeze held-out behavior specifications
```

От этого точного commit будут созданы три изолированных worktree — отдельные рабочие
деревья Git — и параллельно запущены три нативных подагента Codex:

- BG-H01/BG-H02: сброс от входных свойств и асинхронный порядок;
- BG-H03/BG-H04: стабильность идентичности и условный жизненный цикл;
- BG-H05/BG-H06: внешнее хранилище и откат.

Каждый подагент владеет только путями своей пары. Он не может изменять файлы другой
пары или общие реестры и средства интеграции.

## Обязательный состав каждой пары

Подагент обязан точно перенести уже утверждённое поведение в исполняемую
синтетическую фикстуру. Для каждой фикстуры нужны:

- доступные обеим сравниваемым сторонам контракты, средство запуска, поверхность
  отображения и видимые тесты;
- ровно два нейтрально названных варианта решения: сохраняющий поведение и намеренно
  ложноположительный, который проходит видимую проверку, но нарушает замороженный
  скрытый инвариант;
- соответствие двух вариантов правильному и дефектному исходу только в пакете
  независимого оценщика;
- скрытые от сравниваемых сторон эталонная истина, канонический сценарий действий,
  самопроверка оценщика и проверки запрета доступа;
- неизменяемые манифесты и SHA-256, то есть криптографические контрольные суммы,
  происхождение и подтверждение чистой комнаты;
- точные выполненные команды, их результаты и полный естественный русский отчёт.

Публичные функции, экспортируемые типы и важные TypeScript-модули должны иметь
краткий английский JSDoc — структурированный комментарий к программному контракту.
Для границ безопасности, изоляции, скрытого эталона и неочевидных инвариантов нужны
короткие английские комментарии, объясняющие причину решения, а не очевидный
синтаксис.

## Владение общими файлами

Подагентам запрещено менять общий реестр фикстур, общий реестр задач, корневые
сценарии `package.json`, общий индекс контрольных сумм и межфикстурные интеграционные
тесты. Если для пары требуется общее изменение, подагент только записывает запрос в
русском отчёте и передаёт его координатору. Это не разрешает самостоятельно
расширять область файлов.

## Локальная проверка и три fixture-only commit

Fixture-only означает, что изменения относятся только к конкретной паре фикстур.
Подагентам разрешены компиляция их поверхности, видимые тесты, самопроверка
оценщика, проверка запрета доступа, физической изоляции, манифестов и хешей. Общий
официальный или оцениваемый запуск и раскрытие отложенных результатов запрещены.

После полностью зелёной локальной проверки каждая пара может создать не более
одного checkpoint commit без push, строго с соответствующим сообщением:

```text
feat(Evaluation):[BeyondGreen] Add prop reset and async ordering fixtures
feat(Evaluation):[BeyondGreen] Add identity and lifecycle fixtures
feat(Evaluation):[BeyondGreen] Add external store and rollback fixtures
```

Внутренние необработанные события нативных подагентов нельзя представлять как
экспортированные траектории. Вместо ложного заявления каждый подагент сохраняет
точные команды, результаты, сбои, исправления и полный русский отчёт. Существующие
допустимые траектории кодирования D01 и будущая автоматически захваченная
интеграционная траектория координатора считаются репрезентативным покрытием работы
агентов по ранее утверждённой политике.

## Отдельная интеграционная траектория координатора

После трёх разрешённых commit координатор в отдельной автоматически захваченной
траектории объединит только эти три снимка. Только координатор сможет внести
необходимые общие изменения: подключить фикстуры к реестрам и задачам, добавить
межфикстурные тесты и согласовать общий индекс контрольных сумм.

Координатор обязан проверить:

- полную компиляцию и обычные с интеграционными тестами;
- все манифесты, неизменяемые байты и хеши;
- физическую изоляцию видимых пакетов от скрытого эталона и запреты доступа;
- `K=0`, то есть отсутствие любой обратной связи независимого оценщика до
  неизменяемого решения сравниваемой стороны;
- охват и значения контрольных сумм;
- обе разрешённые безопасные предварительные проверки.

После этого координатор обязан остановиться для приёмки владельцем. Интеграционный
commit не разрешён текущим гейтом и потребует отдельного последующего одобрения.

## Исправления без промежуточных одобрений и условия остановки

Внутри уже утверждённого пути конкретной пары не требуют нового решения владельца
обычные детерминированные исправления ошибок компиляции, тестов, хешей, путей и
JSDoc, если они не меняют замороженное поведение, методику, скрытую границу или
область владения.

Работа немедленно останавливается при загрязнении приватным материалом, смысловом
отклонении от заморозки, утечке скрытого эталона, потере физической изоляции,
неясном происхождении или лицензии, внешнем или приватном пути, а также при
необходимости изменить общую методику. Такое событие нельзя исправлять молча или
считать обычной технической поправкой.

## Что остаётся запрещено

Даже после одобрения запрещены официальный или оцениваемый запуск, раскрытие
отложенных результатов, живая продуктовая модель, Claude, Chromium, браузер,
приватный MCP — протокол подключения инструментов к модели, — приватные данные,
push, публикация, упаковка и отправка результата.

Разрешены только четыре точно перечисленных checkpoint commit: один для пакета
заморозки и по одному для каждой пары. Никакой другой commit, включая
интеграционный, текущая фраза не разрешает.

## Риски и пределы текущей карточки

- Содержимое реализации, кандидатов и скрытого эталона ещё не существует и не
  проверялось.
- Компиляция, тесты, самопроверки, изоляция, манифесты, контрольные суммы и
  предварительные проверки на этом гейте не запускались; успешность не заявляется.
- Незакоммиченный пакет заморозки уже одобрен владельцем, но его checkpoint commit
  появится только после точного одобрения этой границы.
- Параллельная работа разделяет владение файлами, но итоговая совместимость станет
  известна только в отдельной интеграционной траектории.
- Эта управляющая беседа сама не является доказательством реализации; допустимость
  будущей траектории зависит от утверждения данной границы и последующей проверки.

## Точная фраза одобрения

Если граница верна, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-011 и единый гейт реализации H01–H06: сначала создать без push ровно один checkpoint commit текущего owner-approved freeze bundle с сообщением docs(Evaluation):[BeyondGreen] Freeze held-out behavior specifications, затем от этого точного commit создать три изолированных worktree и запустить три параллельных нативных подагента Codex для пар BG-H01/BG-H02, BG-H03/BG-H04 и BG-H05/BG-H06; каждому подагенту разрешаю менять только пути своей пары и строго реализовать утверждённую заморозку поведения, включая видимые обеим сравниваемым сторонам контракты, harness, render, visible tests, ровно два нейтрально названных кандидата — сохраняющий поведение и намеренно ложноположительный, причём их соответствие истине доступно только независимому оценщику, — а также скрытые от сравниваемых сторон ground truth, canonical driver, self-check, denied-access probes, неизменяемые manifests/hashes, происхождение, точные команды и результаты и полный русский отчёт; публичные и важные TypeScript API документировать кратким английским JSDoc, ключевые границы и инварианты — короткими английскими why-comments; shared registry, tasks, package scripts, checksum index и integration tests работникам не менять, а только сообщать координатору о необходимом общем изменении; работникам разрешены compile и fixture-specific tests/self-check/isolation, но запрещены общий official/scored run и unblinding; после локальной зелени разрешаю по одному fixture-only checkpoint commit без push с точными сообщениями feat(Evaluation):[BeyondGreen] Add prop reset and async ordering fixtures, feat(Evaluation):[BeyondGreen] Add identity and lifecycle fixtures и feat(Evaluation):[BeyondGreen] Add external store and rollback fixtures; внутренние raw events подагентов не заявлять как экспортированные traces, сохранить точные команды, результаты и русские отчёты, после чего отдельной автоматически захваченной coordinator integration trajectory объединить только три разрешённых commit, изменить только необходимые shared paths, проверить compile, tests, manifests, физическую изоляцию, K=0, checksums и обе preflight-проверки и остановиться для owner acceptance, считая существующие допустимые D01 coding traces вместе с coordinator integration trajectory достаточным representative trace coverage; детерминированные fixture-specific исправления compile/test/hash/path/JSDoc внутри утверждённого объёма не требуют промежуточного одобрения, но contamination, semantic drift, oracle leakage, неясное происхождение или лицензия и изменение shared methodology требуют немедленной остановки; official/scored run, unblinding, live product model, Claude, Chromium, browser/private MCP, private data и push запрещены, commit разрешены только для четырёх перечисленных checkpoint, а integration commit требует отдельного последующего одобрения владельца.

Эта фраза разрешает только описанную fixture-specific реализацию, четыре точных
checkpoint commit и отдельную автоматически захваченную интеграционную проверку с
обязательной остановкой. Она не разрешает интеграционный commit, push, официальный
или оцениваемый запуск, раскрытие отложенных результатов либо публикацию.
