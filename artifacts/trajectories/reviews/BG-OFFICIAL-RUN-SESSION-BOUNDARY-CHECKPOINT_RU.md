# Карточка одобрения единственного официального запуска BeyondGreen

> **Статус: BLOCKED_BEFORE_OWNER_APPROVAL. Фраза одобрения ниже отозвана и не разрешает запуск.**

Граница: `SES-20260830-019`<br>
Ветка: `codex/beyondgreen-official-runner`<br>
HEAD: `3d916e73a13553583660bc88633366f5bc8eede9`

Текущий производный статус: `SYNTHETIC_CONTRACT_PASSED_PRODUCTION_COMPOSITION_BLOCKED`. Отдельная синтетическая коррекция `SES-20260831-020` доказала полный coordinator path, но production role hooks отсутствуют. Эта карточка остаётся заблокированной и не разрешает официальный запуск или раскрытие результатов.

## Что подготовлено сейчас

Создана только граница будущего официального оцениваемого запуска и эта русская карточка. До отдельного явного одобрения не импортировался и не исполнялся ни один кандидат, не запускались status quo или BeyondGreen, observer — наблюдатель после решения, evaluator — независимый оценщик, live model — реальная модель, и не проводилось unblinding — однократное раскрытие held-out результатов после фиксации решений.

На старте подтверждены точная ветка, точный HEAD и чистое рабочее дерево. Полностью перечитаны `AGENTS.md`, challenge/rules/clean-room/trace/preflight контракты, нормативный `docs/PROJECT_SPEC.md@1.1.0`, `docs/EVALUATION.md` и `config/topic.yaml`. Внешняя память, браузер, MCP, связанные приложения, Claude, Chromium, другие рабочие каталоги и приватные ресурсы не использовались.

## Инцидент границы SES-019

После завершения boundary-turn было обнаружено, что задача вне разрешённого scope повторно выполнила существующий unscored D01 demo — неоцениваемую демонстрацию development-сценария BG-D01. Были перегенерированы только `artifacts/evaluation/BG-D01-VERTICAL-SLICE.json` и `.html`. Held-out candidates, live model и official/scored run не запускались; held-out unblinding не выполнялся.

Точная команда не была экспортирована UI задачи, но содержимое и время записи файлов однозначно соответствовали существующему D01 demo path. По явному разрешению владельца ровно два файла восстановлены из checkpoint `3d916e73a13553583660bc88633366f5bc8eede9`; другие файлы не откатывались. Инцидент не является official evidence, а `SES-019` остаётся заблокированной и submission-ineligible.

## Что именно разрешит требуемая фраза

Одобрение разрешит ровно один полный official/scored запуск — официальный оцениваемый проход — `RUN-BG-OFFICIAL-EVAL-V1.1.0-001` для неизменной evaluation v1.1:

- 10 замороженных fixtures — тестовых сценариев;
- 20 неизменяемых candidates — по два кандидата на сценарий;
- две arms — сравниваемые ветви `status-quo` и `beyondgreen`;
- 20 slots — пар «сценарий + кандидат»;
- 40 arm plans — по одному плану каждой ветви на каждый slot;
- один официальный attempt — попытка — на каждую ветвь и кандидата;
- одно раскрытие held-out результатов только после фиксации всех 40 решений.

Перед первым исполнением обязаны пройти статическая проверка manifests и SHA-256, проверка точной численности и членства, проверка физических прав доступа и создание рекурсивно замороженного 20-slot execution plan — плана исполнения. Если существующая repository-local команда всё ещё является только проверкой контракта, отсутствует, неоднозначна или требует изменения кода, запуск не начинается.

## Неизменяемые правила сравнения

Обе ветви получают одни и те же байты кандидата, visible inputs — открытые входы, команды компиляции и открытых тестов, окружение, лимиты и систему записи доказательств. Методика, manifests, visible assertions — открытые проверки, oracle semantics — скрытая эталонная семантика, ground truth — эталонные ответы, цели, формулы, знаменатели и сопоставление reason-correct не изменяются.

`status-quo` принимает кандидата только при успешной компиляции и всех открытых тестах. Доказанный детерминированный сбой компиляции или открытого теста даёт предусмотренный frozen verdict — замороженный вердикт `reject`. Операционная неопределённость даёт `abstain`.

BeyondGreen использует только замороженные открытые контракты, анализ рисков и oracle-free проверки — проверки без доступа к скрытому эталону. Полное корректное доказательство даёт `accept`, воспроизводимый блокирующий дефект — `reject`, а timeout, crash, nondeterminism, неоднозначность, недостающие доказательства, отказ обязательной проверки, запрет доступа или сбой транспорта — `abstain`.

Ни один увиденный результат нельзя исправлять, удалять или переинтерпретировать. Запрещены второй запуск кандидата, повтор модели, замена модели, ремонт кандидата, изменение знаменателя, цели, reason mapping или evaluation v1.1.

## Замороженный транспорт модели

Разрешён только `codex-exec-jsonl-v1` с моделью `gpt-5.6-sol` и точной командой:

```text
codex exec --ephemeral --ignore-user-config --json --output-schema <schema> --sandbox read-only --model gpt-5.6-sol
```

Для каждого BeyondGreen-кандидата допускается один вызов и ноль повторов: максимум 20 вызовов на весь официальный проход. Подмена модели запрещена. Общий предел на кандидата — 180 секунд: 165 секунд движку и 15 секунд на финализацию. Недоступность модели, транспортный сбой или rate limit — ограничение частоты — приводят к `abstain`, а не к повтору. Стоимость в долларах не вычисляется и не оценивается; токены сохраняются только при явном и стабильном сообщении CLI.

## Физическая граница скрытого эталона

Для каждого slot сначала становятся неизменяемыми решения и обоснования обеих ветвей. Только затем observer может зафиксировать нейтральные наблюдения, а evaluator применить скрытый эталон. Arms не могут читать `evaluation/verifier-only/`; evaluator не может импортировать, монтировать или читать candidate roots — корни кандидатов. Observer не получает возможности evaluator.

`K=0` остаётся абсолютным: до финализации решения ни одна ветвь не получает вердикт оценщика, категорию ошибки, ожидаемое значение, диагностическое действие, diff, oracle detail или ground truth. Held-out unblinding выполняется ровно один раз и лишь после неизменяемости всех 40 решений. Несовпадение версии или hash evaluator делает запуск недействительным на уровне harness и не меняет уже зафиксированный arm verdict.

## Куда разрешена запись

Все официальные данные сохраняются только внутри:

`artifacts/evaluation/official/RUN-BG-OFFICIAL-EVAL-V1.1.0-001/`

Заранее перечислены файлы `static-preflight.json`, `execution-plan.json`, `run-manifest.json`, `aggregate.json`, `report.json`, `report.html`, `offline-replay.jsonl`, `provenance.json`, `unblinding.json`, а также ограниченные каталоги `arm-records/`, `observer-records/` и `evaluator-records/`. Итоговая русская карточка может быть записана только в `artifacts/trajectories/reviews/BG-OFFICIAL-RUN-RESULT-OWNER-PACKET_RU.md`.

Отчёт JSON проходит схему, HTML строится только из проверенных агрегатов, а offline replay — автономное повторное воспроизведение доказательств — обязан дать тот же digest. Сохраняются все успехи, ошибки, `abstain`, timeout, resource observations, hashes и отрицательные результаты без сокрытия.

## Риски и стоп-условия

Синтетическая коррекция закрыла второй capture-блокер: полный тестовый проход создал 80 записей, связал их в 40 совпадающих пар и подтвердил остановку до evaluator при divergence, missing или malformed capture. Offline replay совпал, а output collision остановился до arm execution.

Остаётся один фундаментальный блокер. Команда `npm run task -- evaluation:run --evaluation-version eval-v1.1.0` намеренно оставлена pre-unblinding contract validator, потому что adapters D02–D04 и H01–H06 пока являются только статическими capability plans с `candidateExecutionAllowed=false`. Production composition root для arm, observer, evaluator и безопасной передачи скрытого сценария после неизменяемых решений отсутствует. Подключение незавершённого CLI создало бы ложный gate и риск потратить единственную официальную попытку.

Нужна отдельная bounded production-composition session. Она должна реализовать физически изолированные role hooks, не менять candidates/oracles и сохранить доказанный coordinator неизменным. После её synthetic и boundary tests потребуется новый checkpoint HEAD и новая official-run boundary.

Одобрение не гарантирует достижение целей `>=16/20`, `>=6/20`, `>=8/10`, `<=2/10` и `>=18/20`. Оно требует честно сохранить фактические результаты, включая промахи по целям и транспортные отказы.

## Что одобрение не разрешает

Даже после одобрения запрещены изменения product code, tests, dependencies, task registry, candidates, manifests, visible assertions, verifier/oracle, методики, целей и политик. Не разрешаются D01 repair demo, browser, MCP, Claude, Chromium, внешняя или глобальная память, raw trace capture, trace promotion, checksums write, packaging, публикация, commit и push. После проверки доказательств работа останавливается для отдельного решения владельца.

Ошибок или повторов в этой boundary-only стадии не было. Редакций приватных данных не выполнялось, потому что приватные данные не читались и в два файла не переносились.

## Точная фраза одобрения

Фраза одобрения не выдана. `SES-20260830-019` заблокирована до реализации production role hooks и нового checkpoint HEAD.
