# BeyondGreen BG-D03 — карточка одобрения границы сессии фикстуры

## Решение, которое требуется сейчас

Нужно одобрить или отклонить только границу `SES-20260830-007` для одной будущей
синтетической фикстуры BG-D03 класса `derived_state` — производного состояния. До
одобрения созданы только два служебных файла: эта карточка и запись границы сессии.
Ни текст фикстуры, ни код React/Signals, ни варианты решения, скрытый эталон,
тесты, манифесты, результаты оценки, трассы или пакет подачи не создавались.

## Проверенное исходное состояние

- Работа ведётся только в изолированном чистом рабочем дереве на commit
  `18510e26fd38e890c0370481ccce114f90cf4ca6`. Указатель Git отсоединён от ветки;
  это исключает неявную запись в общую ветку.
- На момент границы `git status --short --untracked-files=all` не показал ни
  отслеживаемых, ни неотслеживаемых изменений.
- Прочитаны `AGENTS.md`, все обязательные контракты из него, шаблон
  `templates/SESSION_BOUNDARY.yaml`, а также актуальные проекции продукта,
  оценки и темы. Продуктовый исходный код, содержимое иных фикстур, варианты
  решения и скрытые эталоны не читались.
- Приватные рабочие области, браузер, подключённые приложения и MCP не
  использовались. Прочитан только обязательный устаревший указатель состояния
  сессии и общие правила; в них не было продуктового исходного материала.
- Commit, push, Claude, Chromium, живая модель, официальный или оцениваемый запуск
  запрещены. Никакие проверки сборки, тесты, сканер, захват трассы или оценка не
  запускались.

## Что разрешается после одобрения

Ровно одна траектория `TRC-BG-D03-001` может независимо написать синтетическую
фикстуру миграции React в Signals для BG-D03. Она должна выявлять устаревшее,
дублированное или неверно пересчитанное производное состояние. Обязательны два
неизменяемых варианта: сохраняющий правильное поведение и намеренно ложнозелёный,
который проходит видимые проверки, но нарушает замороженное поведение.

Допускаются только следующие пути:

- `evaluation/behavior-specs/BG-D03*`;
- `evaluation/arm-visible/BG-D03/**`;
- `evaluation/verifier-only/BG-D03/**`;
- `evaluation/manifests/BG-D03/**`;
- `candidates/BG-D03/**`;
- тесты и сценарии, в имени которых есть `BG-D03`.

Публичные и экспортируемые TypeScript API, а также важные модули получают краткий
английский JSDoc. Пояснения ключевых инвариантов пишутся на английском и объясняют
причину ограничения, а не пересказывают синтаксис.

## Что остаётся строго запрещённым

Нельзя менять `package.json`, `package-lock.json`, `tsconfig.json`, общий движок
`src/d01`, общие реестры, `config/topic.yaml`, `docs/PROJECT_SPEC*`,
`docs/EVALUATION.md`, `docs/IMPROVEMENT_CHANGELOG.md`, индексы provenance и трасс,
контрольные суммы, а также файлы BG-D01 и всех других фикстур. Если для BG-D03
понадобится общее подключение, оно фиксируется в отчёте владельцу, но не меняется.

Также запрещены любой официальный или оцениваемый запуск, Claude, Chromium, живая
модель, браузер, MCP, доступ к приватному состоянию, commit, push и публикация.

## План автоматического захвата трассы

После одобрения локальный Codex CLI создаёт машиночитаемый JSONL — последовательность
JSON-событий по одной строке. Его неизменённый стандартный вывод передаётся прямо в
`micro1-safe-trace`, который сохраняет сырой слой вне репозитория. Ручное копирование,
`Copy as Markdown`, `tee`, промежуточный сырой файл в репозитории и вывод сырого
содержимого или внешнего пути запрещены. Стандартный поток ошибок идёт отдельно по
безопасному пути. При первой ошибке транспорта работа прекращается без запасного
ручного способа.

## План проверки и условия остановки

После авторства будут последовательно проверены разрешённость всех изменённых путей,
замороженная спецификация и происхождение, неизменяемость вариантов, физическое
разделение видимого пакета и скрытого эталона, затем только разрешённые тесты и
сценарии с `BG-D03` в имени. После автоматического захвата возможны безопасное
сканирование и ревью трассы. Отдельно проверяются JSDoc, комментарии об инвариантах,
отсутствие общих правок и отсутствие утечки скрытого эталона.

При первом настоящем сбое, необходимости повторить команду, расширить объём,
поправить общий код, обнаружить внешний путь, приватный контекст, неясное
происхождение, сходство с приватной структурой или утечку скрытого эталона работа
останавливается и запрашивается новое решение владельца. Успешная проверка сама по
себе не разрешает официальный запуск, commit, push или публикацию.

## Точная фраза одобрения

Если граница верна, ответьте без изменений:

> Одобряю SESSION_BOUNDARY SES-20260830-007 и разрешаю одну eligible fixture-authoring траекторию TRC-BG-D03-001 строго в указанном BG-D03 scope для derived_state; automatic JSONL → micro1-safe-trace capture обязателен, при первом сбое остановиться; shared integration changes только доложить, official/scored run, Claude, Chromium, live model, browser/MCP/private state, commit и push запрещены.

Эта фраза разрешает только описанное авторство BG-D03, автоматический захват и
ограниченные проверки. Она не разрешает ни одну общую правку или запрещённую операцию.
