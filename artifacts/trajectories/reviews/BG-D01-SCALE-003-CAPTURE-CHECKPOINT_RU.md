# Карточка владельца: подготовка захвата TRC-BG-D01-SCALE-003

## Текущее решение после захвата и проверки

Ограниченная реализация `TRC-BG-D01-SCALE-003`, разрешённая границей
`SES-20260830-003`, завершена, прошла локальные проверки, штатно захвачена и
технически проверена. Внешняя квитанция подтверждает 49 485 байт и SHA-256
`0fe4e5b8be4ffff71b849bda538e4ecc6d2d44b94a9dcfdf423b1209855d4400`.
Совпадение буфера и строгий UTF-8 подтверждены. Raw scan не нашёл приватных
терминов, email или присваиваний секретов; два абсолютных машинных пути не перенесены
в submission-слой. Владелец одобрил включение трассы в `actual_traces`.
`SCALE_READY=false`.

## Что именно изменено

1. Настоящий оркестратор, рабочие процессы групп, наблюдателя и оценщика, повторное
   воспроизведение и отчёт проведены через один общий путь исполнения с внедряемыми
   `FixtureEngineBindings` и описателем. D01 остался корнем композиции только из
   данных. Схема `BoardObservation` вынесена из общего слоя и явно передаётся D01.
   Описатель, существующий только в тестах, проходит настоящий конвейер и доказывает
   вызов внедрённых схем, проверок, рассуждения, оценщика, повторного воспроизведения
   и отчёта.
2. Доказательства возможностей запуска стали опровержимыми: проверяется связь
   запуска с рабочим процессом, канонические хеши песочницы и правил путей, точное
   взаимно-однозначное соответствие роли, профиля, слота исполнения и
   доказательства, автомат состояний событий каждой группы и связь событий оценщика
   с неизменяемым хешем решения соответствующей группы. Автономное повторное
   воспроизведение независимо проверяет `reasonCorrectReject` и
   `oracleAcceptedCandidate`.
3. Рекурсивное сравнение путей пакета корректно обрабатывает разрешённые вложенные
   структуры. Прямой отрицательный тест изменяет байты уже зарегистрированного
   вложенного исходного файла и подтверждает закрытый отказ.
4. Согласованы только разрешённые проекции текущего состояния и добавлены новые
   записи этой итерации. Исторические записи не переписывались.

Пять видимых утверждений, байты кандидатов, спецификация поведения, оракулы,
зафиксированная методика оценки, цели и знаменатели не менялись. BG-D02 и другие
фикстуры не создавались.

## Проверки и доказательства

- `npm run compile` — успешно.
- `npm test` — успешно, 55 из 55 тестов, `TASK_PASSED test:all`.
- `npm run task -- d01:verify` — успешно: видимые проверки 5 из 5, проверка шага 1
  из 1, оракул 3 из 3, физическая граница 4 из 4, вертикальный набор 19 из 19,
  неоцениваемая сквозная проверка и все неизменяемые манифесты.
- Текущий вертикальный манифест содержит 30 файлов и имеет SHA-256
  `d6e199439b41b0edd5c842bcdcf7f8c386863b37cf0e00f737abfef62c3eae5b`.
- SHA-256 JSON сквозной проверки:
  `e68895a8ae55374173ab4dc7462bdffcf820abaa082f01efb0cb2c32cb9a7e70`.
- SHA-256 статического HTML:
  `458e09c1589c013a5753c17e2a215b76b9a2b8aabb6e220bef39acd1a4e014ff`.

## Сбои, остановки и разрешённые повторы

Первый запуск компиляции остановился с `TS2688`: локально отсутствовал пакет типов
Node. После отдельного разрешения владельца команда `npm ci` установила 54 пакета
строго из неизменённого `package-lock.json`, проверила 55 пакетов и не нашла
уязвимостей. Разрешённый повтор компиляции выявил ровно пять ошибок: три ошибки типа
`executionSlot` и две ошибки циклического вывода типа `D01_ENGINE`. Владелец отдельно
разрешил исправить только их и один раз повторить компиляцию; повтор прошёл.

Первый вывод `npm test` оказался неполным и не сохранил итоговый код завершения.
Повтор без изменения исходников дал достоверный результат 51 из 55: четыре D01-теста
остановились с `Candidate observation evidence is invalid`. Безопасная диагностика
показала несовпадение профиля запуска и рабочего процесса: наблюдатель всегда брал
первую группу описателя вместо группы неизменяемого решения. Владелец отдельно
разрешил исправить только эту причину и выполнить по одному запуску компиляции и
тестов. Оба запуска прошли. После окончательной правки текущий 30-файловый манифест
был механически пересчитан до целевой проверки.

## Остаточные риски и границы решения

- Скрытые рассуждения, внутренние события Codex Desktop и полный вывод оболочки не
  входят в `Copy as Markdown` и не заявляются.
- Два абсолютных машинных пути найдены только во внешнем исходном захвате и
  категориально исключены из submission-слоя; их значения не раскрываются.
- Успешные локальные тесты не заменяют отдельную приёмочную матрицу Codex.
- Решение о `SCALE_READY` запрещено принимать автоматически; оно требует отдельной
  координационной проверки и явного решения владельца после захвата и проверки.
- Официальные или оцениваемые запуски, живая модель, Claude, Chromium, commit и push
  не выполнялись.

## Решение владельца

После получения полной русской версии владелец выбрал рекомендованное одобрение в
явном гейте. Это разрешило только продвижение проверенной трассы. Оно не изменяет
`SCALE_READY` и не разрешает масштабирование, официальный или оцениваемый запуск,
живую модель, Claude, Chromium, BG-D02, изменение методики или замороженных входов,
commit и push.
