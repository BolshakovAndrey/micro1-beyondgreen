# Остановка исправления документального паритета BG-D02—BG-D04

**Продолжение:** `TRC-BG-D02-D04-INTEGRATION-001-CONT-012`  
**Исходная траектория:** `TRC-BG-D02-D04-INTEGRATION-001`  
**Граница:** `SES-20260830-009`  
**Статус при создании карточки:** остановлено на первом новом неожиданном отказе;
ожидает захвата и проверки трассы, а также решения владельца

## Разрешённое изменение

В разделе 15 `docs/PROJECT_SPEC.md` и в симметричном разделе 15
`docs/PROJECT_SPEC_RU.md` добавлена одна и та же точная строка:

```text
npm run task -- development:verify
```

В каждом файле строка расположена непосредственно после
`npm run task -- d01:verify`. До записи проверено, что точка вставки единственна, а
новая строка отсутствует. После записи в каждом файле найден ровно один новый маркер
и ровно одна требуемая соседняя пара. Дополнительная побайтовая проверка доказала:
если удалить только новый маркер, каждый файл точно возвращается к исходным байтам.
Иных изменений существовавших строк в рамках этого исправления нет.

Кроме двух строк обновлён только производный статус `SES-20260830-009` в индексе
траекторий, создана эта карточка и детерминированно согласована проекция контрольных
сумм. Раздел `actual_traces` не изменялся, автоматического продвижения трасс не было.

## Команды и безопасные доказательства

- Ограниченная проверка английского и русского маркеров: в каждом документе
  `marker=1`, `adjacency=1`, `prior_bytes_restored=true`.
- `git diff --check` — успешно, ошибок пробелов и структуры различий нет.
- `npm run task -- checksums:check` до согласования — ожидаемый
  `CONTROL_CHECKSUMS_MISMATCH`, ранее зафиксированный в `CONT-009`.
- `npm run task -- checksums:write` — контрольная проекция детерминированно
  пересобрана только по текущему репозиторному составу.
- `npm run task -- checksums:check` после согласования — успешно.
- `micro1-safe-preflight control` — новый неожиданный отказ запуска: безопасный
  интерфейс не признал текущий запуск выполненным из разрешённого корня репозитория.
  Абсолютный путь и значения среды не выводились. Диагностика и повтор запрещены
  стоп-политикой.
- `micro1-safe-preflight implementation` — не запускался после первого нового
  неожиданного отказа.

Неизменённые дорогие проверки не запускались повторно. В соответствии с разрешением
переиспользованы доказательства `CONT-009`: `npm test` — 78/78; `d01:verify`,
`d01:replay`, `d02:verify`, `d03:verify`, `d04:verify`, `development:verify` и
`phase0.5:verify` — успешно. После этих проверок продуктовый код, варианты решения,
скрытые эталоны, видимые утверждения, методика оценивания, цели, зависимости,
`package-lock.json` и политика выполнения не менялись.

## Матрица пригодности семейства траекторий

| Траектория | Классификация | Основание |
| --- | --- | --- |
| `TRC-BG-D02-D04-INTEGRATION-001` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована операционная остановка на отсутствующих локальных зависимостях; самостоятельное продвижение не разрешено. |
| `CONT-001` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована операционная остановка физической проверки из-за ограничения sandbox, а не дефекта интеграции. |
| `CONT-002` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована остановка на устаревших метаданных заморозки BG-D04, возникших в исходном commit фикстуры. |
| `CONT-003` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована остановка на трёх находках машинных путей без публикации исходных значений. |
| `CONT-004` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована безопасная остановка категориальной замены без изменения целевого отчёта. |
| `CONT-005` | пригодна для технической проверки и проверки владельцем | Честно зафиксирован транспортный сбой heredoc до запуска преобразования и без изменения данных. |
| `CONT-006` | пригодна для технической проверки и проверки владельцем | Честно зафиксирована безопасная остановка на несовпадении кардинальности без изменения целевого отчёта. |
| `CONT-007` | ожидает проверки трассы | В выводе проверки были зафиксированы машинно-зависимые абсолютные пути. |
| `CONT-008` | пригодна для технической проверки и проверки владельцем | Безопасно выполнены перечисление интервалов и агрегированная диагностика; самостоятельное продвижение не разрешено. |
| `CONT-009` | ожидает проверки трассы | Остановлена на документальном паритете; стандартный стек вывел абсолютный путь машины. |
| `CONT-010` | непригодна | Неизменяемая неудачная попытка; не переиспользовалась и не изменялась. |
| `CONT-011` | непригодна | Неизменяемая неудачная попытка; не переиспользовалась и не изменялась. |
| `CONT-012` | ожидает проверки трассы | Две вставки и checksum-согласование завершены, затем безопасный control-шлюз отказал; захват, проверка и решение владельца отсутствуют. |

## Сохранённые границы и остаточные риски

Зафиксированное поведение, кандидаты, скрытые эталоны, видимые утверждения,
методика оценивания, цели, знаменатели, зависимости, lockfile, политика выполнения,
неизменяемая семантика и `K=0` сохранены. `actual_traces` не изменён. Официальный
или оцениваемый запуск, живая модель продукта, Claude, Chromium, браузер, приватный
MCP, вложенный Codex CLI, commit и push не выполнялись.

Остаются три риска: безопасный control-шлюз не пройден; implementation-шлюз не
запущен; после превращения этой записи в карточку остановки checksum-проекция снова
требует отдельного разрешённого согласования. Кроме того, `CONT-012` ещё не имеет
отдельного неизменяемого захвата, автоматического сканирования и решения владельца.
Поэтому интеграционное семейство не готово к подаче и не должно автоматически
появляться в `actual_traces`.

## Готовность к решению владельца

Репозиторное состояние не готово к одобрению: требуется отдельное решение владельца
о корректном способе запуска безопасных предварительных шлюзов и о последующем
согласовании checksum-проекции. Любое продолжение должно быть новой явно разрешённой
траекторией; `CONT-012` не возобновляется. Такое решение не разрешает официальный
или оцениваемый запуск, публикацию, commit, push или изменение продукта.

## Точка остановки

До отказа были успешно выполнены две документальные вставки, ограниченная проверка
паритета, `git diff --check`, детерминированная запись 329 контрольных сумм и их
проверка. После отказа не выполнялись диагностика, повтор control-шлюза,
implementation-шлюз, повторная checksum-проверка, захват или продвижение трассы.
