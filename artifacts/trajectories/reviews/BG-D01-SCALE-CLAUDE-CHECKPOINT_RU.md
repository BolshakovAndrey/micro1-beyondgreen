# Повторная независимая проверка готовности BG-D01 к масштабированию

**Класс проверки:** независимая проверка полного текущего состояния BG-D01 без
изменения файлов  
**Граница:** `SES-20260830-001`  
**Запуск проверяющего:** проектная безопасная оболочка Claude, официальный псевдоним
модели `opus`, максимальная глубина рассуждения, только операции чтения
`Read`/`Glob`/`Grep`, безопасный режим, без Chrome и без сохранения сессии  
**Ограничение идентификации модели:** оболочка не вывела полное разрешённое имя
модели; доказан только запрошенный официальный псевдоним `opus`  
**Исходный ответ:** хранился во временном исключённом файле лишь до завершения
сверки; 25 138 байт; SHA-256
`535c7d0a6988aa521d9429492717d0fb9e7b4a7e8472af085f0e6b20e9a59b5a`

## Точный вывод

```text
D01_CHECKPOINT=PASS_WITH_CONCERNS
SCALE_READY=no
```

Claude признал полностью закрытыми 10 из 12 запрошенных направлений исправления, а
два — только частично. Локальная корректность остаётся зелёной, однако текущая
реализация ещё не доказывает единый повторно используемый движок для нескольких
сценариев и не связывает физические права процессов с независимо измеренными
доказательствами среды изоляции.

## Независимая сверка Codex

| Замечание | Вывод Codex | Последствие |
| --- | --- | --- |
| Публичный инвариант прямо указывает на заложенный дефект D01 | Это реальное ограничение конструкции эксперимента, но не утечка скрытого эталона при одобренной владельцем фиксации поведения D01: публичный контракт намеренно доступен обоим вариантам, а ложнозелёный кандидат намеренно его нарушает | Владелец сохранил замороженную методику и разрешил явно раскрыть ограничение валидности конструкции |
| Признаки запрета сети, наследования путей и доступа к эталону/кандидату объявляет родитель | Подтверждено по коду и тестам: порядок событий измеряется, но физические права независимо не измеряются | Блокирует готовность к масштабированию |
| `FixtureDescriptor` по-прежнему обслуживает специализированный движок D01 | Подтверждено: схемы, проверки варианта, рассуждение, типы оркестратора, число запусков оценщика и состав manifest остаются специфичны для D01 | Блокирует масштабирование; нужен единый движок, где D01 является данными |
| На момент проверки точная трасса была сверена только по квитанции | Для момента ответа Claude замечание было верно | После ответа закрыто: байты clipboard точно совпали с внешней квитанцией, UTF-8 прошёл строгую проверку, две замены путей выполнены механически, все 729 строк штатного слоя проверены |
| Карта доказательств завышала покрытие отрицательными тестами | Подтверждено: перестановка событий проверяется, а пропуск события и независимо сфальсифицированные физические права — нет | Документация исправлена; тесты реализации ещё нужны |
| Реестры всё ещё называли захват и checkpoint ожидающими | Подтверждено | Закрыто синхронизацией действующих английских и русских проекций и записей трассы |
| Изоляция через macOS `sandbox-exec` не переносится на Linux/среду судьи | Подтверждено | Оставлено для отдельного решения владельца согласно границе; ослабленный режим не добавлялся |
| Manifest видимого пакета проверяет только список, а не полноту каталога | Подтверждено: незарегистрированный видимый файл не обнаруживается перечислением каталога | Блокирует готовность к масштабированию |
| В основном происхождении D01 указан только первоначальный автор | Подтверждено | Автор исправлений `COD-CODEX-008` записан; перед следующим checkpoint нужен итоговый аудит происхождения |
| Формулировка области компиляции неточна | Подтверждено частично: продуктовые шаблоны выбраны намеренно, но инструкция воспроизведения слишком широко описывает исключённые управляющие утилиты | Нужно исправить документацию; само по себе не блокирует масштабирование |
| Измеренные моменты времени меняют хеш свежего `d01:demo` | Подтверждено и ожидаемо для EV-009 | Следует различать детерминированное воспроизведение и новый измеренный запуск; это не ошибка корректности |

## Что остаётся до следующей проверки

Новая отдельно одобренная граница реализации должна охватывать как минимум:

1. единый движок сценариев, полностью управляемый descriptor, где D01 является
   данными, а не шаблоном для копирования оркестратора, схем и проверок;
2. создаваемые самим runner или sandbox доказательства физических прав, их связь со
   схемой и отрицательные тесты для отсутствующих и противоречивых доказательств;
3. проверку полноты каталога видимого пакета и тесты подмены;
4. точное согласование карты доказательств, происхождения, области компиляции и
   инструкции воспроизведения.

Владелец сохранил одобренную фиксацию публичного инварианта D01 и разрешил явно
раскрыть ограничение валидности конструкции; методика оценивания не открывается
заново. Межплатформенная изоляция и живая модель остаются отдельными решениями
владельца. Эта проверка не запускала и не разрешает официальный или
оцениваемый прогон, живую продуктовую модель, Chromium, BG-D02, commit или push.
