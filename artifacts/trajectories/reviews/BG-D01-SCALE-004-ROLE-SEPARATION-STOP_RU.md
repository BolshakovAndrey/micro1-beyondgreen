# BG-D01 SCALE-004 — остановка после целевой проверки границы ролей

**Статус:** `STOPPED_ON_FIRST_TARGETED_BOUNDARY_FAILURE`  
**SCALE_READY:** `false`

## Точная коррекция

В `scripts/d01-evaluator-worker.ts` выполнено единственное разрешённое изменение:
параметр `input` локальной функции `evaluateFinalized` получил явный тип
`Parameters<FixtureEvaluatorEngineBindings["runtime"]["evaluateFinalized"]>[0]`.
Других изменений кода или тестов не было.

## Выполненная последовательность

1. `npm run compile` — запущена один раз, завершилась успешно.
2. `node --test tests/d01-boundary.test.ts` — запущена один раз после успешной
   компиляции, завершилась с ненулевым кодом. Из пяти целевых проверок одна прошла,
   четыре не прошли.
3. `npm test` не запускалась, поскольку целевая проверка завершилась неуспешно.

Повторов команд не было. После первого ненулевого результата не выполнялись
дополнительные изменения, диагностические команды или последующие проверки.

## Безопасный класс сбоя

`TARGETED_ROLE_BOUNDARY_SYSTEM_SANDBOX_APPLY_DENIED` — рекурсивная проверка графа
импортов arm и observer прошла, но четыре проверки физической границы завершились на
этапе применения системной песочницы с кодом выхода `71`. Содержимое oracle,
кандидатов и скрытых данных не раскрывалось. Этот результат не подтверждает полный
контракт разделения ролей.

## Сохранённые границы и исключения

Списки разрешённого чтения, политика среды выполнения, физическая изоляция oracle,
fixtures, candidates, oracle-семантика, пять видимых утверждений, методика оценки,
цели, знаменатели, зависимости, `package-lock.json`, BG-D02, A1/A2/S1/S2/S3,
A3/S4/P1/P2/D1 и `SCALE_READY` не изменялись. `d01:verify`, replay, demo,
preflight, форматирование, установка зависимостей и официальный или оцениваемый
запуск не выполнялись. Commit и push не выполнялись.

## Оставшиеся ворота и требуемое решение владельца

Для продолжения требуется отдельное явное решение владельца о допустимом способе
повторной проверки целевой границы после этого сбоя системной песочницы. Без нового
разрешения нельзя повторять команду, менять код или тесты и запускать `npm test`.
Даже после будущего успешного прохождения локальных проверок отдельными воротами
остаются `d01:verify`, проверка трассы и решение владельца. `SCALE_READY=false`.
