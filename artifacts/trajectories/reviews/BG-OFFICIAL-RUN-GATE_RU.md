# BeyondGreen — гейт единственного официального прогона

## Что готово

- Production runtime зафиксирован commit
  `57e2e5222f6197ad3e12beded6fe5eda1be671e1`.
- TypeScript-компиляция, синтетическая репетиция `20/40/80/40`, физическая изоляция,
  обе preflight-проверки и контрольные суммы прошли.
- Production transport `codex-exec-jsonl-v1` с моделью `gpt-5.6-sol` однократно
  проверен из обычного Terminal: `PASS`, один вызов, ноль повторов.
- Каталог официального результата отсутствует.
- Статический official preflight повторно прошёл: 10 фикстур, 20 кандидатов,
  20 execution slots и 40 arm plans; кандидаты не исполнялись.
- Live runner намеренно поддерживает только проверенную macOS-среду с
  `/usr/bin/sandbox-exec` и Node `22.22.3`. Это ограничение раскрыто; судейское
  воспроизведение результата будет основано на платформонезависимом offline replay.

## Что разрешит следующее подтверждение

Ровно один запуск `eval-v1.1.0` по 20 неизменяемым кандидатам. Каждое плечо получает
ровно одну попытку; model transport не повторяется. Скрытые сценарии и evaluator
открываются только после фиксации обоих arm-решений для всех слотов.

Точная команда из обычного Terminal:

```bash
MICRO1_OFFICIAL_SESSION_BOUNDARY=SES-20260831-031 npm run task -- evaluation:run --evaluation-version eval-v1.1.0
```

## Стоп-правила

После старта команда не повторяется ни при каком исходе. При ошибке, прерывании,
таймауте или частичном результате сохраняются все созданные файлы и полный вывод;
ничего не удаляется и не исправляется. Модель, adapter, candidates, verifier,
методика и scoring не меняются.

После завершения результат ещё не публикуется: сначала выполняются read-only review,
агрегация, replay, сверка hashes и подготовка русского owner packet.

## Решение владельца

`SES-20260831-031` одобрена. Разрешён checkpoint commit ровно трёх gate-файлов без
push. После commit владелец сам выполняет точную команду из обычного Terminal. Агент
не исполняет её и ждёт полный вывод.

Разрешён ровно один официальный прогон: одна попытка на плечо, ноль повторов.
Post-decision unblinding разрешён только после фиксации всех 40 arm-решений по 20
слотам. При любом исходе каталог результата сохраняется без удаления, перезаписи,
починки или регенерации.
