# Остановка седьмого продолжения интеграции BG-D02—BG-D04

**Продолжение:** `TRC-BG-D02-D04-INTEGRATION-001-CONT-007`  
**Исходная траектория:** `TRC-BG-D02-D04-INTEGRATION-001`  
**Граница:** `SES-20260830-009`  
**Итог:** остановлено на обязательном предусловии до сохранения целевого отчёта

## Разрешённое изменение сканера

Локальный сканер предварительной проверки получил только безопасные
машиночитаемые поля `start`, `end` и `patternId`: начало, конец и устойчивый
идентификатор шаблона. Поля не содержат совпавший текст. Добавлен однофайловый
интерфейс, который читает только явно заданный файл внутри репозитория. Существующая
модель одной находки категории `absolute-user-path` на строку сохранена.

Добавлены проверки, подтверждающие детерминированные границы, устойчивый
идентификатор шаблона, отсутствие исходного значения в JSON и отсутствие чтения
второго файла однофайловым интерфейсом. Команда
`node --test --experimental-strip-types tests/preflight-check.test.ts` подтвердила
первые десять проверок, включая обе новые. Последняя самопроверка всего репозитория
ожидаемо остановилась на ещё не нормализованных находках; последующая команда
`npm run compile` из связки поэтому не запускалась.

При этом вывод среды тестирования включил машинно-зависимые абсолютные пути в
служебном стеке ошибки. Эти значения здесь не повторяются. Такой сырой вывод нельзя
продвигать в представляемую трассу без обычного исключения или безопасной редакции.

## Однофайловая машинная проверка

Ограниченная программа была передана точной формой
`node --input-type=module <<'NODE' … NODE` через буквальный стандартный ввод. Она
импортировала локальный сканер, просканировала только
`evaluation/manifests/BG-D02/SUBAGENT_REPORT_RU.md` и не выводила строки или
совпавшие значения.

Безопасно подтверждено:

- ошибок целевого файла: `3`;
- находок `absolute-user-path`: `3`;
- номера строк: `72`, `80`, `97`;
- у каждой находки присутствует ровно одна пара детерминированных границ и один
  устойчивый идентификатор шаблона;
- на строке `72` внутри ограниченного совпадения найдено `0` маркеров
  `/micro1.hackerearth/`, тогда как обязательное значение равно `1`.

Программа завершилась с безопасной категорией `marker-cardinality` и кодом `1`.
Общий префикс не сравнивался, суффиксы не извлекались и не разрешались, поскольку
более раннее предусловие уже не прошло.

## Гарантия отсутствия частичного редактирования

Сбой произошёл до формирования и до операции записи. Целевой отчёт не сохранялся,
замен выполнено `0`, откат не требовался. Исходные абсолютные значения, общий
машинный префикс, суффиксы, полные строки и полные строки замены не выводились и не
копировались в другой файл.

## Что не выполнялось после остановки

Не выполнялись повторная диагностика или расширение шаблонов, согласование
производных манифестов и контрольных сумм, неизменённый `npm test`, воспроизведение,
межфикстурная регрессия, оставшиеся проверки взаимно-однозначных соответствий,
проверка документационного паритета, `git diff --check` и обе предварительные
проверки. Официальный или оцениваемый запуск, Claude, Chromium, живая модель,
браузер, приватный MCP, вложенный Codex CLI, упаковка, публикация, продвижение
трассы, commit и push не выполнялись.

## Сохранённые поверхности и риск

Предыдущие записи `TRC-BG-D02-D04-INTEGRATION-001` и `CONT-001`—`CONT-006`
сохранены. Замороженные спецификации поведения, скрытые эталоны, варианты решения,
видимые утверждения, методика оценивания, цели, знаменатели, зависимости,
`package-lock.json`, политика выполнения и `K=0` не изменялись.

Новый безопасный span не охватывает обязательный маркер на первой целевой строке.
Поэтому использовать его для нормализации нельзя: расширение границы без отдельного
разрешения стало бы новой диагностикой после первого неожиданного отказа. Для
продолжения требуется новое узкое решение владельца о безопасном правиле выделения,
не раскрывающем исходное содержимое.
