# Контрольная карточка границы проверки координаторских трасс BG-H01—BG-H06

**Граница:** `SES-20260830-012`<br>
**Режим:** только проверка уже существующих свидетельств, без изменений продукта<br>
**Статус:** ожидает отдельного точного одобрения владельца

## Что нужно одобрить

Нужно одобрить только новую границу проверки хронологического комплекта трасс
координатора реализации BG-H01—BG-H06. Под «комплектом трасс координатора» здесь
понимается последовательность пользовательских инструкций, действий Codex, ответов
инструментов, остановок, повторов и человеческих решений, относящихся к объединению
шести held-out фикстур — заранее отложенных проверочных случаев, которые нельзя было
использовать для настройки решения.

Эта граница не разрешает менять продукт, реализацию фикстур, кандидаты, скрытые
эталоны, тесты или методику оценивания. Она также не разрешает запускать компиляцию,
тесты, официальный либо оцениваемый прогон и не разрешает переносить что-либо в
`actual_traces` — каталог уже окончательно принятых трасс — до следующего отдельного
одобрения владельца.

## Проверенное исходное состояние

- Работа ведётся только в сохранённом clean repository root — чистом корне
  репозитория хакатона; его машинный абсолютный путь не записывается в артефакты.
- Ветка: `codex/beyondgreen-d01-scale003-correct`.
- Точный `HEAD`: `b05d0e17a9ae7ff50f3413ca78e8987a3656cf56`.
- Начальный `git status --porcelain=v1` пуст: рабочее дерево было чистым.
- Сообщение integration commit — коммита объединения — в точности равно
  `feat(Evaluation):[BeyondGreen] Integrate held-out fixtures`.
- Полностью прочитаны `AGENTS.md`, `docs/CHALLENGE.md`,
  `docs/HACKATHON_RULES.md`, `docs/CLEAN_ROOM_POLICY.md`,
  `docs/TRACE_POLICY.md`, `docs/PREFLIGHT_CHECKLIST.md`,
  `docs/PROJECT_SPEC.md`, `docs/EVALUATION.md`, `config/topic.yaml` и
  `templates/SESSION_BOUNDARY.yaml`.
- Внешняя память, соседние репозитории, браузерное состояние, private MCP — частные
  подключения инструментов — и содержимое внешних трасс не читались.

До одобрения созданы только два служебных файла: сама граница
`artifacts/trajectories/session-boundaries/SES-20260830-012.yaml` и эта карточка.
Scanner, capture и preflight — проверка по приватному списку, захват трассы и
предварительная комплексная проверка — не запускались. Не изменялись существующие
проверенные кандидаты трасс, индексы, хеши, контрольные суммы и `actual_traces`.

## Что разрешит точное одобрение

После одобрения можно будет выполнить только проверку следующей хронологии:

1. первоначальная попытка координатора;
2. продолжения `CONT-001`, `CONT-002`, `CONT-003` и `CONT-004`;
3. контролируемое владельцем подтверждение после захвата.

Проверка обязана сохранить в правильном порядке все инструкции, ответы инструментов,
ошибки, остановки, человеческие решения и повторы. В частности, нельзя скрывать:

- проблемы транспорта и последующие повторы;
- отказы или ограничения песочницы;
- результаты scanner и связанные с ними повторы;
- нормализацию пробелов и переносов строк;
- все ограничения полноты и достоверности доказательств.

Неудачные попытки и попытки без изменения файлов нельзя выдавать за самостоятельное
доказательство реализации. Они останутся честными записями повторов внутри общего
комплекта. Минимальный репрезентативный набор — то есть наименьший комплект, который
достаточно показывает содержательную работу, — должен быть выбран из содержательных
`CONT-003`, `CONT-004` и свидетельств после захвата. Покрытие нельзя завышать.

## Разрешённый внешний доступ

После одобрения допускаются ровно два узких исключения из запрета на внешний доступ:

- scanner может прочитать приватный список запрещённых совпадений только через
  `MICRO1_PRIVATE_DENYLIST`;
- проверяющая задача может только прочитать неизменяемые исходные захваты через
  `MICRO1_PRIVATE_TRACE_DIR`.

Значения переменных, внешние пути, содержимое приватного списка и необработанное
содержимое исходных захватов нельзя печатать, записывать в репозиторий, включать в
отчёты или раскрывать иным способом. Никакой другой внешний доступ не разрешён.
Исходные необработанные трассы нельзя копировать в репозиторий или редактировать.

## Обязательные проверки

Для каждого включённого захвата необходимо:

1. проверить квитанцию идентичности и соответствие неизменяемому источнику;
2. проверить корректность `JSONL` — формата «один JSON-объект на строку»;
3. восстановить строгую хронологию инструкций, ответов инструментов, ошибок,
   повторов и человеческих контрольных точек;
4. проверить техническую полноту и перечислить каждое исключение по безопасной
   категории без раскрытия исключённого значения;
5. проверить отсутствие секретов, персональных данных и совпадений с приватным
   списком;
6. определить необходимость детерминированного удаления машинных абсолютных путей;
7. убедиться, что исключения и обезличивание не меняют технический смысл clean-room
   истории — истории, созданной без частных исходников и внешнего рабочего контекста;
8. проверить полное соответствие `docs/TRACE_POLICY.md`.

Если идентичность хотя бы одного необходимого источника нельзя проверить либо
обнаружено загрязнение частным контекстом, работа немедленно останавливается. Нельзя
подменять источник, исправлять загрязнённую историю, продвигать сомнительный материал
или продолжать без нового решения владельца.

## Какие записи можно будет создать или обновить

После точного одобрения разрешены только производные артефакты этой проверки:

- проверенные кандидаты трасс в `artifacts/trajectories/reviewed/`;
- их полные английская и естественная русская версии;
- карточки проверки в `artifacts/trajectories/reviews/`;
- непосредственно связанные записи индекса траекторий и индекса происхождения;
- производные хеши и контрольные суммы только для поверхности этого комплекта.

Никакие необработанные исходники не переносятся в репозиторий. После завершения
проверки задача обязана остановиться и выдать полный естественный русский пакет
одобрения владельцу. До отдельной точной фразы владельца запрещено перемещать или
индексировать какой-либо материал как принятый в `actual_traces`.

## Что останется запрещено даже после одобрения этой границы

- Любые изменения product code, fixture, candidate, oracle или test — кода продукта,
  фикстур, кандидатов, скрытого эталона или тестов.
- Изменения конфигурации, продуктовых проекций, замороженной методики или результатов.
- Компиляция и выполнение тестов.
- Официальный либо оцениваемый запуск, раскрытие held-out результатов или вызов
  живой модели продукта.
- Claude, Chromium, браузер, private MCP, подключённые приложения, внешняя память,
  соседние репозитории и любые иные внешние рабочие области.
- `commit`, `push`, публикация, упаковка и отправка.
- Любое продвижение в `actual_traces` без отдельного последующего одобрения.

## Риски и предел одобрения

Главный риск — ошибочно представить технически пустой или неидентифицируемый повтор
как доказательство реализации либо удалить из истории неудачу, меняющую её смысл.
Второй риск — раскрыть машинный путь, приватное совпадение или содержимое внешнего
захвата. Граница требует минимального набора содержательных доказательств, честного
сохранения всех повторов, категориального обезличивания и остановки при любой
непроверяемой идентичности или загрязнении.

Одобрение этой карточки разрешает только описанную проверку. Оно не подтверждает
качество будущего комплекта, не делает его автоматически submission-eligible —
пригодным для отправки, — не разрешает официальный прогон и не заменяет последующее
ручное решение о продвижении в `actual_traces`.

## Точная фраза одобрения

Если граница верна, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-012 и разрешаю только проверку coordinator trace bundle H01–H06 в границах русской owner-карточки; до отдельного одобрения ничего не продвигать в actual_traces.
