# Карточка владельца: чистое восстановление BG-D01 в SES-007

**Статус:** reviewed-слой согласован с доступным более поздним экспортом, но точное сравнение с закреплённым capture не завершено; commit/push запрещены  
**Граница:** `SES-20260829-007`  
**Исходная точка:** `f0418bc6051002d8d42ccf84822d2e1f2e25b02e`  
**Запуск:** `RUN-BG-D01-VERTICAL-001`, неоцениваемая демонстрация разработки

## Что изменилось после source-to-reviewed проверки

Для проверки был доступен более поздний штатный экспорт Codex Desktop размером
79 779 байт. Он хорошо подтверждает основную последовательность инструкций,
действий, повторов и результатов, но не совпадает с ранее закреплённым неизменяемым
capture размером 66 250 байт. Поэтому нельзя утверждать, что проверены именно те же
байты, SHA-256 которых записан во внешнем хранилище.

Reviewed-трасса исправлена: из неё удалены точные детали, которых проверенный экспорт
не подтверждает самостоятельно, включая точные причины двух compiler-коррекций,
число установленных пакетов, длительность ожидания генератора, точный reason code,
метаданные захвата и последующее решение о продвижении. События, произошедшие после
capture, вынесены в отдельный файл
`TRC-BG-D01-RECOVERY-001-POST-CAPTURE.yaml`. Они остаются честной audit history, но
больше не выдаются за содержимое исходной траектории.

Итог проверки — `BLOCKED`: доступный reviewed-слой стал точнее, однако сохранение
технического смысла относительно точного неизменяемого capture ещё не доказано.

## Почему это новая чистая сессия

SES-005 формально исключена под `EXC-003`: во время сравнения воспроизведения была
сделана запись вне репозитория. SES-006 исключена под `EXC-004`: публичный кэш
пакетов был прочитан до явного разрешения владельца. В этой карточке внешние имена и
пути не записываются; очистка внешних данных не предпринималась.

SES-007 не переносила изменения из SES-006. Начальным материалом был только текущий
repo-local diff относительно указанного baseline-коммита. Отчёты SES-005/006 не
принимались как доказательство. Код проверялся заново по `PROJECT_SPEC.md@1.1.0`,
`EVALUATION.md`, `config/topic.yaml` и обязательным clean-room/trace контрактам.

Внешнее разрешение использовано один раз: `npm ci` установил ровно 54 зависимости
по `package-lock.json`; результат аудита — 54 из 54 разрешённых лицензий, ноль
находок. Кэш и его расположение не перечислялись и не инспектировались. Другие
внешние материалы, браузер, подключённые приложения, MCP-ресурсы, Claude, Chromium и
живая модель не использовались.

В первом вызове проверки Git был избыточно запущен список worktree. Он вывел
машинно-зависимые метаданные путей других рабочих деревьев. Содержимое этих деревьев
не читалось, сведения не использовались в реализации и не записаны в репозиторий;
review candidate опускает их по категории. Владелец дословно одобрил эту
категориальную редактуру, подтвердил отсутствие чтения содержимого внешних worktree
и сохранение технического смысла. Сохранены сам факт избыточного Git-вывода, stop
condition, найденные дефекты, исправления, ошибки компиляции, инструментальный
повтор и все результаты проверок.

## Что было независимо переписано

- Каждая ветвь теперь получает собственный полный предел 180 секунд: 165 секунд на
  работу ветви и 15 секунд на независимое завершение оценки. Одна ветвь не может
  расходовать время другой.
- Сбой, timeout (превышение времени), неверный JSON и неверные доказательства arm
  process (процесса ветви) дают единственный неизменяемый `abstain` — отказ от
  решения — без повторной попытки. Повреждение результата оценщика, несовпадение
  manifest или SHA-256 делает недействительным весь запуск, а не маскируется как
  проблема ветви.
- AST-анализ (структурный разбор синтаксического дерева TypeScript) связывает import
  публичной фабрики сигналов, созданные ею контейнеры, снимки и записи через символы
  TypeScript. Имена переменных кандидата не используются как признаки ошибки.
- Причина решения стала типизированной. Независимый оценщик проверяет точный код
  семейства нарушения, а не ищет подходящие слова в свободном тексте.
- Immutable ingest (приём неизменяемого кандидата) связывает разрешённый путь,
  candidate ID, строгие поля manifest, package digest и два заранее замороженных
  SHA-256. Проверки выполняются до решения, после обоих решений и после оценки.
- Offline replay (автономное воспроизведение) проверяет UTF-8/LF framing, порядок и
  количество четырёх записей, каноническую форму JCS, SHA-chain, footer, связи
  request/response/candidate и финальный output. Ядро не импортирует файловую
  систему, сеть, дочерние процессы или запись. JSON и HTML пересобираются и
  сравниваются только в памяти.
- HTML создаётся только из прошедшего Zod JSON; все динамические значения
  экранируются. В отчёте явно показаны typed reason, отдельные итоги компиляции,
  открытых тестов и операционных сбоев, независимые deadlines, `K=0`, ограничения и
  ноль вызовов модели.

## Итог демонстрации

Для замороженного `candidate-b` компиляция и пять открытых тестов зелёные. Status quo
(обычный подход) выдаёт `accept` и пропускает ложноположительный перенос.
BeyondGreen собственной oracle-free проверкой — проверкой без скрытого эталона —
обнаруживает потерю второй прибавки и выдаёт `reject` с типизированной причиной
`ordering_contract_violation`. Только после заморозки обоих решений запускаются
оценщики; они отмечают обычное решение неверным, а решение BeyondGreen верным.

Для сохраняющего поведение `candidate-a` обе ветви выдают `accept`. Кандидаты не
изменились:

- `candidate-a`: `f46b2ed8c85e8558b5c8812491c09d89530099b0890454415579ea4c5d69e8a5`;
- `candidate-b`: `9e2a9151531aa961cf0ace3e20520fdcfa45ff0bff0f13ae214f7e5fbdad7d2b`.

`K=0` означает, что до решения было передано ноль сведений от скрытого оценщика.
Arm processes физически не могут перечислить или прочитать
`evaluation/verifier-only/BG-D01/`; тест требует системную ошибку
`ERR_ACCESS_DENIED`, а не случайное отсутствие файла.

## Воспроизводимые артефакты

- JSON: `artifacts/evaluation/BG-D01-VERTICAL-SLICE.json`;
- HTML: `artifacts/evaluation/BG-D01-VERTICAL-SLICE.html`;
- JSONL replay: `artifacts/evaluation/BG-D01-OFFLINE-REPLAY.jsonl`;
- source manifest: `evaluation/manifests/BG-D01/vertical-slice.manifest.json`.

Два последовательных `npm run task -- d01:replay` дали одинаковый результат и
подтвердили полное совпадение пересобранных отчётов:

- канонический JSON SHA-256:
  `630541a504430b8b0299c8fb83665a2d333128768a302581044117e417f22b39`;
- HTML SHA-256:
  `b564ed1be7dcb78cf46533c0eb489a8536309accc054135f9e348ab92155c518`;
- replay SHA-256:
  `7b68fc5ec091b1cf2dc0307f24de320aa60971ef6d33c32415d45c1c80807300`.

## Результаты команд

- `npm run compile` — успешно.
- `npm test` — после предкоммитного hardening 49 из 49 тестов успешно.
- `npm run task -- d01:verify` — успешно: compile; 5/5 visible tests; 1/1
  step contract; 3/3 oracle self-check; 3/3 physical/network boundary; 16/16 vertical
  contracts; E2E check; четыре foundation manifests; vertical source manifest.
- `npm run task -- phase0.5:verify` — успешно без live execution: license 54/54,
  группы 11/11, 6/6, 3/3, 4/4, 3/3 и 4/4.
- Два `npm run task -- d01:replay` — одинаковые digests, reports matched.
- JSON-проверка — 6 документов корректны; HTML structure/escaping check — успешно;
  `git diff --check` — успешно.
- `checksums:write` и `checksums:check` — 154 repo-local файла, ноль несовпадений.

Обычные `npm run preflight:control` и `npm run preflight:implementation` требуют
внешний приватный denylist (список запрещённых терминов). В SES-007 его чтение не
разрешено. Один repo-local вызов безопасно завершился до сканирования с
`Missing value for --denylist`; implementation-команда из связки не запускалась.
Обход, поддельный список и чтение внешнего пути не предпринимались. Поэтому реальный
private-denylist preflight остаётся отдельным владельческим блокером. Repo-local
self-scan внутри `npm test` при этом прошёл без error findings.

После нового human checkpoint были разрешены только безопасное чтение scanner через
`MICRO1_PRIVATE_DENYLIST` и внешняя запись raw trace через
`MICRO1_PRIVATE_TRACE_DIR`. Затем координатор выполнил разрешённый handoff в основной
authorized repository root. Старый черновик остался в отдельном именованном stash;
он не читался и не удалялся.

Оба безопасных probe прошли без раскрытия значений, путей или содержимого. Реальный
control preflight вернул `READY_FOR_CLEAN_BRANCH`, implementation preflight —
`READY_FOR_IMPLEMENTATION`. Denylist-загрязнений не найдено. Остались только четыре
ожидаемых предупреждения о необходимости человеческой проверки официальных бинарных
доказательств и предупреждение о грязном рабочем дереве.

Отдельная post-capture запись фиксирует, что проверка интерфейса Codex Desktop
установила `RAW_EXPORT_UI_UNAVAILABLE`: штатный
raw/internal export отсутствует, доступны только user-visible Markdown и метаданные
задачи. Владелец выбрал `Copy as Markdown` и передал неизменённые байты напрямую в
`micro1-safe-trace capture`. Успешный source capture для
`TRC-BG-D01-RECOVERY-001` содержит 66 250 байт и имеет SHA-256
`bcf36c6cc64f7695adbdac8a49a888bee8c33b6658a5a469306ae2422db7c612`.
Путь не раскрывался, исходное содержимое не помещалось в репозиторий. Hidden
reasoning и внутренние события приложения не заявляются как захваченные.

Первый успешный захват тех же байтов получил ошибочный ID
`TRC-BG-001-RECOVERY-001`. Он остаётся вне репозитория, не удалялся и не используется
как evidence. Корректирующий захват под ожидаемым ID дал тот же объём и SHA-256, что
подтверждает неизменность payload. Два более ранних вызова без stdin остановились до
внешней записи с `empty trace payload`; все эти события сохраняются в технической
хронологии.

При первой объединённой команде обновления manifest и demo финальный маркер demo не
вернулся в 30-секундном окне инструмента. Генератор был запущен отдельно без
изменения входов и успешно завершился. Это инструментальный повтор неоцениваемой
генерации, не повтор модели, не official attempt и не feedback от оценщика.

## Стоимость, ограничения и риски

Режим полностью synthetic/offline — синтетический и автономный. Вызовы модели,
Codex subprocess, Chromium и браузера: 0. Billing — фиксированная подписка;
стоимость отдельного запуска в долларах не рассчитывалась и не оценивалась. Tokens,
human time и runtime отмечены `not_measured`, а не нулём.

Остаются риски:

- проверен один учебный development fixture, не весь будущий набор 20 решений;
- это не доказательство корректности реальных производственных переносов React;
- живая необязательная reasoning-модель остаётся unverified и не использовалась;
- ручная проверка review candidate прошла: абсолютные пользовательские пути,
  private keys, назначения секретов и email — 0 совпадений;
- реальный private-denylist scan и оба safe preflight прошли без contamination
  findings; native Markdown source capture и digest зафиксированы;
- raw/internal export Codex Desktop недоступен, поэтому hidden reasoning/internal
  events не входят в заявленную полноту source capture;
- владелец ранее одобрил eligibility, но предкоммитное ревью установило, что native
  source capture не сравнивался с reviewed-слоем; прежнее продвижение отменено до
  безопасного сравнения;
- 153 контрольные суммы согласованы; владелец всё равно должен проверить состав
  surface перед последующим commit или выпуском;
- commit и push не выполнялись и запрещены.

## Eligibility verdict и точное следующее решение

Текущий verdict: **BLOCKED**. Reviewed trace согласован с доступным более поздним
экспортом, а post-capture факты отделены от source-derived trajectory. Однако этот
экспорт имеет размер 79 779 байт и не совпадает с неизменяемым capture размером
66 250 байт. Поэтому полнота и сохранение технического смысла именно относительно
закреплённого capture пока не доказаны, а trace остаётся вне `actual_traces`.
Ограничение по hidden reasoning/internal events остаётся явно принятым и не является
этим блокером.

После handoff рабочий набор безопасно возвращён на `impl/beyondgreen`: обе ветки
указывали на один HEAD без уникальных commit, временная локальная ветка удалена,
именованный stash сохранён. Следующий gate — либо сравнить точные 66 250 байт через
разрешённый интерфейс, либо создать новую representative trace, source которой можно
проверить побайтово. Только после этого можно повторно решать eligibility. Текущее
состояние не разрешает commit, push, official/scored run или расширение D01 на другие
fixtures.

Это решение не должно разрешать commit, push, официальный или оцениваемый запуск, repair,
другие fixtures, live model, Claude, Chromium, браузер, GUI, выпуск ZIP или
публикацию. Для каждого такого действия требуется отдельное явное разрешение.
