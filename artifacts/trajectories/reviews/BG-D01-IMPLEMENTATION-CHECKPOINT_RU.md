# Контрольная точка реализации BG-D01 — карточка проверки владельцем

`BG-D01` — идентификатор одного синтетического примера из набора разработки. Пример
описывает музейный стенд распределения посетителей по группам. Эта карточка объясняет,
что именно создано, какие доказательства получены и что всё ещё требует решения
владельца репозитория.

## Что создано

В текущей контрольной точке создана только основа примера `BG-D01`:

- общий исходный вариант на React — библиотеке для пользовательских интерфейсов;
- `candidate-a` — вариант-кандидат A, который сохраняет заданное поведение;
- `candidate-b` — вариант-кандидат B с одной намеренно внесённой ошибкой;
- открытая для проверяемых процессов часть музейного примера;
- отдельный скрытый пакет с эталонной последовательностью действий;
- физическое ограничение доступа проверяемых процессов к скрытому пакету;
- цифровые отпечатки SHA-256 для открытого пакета, скрытого пакета и обоих
  вариантов-кандидатов.

Оба варианта-кандидата компилируются и проходят ровно пять ранее зафиксированных
видимых проверок. Эти пять проверок не менялись и шестая видимая проверка не
добавлялась.

## Что доказала скрытая эталонная проверка

Одна и та же эталонная последовательность действий дала три ожидаемых результата:

1. общий исходный вариант на React принят;
2. `candidate-a` принят;
3. `candidate-b` отклонён на действии `allocate-1x2`.

В `candidate-b` оставлено ровно одно семейство ошибки: две прибавки внутри одного
действия читают один устаревший снимок значений. Поэтому вторая прибавка не
накапливается. Других намеренно внесённых семейств ошибок нет, а исходные файлы обоих
вариантов-кандидатов после независимой проверки не изменились.

Повторное действие `reset` — сброс — теперь проверяется полностью. После него:

- все распределённые значения равны нулю;
- шаг равен единице;
- список выбранных карточек пуст;
- все 300 идентификаторов карточек и их порядок не изменились;
- журнал действий равен каноническому журналу с ещё одной записью `reset` в конце.

## Положительное целое значение шага

Замороженный договор поведения разрешает только положительный целый шаг. Публичный
тип TypeScript — языка со статической проверкой типов для JavaScript — остаётся
`number`, поэтому сам тип не исключает ноль, отрицательное, дробное значение или
`NaN` («не число»).

Для таких значений добавлен безопасный отказ: операция завершается ошибкой
`RangeError` до изменения значений, шага или журнала действий. Отдельная открытая
проверка подтверждает это для исходного варианта и обоих вариантов-кандидатов. Она
не входит в пять видимых старых проверок и не определяет новое пользовательское
поведение для недопустимых действий.

## Как проверена физическая граница доступа

Каждая из двух проверяемых веток запускается в отдельном процессе Node.js — среды
выполнения JavaScript. Процессу разрешено читать только открытый пакет, зависимости и
конкретный вариант-кандидат. При этом процесс действительно запускает разрешённый
вариант-кандидат.

Для каталога `evaluation/verifier-only/`, где хранится скрытый эталонный пакет,
проверены восемь способов обращения. Процесс не может:

- перечислить содержимое каталога;
- прочитать файл;
- вычислить цифровой отпечаток файла;
- получить сведения о файле или каталоге;
- проверить доступность пути;
- определить настоящий путь;
- перечислить родительский каталог;
- отличить существующий запрещённый путь от несуществующего по виду ошибки.

Одинаковый результат получен для обеих проверяемых веток и обоих
вариантов-кандидатов.

## Как теперь запускаются задачи

В корневом `package.json` оставлены только устойчивые общие команды: компиляция,
общие проверки, единое средство запуска задач и две предварительные проверки чистой
среды. Конкретные операции разделены на три обзорных файла:
`scripts/tasks/common.ts` для общих задач, `scripts/tasks/d01.ts` для `BG-D01` и
`scripts/tasks/phase0.5.ts` для безопасного воспроизведения исторического этапа
Phase 0.5. Файл `scripts/tasks/registry.ts` только объединяет эти группы, запрещает
повторяющиеся имена и устанавливает постоянный порядок.

Команда `npm run task -- list` печатает доступные имена и краткие описания. Основной
путь этой контрольной точки запускается командой `npm run task -- d01:verify`.
Неизвестное имя или лишний аргумент отклоняются до запуска процесса с ненулевым
кодом завершения. Процессы получают заранее заданные массивы аргументов и запускаются
с `shell: false`, то есть без командной оболочки; введённая строка не может стать
произвольной командой.

Обычная команда `npm test` теперь вызывает `test:all`. Эта задача без шаблонов
командной оболочки находит файлы `*.test.ts` только внутри заранее разрешённых
каталогов `tests/` и `evaluation/arm-visible/`, сортирует пути и безопасно
останавливается при пустом наборе, символической ссылке, повторе или пути за пределами
разрешённых каталогов. Каталог `evaluation/verifier-only/` со скрытыми эталонными
проверками намеренно не обходится: такие проверки запускаются только явной задачей
`d01:verify`.

Исторические команды, способные выполнить живой вызов модели, диагностику сети или
запустить Chromium, удалены из повседневного реестра. Для Phase 0.5 оставлена одна
задача `phase0.5:verify`: она выполняет аудит лицензий и тестирует ранее созданные
оболочки, но ничего из перечисленного не запускает.

Этот рефакторинг изменил только порядок запуска. Файлы вариантов-кандидатов,
поведение музейного примера, скрытая эталонная проверка, пять видимых проверок и
правила оценки не менялись. Их цифровые отпечатки остались прежними:

- `candidate-a`: `f46b2ed8c85e8558b5c8812491c09d89530099b0890454415579ea4c5d69e8a5`;
- `candidate-b`: `9e2a9151531aa961cf0ace3e20520fdcfa45ff0bff0f13ae214f7e5fbdad7d2b`.

## Результаты команд

- `npm run task -- list` — напечатан полный перечень доступных задач с описаниями.
- `npm run task -- d01:verify` — компиляция прошла; затем прошли ровно 5 из 5
  видимых проверок для обоих вариантов-кандидатов, 1 из 1 проверки положительного
  целого шага, 3 из 3 эталонных проверок и 2 из 2 проверок физической границы;
  совпали все четыре списка цифровых отпечатков.
- `npm test` — прошли 32 из 32 обычных проверок репозитория, включая 8 из 8 проверок
  реестра и ограниченного обнаружения файлов.
- `npm run task -- phase0.5:verify` — аудит лицензий прошёл 54 из 54, а безопасные
  группы тестов прошли 11 из 11, 6 из 6, 3 из 3, 4 из 4, 3 из 3 и 4 из 4; живых
  запусков не было.
- `npm run task -- checksums:check` — совпали цифровые отпечатки всех 122
  учитываемых файлов.
- `git diff --check` — ошибок пробелов и оформления изменений не найдено.

Суммарный цифровой отпечаток пакетов `BG-D01` до и после проверок одинаков:
`5273199f8d591068ae827874d80352947eb511844fdf6ccabbcbce9edacccdcf`.

Обе проверки с настоящим внешним списком запрещённых частных терминов прошли. Первая
вернула `READY_FOR_CLEAN_BRANCH`, то есть чистая ветка готова по автоматическим
правилам. Вторая вернула `READY_FOR_IMPLEMENTATION`, то есть структура готова к
реализации. Ожидаемо осталось предупреждение о незаписанных в Git — системе
управления версиями — изменениях и четыре ранее известные двоичные вложения, которые
должен просмотреть человек. Совпадений с частными терминами не найдено.

## Ошибки, исправления и повторные проверки

Первый запуск `npm run compile` захватил прежние служебные сценарии, которые не были
написаны для строгой проверки TypeScript. Область компиляции сузили до всех новых
файлов `BG-D01`, списков цифровых отпечатков и проверки границы. Варианты-кандидаты и
пять видимых проверок при этом не менялись. После исправления компиляция прошла.

Первая версия проверки физической границы доказывала запрет доступа, но не запускала
сам разрешённый вариант-кандидат. Проверку усилили: теперь она одновременно доказывает
работоспособность разрешённого варианта и недоступность скрытого пакета. Поведение
вариантов-кандидатов и эталонная последовательность действий не менялись.

Первая команда проверки файлов формата YAML — текстового формата структурированных
данных — использовала метод `safe_load_file`, которого нет в установленной версии
библиотеки Psych. Те же пять файлов без изменений успешно проверены поддерживаемой
командой `safe_load(File.read(...))`.

Независимая проверка координатора попросила дополнительно проверить общий исходный
вариант, полный результат повторного сброса, положительное целое значение шага и
точность сведений о модели. Все четыре замечания исправлены в пределах уже
разрешённой области работ.

Первый общий запуск после рефакторинга прошёл 22 из 23 проверок. Последняя проверка
обнаружила, что внутренний сканер ещё не повторял подтверждённое владельцем
исключение локального каталога `.idea/` из `.gitignore`. Содержимое каталога не
использовалось и не менялось. Каталог добавили в уже существующие списки исключений
сканирования и подготовки материалов, после чего ту же команду запустили повторно.

## Что намеренно не выполнялось

- Не было ни одного вызова языковой модели из продукта или проверочных сценариев.
- Не запускались Chromium, графический интерфейс, браузер, подключённые приложения
  или частные ресурсы MCP — протокола подключения внешних инструментов.
- Не выполнялись официальные оцениваемые запуски базовой ветки или BeyondGreen —
  будущего проверяющего процесса.
- Не создавались автономное воспроизведение, демонстрационное исправление, другие
  синтетические примеры или полный управляющий процесс BeyondGreen.
- Внешний список запрещённых частных терминов не раскрывался: разрешённый сканер
  получил его напрямую, без вывода значения или пути.
- Не выполнялись `git commit` и `git push`.

Точная модель, настроенная в настольном приложении Codex, не указана. В материалах
задачи нет автоматически выгруженного точного имени модели, поэтому такое имя было
бы неподтверждённым. Зафиксировано только отсутствие вызовов модели: 0.

Учёт стоимости остаётся простым: используется фиксированная подписка. Стоимость
отдельного запуска в долларах не рассчитывается и не оценивается; допустимые статусы
— `not_applicable` («не применимо») или `not_measured` («не измерялось»). Ограничение
стоимости в долларах не вводилось.

## Оставшиеся риски и неопределённость

- Проверен только синтетический пример `BG-D01`; общая правильность реальных
  производственных миграций не доказана.
- Эта контрольная точка не является официальным сравнительным запуском и не даёт
  оценочного результата.
- Запись траектории реализации ещё должна пройти отдельное сохранение,
  автоматическую проверку и проверку владельцем.
- Полный вертикальный сценарий `BG-D01` с решением BeyondGreen, итоговым отчётом и
  автономным воспроизведением ещё не реализован.
- Четыре ранее известные двоичные вложения требуют отдельного просмотра человеком.

## Что разрешит и чего не разрешит одобрение

Одобрение подтвердит только основу синтетического примера `BG-D01`, происхождение
материалов, отсутствие выявленных частных данных, физическую границу скрытой
проверки и новый безопасный порядок запуска задач. После этого можно будет отдельно
предложить следующую контрольную точку.

Одобрение этой карточки само по себе не разрешает:

- официальные оцениваемые запуски;
- исправление `candidate-b`;
- создание других примеров;
- вызовы языковой модели или Chromium;
- `git commit`, `git push` или выпуск результата.
