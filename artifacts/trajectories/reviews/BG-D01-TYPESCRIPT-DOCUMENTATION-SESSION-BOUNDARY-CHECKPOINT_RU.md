# BeyondGreen D01 — карточка одобрения границы документационной задачи TypeScript

## Решение, которое требуется сейчас

Нужно одобрить или отклонить только границу `SES-20260830-005` для одной новой
задачи, пригодной для включения в материалы подачи. Задача относится только к
документации TypeScript и не должна менять поведение программы.

До этого одобрения созданы только два служебных файла: сама граница сессии и эта
русская карточка. Содержимое `.ts`-файлов не читалось и не менялось; компиляция,
тесты, проверка D01, автономное воспроизведение, сверка манифестов, контрольные суммы,
предварительные проверки и захват трассы не запускались.

## Исходное подтверждённое состояние

- Актуальные проекции SCALE-004 согласованно содержат `SCALE_READY=true`.
- Владелец ранее одобрил восемь захватов SCALE-004, а приёмочная матрица прошла A1,
  A2, A3, S1, S2, S3, S4, P1, P2 и D1.
- Обычные тесты ранее прошли `56/56`; полная проверка D01, автономное
  воспроизведение и Phase 0.5 также ранее прошли. Эта карточка не утверждает, что
  проверки уже повторены для будущих правок документации.
- Официального или оцениваемого запуска не было. Commit и push не разрешены.
- Текущее дерево намеренно грязное и одобрено владельцем как исходная точка. До
  новой границы в нём было ровно 42 изменённых файла TypeScript; их перечень и хеш
  вывода `git status` записаны в границе, чтобы не приписать унаследованные изменения
  новой задаче.

## Зачем нужна задача

Обязательный контракт `AGENTS.md` требует содержательной английской документации
JSDoc для публичных функций, экспортируемых типов, важных модулей и неочевидных
контрактов. Координатор сообщил исходные результаты аудита: 176 экспортирующих
объявлений, 115 без JSDoc и 32 с формально короткими комментариями. Эти числа пока
считаются входными данными координатора и будут повторно проверены после одобрения.

Цель — дать независимому проверяющему достаточно контекста, чтобы понять архитектуру,
критический поток данных, физическую границу скрытого эталона и риски корректности,
не превращая исходники в шум из очевидных комментариев.

## Точный разрешаемый объём

После одобрения разрешается одна траектория `TRC-BG-D01-DOCS-001` только по 42
TypeScript-файлам, перечисленным в `SES-20260830-005`. Наивысший приоритет имеют:

- `src/d01/orchestrator.ts`;
- `src/d01/engine.ts`;
- `src/d01/capability.ts`;
- `src/d01/capability-schemas.ts`;
- `src/d01/schemas.ts`;
- `src/d01/runtime.ts`;
- `src/d01/worker-runtime.ts`;
- `evaluation/arm-visible/BG-D01/contract.ts`;
- `evaluation/verifier-only/BG-D01/canonical-driver.ts`.

Разрешённые изменения:

- краткий английский JSDoc уровня модуля для важных архитектурных модулей;
- содержательный английский JSDoc для публичных и экспортируемых типов, функций,
  классов и критических контрактов;
- теги `@param`, `@returns` и `@throws` только там, где они действительно уточняют
  обязательства вызывающей и вызываемой стороны;
- короткие английские пояснения около решений, защищающих `K=0` — отсутствие
  обратной связи от независимого оценщика до неизменяемого решения, физическую
  изоляцию скрытого эталона, неизменяемость решения, политику процессов, целостность
  автономного воспроизведения и закрытый отказ при неопределённости;
- групповые смысловые пояснения для связанных схем Zod, то есть схем проверки данных
  во время выполнения, без механического комментария каждого псевдонима типа.

Нельзя описывать очевидный синтаксис, комментировать каждую строку или создавать
формальные комментарии только ради счётчика. Любой комментарий должен объяснять
назначение, инвариант, границу безопасности, режим отказа или причину ограничения.

## Что должно остаться неизменным

- Поведение программы и форма публичного API — набора доступных типов, функций и
  параметров.
- Тестовая семантика, фикстуры, байты вариантов решения, скрытый эталон, правильные
  ответы оценивания и пять видимых утверждений D01.
- Методика `eval-v1.1.0`, цели, знаменатели, зависимости, `package-lock.json`,
  ограничения времени и политика одного запуска без повтора.
- `K=0`, физическая изоляция скрытого эталона, неизменяемые решения, привязка
  политики процессов, целостность автономного воспроизведения и закрытый отказ.
- Одобренные доказательства SCALE-004, все десять пройденных критериев и
  `SCALE_READY=true`.

Изменение исполняемых выражений, сигнатур, экспортов, порядка выполнения, схем данных,
тестов или ожидаемых результатов является выходом за границу и требует остановки.

## Автоматический захват без ручного копирования

После одобрения единственная документационная траектория должна выполняться через
локальный Codex CLI с машиночитаемым JSONL — потоком JSON-событий по одному на
строку. Неизменённый стандартный вывод передаётся прямо на стандартный ввод
`micro1-safe-trace`, который сохраняет сырой слой во внешнем защищённом каталоге.

Запрещены `Copy as Markdown`, ручное копирование, `tee`, промежуточный сырой файл в
репозитории, сохранение потока в переменную оболочки и вывод сырого содержимого или
внешнего пути в интерфейс. Поток ошибок должен идти отдельно через одобренный
безопасный механизм. Если автоматический транспорт не проходит проверку политики,
работа останавливается без ручного запасного варианта.

## Быстрый план проверки после правок

Проверки идут строго по порядку. При первом реальном сбое работа останавливается; без
нового решения владельца нельзя повторять команду, исправлять причину, согласовывать
манифесты или расширять объём.

1. Повторный локальный аудит документации ровно 42 разрешённых `.ts`-файлов и ручная
   проверка приоритетных публичных контрактов.
2. `npm run compile`.
3. Целевые проверки документации и структуры D01 без официального или оцениваемого
   запуска.
4. `npm test`.
5. `npm run task -- d01:verify`.
6. Два запуска `npm run task -- d01:replay` с требованием одинаковых хешей JSON и
   HTML.
7. `npm run task -- d01:manifests:check`,
   `npm run task -- d01:vertical:manifest:check` и `npm run task -- d01:hash`.
8. Согласование манифестов допускается только тогда, когда комментарии ожидаемо
   изменили байты уже охваченных файлов. Нельзя менять семантику вариантов решения
   или скрытого эталона.
9. Проверка покрытия контрольными суммами; обновление их проекций допускается только
   для ожидаемо изменившихся хешей документации.
10. Обе контролируемые владельцем предварительные проверки:
    `micro1-safe-preflight control` и `micro1-safe-preflight implementation`.
11. `git diff --check` и итоговое подтверждение, что разница содержит только
    документационные изменения TypeScript и отдельно разрешённые служебные
    согласования.

## Риски и условия остановки

- Любое фактическое изменение поведения или публичного API.
- Первый сбой компиляции, теста, D01, автономного воспроизведения, манифеста,
  контрольной суммы или предварительной проверки.
- Потребность менять тесты, фикстуры, варианты решения, скрытый эталон, методику,
  цели, зависимости или политику среды выполнения.
- Путь вне чистого корня, приватный контекст, неизвестное происхождение или лицензия,
  сходство с запомненной приватной структурой, утечка скрытого эталона или
  неустранимое загрязнение.
- Невозможность автоматически сохранить неизменённый JSONL через
  `micro1-safe-trace`.

В любом из этих случаев работа прекращается и запрашивается отдельное решение
владельца. Успешные проверки сами по себе не разрешают официальный или оцениваемый
запуск, commit, push либо публикацию.

## Что останется запрещено

- BG-D02 и последующие фикстуры, варианты решения или скрытые эталоны.
- Официальный или оцениваемый запуск, сокращение набора измерений, раскрытие
  отложенной части, демонстрация ремонта и изменение методики.
- Claude, Chromium, браузер, подключённые приложения, приватный MCP, глобальная
  память, соседние репозитории и внешние рабочие области.
- Commit, push, публикация и сборка пакета подачи.

## Точная фраза одобрения

Если граница верна, ответьте без изменений:

> Одобряю SESSION_BOUNDARY SES-20260830-005 и разрешаю одну documentation-only траекторию TRC-BG-D01-DOCS-001 строго в указанном TS scope без изменения поведения; automatic JSONL → micro1-safe-trace capture обязателен, при первом сбое остановиться; official/scored run, BG-D02+, Claude, Chromium, commit и push запрещены.

Эта фраза разрешает только описанные документационные изменения, автоматический
захват, перечисленные проверки и ожидаемое согласование хешей документации. Она не
разрешает изменение поведения, новый продуктовый объём или любую отдельно
запрещённую операцию.
