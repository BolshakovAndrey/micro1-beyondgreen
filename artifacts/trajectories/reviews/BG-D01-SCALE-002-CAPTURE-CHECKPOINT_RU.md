# Пакет владельца перед захватом TRC-BG-D01-SCALE-002

**Граница сессии:** `SES-20260830-002`  
**Траектория:** `TRC-BG-D01-SCALE-002`  
**Класс запуска:** только неоцениваемая локальная разработка  
**Текущий статус:** реализация и проверка внутри репозитория завершены; штатный захват и проверка траектории ещё не выполнены; `SCALE_READY=false`

## Что было разрешено и сделано

Владелец разрешил закрыть только оставшиеся замечания второго checkpoint D01:

1. сделать один движок, управляемый описателем, где D01 является данными;
2. получать доказательства физических прав от реально запущенного runner и самих изолированных процессов;
3. добавить отрицательные тесты отсутствующих, подменённых и противоречивых событий и доказательств;
4. проверять полный каталог видимого пакета, а не только список manifest;
5. согласовать происхождение, область компиляции, воспроизведение и английские/русские проекции;
6. доказать внедрение описателя только тестовым вариантом в памяти, не создавая BG-D02.

Все шесть пунктов реализованы. Реальный D01 теперь подключается к общему реестру и плану движка через descriptor. Схемы, проверки варианта, механизм рассуждения, оценщик, воспроизведение и отчёт передаются как контракты. Тестовый описатель использует другие идентификаторы и размеры только в памяти теста; новых fixture, candidate, oracle, behavior spec и manifest не создано.

Runner после фактического запуска сохраняет доказательство применённого `sandbox-exec`, модели разрешений Node, хеша списка разрешённых путей, запрета сети и отсутствия унаследованного `PATH`. Каждый arm, observer и evaluator — соответственно вариант проверки, наблюдатель и независимый оценщик — создаёт хеш собственной process identity и выполняет взаимные проверки разрешённого и запрещённого чтения. Их общий proof digest связан с окончательным решением, наблюдениями, входом и результатом оценщика, событиями выполнения, JSON, HTML и offline replay.

Проверка пакета теперь рекурсивно перечисляет весь разрешённый каталог. Она отклоняет незарегистрированный файл, символическую ссылку, выход за корень, повтор пути, несовпадение исходного файла и подмену manifest до выполнения решения.

## Что осталось неизменным

- Пять видимых утверждений D01 не менялись и проходят `5/5`.
- Байты обоих candidates не менялись; их frozen hashes подтверждены manifest-проверкой.
- Oracle, ground truth и behavior freeze не менялись.
- Публичный invariant contract и evaluation methodology не открывались.
- Новых зависимостей, контейнера, runtime policy и изменений Phase 0.5 нет.
- BG-D02 и другие fixtures не создавались.
- Официальный или оцениваемый запуск, live model продукта и Chromium не выполнялись.
- Commit и push не выполнялись.

## Проверки и доказательства

- `npm run compile` — успешно.
- `npm test` — `55/55`.
- `npm run task -- d01:verify` — успешно после предусмотренной сверки manifest:
  - visible tests `5/5`;
  - positive-step contract `1/1`;
  - canonical oracle `3/3`;
  - physical boundary `4/4`;
  - vertical contracts `19/19`;
  - неоцениваемая сквозная проверка успешна;
  - четыре frozen package manifests подтверждены;
  - vertical manifest охватывает 30 файлов, SHA-256 `6be3ea9671143f2df228102a348df5e0f41d3570535f510d7eec4af331282739`.
- `npm run task -- d01:demo` сформировал:
  - JSON SHA-256 `db3d64776e38bfbfba6be2c174a68b7c96bd3a4a543fc64f892a74a3551aa074`;
  - HTML SHA-256 `dd34b63a3b63a6faac7596e911c5008ec7a8df349059e255a7e7fc2770adbc87`.
- `npm run task -- d01:replay` воспроизвёл оба отчёта с `reportArtifactsMatched=true`.
- Безопасная проверка Phase 0.5 прошла license `54/54` и наборы `11/11`, `6/6`, `3/3`, `4/4`, `3/3`, `4/4` без live model и браузера.
- `git diff --check` — успешно.
- Итоговый manifest контрольных сумм — `187/187`.
- Безопасные scanner/trace probes — успешно; приватные значения, пути и содержимое не раскрывались.
- Control preflight: `READY_FOR_CLEAN_BRANCH`.
- Implementation preflight: `READY_FOR_IMPLEMENTATION`.
- Загрязнений не найдено. Сохраняются ожидаемые предупреждения о dirty working tree и четырёх официальных бинарных материалах, которые требуют ручной проверки перед submission.

## Ошибки, остановки и повторные попытки

1. Первый объединённый capability patch не применился из-за устаревшего контекста строки и ничего не записал. Патч был разделён по владельцам символов.
2. Первое выделение schema factory расширило frozen candidate union до общего `string`; compile показал 15 ошибок. Экспортируемый D01 union оставили точным, а общие схемы сохранили управление через descriptor. Повторный compile прошёл.
3. После обязательного process evidence старый synthetic helper дал единственный fail: targeted suite `18/19`. Helper получил тестовые runner/worker attestations без изменения product behavior; повтор прошёл `19/19`.
4. Первый canonical `d01:verify` прошёл все кодовые и поведенческие этапы и остановился только на ожидаемом устаревшем vertical manifest. Разрешённая сверка добавила новые engine/capability файлы; повторная проверка прошла полностью.
5. Первая проверка control checksums ожидаемо обнаружила изменённые разрешённые файлы. После сверки manifest прошёл.

Все перечисленные остановки относятся непосредственно к одобренному remediation scope. Диагностика за его пределами не выполнялась.

## Ограничения и нерешённые риски

- Публичный D01 invariant прямо указывает на заложенное семейство дефекта. Это честно раскрыто как ограничение валидности конструкции; результат не доказывает обнаружение неизвестного скрытого дефекта.
- Повторная используемость движка доказана тестовым описателем в памяти и одним реальным D01, но не вторым реальным fixture; BG-D02 намеренно не создавался.
- Изоляция остаётся fail-closed и macOS-only из-за `/usr/bin/sandbox-exec`. Linux/judge portability требует отдельного решения владельца; ослабленный `K=0` не добавлялся.
- Live adapter остаётся ранее зафиксированным как не проверенный во вложенной среде; этот checkpoint сделал ноль model calls.
- Свежий `d01:demo` содержит измеренные timestamps, поэтому новый запуск закономерно меняет JSON/HTML digests; offline replay детерминированно воспроизводит конкретный сохранённый запуск.
- `TRC-BG-D01-SCALE-002` пока не захвачена, не проверена, не просканирована как reviewed layer и не включена в `actual_traces`.
- `SCALE_READY=false`. Дополнительный промежуточный Claude review отменён владельцем и не выполнялся. После захвата отдельная координационная задача применит Codex acceptance-матрицу; масштабирование потребует явного одобрения владельца.
- Будущее сокращение benchmark до пяти fixtures и десяти evaluation cases не входит в эту сессию; методика, candidates и freeze здесь не менялись.

## Что требуется от владельца сейчас

Откройте меню текущей задачи Codex Desktop и выполните штатное `Copy as Markdown` без редактирования полученного текста. После этого ответьте точной фразой:

> Copy as Markdown для TRC-BG-D01-SCALE-002 выполнен; разрешаю безопасно захватить точные байты через MICRO1_PRIVATE_TRACE_DIR для последующей проверки, без раскрытия внешнего пути или содержимого приватного denylist.

Эта фраза разрешит только безопасный захват точных байтов и последующую проверку траектории. Она не разрешит official/scored run, live model, Chromium, BG-D02, изменение evaluation methodology, commit, push или масштабирование.
