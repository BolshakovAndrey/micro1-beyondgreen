# BeyondGreen — итоговая карточка владельца POSTDECISION-004

Дата: 2026-08-31<br>
Статус: восстановление после фиксации решений успешно завершено; checkpoint commit и ZIP ещё не разрешены.

## Итог выполнения

Однократное продолжение `RUN-BG-OFFICIAL-EVAL-V1.1.0-002-POSTDECISION-004` успешно обработало 40 неизменяемых решений `RUN-002`, не выполняя кандидатов повторно и не вызывая модель:

- сценариев: 20;
- независимых наблюдений: 80;
- пар наблюдений: 40;
- записей независимого оценщика: 40;
- повторных выполнений плеч: 0;
- вызовов модели: 0;
- повторов: 0;
- версия оценки: `eval-v1.1.0`;
- SHA-256 воспроизводимого доказательства: `972cf43f76d0e68534b481a15bfc9f6b9e2db3bf83b4cb5c80d654d07b378f07`.

Первый вызов с ошибочным значением `SES-20260831-038` был остановлен самой первой проверкой конфигурации. Он не создал каталог результата, не прочитал `RUN-002`, не выполнял плечи и не вызывал модель. Владелец отдельно признал его неисполнявшейся конфигурационной попыткой. Успешный запуск использовал требуемую границу `SES-20260831-037`.

## Официальные результаты

| Метрика | Status quo | BeyondGreen | Предварительно заданная цель BeyondGreen | Итог |
| --- | ---: | ---: | ---: | --- |
| Правильные решения | 10/20 | 19/20 | не менее 16/20 | PASS |
| Преимущество над status quo | — | +9/20 | не менее +6/20 | PASS |
| Полнота решений | 20/20 | 19/20 | не менее 18/20 | PASS |
| Ложные блокировки сохраняющих кандидатов | 0/10 | 1/10 | не более 2/10 | PASS |
| Причинно корректное обнаружение дефектов | 0/10 | 3/10 | не менее 8/10 | **FAIL** |

Контрольное плечо status quo дало заранее ожидаемый результат 10/20, 0/10 причинно корректных обнаружений и 0/10 ложных блокировок. Это подтверждает пригодность набора кандидатов для сравнения.

BeyondGreen правильно классифицировал все восемь development-записей и 11 из 12 held-out-записей. Единственный неправильный результат — `abstain` для сохраняющего кандидата `BG-H06/candidate-a`; он одновременно уменьшает полноту и считается ложной блокировкой.

Причинно корректные отклонения зафиксированы только для false-green кандидатов `BG-D03`, `BG-H01` и `BG-H04`. Поэтому цель 8/10 не достигнута, несмотря на правильную итоговую классификацию девяти из десяти false-green кандидатов. Этот результат нельзя скрывать, округлять или исправлять повторным официальным запуском.

## Проверка целостности и воспроизводимости

Отдельная офлайн-проверка подтвердила:

- все 40 записей оценщика соответствуют строгой схеме;
- `offline-replay.jsonl` полностью воспроизводится;
- `aggregate.json`, `report.json` и `report.html` совпадают с воспроизведёнными данными побайтово;
- исходный пакет `RUN-002` по-прежнему содержит те же 44 файла и 40 решений;
- SHA-256 исходного пакета остаётся `5df68dc714d66ca44852d2487213ae5bb222f8444cedc734ccaabdf2abbe6d95`;
- SHA-256 каждого кандидата совпадает с неизменяемым execution plan;
- `RUN-001` и `RUN-002` не изменялись;
- изменение общего inventory SHA-256 раскрыто в provenance как следствие одобренных исправлений verifier runtime в `SES-20260831-034` и `SES-20260831-037`, а не как изменение кандидатов или решений.

## Решение и оставшиеся риски

Официальный результат валиден и существенно превосходит status quo по точности: 95% против 50%, преимущество 45 процентных пунктов. Четыре из пяти заранее заданных порогов достигнуты. Порог причинно корректного объяснения дефектов не достигнут: 3/10 вместо 8/10. В материалах для жюри следует честно разделить сильную классификационную точность и ограниченную точность формулировки причин.

Повторять `evaluation:run` или `evaluation:recover-post-decision`, изменять решения, кандидатов, оракулы, scoring либо содержимое `POSTDECISION-004` запрещено. Следующий допустимый этап — согласование производных контрольных сумм и обеих preflight-проверок, затем отдельный checkpoint commit. ZIP, push и публикация требуют отдельного разрешения.
