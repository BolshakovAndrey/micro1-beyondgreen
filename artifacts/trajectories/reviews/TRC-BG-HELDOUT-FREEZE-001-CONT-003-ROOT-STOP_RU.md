# Корневая карточка остановки после CONT-003

**Граница:** `SES-20260830-010`  
**Захваченная траектория:** `TRC-BG-HELDOUT-FREEZE-001-CONT-003`  
**Результат:** `STOPPED_CONTAMINATION_REVIEW_REQUIRED`

## Безопасное свидетельство

После owner-authorized handoff корневой координатор выполнил разрешённое
детерминированное согласование через зарегистрированные команды
`npm run task -- checksums:write` и `npm run task -- checksums:check`; обе прошли и
на тот момент проверили 377 путей. Локальная структурная проверка подтвердила 34/34
SHA-256, разделение 4/6, десять уникальных классов и единственный сложный случай
`BG-H02`. `git diff --check` прошёл до безопасного preflight.

Затем ровно один раз выполнен `micro1-safe-preflight control`. Он вернул `BLOCKED`
и четыре ошибки категории `private-denylist-content`: один и тот же подавленный
приватный термин найден в следующих уже существовавших служебных файлах:

- `artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`, строка 45;
- `artifacts/trajectories/reviews/BG-HELDOUT-FREEZE-SESSION-BOUNDARY-CHECKPOINT_RU.md`, строки 68 и 86;
- `artifacts/trajectories/session-boundaries/SES-20260830-010.yaml`, строка 171.

Значение приватного термина не раскрывалось и не восстанавливалось. Четыре обычных
предупреждения о необходимости человеческого просмотра официальных двоичных
свидетельств сохранены как предупреждения, а не причина блокировки.

## Действие после находки

В соответствии с clean-room policy работа немедленно остановлена.
`micro1-safe-preflight implementation` не запускался. Совпадения не редактировались,
не удалялись и не подвергались автоматической санации. Внешние пути и содержимое
приватного списка запретов не читались и не записывались. Никакого второго Codex CLI,
повторного preflight или другого обхода не было.

Подготовленная финальная карточка владельца помечена как недействительная для
одобрения. Эта безопасная отметка и настоящая stop-card созданы после последнего
успешного checksum-check; поэтому производный реестр контрольных сумм и хеш русской
карточки в freeze-манифесте теперь сознательно устарели и не должны считаться
валидными. Их нельзя пересчитывать до человеческого решения по загрязнению.

## Что остаётся запрещено

Заморозка не одобрена. Реализация фикстур, кандидаты, скрытые эталоны, тестовый код,
официальный или оцениваемый запуск, живая продуктовая модель, Claude, Chromium,
commit и push не разрешены и не выполнялись. Сохранённый stash
`bg-heldout-freeze-handoff` не удалялся.

Для продолжения требуется человеческий review происхождения совпадений и решение,
можно ли независимо пересоздать затронутую служебную поверхность в новой разрешённой
траектории или текущая `SES-010` должна быть исключена. Автоматическая редактура сама
по себе не может очистить артефакт для выпуска.
