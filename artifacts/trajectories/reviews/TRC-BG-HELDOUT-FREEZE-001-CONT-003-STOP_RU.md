# Карточка остановки завершения заморозки отложенных фикстур

**Продолжение:** `TRC-BG-HELDOUT-FREEZE-001-CONT-003`  
**Граница:** `SES-20260830-010`  
**Результат:** `STOPPED_FAIL_CLOSED` — работа остановлена при первом новом отказе

## Причина остановки

В текущем разрешённом корне были подтверждены требуемые ветка и commit
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Ожидаемая переданная поверхность и
вся прежняя история остановок присутствовали. После чтения обязательных договоров и
служебной истории были добавлены только недостающие текстовые материалы паритета:
русские проекции и записи заморозки для `BG-H01/BG-H02`, а также отдельные записи
происхождения и русские карточки назначения для `BG-H03`–`BG-H06`.

Первая обязательная команда сверки контрольных сумм была запущена точно в форме:

```text
npm run checksums:write
```

Она завершилась с кодом `1`, потому что в `package.json` отсутствует сценарий
`checksums:write`. Существующий реестр задач содержит одноимённую вложенную задачу,
но инструкция требовала точную прямую npm-команду, а разрешённая поверхность не
позволяет добавлять новый сценарий в `package.json` или подменять команду другой
формой.

При сообщении об ошибке npm автоматически указал машинно-зависимое расположение
своего внешнего каталога журналов. Этот путь не открывался, не исследовался, не
сохранялся в репозиторный файл и намеренно не воспроизводится здесь. npm отдельно
сообщил, что файл журнала не был записан. Появление внешнего машинного пути вместе с
непредусмотренным отказом требует немедленной безопасной остановки по `CONT-003`.

## Честный статус проверок

- `npm run checksums:write` — `FAILED_MISSING_SCRIPT`, код завершения `1`.
- `npm run checksums:check` — `NOT_RUN_AFTER_FAIL_CLOSED_STOP`.
- `micro1-safe-preflight control` — `NOT_RUN_AFTER_FAIL_CLOSED_STOP`.
- `micro1-safe-preflight implementation` — `NOT_RUN_AFTER_FAIL_CLOSED_STOP`.
- итоговая проверка YAML/Markdown, хешей, точного охвата и `git diff --check` —
  `NOT_RUN_AFTER_FAIL_CLOSED_STOP`.

Команда не повторялась. Эквивалентная задача через общий реестр не запускалась.
`package.json`, зависимости, lockfile и runtime-политика не менялись.

## Подготовленные, но не утверждённые материалы

Для всех шести отложенных фикстур теперь существует явное покрытие английского и
русского поведения, машинной записи заморозки, публичного происхождения и русской
карточки назначения владельцу. Назначения не менялись:

- `BG-H01` — сброс состояния по новому `cardId`;
- `BG-H02` — логический порядок асинхронных завершений, единственный заранее
  объявленный сложный случай;
- `BG-H03` — устойчивость ссылочной идентичности;
- `BG-H04` — условное размонтирование, очистка и свежий жизненный цикл;
- `BG-H05` — согласованные снимки и подписки внешнего хранилища;
- `BG-H06` — откат к точному подтверждённому до запроса значению.

Добавленные материалы основаны только на уже переданных синтетических пакетах и их
зафиксированных публичных источниках. Они не прошли окончательную хеш-сверку,
контрольные суммы и безопасные предварительные проверки, поэтому текстовая заморозка
не считается завершённой или одобренной. Черновой общий манифест и прежняя итоговая
карточка владельца не обновлялись после добавления файлов и не могут использоваться
как пакет одобрения.

## История и ограничения доказательств

Все четыре прежние карточки остановки сохранены без изменения. Сохраняется один
служебный повтор пары H03/H04: Ruby/Psych не предоставил `safe_load_file`, после
чего `safe_load(File.read(...))` структурно прочитал те же данные без изменения
продуктового содержания. Внутренние необработанные события трёх нативных авторских
подагентов не заявляются как экспортированные трассы. Текущее продолжение также не
объявляется допустимым для отправки до отдельного захвата, сканирования, просмотра и
решения владельца.

## Что не выполнялось и не создавалось

Не запускались компиляция, `npm test`, проверка фикстур, самопроверка оценщика,
воспроизведение, эталонное сравнение, официальный или оцениваемый запуск, живая
продуктовая модель, Claude, Chromium, браузер, другой Codex CLI или подагент. Не
создавались реализация фикстур, кандидаты, тестовый код, исполняемые фикстуры, код или
данные скрытого эталона, скрытые ожидаемые результаты, код оценщика, результаты
раскрытия или оцениваемые артефакты. Stash не применялся, не удалялся и не
изменялся. Commit и push не выполнялись.

## Требуемое решение владельца

Для продолжения нужна новая точная инструкция: либо разрешить существующую
репозиторную форму `npm run task -- checksums:write` и соответствующую проверку,
либо отдельно разрешить минимальное добавление прямых npm-сценариев. Инструкция
также должна определить допустимую обработку уже категориально зафиксированного
машинного пути из stderr npm. До этого нельзя пересчитывать итоговый манифест,
запускать безопасные предварительные проверки или предлагать фразу одобрения.

Новая инструкция не должна разрешать реализацию, кандидаты, скрытый эталон, тесты,
официальный или оцениваемый запуск, живую продуктовую модель, Claude, Chromium,
commit или push.
