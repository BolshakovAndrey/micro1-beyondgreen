# BeyondGreen D01 SCALE-004 — карточка одобрения границы ограниченной сессии исправлений

## Решение, которое требуется сейчас

Нужно одобрить или отклонить только новую границу `SES-20260830-004`. До одобрения
не читались продуктовые исходники, тесты реализации, манифесты, фикстуры, варианты
решения или скрытые эталоны. Не выполнялись компиляция, тесты, `d01:verify`,
автономное воспроизведение, внешние безопасные обёртки, вложенный Codex CLI или
захват трассы. Продуктовые файлы и активные проекции не менялись.

Одобрение разрешит ровно одну ограниченную траекторию реализации
`TRC-BG-D01-SCALE-004`, направленную только на пять подтверждённых блокеров A1, A2,
S1, S2 и S3. Траектория — это последовательная запись инструкций, действий,
ответов инструментов, ошибок, повторов и решений агента. Даже после выполнения она
не станет допустимым доказательством автоматически: потребуются неизменяемый захват,
автоматическая проверка, полная техническая проверка на английском и русском языках
и отдельное одобрение владельца.

## Проверенное исходное состояние

- Работа ведётся внутри разрешённого чистого репозитория. Абсолютный машинный путь
  намеренно не записан в новые файлы.
- Активна локальная ветка `codex/beyondgreen-d01-scale003-correct`; исходный commit —
  `f0418bc6051002d8d42ccf84822d2e1f2e25b02e`.
- Рабочее дерево уже содержало 26 отслеживаемых изменений и 80 новых файлов до
  создания этой границы. Обезличенный SHA-256 снимка `git status` —
  `859119d6725ae20ffc14c925521c64ed81af4397d4577a6768cdf0fa9171fc02`.
- Все унаследованные изменения принадлежат прежнему состоянию рабочего дерева и не
  приписываются `SES-20260830-004`.
- `TRC-BG-D01-SCALE-003` одобрена владельцем, допустима как доказательство
  реализации и включена в `actual_traces` — фактический список принятых трасс.
- Приёмочная матрица Codex сохранила зелёными A3, S4, P1, P2 и D1, но подтвердила
  пять блокеров A1, A2, S1, S2 и S3.
- `SCALE_READY=false`: D01 не признан готовым к масштабированию.
- Удалённое состояние Git не проверялось; commit и push не выполнялись.

## Что было сделано до гейта

Полностью прочитаны `AGENTS.md`, финальный контракт задачи, правила хакатона,
политика чистой комнаты, политика трасс, предварительный контрольный список,
нормативная спецификация продукта, проекция оценивания и `config/topic.yaml`.
Также полностью прочитаны английская и русская приёмочные матрицы SCALE-003 и
владелец-одобренная запись `TRC-BG-D01-SCALE-003`.

Для совместимости служебного формата прочитаны только repository-local шаблон
`templates/SESSION_BOUNDARY.yaml`, предыдущая граница и её русская карточка.
Проверены только локальные метаданные Git и доступность двух новых путей. Созданы
только эта карточка и `SES-20260830-004`; далее допускается лишь их структурная
проверка.

Не запускались автоматический сканер, безопасные внешние обёртки, компиляция,
тесты, `d01:verify`, автономное воспроизведение, демонстрация, Phase 0.5, фикстуры,
варианты решения, скрытые эталоны, официальный набор измерений, Claude, живая
продуктовая модель, Chromium, вложенный Codex CLI или захват трассы. Глобальная
память, браузер, подключённые приложения, приватный MCP, соседние репозитории и
внешние рабочие области не открывались. Значения, пути и содержимое
`MICRO1_PRIVATE_DENYLIST` и `MICRO1_PRIVATE_TRACE_DIR` не читались и не выводились.

## Ровно пять разрешённых исправлений после одобрения

### A1 — настоящий общий движок

Настоящий управляющий процесс и рабочие процессы должны получать
`FixtureEngineBindings` как явно внедряемые зависимости движка. D01 должен остаться
тонкой обвязкой композиции, содержащей данные и сборку зависимостей, но не общую
логику исполнения.

### A2 — настоящий тестовый конвейер

Тестовый descriptor — декларативное описание тестовой фикстуры — должен пройти
настоящую параметризованную цепочку: управляющий процесс, рабочие процессы,
оценщик, автономное воспроизведение и построение отчёта. Ручной диспетчер с
фиктивными функциями обратного вызова больше не считается доказательством. Транспорт
процессов в тесте должен быть ограниченным и явно внедрённым.

### S1 — точные политики каждого слота

Для каждого слота исполнения необходимо вывести из замороженного контракта точную
ожидаемую политику изоляции и точный список разрешённых путей. И при живой локальной
проверке, и при автономном воспроизведении нужно сравнивать канонические SHA-256
этих точных политик. Лишний, отсутствующий, изменённый или переставленный между
слотами путь обязан приводить к закрытому отказу.

### S2 — позиционная связь каждого результата

Каждое наблюдение и каждый результат оценщика должны быть связаны ровно с одним
ожидаемым доказательством процесса, ролью, профилем прав и слотом исполнения.
Нужны отрицательные тесты, которые переставляют результаты между ролями и
подтверждают обязательный закрытый отказ.

### S3 — хеш фактического неизменяемого решения

Поле `decisionSha256` каждого события ветви сравнения должно точно совпадать с
SHA-256 фактического неизменяемого решения этой ветви. Сверка обязательна и при
живой локальной проверке, и при автономном воспроизведении. Отдельный отрицательный
тест должен использовать внутренне согласованный, но произвольный хеш и доказать,
что такая подмена отклоняется.

## Что обязательно сохранить зелёным

- A3: `BoardObservation` остаётся вне общей фабрики и передаётся явно.
- S4: автономное воспроизведение самостоятельно пересчитывает
  `reasonCorrectReject` и `oracleAcceptedCandidate`, а их инверсия отклоняется.
- P1: рекурсивный перечень файлов остаётся глобально отсортированным и поддерживает
  сложный порядок `a/source.ts` вместе с `a.ts`.
- P2: изменение байтов уже зарегистрированного вложенного исходника продолжает
  приводить к закрытому отказу из-за несовпадения хеша.
- D1: активные проекции продолжают сообщать, что SCALE-003 одобрена и допустима, а
  `SCALE_READY=false` сохраняется до отдельного будущего решения.

## Автоматическая передача трассы без ручного копирования

После одобрения реализация должна выполняться через локальный Codex CLI в режиме
машиночитаемого JSONL — последовательности JSON-событий по одному на строку. Его
стандартный вывод должен без изменения байтов напрямую поступать на стандартный
ввод `micro1-safe-trace`, который сохраняет сырой слой только во внешнем каталоге,
выбранном через `MICRO1_PRIVATE_TRACE_DIR`.

Перед фактическим запуском вложенного Codex CLI этот транспорт обязан пройти
проверку на соответствие текущим политикам репозитория. Проверка должна подтвердить:

- контекст и пути ограничены этой границей и пятью блокерами;
- Codex CLI выдаёт JSONL в стандартный вывод и не требует сырого файла в
  репозитории;
- `micro1-safe-trace` принимает поток через стандартный ввод и сохраняет неизменные
  байты во внешнем сыром слое;
- сырой поток не выводится в интерфейс, не дублируется через `tee`, не попадает в
  журналы, подстановку команды или переменную оболочки;
- код завершения, число байтов и SHA-256 сохраняются без раскрытия сырого
  содержимого или внешнего пути;
- ошибка любой стороны закрывает траекторию и не допускает скрытого продолжения.

Промежуточный сырой файл в репозитории запрещён. Смешивать диагностический поток
ошибок с JSONL также запрещено. Если безопасное выполнение нельзя доказать, работа
останавливается и запрашивается решение владельца. Самовольный возврат к `Copy as
Markdown`, ручному копированию или отредактированному экспорту запрещён.

## Что останется запрещено

- Любые изменения фикстуры D01, байтов кандидатов, скрытого эталона, правильных
  ответов оценивания и пяти видимых утверждений.
- Любые изменения методики `eval-v1.1.0`, целевых показателей или знаменателей.
- BG-D02 и любые другие фикстуры, варианты решения, скрытые эталоны или отложенная
  часть набора.
- Сокращение набора измерений, официальный или оцениваемый запуск, раскрытие
  отложенной части и неоцениваемая демонстрация ремонта.
- Claude, живая продуктовая модель, Chromium, браузер, подключённые приложения,
  приватный MCP, глобальная память, соседние репозитории и внешние рабочие области.
- Изменения зависимостей, `package-lock.json`, политики среды выполнения,
  контейнеров, Phase 0.5, переносимости на Linux или требования `K=0`.
- Перезапись исторических границ, трасс, решений и результатов запусков.
- Commit, push, публикация, масштабирование и автоматическое изменение
  `SCALE_READY`.

После появления кодовых доказательств разрешается согласовать только непосредственно
затронутые тесты, манифесты, происхождение артефактов, журнал улучшений, материалы
новой трассы и активные проекции текущего состояния.

## Порядок работы после одобрения

1. Сначала проверить автоматический транспорт Codex CLI JSONL →
   `micro1-safe-trace` по политике репозитория, не запуская сам вложенный Codex CLI.
2. При невозможности доказать безопасность остановиться и запросить решение без
   ручного запасного варианта.
3. Запустить ровно одну ограниченную SCALE-004 траекторию через подтверждённый
   транспорт.
4. Читать только минимальную поверхность реализации, необходимую для A1, A2, S1,
   S2 и S3, и сохранить зелёными A3, S4, P1, P2 и D1.
5. Для каждой существенной итерации записывать гипотезу, изменение, точную команду,
   доказательство, сбой или повтор и решение сохранить либо пересмотреть изменение.
6. После реализации запустить компиляцию, обычные тесты и целевую неоцениваемую
   проверку D01.
7. При первом детерминированном сбое остановиться и запросить отдельное решение до
   повтора или изменения курса.
8. Провести автоматическую проверку трассы, полную техническую проверку на
   английском и русском языках и отдельное одобрение владельца.
9. Повторить приёмочную матрицу Codex. До отдельного явного решения владельца
   `SCALE_READY` остаётся `false`.

## Риски, ошибки и условия остановки

- Любой путь за пределами чистого корня, приватный контекст, неизвестное
  происхождение или лицензия, сходство с запомненной приватной структурой, утечка
  скрытого эталона либо нерешаемое загрязнение требуют немедленной остановки.
- Любая потребность изменить зависимость, политику среды выполнения, методику,
  фикстуру, кандидата, скрытый эталон, видимое утверждение или ослабить `K=0` выходит
  за границу.
- Успешные локальные проверки не делают трассу автоматически допустимой и не дают
  права менять `SCALE_READY`.
- При первом чтении двух крупных контрактов объединённый вывод инструмента был
  усечён. Оба файла затем полностью перечитаны неперекрывающимися диапазонами; это
  был повтор чтения, а не продуктовой команды.
- Первичная команда проверки Git временно показала абсолютный машинный путь в
  интерфейсе задачи, но путь не был записан в репозиторий. В проверяемом слое будущей
  трассы этот путь должен быть категориально исключён без удаления технического
  смысла Git-проверки. Если это невозможно, трасса исключается и работа
  перезапускается в новой чистой сессии.
- Приватные редактирования не применялись, внешний сырой захват не выполнялся,
  повторных запусков продукта не было.

## Точный объём одобрения

Если граница верна, ответьте точной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-004 и разрешаю одну eligible implementation-траекторию TRC-BG-D01-SCALE-004 строго для пяти блокеров A1, A2, S1, S2 и S3 с автоматическим trace transport через repository-policy-validated Codex CLI JSONL → micro1-safe-trace; A3, S4, P1, P2 и D1 сохранить, SCALE_READY не менять, commit и push не выполнять.

Эта фраза разрешит только проверку автоматического транспорта, одну ограниченную
реализацию пяти блокеров, непосредственно связанные тесты и служебные согласования,
компиляцию, обычные тесты, целевую неоцениваемую проверку D01 и последующую
проверку новой трассы. Она не разрешит официальный или оцениваемый запуск,
сокращение набора измерений, живую продуктовую модель, Claude, Chromium, BG-D02,
изменение методики, зависимостей или политики среды выполнения, масштабирование,
commit, push либо публикацию.
