# Остановка продолжения интеграции BG-D02—BG-D04

**Продолжение:** `TRC-BG-D02-D04-INTEGRATION-001-CONT-001`  
**Исходная траектория:** `TRC-BG-D02-D04-INTEGRATION-001`  
**Граница:** `SES-20260830-009`  
**Результат:** остановлено при первом неожиданном сбое проверки

## Исходное состояние продолжения

Владелец классифицировал прежнюю ошибку TypeScript `TS2688` как операционную
недоступность зависимости в чистом рабочем дереве, а не как дефект интеграции.
Координатор подтвердил неизменность `package-lock.json` относительно точной базы
`18510e26fd38e890c0370481ccce114f90cf4ca6`: SHA-256 равен
`1beb2987e3e0654cb525b80c2f97a2f6577f92399e8a78fcd8eb9f9ab6487d4c`. До этого
продолжения была выполнена ровно одна успешная команда `npm ci --ignore-scripts` с
публичным реестром npm. Три разрешённых commit фикстур уже находились в индексе и
рабочем дереве после `git cherry-pick --no-commit`; повторное применение не
выполнялось. Исходная карточка ошибки `TS2688` сохранена без изменений.

## Выполненные команды и результаты

Продолжение началось с точно предписанной команды:

```text
npm run compile
```

Она завершилась успешно, без диагностик TypeScript. После разрешённых
интеграционных изменений команда была выполнена ещё раз и также завершилась
успешно.

Для обнаружения локальных имён задач без запуска оцениваемого процесса выполнена
команда:

```text
npm run task -- list
```

Затем начата полная ранее одобренная проверка D01—D04 с первой существующей
вертикальной проверки:

```text
npm run task -- d01:verify
```

До сбоя получены такие результаты:

- компиляция D01 прошла;
- пять из пяти замороженных видимых проверок D01 прошли;
- отдельный контракт положительного шага прошёл, один из одного;
- самопроверки независимого оценщика прошли, три из трёх;
- в наборе физической границы полномочий прошла одна проверка из пяти, четыре
  завершились ошибкой.

## Неожиданный сбой

Сбой возник в `tests/d01-boundary.test.ts`, то есть в ранее существующей проверке
физического разделения полномочий D01, до выполнения новых проверок D02—D04.

Не прошли четыре проверки:

1. процесс `status-quo` — базовый сравниваемый вариант — не завершил проверку
   запрета доступа к скрытому эталону: получен код завершения `71` вместо `0`;
2. проверка запрета локальной сети и отсутствия унаследованного `PATH` получила
   `EPERM` при попытке слушать `127.0.0.1`;
3. процесс `BeyondGreen` получил такой же код завершения `71` при проверке запрета
   доступа к скрытому эталону;
4. процесс независимого оценщика получил код завершения `71` при проверке запрета
   доступа к обоим кандидатам.

Доступный вывод указывает на отказ среды в операции (`EPERM`, операция не
разрешена), но в рамках правила немедленной остановки причинная диагностика не
продолжалась. Команда не повторялась. Нельзя утверждать, является ли это только
ограничением текущей среды или дефектом проверяемой границы, пока отдельное решение
владельца не разрешит ограниченную диагностику.

## Разрешённые изменения, сделанные до сбоя

До начала полного набора проверок были добавлены только интеграционные поверхности,
предусмотренные границей:

- единый типизированный реестр ровно четырёх фикстур разработки BG-D01, BG-D02,
  BG-D03 и BG-D04;
- задачи `d02:verify`, `d03:verify`, `d04:verify` и `development:verify` в существующем
  типизированном реестре задач;
- проверка замороженных манифестов D02—D04 без изменения их значений;
- интеграционные тесты точного соответствия fixture/descriptor/task — фикстуры,
  описателя и задачи — а также ровно четырёх классов поведения;
- взаимные проверки запрета доступа для процессов обоих кандидатов и оценщика D03 и
  D04;
- проверка сохранения TypeScript-семейства `ParcelDispatchBoard` и отсутствия
  отброшенного MJS-прототипа `QueueBatchCounter`.

Замороженные спецификации поведения, скрытые эталоны, кандидаты, видимые
утверждения, методика оценивания, цели, знаменатели, зависимости, `package-lock.json`,
политика выполнения, `K=0` и семантика кандидатов/эталонов не изменялись.

## Что не было выполнено после остановки

Не запускались:

- отдельные полные проверки D02, D03 и D04;
- новый межфикстурный регрессионный тест D01—D04;
- полная проверка взаимно-однозначных соответствий ролей, профилей полномочий,
  доказательств процессов, средств запуска и слотов выполнения;
- детерминированное автономное воспроизведение;
- обычный полный набор `npm test`;
- согласование README, текущих английской и русской проекций, журнала улучшений,
  индексов происхождения и траекторий;
- согласование манифестов или контрольных сумм;
- контрольная и реализационная безопасные предварительные проверки.

Манифесты и контрольные суммы не переписывались, потому что ожидаемое расхождение
ещё не было получено соответствующей командой. Новые захваты не переводились в
допустимые доказательства.

## Сохранённые запреты и состояние

Официальный или оцениваемый запуск, раскрытие отложенных фикстур, демонстрация
ремонта, живая модель продукта, Claude, Chromium, браузер, подключённые приложения,
приватный MCP, глобальная память, соседние репозитории, внешние рабочие области,
commit и push не выполнялись.

Рабочее дерево сохраняет три ранее применённые без commit фикстуры, перечисленные
интеграционные изменения и эту карточку остановки. Индекс не переоформлялся ради
перезаписи истории исходников.

## Риск и необходимое следующее решение владельца

Главный незакрытый риск — физические проверки D01 не могут подтвердить границу
полномочий в текущем запуске, хотя компиляция и функциональные гейты до неё прошли.
Продолжать проверку D02—D04 или изменять тесты без нового решения нельзя: это
скрыло бы первый неожиданный сбой и нарушило предписанный порядок.

Отдельное последующее одобрение может разрешить только ограниченную диагностику
кода завершения `71` и `EPERM`, определить допустимый способ проверки физической
границы в этой среде и, если причина подтверждена как операционная, один повтор
полного неизменённого набора D01—D04. Такое одобрение само по себе не должно
разрешать изменение замороженной семантики, официальный или оцениваемый запуск,
публикацию, commit, push либо перевод новых захватов в допустимые доказательства.
