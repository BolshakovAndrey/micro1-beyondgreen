# Контрольная карточка границы заморозки отложенных фикстур BeyondGreen

**Граница:** `SES-20260830-010`  
**Будущая координаторская траектория:** `TRC-BG-HELDOUT-FREEZE-001`  
**Состояние:** ожидает отдельного точного одобрения владельца; материалы отложенных фикстур не создавались

## Что владелец проверяет сейчас

Эта карточка сопровождает только новую служебную границу будущей заморозки поведения
и происхождения шести held-out fixtures — отложенных синтетических тестовых примеров,
которые не используются для настройки решения. Сейчас владелец не принимает сами
описания поведения, происхождение, реализацию, варианты решения, скрытые эталоны или
результаты проверок. Он проверяет точную исходную точку, уже закреплённые классы,
будущий объём только текстовой работы, разделение ответственности между подагентами,
способ захвата координаторской траектории, запреты и обязательную остановку перед
любым кодом.

До отдельного одобрения созданы только
`artifacts/trajectories/session-boundaries/SES-20260830-010.yaml` и эта русская
карточка. Разрешена только структурная проверка этих двух новых служебных файлов.
Никакие описания шести отложенных фикстур, продуктовые файлы или доказательства
реализации не создавались.

## Точная исходная точка и чистая граница

Текущий изолированный worktree — отдельное рабочее дерево Git — открыт ровно на
commit `f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Commit означает неизменяемый снимок
состояния Git. В начале задачи рабочее дерево было чистым, а корень Git совпал с
разрешённым корнем этой задачи. Основная рабочая копия, соседние репозитории,
глобальная память, внешнее или приватное состояние не читались. Удалённое состояние
репозитория не проверялось. Commit и push — локальная фиксация и отправка изменений —
не выполнялись и не разрешены.

Полностью прочитаны локальный `AGENTS.md`, обязательные договоры challenge — условий
задачи, правил хакатона, чистой комнаты, трасс и предварительной проверки — а также
утверждённая спецификация BeyondGreen v1.1, её проекция оценивания,
`config/topic.yaml` и шаблон `SESSION_BOUNDARY`. Для формы служебной записи прочитан
только минимальный предыдущий пример границы и его русская карточка. Скрытое
содержимое результатов или эталонов не просматривалось.

## Шесть уже закреплённых классов

Будущая волна ограничена ровно шестью ранее назначенными отложенными фикстурами:

- `BG-H01` — `prop_reset`, сброс состояния при изменении входного свойства;
- `BG-H02` — `async_ordering`, порядок асинхронных событий; это заранее объявленный
  сложный случай;
- `BG-H03` — `identity_stability`, стабильность идентичности объектов или функций;
- `BG-H04` — `conditional_lifecycle`, условный жизненный цикл;
- `BG-H05` — `external_store`, согласование с внешним хранилищем;
- `BG-H06` — `rollback`, откат после неуспешного изменения.

Эта граница не разрешает менять назначение класса, принадлежность к отложенной
части или метку сложного случая `BG-H02`. После интеграции должно сохраняться ровно
четыре фикстуры разработки и шесть отложенных фикстур, а десять классов поведения
должны взаимно-однозначно соответствовать десяти фикстурам: каждому классу — ровно
одна фикстура и каждой фикстуре — ровно один класс.

## Что именно разрешит отдельное одобрение

Одобрение разрешит только подготовку и заморозку текстовых пакетов поведения и
происхождения. Заморозка означает, что утверждения наблюдаемого поведения получают
неизменяемые хеши SHA-256 до появления реализации и последующей оптимизации.

Для каждой пары пакет должен независимо и без заимствования приватного выражения
определить:

1. Нейтрального пользователя и независимо синтетическую публичную область, не связанную ни с какой
   частной системой.
2. Наблюдаемые действия, результаты и инварианты — свойства, которые обязаны
   оставаться истинными.
3. Пробел видимых тестов: какое существенное поведение они намеренно не доказывают.
4. Контракт behavior-preserving candidate — сохраняющего поведение варианта — без
   создания самого варианта.
5. Контракт seeded false-green candidate — заранее заложенного ложноположительного
   варианта, проходящего видимый гейт, но нарушающего скрытый инвариант, — также без
   создания самого варианта.
6. Границу скрытого эталона и полномочий процессов без кода, данных или ожидаемых
   значений эталона.
7. Публичные источники, автора или проект, дату доступа, лицензию либо условия
   использования и подтверждение независимого синтетического происхождения.
8. Только план будущих файлов, тестов и манифестов, не создавая их в этой волне.
9. Ожидаемые режимы отказа и условия немедленной остановки.

Каждый пакет должен быть независимо синтетическим, безопасным по NDA — соглашениям
о неразглашении, и не связанным с каким-либо частным
исходным кодом, тестами, данными, снимками экрана, трассами или документацией.
Переименование приватной структуры не считается независимым авторством.

## Три параллельных подагента и разделение ответственности

После одобрения работают три нативных подагента Codex, каждый в собственном
изолированном чистом worktree и с одной исключительной парой:

- первый владеет только `BG-H01` и `BG-H02`;
- второй владеет только `BG-H03` и `BG-H04`;
- третий владеет только `BG-H05` и `BG-H06`.

Подагенты могут работать параллельно, но не должны совместно сочинять одну пару или
читать внешнее и приватное состояние. Их внутренние необработанные события не
заявляются как экспортированные трассы и не должны представляться судьям как полный
сырой журнал. Допустимым захваченным доказательством будущей интеграции является
только отдельно описанная координаторская траектория.

## Один автоматически захваченный координатор

Шесть подготовленных текстовых пакетов интегрирует один последующий координатор в
траектории `TRC-BG-HELDOUT-FREEZE-001`. Захват должен использовать уже проверенный
launcher — средство запуска — которое получает файл инструкции через стандартный
ввод. Строить текст задания прямо внутри команды оболочки запрещено.

Координатор запускает ровно один вложенный Codex CLI — интерфейс командной строки
Codex — с `--ignore-user-config`, то есть без наследования пользовательской
конфигурации. Его JSONL, поток JSON-событий по одной записи на строку, передаётся
напрямую и без промежуточного сырого файла в `micro1-safe-trace`, безопасное средство
внешнего захвата, из нераскрываемого разрешённого корня. Путь к этому корню, значения
переменных среды и содержимое внешнего сырого слоя не должны печататься, записываться
в репозиторий или попадать в отправляемые материалы.

Одновременный второй вложенный CLI, ручное копирование, редактированный запасной
экспорт, промежуточный сырой файл в репозитории и построение подсказки строкой
оболочки запрещены. Если проверенный транспорт, входной файл инструкции или
единственность вложенного процесса нельзя подтвердить, работа останавливается без
повтора и подмены способа захвата.

## Обязательная проверка текстовой заморозки

Внутри координаторской траектории требуется проверить:

- ровно четыре фикстуры разработки и шесть отложенных;
- взаимно-однозначное соответствие всех десяти классов и десяти идентификаторов;
- сохранение у `BG-H02` заранее объявленной метки сложного случая и объяснения его
  сложности;
- полноту английских машинно-читаемых материалов и естественных русских материалов
  для владельца, включая смысловое соответствие между ними;
- SHA-256 описаний поведения и самих пакетов;
- публичное происхождение, даты доступа, лицензии или условия использования;
- охват и значения манифестов и контрольных сумм только для текстовой поверхности
  заморозки;
- обе безопасные предварительные проверки: контрольную и реализационную, через
  уже разрешённый безопасный интерфейс.

Владелец не должен читать английские материалы, чтобы понять доказательства,
ошибки, повторы, риски, ограничения и точный объём следующего решения. Все карточки
для владельца пишутся полностью и естественно по-русски. Английскими остаются только
точные команды, пути, идентификаторы, названия библиотек и официальные названия.
Если позднее отдельным решением будет разрешён код, JSDoc — структурированные
комментарии публичных контрактов — и обычные комментарии остаются на английском.

## Что эта волна категорически не создаёт

Даже после одобрения этой границы запрещено создавать:

- реализацию любой фикстуры;
- сохраняющие поведение или ложноположительные варианты решения;
- код, данные, ожидаемые значения или исполняемые проверки скрытого эталона;
- тестовый код или исполняемые тестовые фикстуры;
- артефакты эталонного сравнения, оцениваемого либо официального запуска;
- результаты живой продуктовой модели или Chromium-артефакты;
- раскрытие отложенных результатов или настройку решения по ним.

Также запрещены основная рабочая копия, соседние репозитории, внешние или приватные
рабочие области, глобальная память, браузер и его приватное состояние, подключённые
приложения, приватный MCP — протокол подключения инструментов к модели, Claude,
commit, push, публикация, упаковка и отправка результата.

Единственный будущий вызов агента, предусмотренный этой границей помимо трёх
нативных подагентов, — ровно один автоматически захваченный координаторский запуск.
Он служит только интеграции и проверке текстовых пакетов и не является живым
продуктовым вызовом или официальным оцениванием.

## Ошибки, риски и условия остановки

Работа немедленно останавливается при внешнем пути, приватном контексте, неясном
происхождении или лицензии, сходстве с запомненной частной структурой, утечке
скрытого эталона, нарушении изоляции, неожиданном создании кода или загрязнении,
которое нельзя устранить независимым пересозданием. Нельзя самовольно повторять
координаторский запуск, менять транспорт, расширять предметную область или
подменять назначенные классы.

На текущем гейте не проверялись будущие публичные источники, лицензии, хеши,
контрольные суммы, паритет материалов или предварительные проверки: самих пакетов
ещё нет. Не запускались компиляция, тесты, захват трассы, предварительные проверки,
модель, браузер или Chromium. Поэтому карточка не заявляет ни успешность будущей
заморозки, ни готовность реализации, ни допустимость будущей трассы.

После завершения текстовой волны координатор обязан остановиться. До любой реализации
фикстуры, кандидата, скрытого эталона или теста требуется отдельная русская карточка
результата и отдельное точное одобрение владельцем самой заморозки. Автоматическая
проверка не заменяет это решение.

## Точный объём одобрения

Если граница верна, ответьте ровно одной фразой:

> Одобряю SESSION_BOUNDARY SES-20260830-010 и разрешаю только одну ограниченную волну заморозки TRC-BG-HELDOUT-FREEZE-001: тремя параллельными нативными подагентами Codex в отдельных изолированных worktree подготовить только независимо синтетические и безопасные по NDA пакеты поведения и происхождения для пар BG-H01/BG-H02, BG-H03/BG-H04 и BG-H05/BG-H06 по уже закреплённым классам, не заявляя внутренние необработанные события подагентов как экспортированные трассы; затем одним автоматически захваченным координатором через проверенный stdin-launcher с файлом инструкции, --ignore-user-config, ровно одним вложенным CLI и прямой передачей JSONL в micro1-safe-trace из нераскрываемого разрешённого корня интегрировать и проверить только эти шесть пакетов, включая разделение 4/6, взаимно-однозначное соответствие классов, метку сложного случая BG-H02, русско-английские материалы, хеши, происхождение, лицензии, контрольные суммы и обе безопасные предварительные проверки, после чего остановиться для отдельного одобрения заморозки; не создавать реализацию фикстур, кандидаты, код или данные скрытого эталона, тестовый код, оцениваемые артефакты, вызовы живой модели продукта или Chromium-артефакты, не выполнять официальный либо оцениваемый запуск, Claude, commit или push.

Эта фраза не разрешает ничего за пределами описанной текстовой волны. Она не
разрешает считать шесть пакетов замороженными без их будущей проверки и отдельного
решения владельца, не разрешает включать трассу в допустимые доказательства без
отдельного просмотра и не разрешает переходить к реализации.
