# Карточка владельца: полный вертикальный сценарий BG-D01

> **ИСКЛЮЧЕНО:** эта карточка относится к SES-005, исключённой под `EXC-003`, и не
> является доказательством clean recovery. Актуальная независимо проверенная
> карточка: `artifacts/trajectories/reviews/BG-D01-CLEAN-RECOVERY-CHECKPOINT_RU.md`.

**Статус:** реализация завершена, требуется проверка владельцем  
**Граница:** `SES-20260829-005`  
**Запуск:** `RUN-BG-D01-VERTICAL-001`, неоцениваемая демонстрация разработки

`BG-D01` — первый синтетический пример разработки: музейный стенд распределения
посетителей по группам. «Вертикальный сценарий» означает полный путь одного задания
от приёма неизменяемого варианта кода до решения, независимой проверки и готового
отчёта.

## Что реализовано

Система принимает только заранее разрешённый путь одного из двух вариантов и
сверяет его с зафиксированным цифровым отпечатком SHA-256 до и после проверки.
Абсолютный путь, выход за корень репозитория, неверная связь имени и пути либо
несовпадение отпечатка приводят к безопасной остановке.

Обе сравниваемые ветви получают один и тот же неизменяемый вариант:

- обычный подход проверяет компиляцию и ровно пять ранее зафиксированных открытых
  тестов;
- BeyondGreen дополнительно строит типизированный перечень рисков из исходного кода,
  создаёт `ProbePlan` — план дополнительных проверок, выведенный только из открытого
  контракта, — и выполняет собственную проверку накопления двух обновлений.

Решение каждой ветви может быть `accept` («принять»), `reject` («отклонить») или
`abstain` («отказаться от решения из-за неполных или ненадёжных доказательств»).
Полный набор доказательств проверяется библиотекой Zod, после чего решение
замораживается и получает собственный SHA-256. Только когда оба решения уже
неизменяемы, запускается отдельный скрытый оценщик.

## Что показал неоцениваемый сценарий

Демонстрация использует `candidate-b` — вариант с одной намеренно зафиксированной
ошибкой потери второй прибавки:

- компиляция и все пять открытых тестов проходят;
- обычный подход выдаёт `accept`, поэтому пропускает ложноположительный результат;
- BeyondGreen собственной открытой проверкой обнаруживает потерю обновления и выдаёт
  `reject`;
- независимый оценщик, запущенный после фиксации решений, подтверждает: решение
  обычного подхода неверно, решение BeyondGreen верно;
- отпечаток `candidate-b` до и после полностью совпадает:
  `9e2a9151531aa961cf0ace3e20520fdcfa45ff0bff0f13ae214f7e5fbdad7d2b`.

Для `candidate-a` — сохраняющего поведение варианта — отдельный сквозной тест
подтверждает `accept` обеих ветвей. Это защищает от решения, которое отклоняло бы
любой перенос на сигналы без разбора.

## Как защищён скрытый эталон

Каждая сравниваемая ветвь работает в отдельном процессе с разрешением чтения только
своего варианта, открытого пакета, проверенного кода сценария и зависимостей. Она не
может перечислять или читать `evaluation/verifier-only/BG-D01/`. Проверка требует
точного системного ответа `ERR_ACCESS_DENIED` — «доступ запрещён», поэтому отсутствие
файла или другая случайная ошибка не считаются доказательством изоляции.

`K=0` означает ноль сведений от скрытого оценщика до решения. Порядок зафиксирован
так: сначала заморожено решение обычной ветви, затем решение BeyondGreen, и лишь
после этого открыта возможность запуска независимых оценщиков. Ни ожидаемые
значения, ни место ошибки, ни различия, ни подсказка по исправлению не передавались
ветвям.

## Отчёты и автономное воспроизведение

Команда интерфейса командной строки (CLI)

```bash
npm run task -- d01:demo
```

создаёт проверенный структурированный отчёт JSON и статическую страницу HTML:

- `artifacts/evaluation/BG-D01-VERTICAL-SLICE.json`;
- `artifacts/evaluation/BG-D01-VERTICAL-SLICE.html`.

HTML строится только из уже проверенного Zod документа JSON. Передать неверную
структуру и всё равно получить отчёт нельзя.

Команда

```bash
npm run task -- d01:replay
```

автономно пересоздаёт обе формы отчёта в памяти. Ядро воспроизведения не имеет
доступа к сети, запуску дочерних процессов, часам, случайным значениям или записи в
рабочий каталог. Два последовательных запуска дали одинаковые отпечатки:

- канонический JSON:
  `ec923672c8b12b02d65e0831cb1d81d6bc4769057ec0a78c5691823a2f210894`;
- HTML:
  `2e1bc802070b242fba163a46779607b53a8c99ed168b40298f9c85799a255d9c`.

## Проверки

- строгая компиляция TypeScript прошла;
- обычный `npm test` прошёл: 39 из 39;
- ровно пять зафиксированных открытых тестов прошли для обоих вариантов;
- отдельный контракт положительного целого шага прошёл;
- три самопроверки скрытого эталона прошли;
- две проверки физического запрета доступа прошли для обеих ветвей и обоих
  вариантов;
- семь новых проверок полного сценария и отказов прошли;
- полный `npm run task -- d01:verify` прошёл;
- четыре прежних неизменяемых списка отпечатков и новый список исходников полного
  сценария совпадают;
- объединённый отпечаток прежней основы остался
  `5273199f8d591068ae827874d80352947eb511844fdf6ccabbcbce9edacccdcf`;
- кандидаты, пять открытых тестов и смысл скрытого эталона не менялись.

Итоговый список контрольных сумм охватывает 145 файлов и не содержит несовпадений
или пропусков. Документы JSON и YAML корректны, HTML точно пересоздаётся из
проверенного JSON, а `git diff --check` не нашёл ошибок пробелов. Обе безопасные
предварительные проверки прошли без признаков загрязнения: контрольная вернула
`READY_FOR_CLEAN_BRANCH` — «чистая ветка готова», проверка реализации вернула
`READY_FOR_IMPLEMENTATION` — «среда готова к реализации» — с ожидаемым
предупреждением о незакоммиченном рабочем дереве. Сохраняются четыре ранее известных
уведомления о двоичных вложениях, требующих просмотра человеком.

## Обнаруженное уточнение и повтор команды

При внутренней проверке до фиксации доказательств найдено важное различие: сбой
запуска открытого теста нельзя считать доказанной ошибкой кандидата. Политика
уточнена так, что операционный сбой или недетерминизм теперь дают `abstain`, а
`reject` используется только для воспроизводимого нарушения. Добавлена отдельная
проверка этого случая. Кандидаты, тесты и скрытый эталон не менялись.

Первый полный запуск `d01:verify` превысил один интервал получения вывода терминала
после прохождения всех предыдущих групп; оболочка не сохранила идентификатор
продолжающегося процесса. Та же неоцениваемая команда без изменений была повторена
только для получения конечной отметки и полностью прошла. Это не повтор обращения к
модели, не официальная попытка и не обратная связь от скрытого оценщика.

## Стоимость и сведения о модели

Используется фиксированная подписка. Стоимость отдельного запуска в долларах не
применяется или не измерялась; она не рассчитывалась, не оценивалась и не
ограничивалась. Обращений к модели и запусков через Codex из продукта: 0. Точное имя
настроенной модели не заявляется: в машинных доказательствах задачи его нет.
Значения токенов, времени ручного труда и времени выполнения отмечены как
`not_measured` — «не измерялось», а не как ноль.

## Что не выполнялось и какие риски остаются

Не выполнялись официальный или оцениваемый запуск, Chromium, браузер, графический
интерфейс, исправление кандидата, другие синтетические примеры, commit или push.

Остаются ограничения:

- проверен только один учебный пример разработки, а не все двадцать будущих решений;
- результат не доказывает правильность производственных переносов React;
- живая необязательная модель не проверялась и не использовалась;
- траектория этой реализации ещё должна пройти внешнее неизменяемое сохранение,
  автоматическую проверку и проверку владельцем;
- четыре ранее известные двоичные вложения всё ещё требуют отдельного просмотра
  человеком.

## Что будет означать одобрение владельца

Одобрение подтвердит только полноту и корректность этого неоцениваемого вертикального
сценария BG-D01, его доказательств, происхождения и границы скрытого оценщика. Оно
позволит запросить предусмотренную независимую проверку полного D01 и предложить
следующую отдельную границу работ.

Одобрение этой карточки само по себе не разрешает официальный или оцениваемый
запуск, исправление кандидата, другие примеры, обращение к модели, Chromium,
браузер, графический интерфейс, commit, push или выпуск материалов.
