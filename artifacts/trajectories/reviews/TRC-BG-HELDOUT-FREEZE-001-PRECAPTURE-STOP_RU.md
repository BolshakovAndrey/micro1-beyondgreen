# Остановка перед захватом заморозки отложенных фикстур

**Траектория:** `TRC-BG-HELDOUT-FREEZE-001`  
**Граница:** `SES-20260830-010`  
**Результат:** остановлено до запуска вложенного Codex CLI и до интеграции пакетов

## Что было выполнено

После точного одобрения границы созданы три изолированных worktree на commit
`f2afbdb2cde3f4677c351093baccfcb5a202c7d5`. Три нативных подагента Codex
параллельно подготовили только назначенные пары: BG-H01/BG-H02, BG-H03/BG-H04 и
BG-H05/BG-H06. Их внутренние необработанные события не заявляются как
экспортированные трассы.

В координаторский worktree перенесены 18 текстовых и служебных файлов: английские
описания поведения, русские карточки, записи происхождения и записи будущей
заморозки. Реализация фикстур, варианты решения, код или данные скрытого эталона,
тестовый код и оцениваемые артефакты не создавались. Создан файл инструкции
`artifacts/trajectories/instructions/TRC-BG-HELDOUT-FREEZE-001.md`, но вложенный
координатор его не запускал.

## Причина остановки

Перед единственным разрешённым вложенным процессом был выполнен безопасный
`micro1-safe-trace probe`. Команда вернула ошибку с безопасной категорией:
launcher — средство запуска — должен выполняться из разрешённого корня репозитория.
Причина ограничена текущим рабочим каталогом вызова: probe был запущен из
изолированного worktree, тогда как утверждённый транспорт требует запускать только
`micro1-safe-trace` из нераскрываемого разрешённого корня.

Значение внешнего корня, внешние пути, переменные среды и содержимое сырого слоя не
печатались и не раскрывались. Вложенный Codex CLI не запускался; допустимый предел
в один вложенный процесс не израсходован. JSONL не создавался и не передавался,
запись внешней трассы не выполнялась.

В соответствии с правилом остановки повторный probe из другого каталога не
выполнялся. Не предпринималась попытка подменить транспорт, вручную скопировать
трассу, создать промежуточный сырой файл или запустить второй процесс.

## Состояние трёх пар

- `BG-H01` сохраняет класс `prop_reset`.
- `BG-H02` сохраняет класс `async_ordering` и заранее объявленную метку сложного
  случая.
- `BG-H03` сохраняет класс `identity_stability`.
- `BG-H04` сохраняет класс `conditional_lifecycle`.
- `BG-H05` сохраняет класс `external_store`.
- `BG-H06` сохраняет класс `rollback`.

Подагенты сообщили о полном покрытии пользователя и нейтральной области,
наблюдаемых инвариантов, пробела видимых тестов, текстовых контрактов сохраняющего
и ложноположительного вариантов, физической границы скрытого эталона, публичного
происхождения и лицензий, будущих планируемых файлов и ожидаемых отказов.

Для пары H03/H04 честно зафиксирован один служебный повтор структурной проверки:
использованный Ruby/Psych не поддержал `safe_load_file`; исправленный вызов
`safe_load(File.read(...))` прошёл, не меняя продуктового содержания. Это не был
повтор компиляции, теста, модели или оценивания.

Эти сообщения ещё не являются координаторской проверкой. Хеши, общая биекция,
разделение 4/6, русско-английское соответствие, происхождение, лицензии,
контрольные суммы и обе безопасные предварительные проверки пока не подтверждены
единой захваченной траекторией.

## Что не выполнялось

- вложенный Codex CLI и автоматический JSONL-захват;
- семантическая интеграция и единая проверка шести пакетов;
- обновление общих записей происхождения, сложного случая, манифеста заморозки и
  производных контрольных сумм;
- `micro1-safe-preflight control` и `micro1-safe-preflight implementation`;
- компиляция, тесты, воспроизведение, эталонная самопроверка, benchmark — эталонное
  сравнение — официальный или оцениваемый запуск;
- живая продуктовая модель, Claude, Chromium, браузер, приватный MCP, commit и push.

## Узкое исправление транспорта

Продолжение требует отдельного решения владельца только на исправленный способ
вызова. В нём Codex CLI должен остаться в текущем изолированном worktree, получить
файл инструкции через стандартный ввод и запуститься ровно один раз с
`--ignore-user-config`. Только принимающая обёртка `micro1-safe-trace` должна быть
запущена в отдельной подоболочке из нераскрываемого разрешённого корня, безопасно
определённого через `git rev-parse --git-common-dir`; путь нельзя выводить или
сохранять. JSONL передаётся напрямую из стандартного вывода Codex CLI в стандартный
ввод обёртки.

Исправление не разрешает читать основную рабочую копию: смена каталога нужна только
для проверки политики самой обёрткой. Оно также не разрешает второй вложенный CLI,
ручной экспорт, промежуточный сырой файл, расширение предметной области или любую
реализацию.

Если исправленный probe снова не пройдёт, работа обязана остановиться без запуска
Codex CLI и без дополнительного повтора.

## Точный объём разрешения на продолжение

Для одного исправленного транспортного запуска ответьте ровно одной фразой:

> Разрешаю один исправленный транспортный запуск TRC-BG-HELDOUT-FREEZE-001: безопасно определить нераскрываемый разрешённый корень через git rev-parse --git-common-dir, повторить micro1-safe-trace probe только из этого корня и лишь при успешном probe ровно один раз запустить Codex CLI в текущем изолированном worktree с файлом инструкции через stdin и --ignore-user-config, напрямую передав JSONL в micro1-safe-trace из разрешённого корня; путь корня и внешние значения не раскрывать, при любой новой ошибке остановиться без повтора, реализацию, кандидаты, скрытый эталон, тестовый код, официальный или оцениваемый запуск, живую продуктовую модель, Claude, Chromium, commit и push не выполнять.

Эта фраза разрешает только исправление каталога запуска принимающей обёртки и
сохраняет весь остальной объём `SES-20260830-010` без расширения.
