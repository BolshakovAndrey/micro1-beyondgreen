# Готовность инфраструктуры BeyondGreen к официальному запуску

**Граница:** `SES-20260830-018`
**Ветка:** `codex/beyondgreen-official-runner`
**HEAD:** `fcfa5a88dce7683c629318266835995d0bfa05ee`
**Итог:** `READY_FOR_OFFICIAL_RUN`

`READY_FOR_OFFICIAL_RUN` означает только то, что все разрешённые до официального запуска статические и синтетические проверки инфраструктуры прошли. Этот статус не разрешает официальный или оцениваемый запуск и не является результатом официальной оценки.

## Что создано

До параллельной работы были вычислены SHA-256 всех 24 существующих файлов `src/official`; их байты после работы не изменились. `src/d01` остался замороженным и не импортировался общим слоем.

Три настоящих параллельных `subagents` — вспомогательных агента с раздельным владением — добавили:

1. `src/official/process/**`: отдельные точки входа процессов для ролей `arm` (вариант оценки), `observer` (наблюдатель) и `evaluator` (проверяющая роль), строгие IPC-схемы обмена сообщениями и только синтетические тестовые модули;
2. `src/official/inventory/**`: статический реестр и проверку только для чтения десяти фикстур и двадцати кандидатов по замороженным манифестам, путям, явным объявлениям экспортов и SHA-256;
3. `src/official/reasoning/**`: замороженный контракт `codex-exec-jsonl-v1`, автономный разбор JSONL, пределы времени 165+15 секунд и безопасное отображение ошибок в `abstain` без повтора или другой модели.

Координатор добавил `src/official/integration/**`, единую задачу `official:preflight`, неизменяемый план из 20 позиций кандидатов и 40 планов вариантов оценки, а также связал синтетические записи процессов с агрегацией, отчётами JSON/HTML и автономным воспроизведением.

Ни один вспомогательный агент не менял общие пути, `src/d01`, зависимости проекта, замороженные манифесты и кандидаты, видимые утверждения или семантику проверяющего слоя.

## Доказанные границы

- В каждой позиции ровно два варианта оценки: `status-quo` и `beyondgreen`; номер попытки равен 1.
- Весь план исполнения, позиции, привязки кандидатов и дескрипторов, а также массивы вариантов оценки рекурсивно заморожены; попытки изменения завершаются ошибкой.
- Два неизменяемых решения вариантов оценки существуют до запуска наблюдателя и проверяющей роли.
- Шесть отдельных синтетических процессов выполняются в порядке: два варианта оценки → два наблюдателя → две проверяющие роли.
- Модель разрешений Node физически запрещает каждой тестовой роли читать назначенные запрещённые байты.
- Повреждённые IPC/JSONL, неполные решения, превышение времени и сбои транспорта закрываются в `abstain`; повтор и замена модели запрещены.
- Статический реестр подтверждает ровно 10 фикстур × 2 кандидата, разделение 4/6 и десять разных классов поведения.
- JSON и статический HTML воспроизводятся автономно с точным совпадением контрольной суммы доказательств.

## Проверки

Финальная компиляция после последнего разрешённого исправления:

```text
npm run compile
```

Результат: PASS.

Основная часть `tests/official`, без отдельно запускаемой единственной репетиции нескольких процессов:

```text
node --test tests/official/aggregation-report-replay.test.ts tests/official/fixtures/bg-d02-d04-adapters.test.ts tests/official/fixtures/bg-d03-adapter.test.ts tests/official/fixtures/bg-d03-evaluator-bridge.test.ts tests/official/fixtures/bg-h01-h03-adapters.test.ts tests/official/fixtures/bg-h04-h06-adapters.test.ts tests/official/integration/execution-plan.test.ts tests/official/inventory/static-inventory.test.ts tests/official/process/ipc-schema.test.ts tests/official/process/process-entrypoints.test.ts tests/official/process/source-boundary.test.ts tests/official/reasoning/transport.test.ts tests/official/role-capability-boundary.test.ts tests/official/shared-interfaces.test.ts tests/official/synthetic-e2e-rehearsal.test.ts tests/official/worker-contract.test.ts
```

Результат: 34/34 PASS.

Статический `preflight` — предварительная проверка без исполнения кандидатов:

```text
npm run task -- official:preflight
```

Результат: PASS; 10 фикстур, 20 кандидатов, 20 позиций, 40 планов вариантов оценки. SHA-256 реестра: `b2fb8077ddd9f9b132326b99a681e842fd44578a024a44624b96ed18c0248f09`.

Единственная отдельная сквозная репетиция нескольких процессов:

```text
node --test tests/official/integration/multiprocess-rehearsal.test.ts
```

Результат: 1/1 PASS. Полное объединение `tests/official`: 35/35 PASS.

## Ошибки, остановки и разрешённые исправления

Первый локальный тест процессного потока прошёл 2/4: Node в режиме удаления типов `strip-only` не поддержал свойство-параметр TypeScript. Вспомогательный агент заменил его обычным полем `readonly` с присваиванием; процессный контракт не изменился, итог — 5/5 PASS.

Одна команда координатора для проверки владения только на чтение не разобралась оболочкой из-за кавычек. Тело команды не выполнилось, состояние не менялось. Она была повторена отдельными простыми командами; это транспортный повтор без изменений.

Первая общая компиляция остановилась на `TS2322`: `invocationCount` расширился из точного значения `1` в тип `number`. После отдельного разрешения владельца успешный результат получил явный тип `CodexExecParseSuccess<Output>`. Разбор JSONL, команда, пределы времени и семантика ошибок не менялись.

Следующий официальный набор прошёл 33/34: результат проверки Zod не был заморожен. После разрешения добавлена рекурсивная заморозка. Первый вариант снова дал 33/34, потому что уже замороженный Zod-массив не пропускал обход к дочерним позициям. После нового разрешения обход стал всегда посещать дочерние значения с защитой `WeakSet`; значения и порядок не менялись. Финальная проверка доказала заморозку верхнего и всех вложенных уровней и отказ при попытке изменения.

## Что ещё не доказано

- Новое управление процессами не импортировало и не исполняло ни одного реального кандидата D/H.
- Работоспособность реального `codex exec`, локальной авторизации, сети и ответа модели здесь не проверялась; стоимость в долларах не вычислялась и не заявлялась.
- Не выполнены реальные цепочки наблюдателя и проверяющей роли для всех фикстур D/H и не измерен трёхминутный официальный предел времени.
- Не получены скрытые результаты, итоговая оценка, достижение целей, записи ресурсов или полный отчёт по 40 запускам вариантов оценки.
- Не использованы единственная официальная попытка и единственное раскрытие скрытых результатов; официальная запись запуска не создана.

## Сохраняющийся запрет

Не выполнялись исполнение реальных кандидатов, официальный или оцениваемый запуск, раскрытие скрытых результатов, вызов рабочей модели, Claude, Chromium, browser/MCP, продвижение трассировки, commit или push. Переход к любому из этих действий требует отдельного явного разрешения владельца.

## Решение владельца о контрольном коммите

Владелец явно одобрил `READY_FOR_OFFICIAL_RUN` и разрешил ровно один контрольный commit с сообщением `feat(Evaluation):[BeyondGreen] Build official evaluation runner` после полного финального preflight. Push, официальный или оцениваемый запуск и раскрытие скрытых результатов этим решением не разрешены.
