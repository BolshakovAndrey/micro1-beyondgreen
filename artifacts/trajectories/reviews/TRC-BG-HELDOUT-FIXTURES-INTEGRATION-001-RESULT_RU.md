# Остановка автоматического запуска интеграции H01—H06

**Траектория:** `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001`<br>
**Граница:** `SES-20260830-011`<br>
**Исходный commit:** `830793ce16cb2273e4c9b294a11c7aca952bc7a7`<br>
**Состояние:** интеграция и owner-controlled post-capture проверки зелёные;
ожидается отдельная приёмка владельца, integration commit не разрешён

## Что было готово до запуска

Три разрешённых fixture-only checkpoint commit — контрольных снимка только своих
пар фикстур — были созданы без push и подтверждены как полностью зелёные:

- BG-H01/BG-H02:
  `2b61a9954d43686f4c07f10d48bb726d5ed156f0`;
- BG-H03/BG-H04:
  `bf94ab99a0162d9a940b9ba129602b0cc59b0f67`;
- BG-H05/BG-H06:
  `745b118b4c4da34dde367d84eae8005c3af3c4a2`.

Все три основаны на точном freeze checkpoint
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`. Перед запуском
`micro1-safe-trace probe` подтвердил, что внешний каталог необработанных трасс
находится вне репозитория, является каталогом и доступен для чтения и записи. Его
путь и внешние значения не раскрывались.

## Автоматический транспорт и отказ

Инструкция координатора была передана локальному Codex CLI через стандартный ввод
из файла
`artifacts/trajectories/instructions/TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001.md`.
Запуск использовал `--ignore-user-config`, то есть не наследовал пользовательскую
конфигурацию. Машиночитаемый JSONL — поток JSON-событий по одному на строку — должен
был напрямую поступить в `micro1-safe-trace`. Диагностический поток ошибок был
отделён и направлен в тот же внешний безопасный интерфейс под отдельным
идентификатором.

Основной приёмник остановился с безопасной категорией `empty trace payload`: не
получено ни одного байта JSONL. Диагностический поток ошибок был сохранён отдельно:

- идентификатор: `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-STDERR`;
- размер: `206` байт;
- SHA-256:
  `a4579eae6ed0babd16921d1d981b791d153330ecd6040053025a71a3cbab52ab`;
- внешний каталог подтверждён; его путь не раскрыт.

Сырое содержимое stderr не читалось, не печаталось, не копировалось в репозиторий и
не использовалось для предположения о причине. Основная неизменяемая JSONL-трасса не
существует, поэтому этот запуск не является доказательством реализации или
интеграции.

## Что не произошло

- Ни один из трёх fixture commit не применён.
- Общие реестры, задачи, сценарии пакета, интеграционные тесты, проекции,
  контрольные суммы и продуктовые файлы не изменены.
- Компиляция, тесты, самопроверки оценщика, изоляция, манифесты, `K=0`, контрольные
  суммы и безопасные предварительные проверки не запускались.
- Официальный или оцениваемый запуск, раскрытие отложенных результатов, живая модель
  продукта, Claude, Chromium, браузер, приватный MCP, commit интеграции и push не
  выполнялись.

Текущий HEAD остаётся равен
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`. До этой карточки единственным новым
файлом основной копии была инструкция автоматической интеграции.

## Почему работа остановлена

Политика траекторий требует неизменяемый автоматический источник. Пустой основной
поток нельзя заменить ручным пересказом, отредактированным экспортом или чтением
сырого диагностического содержимого. Повторный вложенный Codex CLI будет новым
запуском и не должен выполняться без отдельного решения владельца о точной
скорректированной команде и новом идентификаторе продолжения.

## Требуемое решение владельца

Для продолжения нужен отдельный явно разрешённый автоматически захватываемый запуск
с новым идентификатором, сохранением `--ignore-user-config`, инструкцией через stdin,
неизменённым прямым JSONL-транспортом в `micro1-safe-trace` и отдельным безопасным
stderr. Разрешение должно точно указать допустимую коррекцию launcher — средства
запуска — и запретить чтение уже сохранённого сырого stderr.

Даже такое разрешение не должно разрешать integration commit, push, официальный или
оцениваемый запуск, раскрытие отложенных результатов, живую модель продукта,
Claude, Chromium, браузер или приватный MCP. После успешной интеграции всё равно
обязательна остановка для отдельной приёмки владельцем.

## Результат разрешённого продолжения CONT-001

Владелец разрешил ровно один исправленный автоматический запуск
`TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-001`. В нём сохранены инструкция через
stdin, `--ephemeral`, `--ignore-user-config`, `--json`, прежняя модель, прямой
неизменённый JSONL в `micro1-safe-trace` и отдельный безопасный stderr. Параметры
`--approve-for-me` и `--sandbox workspace-write` полностью отсутствовали. Сырой
stderr первой попытки не читался и не удалялся.

Автоматический транспорт продолжения завершился успешно:

- основной JSONL:
  `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-001`, `294516` байт,
  SHA-256
  `e91cd09f95bf6ede29b1da89148f83d981fd19a691bc9d088e25c585e2864d6f`;
- отдельный stderr:
  `TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-001-STDERR`, `2450` байт,
  SHA-256
  `5c39e323d8e8e95597dd155d165ab003cf3beb253a8c0b02f0e911bb856ae3cd`;
- оба слоя находятся вне репозитория, их пути не раскрыты.

Сырые байты обоих захватов не читались и не переносились в репозиторий. Успешный
захват доказывает только работоспособность исправленного транспорта, но не
выполнение интеграции.

## Проверка фактического состояния после CONT-001

После завершения процесса координатор отдельно проверил только Git HEAD, статус и
репозиторную русскую карточку результата. Фактическое состояние осталось прежним:

- HEAD — `830793ce16cb2273e4c9b294a11c7aca952bc7a7`;
- три разрешённых fixture commit не применены;
- staged или unstaged продуктовых, fixture-specific и shared изменений нет;
- исходная карточка не была преобразована вложенным координатором в результат
  интеграции;
- присутствуют только три новые служебные записи: исходная инструкция, инструкция
  `CONT-001` и эта русская карточка.

Следовательно, компиляция, тесты, самопроверки, изоляция, `K=0`, манифесты,
контрольные суммы и обе предварительные проверки в рамках интеграции не выполнены и
их успех не заявляется. Integration commit и push отсутствуют.

Без чтения сырого stderr или JSONL точная внутренняя причина отсутствия изменений не
утверждается. Наблюдаемая граница состоит в том, что corrected transport работает,
а запущенный без явного режима `workspace-write` координатор не создал разрешённые
изменения. Любой новый запуск будет отдельным продолжением и требует нового точного
решения владельца. Наиболее узкая предлагаемая коррекция — вернуть только
`--sandbox workspace-write`, сохранив отсутствие `--approve-for-me` и все остальные
ограничения `CONT-001`; это предложение, а не разрешение на запуск.

## Результат разрешённого продолжения CONT-002

Владелец разрешил единственное продолжение
`TRC-BG-HELDOUT-FIXTURES-INTEGRATION-001-CONT-002` с добавлением
`--sandbox workspace-write`. До действий координатор полностью прочитал обязательные
контракты, границу `SES-20260830-011`, русские карточки владельца, исходную
инструкцию и обе инструкции продолжения. Сохранённые внешние JSONL и stderr первых
двух попыток не читались, не декодировались, не воспроизводились и не продвигались.

Проверка исходного состояния прошла: HEAD остался равен
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`, а незакоммиченными были ровно четыре
ожидаемых служебных файла — исходная инструкция, `CONT-001`, `CONT-002` и эта
русская карточка. Для трёх разрешённых source commit подтверждены точные Git
идентификаторы, общий родитель, разрешённые сообщения и fixture-only состав путей.

Первая команда применения
`git cherry-pick --no-commit 2b61a9954d43686f4c07f10d48bb726d5ed156f0`
остановилась до изменения индекса: Git не смог создать служебный файл блокировки
индекса из-за запрета записи в `.git`. Это новая блокирующая ошибка транспортной
песочницы. Команда не повторялась и обход ограничения не предпринимался.

Ни один из трёх fixture commit не применён. Общие реестры, задачи, сценарии пакета,
интеграционные тесты, проекции, контрольные суммы, кандидаты, видимые пакеты и
скрытые пакеты оценщика не изменены. Компиляция, тесты, самопроверки оценщика,
проверки физической изоляции и запрета доступа, `K=0`, манифесты, воспроизведение,
контрольные суммы и обе предварительные проверки не запускались, поэтому их успех
не заявляется.

Официальный или оцениваемый запуск, раскрытие отложенных результатов, живая модель
продукта, Claude, Chromium, браузер, приватный MCP, упаковка, публикация, commit
интеграции, push и новый вложенный Codex CLI не выполнялись. Внутренние
необработанные события нативных подагентов по-прежнему не заявляются как
экспортированные траектории.

Текущее продолжение является правдивым автоматически захваченным свидетельством
третьей остановки, но не доказательством интеграции. Для продолжения требуется новое
отдельное решение владельца и режим, который разрешает именно служебную запись Git в
текущем репозитории; простого доступа `workspace-write` к рабочим файлам оказалось
недостаточно. Такое решение не должно автоматически разрешать интеграционный commit,
push, официальный или оцениваемый запуск, раскрытие результатов либо публикацию.

## Результат разрешённого продолжения CONT-003

`CONT-003` был запущен как единственный разрешённый координатор с записью только в
рабочее дерево и без записи в `.git`. Сохранённые необработанные JSONL и stderr всех
предыдущих попыток не читались, не декодировались, не воспроизводились, не удалялись
и не продвигались. Новый Codex CLI, подагент, Claude, Chromium или браузер не
запускался.

Исходная проверка прошла: HEAD совпал с
`830793ce16cb2273e4c9b294a11c7aca952bc7a7`; кроме пяти ожидаемых служебных файлов
инструкций и этой карточки не было staged, unstaged, fixture, product или shared
изменений. Для source commit
`2b61a9954d43686f4c07f10d48bb726d5ed156f0`,
`bf94ab99a0162d9a940b9ba129602b0cc59b0f67` и
`745b118b4c4da34dde367d84eae8005c3af3c4a2` подтверждены точные идентификаторы,
разрешённые сообщения, общий freeze-родитель и пути только соответствующих пар.

Для каждого commit строго в заданном порядке выполнен прямой бинарный поток
`git diff --binary ... | git apply --check --binary`, а после успешной проверки —
повторно сгенерированный прямой поток `git diff --binary ... | git apply --binary`.
Все шесть команд завершились с кодом `0`. Последний поток сообщил предупреждения о
пробелах, уже присутствующих в исходном патче; проверки и применение не
рассогласовались. Временный patch-файл, `tee`, shell-переменная с diff,
`cherry-pick`, `--index` и `--cached` не использовались.

Доказательство байтов потребовало четыре исправления только читающего shell-скрипта:
сначала имя переменной затенило системный `PATH`, затем разбор стандартного формата
`git ls-tree` дал пустое имя, после этого выяснилось, что установленная версия Git не
поддерживает `ls-tree --format`, и следующая проверка ошибочно включила существующие
freeze-файлы в контроль количества. Эти неуспешные доказательные попытки не меняли
рабочее дерево. Итоговая проверка использовала точные path set из `diff-tree` и
сравнила рабочий Git blob каждого пути с `<commit>:<path>`: все `103` файла
совпали. Отдельно подтверждено отсутствие tracked-изменений freeze base и
посторонних путей на этом этапе.

После этого начата минимальная shared-интеграция. В рабочем дереве добавлены общий
десятифайловый descriptor registry, шесть типизированных задач `h01:verify`—
`h06:verify`, агрегат `held-out:verify`, гетерогенная проверка манифестов, проверка
утечки mapping/oracle-маркеров и интеграционный тест точной class-биеекции,
соотношения `4/6`, единственного challenging case `BG-H02`, двух нейтральных
кандидатов, взаимной физической изоляции и `K=0`. Реестр задач подключён к общей
архитектуре. Первая компиляция выявила одну пропущенную закрывающую скобку в новом
`Object.freeze`; после детерминированного исправления повторный `npm run compile`
прошёл с кодом `0`.

## Обязательная остановка CONT-003

Следующий `npm test` запустил существующий тест предварительного сканера. Этот тест
сам включил находящиеся под корнем репозитория служебные каталоги других Git
worktree и сообщил находки машинных путей и возможных секретоподобных строк в их
зависимостях. Точные машинные пути и найденный текст здесь намеренно не
воспроизводятся. Координатор не открывал эти файлы и не пытался интерпретировать их
содержимое, однако сам факт автоматического чтения другого worktree нарушает прямой
запрет текущей инструкции и является stop condition внешнего/неразрешённого
контекста.

`npm test` поэтому завершился неуспешно: `111` тестов, `100` прошли и `11` не
прошли. Один новый интеграционный тест также имел слишком широкую проверку текста
всего descriptor-файла и увидел старые ненейтральные имена D04; это известная
детерминированная ошибка нового теста, но после срабатывания stop condition она не
исправлялась. Остальные неуспешные проверки относятся к непреднамеренному охвату
служебных worktree существующим сканером. Повторный тест, fixture-specific задачи,
самопроверки оценщика, полные reciprocal probes, манифесты, leak scan,
development replay, checksum reconciliation и обе предварительные проверки не
выполнялись.

Текущая интеграция неполна и не готова к приёмке как зелёный результат. Проекции
EN/RU, Improvement Changelog, provenance/trajectory indexes и checksum projection
не обновлены. Fixture-local байты после применения были точны, но последующие
shared-изменения не прошли полный комплект проверок. Integration commit, запись в
`.git`, push, официальный или оцениваемый запуск, раскрытие отложенных результатов,
живая продуктовая модель, Claude, Chromium, браузер, приватный MCP, упаковка,
публикация и submission не выполнялись.

Для продолжения требуется отдельное решение владельца о том, как безопасно исключить
или удалить из текущего корня служебные каталоги других worktree до повторного
сканирования. CONT-003 не предоставляет доказательство полной интеграции и не
просит принять незелёное дерево как завершённое.

## Результат разрешённого продолжения CONT-004

До запуска владелец отдельно подтвердил чистоту и точные commit трёх служебных
рабочих деревьев, затем удалил только их обычной командой без принудительного режима.
В `CONT-004` подтверждены неизменный HEAD
`830793ce16cb2273e4c9b294a11c7aca952bc7a7` и отсутствие ровно трёх разрешённых
каталогов. Другие рабочие деревья и служебные метаданные не перечислялись и не
инспектировались. Все 103 fixture-local файла повторно сверены с тремя разрешёнными
commit и сохранены побайтно; повторное применение или переписывание не выполнялось.

Исправлены две детерминированные ошибки только новой общей интеграции. Проверка
нейтральных имён кандидатов была сужена с полного `development.ts` до объявления
шести `HELD_OUT_FIXTURES`, поэтому исторические имена D04 больше не дают ложное
срабатывание. Первый запуск общего held-out leak scan — проверки утечки соответствия
кандидатов истине — увидел H05/H06 `isolation.test.ts`, которые по назначению
называют обе файловые границы для доказательства запрета. Сканер был сужен только
путём исключения явных `isolation.test.ts` и `denied-probe.mjs`; все 12 кандидатов и
рабочий arm-visible код продолжают проверяться. Повтор прошёл для 43 файлов.

Проверка дала следующие результаты:

- `npm run compile` прошёл;
- `npm run task -- held-out:verify` прошёл полностью: видимые тесты H01–H06,
  самопроверки независимого оценщика, обе стороны reciprocal denial — взаимного
  физического запрета доступа, манифесты и их хеши, интеграция 5/5 и leak scan;
- проверено 22, 23, 13, 13, 18 и 18 неизменяемых привязок манифестов соответственно;
- подтверждены точная биекция десяти классов, состав 4/6, единственный сложный случай
  BG-H02, ровно два нейтральных кандидата на held-out фикстуру и `K=0`;
- `npm run task -- development:verify` прошёл, включая D01–D04 манифесты,
  изоляцию и детерминированное воспроизведение;
- отдельный `npm run task -- d01:replay` прошёл с совпадением JSON/HTML;
- официальный или оцениваемый запуск и раскрытие held-out результатов не выполнялись.

Полный `npm test` после удаления служебных деревьев не является зелёным: из 111
тестов прошли 102, а девять существующих D01 тестов процессов и loopback — локального
сетевого соединения — были заблокированы ограничениями вложенной песочницы
(`EPERM` и код дочернего процесса 71). Новый repository self-scan прошёл и новой
контаминации не сообщил. Эти D01 ошибки не относятся к интеграционному коду;
политика исполнения и D01 не менялись, а ограничение не скрывается.

Проекции EN/RU, журнал улучшений, индексы происхождения/траекторий и контрольные
суммы обновлены в пределах интеграции. Автоматически захваченный JSONL этого
продолжения остаётся внешним исходным свидетельством; внутренние необработанные
события нативных подагентов не заявляются как экспортированные трассы. Integration
commit, запись в `.git`, push, живая модель, Claude, Chromium, браузер, приватный MCP,
упаковка, публикация и submission не выполнялись.

Состояние оставлено незакоммиченным для отдельной приёмки владельцем. Остаточный
риск: полный обычный набор тестов должен быть повторён в разрешённой среде, где
существующие D01 subprocess/loopback проверки могут исполняться; до этого нельзя
заявлять `npm test` полностью зелёным.

Обе требуемые безопасные предварительные проверки были запущены точными командами
`npm run preflight:control` и `npm run preflight:implementation`. Обе остановились
до сканирования с безопасной ошибкой конфигурации: вложенный launcher не передал
обязательное внешнее значение `MICRO1_PRIVATE_DENYLIST`. Значение и внешний путь не
печатались и не выяснялись; подстановка, обход или синтетический список не
использовались. Поэтому новой находки контаминации нет, но успешный результат обеих
предварительных проверок также не заявляется. Приёмка остаётся заблокированной до их
owner-controlled повтора вместе с полным `npm test` в разрешённой среде.

## Owner-controlled post-capture acceptance

Владелец разрешил после завершения `CONT-004` выполнить только пять неизменяющих
проверок непосредственно из текущего разрешённого main root, без вложенного Codex
CLI и без изменения файлов. Проверки завершены `2026-08-30T17:54:33Z`.

Первый процесс `npm test` превысил 30-секундное окно чтения управляющего инструмента
и завершился без возвращённого в текущий ход exit status. Этот результат не был
засчитан ни как успех, ни как отказ. После безопасного подтверждения, что исходный
процесс уже завершён, та же точная команда была повторена только для получения
проверяемого результата; продуктовые файлы между попытками не менялись. Повтор дал:

- `111` тестов;
- `111` прошли;
- `0` не прошли;
- `TASK_PASSED test:all`;
- exit status `0`.

Таким образом, девять прежних D01 subprocess/loopback отказов подтверждены как
ограничение вложенной песочницы `CONT-004`, а не дефект интеграции. В разрешённой
owner-controlled среде полный обычный набор тестов зелёный.

Безопасные предварительные проверки выполнены через owner-controlled интерфейс без
печати значения или внешнего пути `MICRO1_PRIVATE_DENYLIST`:

- `micro1-safe-preflight control` — exit status `0`,
  `READY_FOR_CLEAN_BRANCH`;
- `micro1-safe-preflight implementation` — exit status `0`,
  `READY_FOR_IMPLEMENTATION`.

Обе проверки сохранили четыре ранее известные записи обязательного ручного просмотра
официальных бинарных материалов. Implementation preflight также честно сообщил
ожидаемое предупреждение о незакоммиченном рабочем дереве. Новых находок
контаминации, приватных путей или приватного содержимого нет.

До обновления этой карточки `npm run task -- checksums:check` подтвердил
`CONTROL_CHECKSUMS_VERIFIED 494` и exit status `0`. `git diff --check` также
завершился с exit status `0`. После записи этого раздела производная checksum-
проекция детерминированно пересобирается только для отражения новых байтов русской
карточки; затем разрешены ровно повторные `checksums:check` и `git diff --check`.

## Итоговый объём приёмки владельца

Интегрированное незакоммиченное дерево содержит шесть held-out фикстур H01—H06,
побайтно перенесённых из трёх разрешённых fixture-only commit, и минимальные общие
изменения координатора. Подтверждены compile, полный `npm test` 111/111,
`held-out:verify`, все видимые тесты, самопроверки оценщика, взаимная физическая
изоляция, `K=0`, неизменяемые манифесты и хеши, отсутствие утечки mapping,
`development:verify`, D01 replay, обе безопасные предварительные проверки,
контрольные суммы и структура diff.

Внутренние необработанные события нативных подагентов не заявляются как
экспортированные трассы. Автоматически захваченные coordinator attempts сохранены с
их честными результатами и ограничениями; автоматическое продвижение в
`actual_traces` не выполнялось.

Эта карточка просит владельца принять только текущее незакоммиченное состояние и
результаты интеграционной проверки. Integration commit, push, официальный или
оцениваемый запуск, раскрытие held-out результатов, живая модель продукта, Claude,
Chromium, браузер, приватный MCP, упаковка, публикация и submission по-прежнему не
разрешены. Для integration commit требуется отдельное точное owner approval.

## Решение владельца и разрешённый checkpoint commit

Владелец принял текущее интегрированное состояние H01—H06 и результаты всех
owner-controlled post-capture проверок `2026-08-30T17:56:31Z`. Одобрение охватывает
именно проверенные fixture-local байты, минимальные общие интеграционные изменения,
русскую карточку и согласованные производные контрольные суммы.

Разрешён ровно один integration checkpoint commit без push с точным сообщением:

```text
feat(Evaluation):[BeyondGreen] Integrate held-out fixtures
```

Одноразовый Git-гейт активирован только для этого сообщения. Перед commit должны
пройти согласование производных хешей и контрольных сумм, `checksums:check` и
`git diff --check`. Любой отказ останавливает создание commit.

Это решение не продвигает автоматически ни одну coordinator trajectory в
`actual_traces`, не разрешает официальный или оцениваемый запуск, раскрытие
held-out результатов, живую модель продукта, Claude, Chromium, браузер, приватный
MCP, push, публикацию, упаковку или submission. После checkpoint commit следующим
обязательным гейтом остаётся отдельная проверка, сканирование и owner approval
траекторий до любого продвижения; official/scored run и unblinding требуют своих
отдельных последующих решений.

## Разрешённая нормализация пробельных символов перед commit

После индексирования полный `git diff --cached --check` обнаружил пробельные
нарушения, которые не были видны в прежнем `git diff --check`, пока новые файлы
оставались неотслеживаемыми: три Markdown-переноса с двумя конечными пробелами в
заголовке этой карточки и по одной лишней конечной пустой строке в тридцати файлах
пары H05/H06. Commit не выполнялся.

Владелец отдельно разрешил только детерминированную замену трёх Markdown-переносов
на `<br>` и удаление ровно тридцати конечных пустых строк H05/H06. Другие байты
фикстур не менялись. Семантика контрактов, кандидатов, оценщика и методики оценки не
изменена. Производные SHA-256 в пакетных и implementation-манифестах H05/H06
пересчитаны и согласованы; проекции происхождения, контрольных сумм и эта русская
карточка обновлены только для отражения разрешённой нормализации.

Окончательный integration checkpoint commit остаётся разрешён только при полном
успехе повторных compile, `npm test`, `held-out:verify`, проверок манифестов,
изоляции, `K=0`, отсутствия утечки, `development:verify`, D01 replay, обеих
безопасных предварительных проверок, `checksums:check`, `git diff --check` и
`git diff --cached --check`. Push, продвижение `actual_traces`, официальный или
оцениваемый запуск и раскрытие held-out результатов не разрешены.

Повторная проверка разрешённой нормализации завершена
`2026-08-30T18:04:28Z`. Успешно прошли:

- TypeScript compile;
- полный `npm test`: `111` из `111`, отказов нет;
- `held-out:verify` для H01—H06, включая видимые проверки, самопроверки
  оценщика, denied-access probes, неизменяемые манифесты, взаимную физическую
  изоляцию, `K=0` и отсутствие утечки mapping;
- `development:verify` и отдельный D01 replay с точным совпадением JSON/HTML;
- обе безопасные предварительные проверки: `READY_FOR_CLEAN_BRANCH` и
  `READY_FOR_IMPLEMENTATION`; сохранились только четыре ранее известные записи
  ручного просмотра официальных бинарных материалов и ожидаемое предупреждение
  implementation-проверки о незакоммиченном дереве.

После этой записи контрольная checksum-проекция пересобирается детерминированно.
Commit допустим только после финальных успешных `checksums:check`,
`git diff --check` и `git diff --cached --check`.
